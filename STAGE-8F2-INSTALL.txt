99's Guide — Stage 8F-2 Final Safe Read Cutover
===============================================

STATUS
------
Stage 8A: PASS
Stage 8B: PASS
Stage 8C: PASS
Stage 8D: PASS
Stage 8E: PASS
Stage 8F-1: PASS
Stage 8F: CURRENT

PURPOSE
-------
Finish the high-value SAFE read cutover before Stage 8G egress measurement.

Newly covered:
- full user-state bundle subreads used by /api/auth/me and profile payloads:
  LectureProgress
  PointsLog
  personal CalendarEvent
- blocked users / blocked ID reads
- owner roster + lecture progress aggregation

Still intentionally Supabase-authoritative:
- requireUser / requireAdmin / requireOwner
- sessionVersion
- current ban enforcement during authentication
- password hashes
- OAuthIdentity
- reset / verification tokens
- same-request write validation
- /api/auth/sync final response immediately after its writes
- security-sensitive write-path checks
- low-frequency moderation/report administrative reads

STEP 1 — UPDATE THE PRIVATE WORKER
----------------------------------
Replace only:

cloudflare-private-data-api\src\index.ts
cloudflare-private-data-api\wrangler.jsonc

Deploy:

cd cloudflare-private-data-api
npx wrangler deploy -c wrangler.jsonc
cd ..

STEP 2 — SHADOW PARITY FOR THE TWO NEW WORKER CONTRACTS
-------------------------------------------------------
Make sure PRIVATE_DATA_SYNC_SECRET exists in this local CMD session only.
Do NOT send it to ChatGPT.

Run:

node verify-stage8f2-shadow-parity.mjs

Expected final:
STAGE 8F-2 SHADOW PARITY PASS

STEP 3 — COPY BACKEND FILES
---------------------------
Replace:

server.ts
server\services\userService.ts
server\services\privateD1Read.ts

Do not change privateD1Sync.ts.

STEP 4 — STATIC CHECK + BUILD
-----------------------------
node verify-stage8f2-static.mjs

Expected:
STAGE 8F-2 STATIC CHECK PASS

Then:

npm run build

STOP if build fails.

STEP 5 — ADD NEW RENDER FLAGS AS FALSE
--------------------------------------
PRIVATE_D1_PROFILE_BUNDLE_READS_ENABLED=false
PRIVATE_D1_POINTS_READS_ENABLED=false
PRIVATE_D1_BLOCKS_READS_ENABLED=false
PRIVATE_D1_ROSTER_READS_ENABLED=false

Keep the Stage 8F-1 flags true and keep:
PRIVATE_D1_WRITE_MIRROR_ENABLED=true

Deploy with these four new flags false first.
Confirm login/session restore and normal app operation.

STEP 6 — ENABLE ONE AT A TIME
-----------------------------
Enable in this order:

1. PRIVATE_D1_POINTS_READS_ENABLED=true
   (This alone does not activate the profile bundle until the master flag is on.)

2. PRIVATE_D1_PROFILE_BUNDLE_READS_ENABLED=true
   Test:
   - login/session restore
   - profile
   - progress
   - points
   - personal calendar

3. PRIVATE_D1_BLOCKS_READS_ENABLED=true
   Test:
   - Blocked Users page
   - block ID filtering / Q&A behavior

4. PRIVATE_D1_ROSTER_READS_ENABLED=true
   Test:
   - owner roster
   - progress counts
   - user ordering

If a feature fails, set only its flag back to false.

STEP 7 — WRITE MIRROR HEALTH
----------------------------
node verify-stage8d2b-outbox.mjs

Expected:
Pending outbox rows: 0
Rows with lastError: 0
STAGE 8D-2B OUTBOX DRAIN PASS

STEP 8 — FINAL APP SMOKE TEST
-----------------------------
Test:
- login/session restore
- profile
- roster
- progress/flashcards
- points
- notifications
- calendar
- Q&A
- blocked users
- admin/owner access
- normal writes still persist after reload

Expected:
No repeated [PrivateD1Read] fallback warnings.
All app behavior unchanged.

Then report:
STAGE 8F-2 PASS

After that, Stage 8F is complete and the next stage is Stage 8G:
FINAL EGRESS VERIFICATION.

SECURITY
--------
Never send PRIVATE_DATA_SYNC_SECRET, JWTs, cookies, or database credentials to ChatGPT.
