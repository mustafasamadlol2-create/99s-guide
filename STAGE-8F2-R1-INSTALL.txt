99's Guide — Stage 8F-2 R1 Order-Only Repair
============================================

WHY R1 EXISTS
-------------
The diagnostic proved:
- Supabase rows = 119
- D1 rows = 119
- no missing rows on either side
- all field values match by user ID and lecture ID
- ONLY nested LectureProgress array order differs

Therefore this is NOT a data-sync failure.

R1 makes the Admin Roster contract deterministic on BOTH paths:
- users: stable tie-break by id
- nested LectureProgress: lectureId ascending

This avoids D1/Supabase fallback producing different array order.

THIS ZIP SUPERSEDES THE PREVIOUS STAGE 8F-2 ZIP.

STEP 1 — REDEPLOY ONLY THE PRIVATE WORKER FIRST
-----------------------------------------------
Replace:

cloudflare-private-data-api\src\index.ts
cloudflare-private-data-api\wrangler.jsonc

Then:

cd cloudflare-private-data-api
npx wrangler deploy -c wrangler.jsonc
cd ..

Do NOT change Render flags yet.

STEP 2 — RERUN SHADOW PARITY
----------------------------
In the CMD session where PRIVATE_DATA_SYNC_SECRET is set:

node verify-stage8f2-shadow-parity.mjs

Expected final:

Admin roster contract: PASS
STAGE 8F-2 SHADOW PARITY PASS

If it does not PASS, STOP and send the new output.

STEP 3 — ONLY AFTER SHADOW PARITY PASS
--------------------------------------
Replace backend files from THIS R1 ZIP:

server.ts
server\services\userService.ts
server\services\privateD1Read.ts

Then:

node verify-stage8f2-static.mjs
npm run build

Expected:
STAGE 8F-2 STATIC CHECK PASS
and build success.

STEP 4 — RENDER FLAGS
---------------------
Add as false first:

PRIVATE_D1_PROFILE_BUNDLE_READS_ENABLED=false
PRIVATE_D1_POINTS_READS_ENABLED=false
PRIVATE_D1_BLOCKS_READS_ENABLED=false
PRIVATE_D1_ROSTER_READS_ENABLED=false

Deploy and confirm app health.

Then enable one at a time:

1. PRIVATE_D1_POINTS_READS_ENABLED=true
2. PRIVATE_D1_PROFILE_BUNDLE_READS_ENABLED=true
3. PRIVATE_D1_BLOCKS_READS_ENABLED=true
4. PRIVATE_D1_ROSTER_READS_ENABLED=true

After each deployment test the corresponding feature.

STEP 5 — FINAL HEALTH
---------------------
Run:

node verify-stage8d2b-outbox.mjs

Expected:

Pending outbox rows: 0
Rows with lastError: 0
STAGE 8D-2B OUTBOX DRAIN PASS

Then smoke-test:
- login/session restore
- profile
- points/progress
- personal calendar
- blocked users
- owner roster
- notifications
- Q&A
- admin/owner access
- writes persist after reload

If all good, report:

STAGE 8F-2 PASS

The Node/Windows UV_HANDLE_CLOSING assertion after script exit is unrelated to
the roster parity result.
