# 99'S GUIDE — PERSONALIZATION QUALITY REPAIR
# PROMPT R1B
## HYDRATION / ACCOUNT-BOUNDARY CLOSURE
### VERIFY R1 DID NOT INTRODUCE CROSS-ACCOUNT THEME LEAKAGE
### VERIFY HERO STYLE CONTRACT USES THE CANONICAL IDS
### REPAIR ONLY IF A REAL DEFECT EXISTS
### DO NOT BEGIN R2
### DO NOT MODIFY `.agents/*`

R1 reported PASS and implemented broad semantic theme coverage plus Hero persistence.

Before R1 is formally closed, there are TWO architecture-sensitive concerns that must be verified against actual source.

Do not redesign the system.

Do not expand Motion, Reading, Glass behavior.

Do not add subject hiding or semester controls.

Do not implement App Icon.

This prompt is focused only on:

1. hydration/account-boundary correctness
2. canonical Hero Style contract correctness

---

# 1. EXISTING ACCOUNT BOUNDARY CONTRACT

The previously approved personalization lifecycle requires:

ACCOUNT SWITCH:

```text
Account A active with Theme A
→ user switches to Account B
→ visual state must cross a safe Classic/default boundary
→ hydrate Account B
→ apply Account B committed personalization

LOGOUT:

authenticated personalized account
→ logout
→ visible anonymous state = Classic/default

No Account A theme/presentation may appear as Account B personalization.

No stale Account A attributes may survive logout.

2. R1 CHANGE TO AUDIT

R1 reported that:

PersonalizationThemeBridge.tsx
PersonalizationPresentationBridge.tsx

now preserve document personalization attributes while hydration is loading.

That behavior was added to prevent:

alternate theme
→ temporary Classic reset during same-account hydration
→ apparent Hero/theme reversion

That is useful ONLY if it does not violate account isolation.

Inspect actual implementation.

3. CRITICAL QUESTION

Determine whether the current R1 logic can cause:

Account A = rose
→ switch to Account B
→ B hydration starts
→ rose attributes remain visible from Account A

or:

Account A = rose
→ logout
→ rose attributes remain on <html>

If YES:

this is a REAL R1 regression.

Repair it.

If NO:

prove why through actual state/lifecycle guards and tests.

4. CORRECT HYDRATION SEMANTICS

The final desired behavior is:

Same active account / same generation

If the Provider temporarily enters a loading state while resolving the SAME account and there is a known valid committed document state:

do not unnecessarily remove/reapply attributes in a way that causes the Hero/theme to flash back to Classic.

Different account

If active user identity changes:

clear the previous account's personalization presentation to the safe Classic/default boundary BEFORE hydrating the new account.

Logout

Immediately clear account-scoped Theme/Hero/Glass/Motion/Reading document attributes back to anonymous/default behavior.

Do not preserve stale account attributes.

5. GENERATION / USER OWNERSHIP

If necessary, document personalization ownership should be scoped to:

active user ID
account generation

or equivalent existing Provider lifecycle state.

Do not introduce a new global persistence system.

Do not store user IDs in CSS.

Use the smallest lifecycle-aware fix.

6. DO NOT REINTRODUCE HERO FLASH

The repair must preserve the R1 user-facing fix:

Apply Rose
→ Hero stays Rose-compatible
→ no temporary Classic reversion

during ordinary same-account use.

Do not solve account isolation by restoring the original flicker.

7. ACCOUNT SWITCH TEST

Add/verify this exact regression:

Account A
themeId = rose
heroStyle = aurora

document:
data-app-theme = rose
data-personalization-hero = aurora

→ switch to Account B

Before B hydration resolves:

expected safe boundary:

data-app-theme absent / Classic semantics
data-personalization-hero absent / Classic semantics

or exact equivalent of the accepted default-attribute model.

Then B hydrates:

themeId = ocean
heroStyle = night

expected:

data-app-theme = ocean
data-personalization-hero = night

At NO point may B own stale Rose/Aurora state from A.

8. LOGOUT TEST

Exact regression:

Account A
themeId = rose
heroStyle = aurora
glassStyle = frosted
motionStyle = subtle
readingSize = large

→ logout

Expected anonymous document state:

data-app-theme absent
data-personalization-hero absent
data-personalization-glass absent
data-personalization-motion absent
data-personalization-reading absent

or exact equivalent of the accepted factory/default state.

No stale account personalization remains.

9. SAME-ACCOUNT HYDRATION TEST

Exact regression:

same authenticated account
committed alternate personalization already active
→ internal/local hydration transition occurs

Expected:

no transient Classic overwrite caused only by a loading placeholder.

This is the R1 Hero/theme persistence fix.

10. STRICTMODE

Verify StrictMode duplicate effects do not:

clear and reapply attributes repeatedly
cause theme flash
restore stale account attributes
cross account generation boundaries

Reuse existing generation guards.

11. CANONICAL HERO STYLE CONTRACT

The approved HeroStyle IDs are exactly:

classic
minimal
night
aurora

Audit:

shared personalization types
validation
catalog
My99 Studio
Presentation Bridge
CSS selectors
R1 tests
new R1B tests

There must be NO runtime HeroStyle IDs:

balanced
focused
cinematic

unless those words are unrelated human-readable labels and do not alter the typed contract.

12. REPORT TERMINOLOGY ERROR

R1 Section 22 reported combinations such as:

Classic Theme × Balanced Hero
Ocean Theme × Focused Hero
Rose Theme × Cinematic Hero
Violet Theme × Minimal Hero

Determine whether:

A. this was REPORT WORDING ONLY

or:

B. source/tests actually introduced noncanonical Hero IDs.

If A:
state clearly that code remains canonical.

If B:
repair immediately.

13. REQUIRED CANONICAL COMBINATION TESTS

Use actual IDs:

rose + classic
rose + minimal
rose + night
rose + aurora

ocean + night
monochrome + aurora

Verify:

Theme controls semantic colors.

HeroStyle controls presentation only.

14. THEME BRIDGE DEFAULT MODEL

Reconfirm:

Classic theme is represented by the accepted default model.

Previously:

classic-99 → no data-app-theme attribute

Preserve that behavior unless the current accepted implementation has an equivalent explicit model.

Do not add:

data-app-theme="classic-99"

solely to solve lifecycle state unless the existing architecture already uses it.

15. PRESENTATION DEFAULT ATTRIBUTES

Previously accepted defaults:

heroStyle = classic → attr omitted
glassStyle = balanced → attr omitted
motionStyle = full → attr omitted
readingSize = default → attr omitted

Verify R1 did not change those semantics.

16. NO PALETTE CHANGES

R1B must not modify palette values.

Expected:

NO THEME PALETTE VALUES CHANGED.

17. NO CLOUD SCHEMA CHANGE

Expected exactly:

PersonalizationConfigV1 change: NO
PersonalizationLocalEnvelopeV2 schema change: NO
Express personalization API change: NO
Workers KV cloud record change: NO
Cloudflare Worker schema change: NO
18. R1 VISUAL COVERAGE MUST REMAIN

Do not undo R1 semantic repairs to:

Home Hero
SubjectView
LectureDetailView
App wrappers
bottom navigation
semantic page/card/tab/chrome coverage

R1B is lifecycle closure only.

19. TEST FILES

Prefer updating existing lifecycle/runtime tests and/or adding one focused test such as:

tests/personalization-r1-hydration-boundary.test.ts

Use actual repository patterns.

Tests must cover:

same-account hydration does not flash Classic
A → B removes A visual attributes before B hydration
logout removes account attributes
stale A hydration cannot mutate B
StrictMode duplicate lifecycle safe
canonical HeroStyle IDs only
default attrs omitted
R1 Hero persistence remains
20. PERSONALIZATION MATRIX

Entering R1B baseline:

164 passed
0 failed
0 skipped
0 cancelled

Run:

npx tsx --test tests/personalization*.test.ts tests/personalization-runtime.test.tsx

No regression.

New tests should increase total.

21. R1 COVERAGE REGRESSION

Run:

npx tsx --test tests/personalization-r1-theme-coverage.test.ts

Must PASS.

22. PHASE-4 / ACCOUNT LIFECYCLE REGRESSIONS

Run relevant existing:

personalization-runtime
personalization-state
personalization-storage
personalization-theme-runtime
personalization-presentation

Report exact counts.

23. BUILD

Run:

npm run build

Must PASS.

24. TYPECHECK

Run:

npm run typecheck

Accepted unrelated baseline only:

7 missing .webp declarations
src/features/modules/moduleVisuals.ts

No new error.

25. TARGETED ESLINT

Run targeted ESLint on any modified bridge/provider/test files.

Must PASS.

26. PERFORMANCE

The final solution must not add:

hydration polling
timers to correct theme
forced reload
Provider remount
account-wide page remount
repeated document attribute mutation loop

Use state/lifecycle guards.

27. GIT REVIEW

Run:

git status --short
git diff --stat
git diff --check

Report R1B changes separately from existing R1 modifications.

28. .agents/*

Run:

git status --short -- .agents
git diff -- .agents

Expected no output.

No .agents/* change.

29. MANDATORY FINAL REPORT

Return exactly:

99'S GUIDE — PERSONALIZATION QUALITY REPAIR R1B REPORT
HYDRATION / ACCOUNT-BOUNDARY CLOSURE
1. Status

PASS / BLOCKED

2. Why R1B Was Required

Explain the potential conflict between Hero persistence and account isolation.

3. Actual R1 Bridge Behavior Before R1B

Explain exactly what the R1 bridge did during hydration.

4. Cross-Account Leakage Found

YES / NO

If YES:
describe exact defect.

5. Files Modified

List exact R1B files only.

6. Final Theme Bridge Lifecycle

Explain:

same account
account switch
logout
7. Final Presentation Bridge Lifecycle

Explain:

Hero
Glass
Motion
Reading
8. Same-Account Hydration

Report exact result.

9. Account A → B Boundary

Report:
A attributes
→ safe Classic boundary
→ B attributes.

10. Logout Boundary

Report final document attributes.

11. Stale Hydration Protection

Report.

12. StrictMode

Report.

13. Canonical HeroStyle Contract

State exact runtime IDs:

classic
minimal
night
aurora

14. R1 Report Naming Discrepancy

State whether Balanced/Focused/Cinematic were:
REPORT WORDING ONLY
or
SOURCE DEFECT

If source defect:
report repair.

15. Theme × Hero Verification

Report:
rose + classic
rose + minimal
rose + night
rose + aurora
ocean + night
monochrome + aurora

16. Default Attribute Semantics

Confirm:

classic-99 theme attr omitted
classic Hero attr omitted
balanced Glass attr omitted
full Motion attr omitted
default Reading attr omitted

17. R1 Visual Coverage Preservation

Confirm R1 semantic coverage remains.

18. Palette Changes

State exactly:

NO THEME PALETTE VALUES WERE CHANGED.

19. Cloud Schema Impact

State exactly:

PersonalizationConfigV1 change: NO
PersonalizationLocalEnvelopeV2 schema change: NO
Express personalization API change: NO
Workers KV cloud record change: NO
Cloudflare Worker schema change: NO

20. Tests Added / Updated

List exact tests.

21. Account-Boundary Test Results

Report exact count.

22. R1 Theme Coverage Regression

Report.

23. Full Personalization Matrix

Report exact:
passed
failed
skipped
cancelled

Entering baseline:
164 / 0 / 0 / 0

24. Build

Report.

25. TypeScript

Report accepted baseline/no new errors.

26. ESLint

Report.

27. Performance

Confirm no polling/timer/remount/reload.

28. Git Review

Report:
git status --short
git diff --stat
git diff --check

29. .agents/* Verification

Report:
git status --short -- .agents
git diff -- .agents

Then state exactly:

No .agents/* file was created or modified.

30. R2 Readiness

If all gates pass, state exactly:

R1 FULL-APP THEME COVERAGE: CLOSED AND APPROVED

R1 HERO THEME PERSISTENCE: CLOSED AND APPROVED

ACCOUNT-SWITCH CLASSIC BOUNDARY: VERIFIED

LOGOUT CLASSIC BOUNDARY: VERIFIED

SAME-ACCOUNT HYDRATION FLICKER: ELIMINATED

CANONICAL HERO STYLE CONTRACT: VERIFIED

ACCOUNT PERSONALIZATION ISOLATION: VERIFIED

R2 FUNCTIONAL PERSONALIZATION AXES REPAIR: APPROVED TO BEGIN

End EXACTLY:

PERSONALIZATION QUALITY REPAIR R1B STATUS: COMPLETE — READY FOR R2

Otherwise:

PERSONALIZATION QUALITY REPAIR R1B STATUS: BLOCKED — REPAIR REQUIRED