# 99'S GUIDE — PERSONALIZATION QUALITY REPAIR
# PROMPT R2
## FUNCTIONAL PERSONALIZATION AXES REPAIR
### HERO STYLE + GLASS STYLE + MOTION STYLE + READING SIZE
### MAKE EVERY EXISTING OPTION PRODUCE A REAL, VISIBLE, SYSTEMATIC RESULT
### PRESENTATION ONLY
### NO SUBJECT HIDING / SEMESTER CONTROLS YET
### NO APP ICON IMPLEMENTATION
### NO CLOUD SCHEMA CHANGE
### DO NOT MODIFY `.agents/*`

R1 and R1B are CLOSED AND APPROVED.

Entering R2 we have:

- Full-app Theme Pack semantic coverage repaired.
- Rose/Ocean/etc. now affect shared application chrome.
- Hero Theme Pack colors persist.
- Same-account hydration flicker eliminated.
- Account A → Account B safe Classic boundary verified.
- Logout Classic boundary verified.
- Canonical HeroStyle IDs verified.
- Full personalization matrix entering R2:

```text
170 passed
0 failed
0 skipped
0 cancelled

R2 addresses a separate user-visible problem:

Several My99 presentation options exist and persist correctly, but their visible effect is too weak, too localized, or effectively absent in real application usage.

The affected axes are exactly:

Hero Style:
classic
minimal
night
aurora

Glass Style:
clear
balanced
frosted

Motion Style:
full
subtle
reduced

Reading Size:
small
default
large

The user must be able to change each setting, press Apply, and immediately SEE a meaningful difference.

This is an IMPLEMENTATION task.

Audit actual source first, then repair the functional presentation behavior.

1. HARD SCOPE

R2 may modify presentation implementation for:

Hero Style
Glass Style
Motion Style
Reading Size
My99 local preview of those settings where required
generic/shared presentation CSS/components

R2 must NOT implement:

Subject hide/show
Semester 1 / Semester 2 controls
App Icon
new Theme Packs
new study features
new business logic
new navigation behavior
new cloud schema

Subject visibility is R3.

2. EXISTING CONFIGURATION IS FROZEN

Do NOT rename or add values.

Canonical configuration remains:

heroStyle:
  | "classic"
  | "minimal"
  | "night"
  | "aurora";

glassStyle:
  | "clear"
  | "balanced"
  | "frosted";

motionStyle:
  | "full"
  | "subtle"
  | "reduced";

readingSize:
  | "small"
  | "default"
  | "large";

Do not introduce aliases such as:

cinematic
focused
balanced hero
compact
extra-large
3. ARCHITECTURE MUST REMAIN

Existing runtime remains conceptually:

committed personalization
→ PersonalizationPresentationBridge
→ document presentation attributes
→ CSS/shared presentation components

Draft remains globally inert.

The My99 contained preview may use Draft locally.

Do NOT replace this with scattered JavaScript conditionals across pages.

4. FIRST AUDIT WHY OPTIONS FEEL INACTIVE

Before editing, trace actual runtime effects of all four axes.

For each value determine:

where its document attribute is applied
which CSS selectors consume it
which components consume it
which surfaces are actually affected
whether selectors match current DOM/classes
whether later CSS overrides them
whether hardcoded component styles bypass them
whether effect is too subtle to distinguish
whether mobile/tablet/desktop use different surfaces
whether Preview differs from committed runtime

Report real root causes.

Do not guess.

5. QUALITY PRINCIPLE

The goal is NOT exaggerated personalization.

The goal is:

clearly perceptible
+
premium
+
consistent
+
accessible
+
stable

A user switching:

Balanced → Frosted

or:

Default → Large

must not wonder whether Apply worked.

At the same time:

no cartoonish animations
no unreadably huge fonts
no excessive blur
no broken layouts
no saturated visual noise
6. HERO STYLE — PRODUCT SEMANTICS

The four Hero Styles must become clearly distinguishable while Theme Pack continues to own color.

Exact styles:

classic
minimal
night
aurora

Theme Pack:

owns color

Hero Style:

owns composition / density / decorative treatment / depth

Do not hardcode Rose/Ocean/etc. inside HeroStyle selectors.

7. HERO — CLASSIC

classic is the accepted current/default 99's Guide Hero presentation.

It must remain the baseline.

Expected character:

current premium Hero hierarchy
current default ambient treatment
current standard decorative density
current layout
theme-aware colors from semantic Hero tokens

Do not substantially redesign Classic.

8. HERO — MINIMAL

minimal must be visibly calmer than Classic.

Recommended presentation characteristics:

strongly reduced decorative particle density
restrained glow layers
less ambient ornament
cleaner background
reduced decorative overlays
same Hero content and hierarchy
same Theme Pack color identity

Do not remove important text/actions.

Minimal should feel:

clean / quiet / focused

not broken or empty.

9. HERO — NIGHT

night must have a clearly deeper presentation even when used with a non-dark Theme Pack.

Important:

Night does NOT mean:

force Dark Appearance

It is a Hero presentation style.

Possible characteristics:

deeper localized Hero depth
stronger vignette/depth treatment
tighter/less diffuse glow placement
denser but restrained star/particle atmosphere
more cinematic contrast inside Hero only

It must remain readable in both:

Appearance Light
Appearance Dark

and remain Theme Pack-colored.

Do not replace the whole page with black.

10. HERO — AURORA

aurora must visibly use the theme's primary/secondary Hero glow colors in a more dynamic layered composition.

Possible characteristics:

larger flowing gradient fields
offset glow layers
more ambient color movement
richer but controlled particle atmosphere
theme-colored aurora depth

Do NOT create arbitrary fixed green/purple aurora colors.

Rose Aurora must feel Rose.

Ocean Aurora must feel Ocean.

Monochrome Aurora must remain Monochrome.

11. HERO CONTENT MUST NOT CHANGE

Hero Style must not modify business/content:

motto
schedule
current user
greeting
course data
navigation
CTA behavior

Presentation only.

12. HERO STYLE PERSISTENCE

R1/R1B behavior must remain:

Apply style
→ wait
→ navigate away
→ return
→ reload
→ same account hydration

Style remains committed.

Account switch/logout boundaries remain safe.

13. HERO STYLE + THEME MATRIX

Verify representative combinations:

classic-99 + classic
rose + minimal
rose + night
rose + aurora
ocean + aurora
ocean + night
monochrome + aurora
amber + minimal

Every combination must compose correctly.

14. HERO STYLE + MOTION

Hero visual composition and Motion Style must compose.

Example:

Hero = aurora
Motion = full

may show the full decorative aurora motion.

Hero = aurora
Motion = subtle

same visual composition, calmer motion.

Hero = aurora
Motion = reduced

same general visual identity without unnecessary continuous animation.

Hero Style must not override Motion Style.

15. GLASS STYLE — PRODUCT GOAL

Currently Glass Style is too weak or does not visibly change enough surfaces.

Repair it so the user can clearly distinguish:

clear
balanced
frosted

across ELIGIBLE application glass surfaces.

Do not make every ordinary card glass.

16. GLASS ELIGIBLE SURFACES

Audit and include shared surfaces such as appropriate:

floating bottom navigation
floating search
lecture top/header glass
sticky headers
sidebar floating/elevated sections
mobile/tablet floating chrome
modal/dialog chrome where already glass-like
My99 preview
reusable glass panels
eligible overlays/toolbars

Do not convert:

PDF pages
video contents
ordinary study content cards that are intentionally solid
destructive/status surfaces

into glass just to increase coverage.

17. GLASS — CLEAR

clear should be the most transparent/lightweight variant.

Expected direction:

lower surface opacity
lower/more restrained blur
subtle border
more background visibility
still enough contrast for text/icons

It must never become unreadably transparent.

18. GLASS — BALANCED

balanced remains the default/current production character.

It must preserve Classic parity.

Expected:

medium opacity
medium blur
standard border/depth

Default attribute remains omitted according to existing architecture.

19. GLASS — FROSTED

frosted should be clearly more opaque/blurred/depth-rich than Balanced.

Expected:

stronger blur where supported
slightly stronger surface opacity
clearer glass edge
controlled shadow/elevation

Do not make it milky white in dark mode.

Do not make Rose Frosted a pink slab.

Theme tokens continue to provide color.

20. GLASS IMPLEMENTATION ARCHITECTURE

Use shared CSS variables/tokens for glass treatment.

Prefer conceptual variables such as existing/current equivalents of:

glass surface
glass border
glass blur
glass opacity
glass shadow/elevation

If treatment variables are currently missing, adding a SMALL generic presentation variable layer is allowed.

Do NOT add theme × glass combinatorial selectors such as:

[data-app-theme="rose"][data-app-glass-style="frosted"] ...

The architecture must remain:

Theme → color
Glass Style → treatment
21. IOS 16 GLASS SAFETY

Preserve:

current iOS 16.0/16.1 fallback strategy
guarded color-mix() where used
prefers-reduced-transparency
forced-colors accessibility

Reduced-transparency preference must override decorative transparency/blur where appropriate.

22. MOTION STYLE — PRODUCT GOAL

Motion Style must create a meaningful whole-application presentation difference WITHOUT breaking functional interaction.

Canonical:

full
subtle
reduced

OS:

prefers-reduced-motion: reduce

remains the highest-priority safety behavior.

23. MOTION — FULL

full is current/default production presentation.

Preserve existing accepted premium motion.

Default attribute remains omitted.

Do not increase animation beyond current accepted production behavior.

24. MOTION — SUBTLE

subtle must visibly reduce non-essential motion application-wide.

Eligible changes may include:

smaller entrance travel distance
reduced scale effects
shorter/smoother decorative transitions
lower particle movement/intensity
reduced hover lift
calmer glow movement
restrained decorative pulse/shimmer
reduced page chrome entrance movement

Subtle must NOT simply be identical to Full with one Hero particle change.

25. MOTION — REDUCED

reduced must significantly suppress non-essential animation.

Expected:

decorative continuous animations stopped/hidden/static
unnecessary entrance translation removed
hover scale/lift removed
decorative shimmer/pulse suppressed
Hero decorative movement static or visually suppressed
transitions reduced to minimal state feedback where necessary

But preserve functionality.

26. FUNCTIONAL MOTION EXCLUSIONS

DO NOT break or disable:

native-like swipe-back tracking
drag gestures
scrolling
calendar functional navigation
tab switching logic
MCQ answer operation
flashcard reveal/flip functionality
loading/progress state communication
pull-to-refresh functionality where present
bottom navigation state logic
search expansion/collapse logic
accessibility-required state changes

A functional gesture may still track the finger even under Reduced.

You may reduce its decorative easing AFTER completion, but do not disable interaction.

27. MOTION COVERAGE

Audit at least:

Hero
StarField/particles
shared cards
buttons
tabs
navigation indicators
floating nav
search
settings rows
My99 Studio
modal/panel appearance
common decorative hover/press animation
page chrome transitions

Prefer shared variables/classes rather than patching every component.

28. MOTION CSS VARIABLES

If useful, create centralized presentation variables conceptually like:

--personalization-motion-duration
--personalization-motion-distance
--personalization-motion-scale
--personalization-motion-intensity

Names may differ.

Do not force all functional JS animations to use one unsuitable CSS duration.

Use them where presentation motion is appropriate.

29. OS REDUCED MOTION PRECEDENCE

Required precedence:

OS prefers-reduced-motion = reduce

must be at least as restrictive as user-selected:

reduced

OS safety setting must not be defeated by:

Motion Style = full
30. STARFIELD / CANVAS

Previous known limitation:

user-selected Reduced could visually suppress StarField while its RAF loop continued.

R2 should audit whether this can now be improved safely.

Preferred:

if user Motion Style = reduced:

stop/pause decorative RAF when practical
or enter a true static render path

rather than merely hiding an actively running canvas.

However:

do not introduce lifecycle instability.

If changing RAF safely is possible, implement it.

If not, keep visual suppression and document the accepted performance limitation honestly.

OS reduced-motion behavior must continue to work.

31. READING SIZE — USER EXPECTATION

Current user feedback:

Small / Default / Large appears to change little or nothing.

Repair it.

Reading Size should now have a CLEAR and CONSISTENT effect across ordinary application text, not only a tiny subset of MCQ/Flashcard surfaces.

This is a deliberate R2 product expansion of presentation coverage.

32. READING SIZE IS TYPOGRAPHY, NOT LAYOUT ZOOM

Do NOT implement Reading Size using:

browser zoom
CSS transform scale
page zoom
viewport scaling

Reading Size must affect TYPOGRAPHY.

It must not make the entire application physically zoom in/out.

33. READING SIZE COVERAGE

Reading Size should visibly influence ordinary readable text across shared application UI/content such as:

page titles
section titles
card titles
body text
lecture titles
notes text
MCQ question/explanation text
flashcard text
Q&A text
buttons with textual labels
tabs where safe
Settings labels/descriptions
Profile textual content
Calendar textual content
metadata/captions within safe limits
My99 Studio labels/descriptions

Do NOT affect:

icon geometry
image/PDF contents
video pixels
canvas drawings
fixed-size accessibility/system icons
structural layout dimensions directly
34. READING SIZE HIERARCHY

Do not flatten typography.

Large must still preserve:

title > section heading > body > caption

Small must preserve the same hierarchy.

Do not make everything one font size.

35. READING SIZE VALUES

Existing accepted multipliers were approximately:

small = 0.94
default = 1.00
large = 1.10

Audit actual current implementation.

You MAY preserve these exact multipliers if the problem is coverage rather than magnitude.

If user-visible testing proves those values are too subtle across current typography, you may adjust them conservatively.

Any multiplier change must:

remain comfortable
avoid clipping
preserve layout
be documented exactly
be tested on mobile/iPad/desktop contracts

Do NOT invent extreme values.

36. DEVICE TEXT SCALE COMPOSITION

Existing device-local:

app_text_scale

remains separate and device-local.

Reading Size is account personalization.

Final effective typography must compose both correctly.

Conceptually:

effective text size
=
base typography
× device app_text_scale
× account Reading Size multiplier

Do not:

ignore app_text_scale
double-apply either multiplier
sync app_text_scale
move app_text_scale into PersonalizationConfigV1
37. REM VS INHERITED FONT AUDIT

Audit actual typography implementation carefully.

Many Tailwind classes may emit explicit rem sizes that do NOT inherit a parent font-size.

Do not assume setting one wrapper font size automatically affects all text.

Determine the real reason Reading Size currently appears inactive.

Use a robust shared strategy.

Possible valid approaches include:

centralized semantic typography variables/classes
safe scoped typography multipliers
reusable text-role classes
other maintainable CSS architecture

Do NOT globally mutate HTML root font-size if that would unintentionally scale layout dimensions, paddings, widths, icon sizes, or break responsive geometry.

38. READING SIZE EXCLUSIONS

Keep appropriate minimum/readability bounds for:

compact metadata pills
code-like IDs
badges
tab rails with constrained mobile width

But "excluded" must not become an excuse for most of the app to remain unchanged.

If a constrained area cannot take full 1.10 scaling:

use a safe clamped/role-specific multiplier.

39. MOBILE TAB SAFETY

The Lecture tabs:

PDF
Notes
MCQ
Anki
Video
Q&A

must not become overlapping/broken under Large.

Reading Size should affect them only within safe layout constraints.

Use responsive/clamped typography if required.

Do not restore the old Arabic overlap problem.

40. IPHONE / IPAD / DESKTOP

Verify Reading Size and Glass/Motion/Hero behaviors across:

iPhone/mobile
iPad/tablet
desktop/PWA

Do not create platform-specific behavior unless necessary.

41. ARABIC / RTL

All four axes must remain safe in:

English
Arabic
RTL

Large Reading Size must be checked for:

Arabic line wrapping
Settings rows
tabs
lecture headers
cards

Do not introduce fixed-height clipping.

42. MY99 STUDIO PREVIEW

The contained Draft preview must meaningfully preview:

Hero Style
Glass Style
Motion Style
Reading Size

without leaking Draft globally.

Expected:

change Draft
→ preview changes locally
→ rest of application unchanged
→ Apply
→ committed runtime changes globally

Cancel:

preview returns to committed
global remains committed

Reset:

factory Draft preview only until Apply according to existing semantics.

43. PREVIEW ACCURACY

Do not create a fake preview disconnected from runtime behavior.

Preview should reuse the same presentation variables/attributes where practical.

For example:

data-personalization-preview-hero
data-personalization-preview-glass
data-personalization-preview-motion
data-personalization-preview-reading

or actual existing equivalents.

The preview may be simplified, but must visually communicate the real difference.

44. DRAFT MUST REMAIN GLOBALLY INERT

Critical regression:

Changing:

Hero
Glass
Motion
Reading

inside My99 Studio WITHOUT Apply must produce:

0 global presentation mutation
0 cloud PUT
0 local committed persistence

Only the contained preview may change.

45. APPLY

Apply remains one combined account personalization Apply.

Do NOT create separate persistence writes for each axis.

Existing local durability/cloud sync semantics remain.

R2 only improves how committed values render.

46. CANCEL / RESET

Preserve existing semantics.

Do not make App-wide styles change merely because Reset Draft was selected.

Global committed state changes only after Apply.

47. ACCOUNT SWITCH / LOGOUT

R1B boundaries remain mandatory.

A → B:

safe Classic/default boundary
→ B committed presentation

Logout:

Classic/default presentation

Do not regress this while expanding selectors.

48. THEME COMPOSITION

Every R2 axis must compose with all Theme Packs.

Especially:

Rose + Frosted
Ocean + Clear
Violet + Frosted
Monochrome + Aurora
Rose + Night + Reduced
Amber + Minimal + Large

No style should restore Classic colors.

49. APPEARANCE COMPOSITION

Test representative:

Light + Rose + Frosted
Dark + Rose + Frosted
Light + Ocean + Clear
Dark + Ocean + Clear

Glass treatment changes.

Theme owns color.

Appearance owns light/dark mode.

50. DEVICE TEXT SCALE ORTHOGONALITY

Test representative combinations:

app_text_scale = 1.0 + Reading Large
app_text_scale = 1.1 + Reading Large
app_text_scale = 0.9 + Reading Small

Ensure deterministic multiplicative/defined composition.

51. NO NEW CLOUD FIELDS

R2 must require ZERO schema changes.

Expected:

PersonalizationConfigV1 change: NO
PersonalizationLocalEnvelopeV2 schema change: NO
Workers KV record schema change: NO
Express personalization API shape change: NO
Cloudflare Worker schema change: NO

Existing fields already contain these axes.

52. NO APP ICON

Prompt 23 App Icon architecture is separate.

Do not implement Prompt 24.

No Swift.

No Xcode changes.

No asset changes.

53. NO SUBJECT VISIBILITY

Do not add:

hiddenSubjectIds
semester visibility
hide buttons
Semester 1
Semester 2

That is R3.

54. PERFORMANCE

R2 must not add:

CSS mutation loops
periodic presentation polling
frequent MutationObserver work across the entire subtree
forced layout loops
page remounts
reloads
unnecessary RAF loops

Use document attributes + CSS/shared component state.

55. STARFIELD PERFORMANCE

If StarField observes only document presentation attributes:

keep observation narrow.

Do not observe the whole DOM subtree unnecessarily.

Disconnect observers correctly.

56. ACCESSIBILITY

Preserve:

prefers-reduced-motion
prefers-reduced-transparency
forced colors
focus-visible
sufficient text contrast
touch targets
keyboard navigation

Large Reading Size must not reduce touch targets.

57. TEST — HERO STYLE

Add focused tests proving all exact IDs create distinct presentation contracts:

classic
minimal
night
aurora

At least verify distinct selector/variable behavior.

Do not rely only on strings existing in a catalog.

58. TEST — HERO + THEME

Verify Theme semantic colors remain source of Hero colors under every HeroStyle.

No hardcoded Classic palette inside non-Classic Hero selectors.

59. TEST — GLASS

Verify:

clear != balanced != frosted

in actual treatment variables/rules.

Test representative shared surfaces consume the treatment system.

60. TEST — GLASS DEFAULT

Balanced remains factory default and omitted document attribute.

Classic parity remains.

61. TEST — MOTION

Verify:

full
subtle
reduced

produce distinct presentation behavior.

Test at least several shared animation classes/surfaces, not only Hero.

62. TEST — OS REDUCED MOTION

OS reduced-motion must override or equal Reduced restrictions.

Full must not defeat OS accessibility preference.

63. TEST — FUNCTIONAL GESTURES

Use existing source contracts/tests where possible to prove R2 does not disable:

swipe back
navigation logic
tab actions
MCQ interaction
flashcard interaction

Do not need full E2E device gestures if unavailable in Replit.

64. TEST — READING SIZE GLOBAL COVERAGE

Test representative typography from:

Home
Subject/Module
Lecture
Notes
MCQ
Flashcards
Q&A
Calendar
Settings/My99
navigation/chrome where safe

Prove Small/Default/Large have real semantic typography effect.

65. TEST — TYPOGRAPHY HIERARCHY

Large/Small must preserve relative hierarchy.

Do not flatten title/body/caption relationships.

66. TEST — app_text_scale COMPOSITION

Retain explicit regression proving the effective account/device typography composition.

67. TEST — PREVIEW

Draft changes:

affect preview
do not affect global document committed attributes

Apply:

global attributes update

Cancel:

preview returns to committed
68. TEST — R1/R1B REGRESSION

Must continue passing:

tests/personalization-r1-theme-coverage.test.ts
tests/personalization-r1b-hydration-boundary.test.ts

No theme coverage or account-boundary regression.

69. PERSONALIZATION MATRIX

Entering baseline:

170 passed
0 failed
0 skipped
0 cancelled

Run:

npx tsx --test tests/personalization*.test.ts tests/personalization-runtime.test.tsx

No regression.

70. FEATURE SURFACE REGRESSIONS

Run the existing:

tests/personalization-lecture-surfaces.test.ts
tests/personalization-calendar-surfaces.test.ts
tests/personalization-mcq-flashcard-surfaces.test.ts

No regression.

71. NAVIGATION REGRESSION

Run:

npx tsx --test tests/client-navigation.test.ts

Accepted baseline:

4 passed
0 failed
72. AUTH / SECURITY

Run:

npm run test:auth
npm run security:check

Accepted:

Auth: 4 passed
Security: 13 passed
73. BUILD

Run:

npm run build

Must PASS.

74. TYPESCRIPT

Run:

npm run typecheck

Accepted unrelated baseline only:

7 missing .webp declarations
src/features/modules/moduleVisuals.ts

No new R2 errors.

75. TARGETED ESLINT

Run targeted ESLint across all R2-modified TS/TSX/tests.

No new issues.

76. PDF / MEDIA SAFETY

If shared Lecture/PDF chrome is modified:

run:

npx tsx --test tests/pdf-access.test.ts

Accepted historical baseline:

2 passed
1 known failure:
pdf_download_expiry_invalid

Do not alter actual PDF content sizing through Reading Size.

77. VISUAL CHECKS IN REPLIT

Where browser preview is available, inspect representative combinations.

At minimum:

Hero
Rose + Classic
Rose + Minimal
Rose + Night
Rose + Aurora
Glass
Clear
Balanced
Frosted
Motion
Full
Subtle
Reduced
Reading
Small
Default
Large

Check at least:

Home
Subject page
Lecture page
Settings/My99

Use:

mobile-sized viewport
tablet-sized viewport
desktop-sized viewport

Do not claim native iOS-specific verification from Replit.

78. USER-VISIBLE PASS CRITERION

This is critical.

Do NOT mark R2 PASS merely because:

attributes exist
tests pass

The browser/runtime inspection must establish that the difference between options is actually perceptible.

The report must state what is visibly different.

79. DO NOT OVERDO

If the distinction is technically present but still difficult to perceive:

improve it before PASS.

If the distinction breaks hierarchy/layout:

reduce it before PASS.

Target:

obvious enough to trust the setting
subtle enough to remain premium
80. GIT REVIEW

Run:

git status --short
git diff --stat
git diff --check

Report exact files.

81. .agents/* ABSOLUTE PROHIBITION

Run:

git status --short -- .agents
git diff -- .agents

No output.

No .agents/* change.

82. MANDATORY FINAL IMPLEMENTATION REPORT

Return exactly:

99'S GUIDE — PERSONALIZATION QUALITY REPAIR R2 REPORT
FUNCTIONAL HERO / GLASS / MOTION / READING AXES
1. Status

PASS / BLOCKED

2. User-Visible Result

Explain what the user can now visibly distinguish.

3. Root Causes Found

For:

Hero Style
Glass Style
Motion Style
Reading Size
4. Files Modified
File	Exact Change	Why
5. Configuration Contract

Confirm exact canonical values.

6. Hero Style — Final Architecture

Explain presentation ownership.

7. Classic Hero

Report visible behavior.

8. Minimal Hero

Report visible behavior.

9. Night Hero

Report visible behavior.

10. Aurora Hero

Report visible behavior.

11. Hero × Theme Composition

Report representative combinations.

12. Hero × Motion Composition

Report.

13. Hero Persistence

Confirm R1/R1B lifecycle remains.

14. Glass Style — Final Architecture

Explain treatment vs Theme color.

15. Clear Glass

Report exact visible treatment.

16. Balanced Glass

Report exact visible treatment.

17. Frosted Glass

Report exact visible treatment.

18. Glass Surfaces Covered

List exact shared components/surfaces.

19. Reduced Transparency / iOS 16 Safety

Report.

20. Motion Style — Final Architecture

Explain.

21. Full Motion

Report.

22. Subtle Motion

Report.

23. Reduced Motion

Report.

24. OS Reduced Motion Precedence

Report.

25. Functional Motion Exclusions

List gestures/interactions preserved.

26. StarField / RAF Behavior

State exactly what Reduced now does.

If RAF remains active while hidden:
state limitation honestly.

27. Reading Size — Final Architecture

Explain how typography scaling works.

28. Reading Small

Report exact multiplier.

29. Reading Default

Report exact multiplier.

30. Reading Large

Report exact multiplier.

31. Reading Coverage

List page/component categories affected.

32. Reading Exclusions

List intentional exclusions.

33. Typography Hierarchy

Explain preservation.

34. app_text_scale Composition

Print exact effective formula/implementation semantics.

35. Mobile Tab / Constrained Layout Safety

Report.

36. Arabic / RTL

Report.

37. My99 Preview

Explain how each Draft axis previews locally.

38. Draft Global Inertness

Confirm no global mutation before Apply.

39. Apply / Cancel / Reset

Confirm semantics unchanged.

40. Account Switch / Logout

Confirm R1B boundaries remain.

41. Theme / Appearance Composition

Report representative combinations.

42. Cloud Schema Impact

State exactly:

PersonalizationConfigV1 change: NO
PersonalizationLocalEnvelopeV2 schema change: NO
Workers KV record schema change: NO
Express personalization API change: NO
Cloudflare Worker schema change: NO

43. R3 Scope Safety

State:

Subject hiding: NOT IMPLEMENTED IN R2
Semester controls: NOT IMPLEMENTED IN R2

44. App Icon Scope Safety

State:

APP ICON IMPLEMENTATION: NOT STARTED IN R2

45. Tests Added / Updated

List exact files/invariants.

46. Hero Tests

Report exact count.

47. Glass Tests

Report exact count.

48. Motion Tests

Report exact count.

49. Reading Tests

Report exact count.

50. Preview Tests

Report exact count.

51. R1 Theme Coverage Regression

Report.

52. R1B Account Boundary Regression

Report.

53. Full Personalization Matrix

Report exact:

passed
failed
skipped
cancelled

Entering baseline:

170 / 0 / 0 / 0

54. Feature Surface Regressions

Report Lecture/Calendar/MCQ-Flashcard results.

55. Navigation Regression

Report.

56. Auth Regression

Report.

57. Security Regression

Report.

58. PDF Baseline

Report if applicable.

59. Build

Report.

60. TypeScript

Report accepted baseline/no new errors.

61. ESLint

Report.

62. Visual Verification

Report ACTUAL inspected viewports and ACTUAL visible differences.

Do not claim a viewport/device not inspected.

63. Performance Review

Confirm no polling/reload/remount/layout loop.

64. Git Review

Report:

git status --short
git diff --stat
git diff --check

65. .agents/* Verification

Report:

git status --short -- .agents
git diff -- .agents

Then state exactly:

No .agents/* file was created or modified.

66. R3 Readiness

If R2 is genuinely complete, state exactly:

HERO STYLE FUNCTIONAL DIFFERENTIATION: VERIFIED

GLASS STYLE FUNCTIONAL DIFFERENTIATION: VERIFIED

MOTION STYLE FUNCTIONAL DIFFERENTIATION: VERIFIED

READING SIZE FULL-APP TYPOGRAPHY COVERAGE: VERIFIED

MY99 DRAFT PREVIEW ACCURACY: VERIFIED

R1 THEME COVERAGE: PRESERVED

R1B ACCOUNT BOUNDARIES: PRESERVED

ACCOUNT PERSONALIZATION CLOUD SCHEMA: UNCHANGED

R3 HOME SUBJECT VISIBILITY / SEMESTER CONTROLS: APPROVED TO BEGIN

End EXACTLY:

PERSONALIZATION QUALITY REPAIR R2 STATUS: COMPLETE — READY FOR R3

Otherwise:

PERSONALIZATION QUALITY REPAIR R2 STATUS: BLOCKED — REPAIR REQUIRED