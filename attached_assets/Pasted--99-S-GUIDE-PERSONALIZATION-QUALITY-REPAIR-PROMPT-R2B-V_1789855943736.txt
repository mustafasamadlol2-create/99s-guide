# 99'S GUIDE — PERSONALIZATION QUALITY REPAIR
# PROMPT R2B
## VISUAL / BEHAVIOR CLOSURE FOR HERO + GLASS + MOTION + READING
### VERIFY R2 ACTUALLY LOOKS DIFFERENT IN REAL RENDERED UI
### REPAIR ONLY IF A REAL DEFECT IS OBSERVED
### ACCEPT PRE-EXISTING REPOSITORY BASELINES
### DO NOT BEGIN R3
### DO NOT MODIFY `.agents/*`

R2 implementation is provisionally complete.

The following entering baselines are ACCEPTED and must NOT block R2 closure:

1. Root TypeScript:
   - 7 existing missing `.webp` module errors
   - all under `src/features/modules/moduleVisuals.ts`

2. PDF baseline:
   - `tests/pdf-access.test.ts`
   - 2 pass / 1 known failure
   - known failure: `pdf_download_expiry_invalid`

3. Repository-wide unrelated lint:
   - pre-existing unrelated errors outside the R2 personalization scope

DO NOT fix these in R2B.

The only remaining R2 acceptance issue is visual/behavior verification of:

- Hero Style
- Glass Style
- Motion Style
- Reading Size

R2B must prove these are perceptibly different in rendered application-equivalent UI.

---

# 1. DO NOT REDESIGN R2

Do not rewrite R2 architecture.

Current R2 implementation includes:

- Hero presentation variants
- Glass treatment variants
- Motion presentation variables
- Reduced StarField static mode
- Reading semantic typography scaling
- My99 contained preview integration

Audit and verify first.

Repair only if actual rendered behavior proves one of these is not meaningful or is broken.

---

# 2. ACCEPTED R2 SOURCE BASELINE

Canonical values remain:

```text
Hero:
classic
minimal
night
aurora

Glass:
clear
balanced
frosted

Motion:
full
subtle
reduced

Reading:
small
default
large

Do not add/rename values.

3. VISUAL VERIFICATION MUST NOT DEPEND ON REAL USER AUTH

The previous Replit preview stopped at the launch/auth screen.

Do NOT:

create an auth bypass
weaken authentication
use production credentials
use real user credentials
disable route security
modify production auth

Instead, inspect existing repository test/browser capabilities and choose the safest TEST-ONLY way to render representative authenticated application surfaces.

Preferred options, in order:

A. Existing component/browser test harness with mocked providers/data.

B. Existing Storybook/component-preview mechanism if already present.

C. A temporary test-only Vite/browser harness outside production routing that renders actual production components with deterministic mock data.

D. If necessary, a temporary /tmp visual harness using the actual built CSS and representative DOM/classes.

Do NOT add a production-accessible debug page.

Do NOT commit a permanent QA route.

Any temporary harness must be removed before final closure unless it belongs under tests and is clearly test-only.

4. USE ACTUAL PRODUCTION CSS

Visual verification must use:

src/index.css

and the actual R2 presentation selectors/variables.

Do not visually simulate the values with unrelated fake CSS.

5. USE REPRESENTATIVE REAL COMPONENTS WHERE PRACTICAL

Prefer actual production components for at least:

Home/Hero
Lecture Detail header/tabs
floating navigation/search
Settings/My99 or representative typography surface

If full component dependencies make one surface impractical, use exact production classes/semantic variables in a test-only fixture and state that limitation honestly.

6. VIEWPORTS

Verify at least these three rendered viewport classes:

Mobile:
390 × 844

Tablet:
1024 × 1366

Desktop:
1440 × 900

Small deviations are acceptable if the browser harness requires them.

Report exact viewport sizes actually used.

7. HERO VISUAL VERIFICATION

For the SAME Theme Pack, render:

classic
minimal
night
aurora

Use:

theme = rose

for the primary comparison.

Required user-visible distinction:

Classic

Baseline premium Hero.

Minimal

Must visibly feel quieter:

less decorative density
less glow/depth
less visual noise
Night

Must visibly feel deeper:

stronger local Hero depth/vignette
clearly different contrast/depth from Classic
must NOT force whole app Dark Appearance
Aurora

Must visibly feel more ambient/dynamic:

stronger layered semantic glow composition
clearly different from Classic and Night
Rose colors remain Rose-derived

If any two variants are visually almost indistinguishable:
repair before PASS.

8. HERO × THEME CHECK

Repeat representative screenshots/render checks:

rose + aurora
ocean + aurora
monochrome + aurora

Verify Aurora does not introduce fixed Rose/green/purple colors.

Theme owns color.

9. HERO + LIGHT / DARK

Check:

Rose + Light + Night
Rose + Dark + Night

Ocean + Light + Aurora
Ocean + Dark + Aurora

Night is Hero presentation only.

It must not force global dark mode.

10. GLASS VISUAL VERIFICATION

Render the SAME eligible glass surface with:

clear
balanced
frosted

At minimum use:

floating bottom navigation
floating search
lecture header

Required:

Clear

Visibly more transparent/lighter material.

Balanced

Accepted baseline.

Frosted

Visibly denser/more blurred/deeper.

If Clear vs Balanced or Balanced vs Frosted are difficult to distinguish:
repair the treatment variables conservatively.

Do NOT exaggerate them into unreadable glass.

11. GLASS + THEME

Check at least:

Rose + Clear
Rose + Frosted
Ocean + Frosted

Verify Glass changes treatment, not palette ownership.

12. REDUCED TRANSPARENCY

Where test tooling permits, emulate:

prefers-reduced-transparency: reduce

or inspect exact fallback rule if browser emulation is unavailable.

The accessibility fallback must remain readable.

13. MOTION VISUAL / BEHAVIOR VERIFICATION

The R2 report says Subtle:

"Slows shared entrance motion."

This wording is suspicious.

Subtle should feel LESS MOTION, not simply LONGER MOTION.

Audit actual variables and behavior.

A valid Subtle implementation should generally reduce one or more of:

travel distance
scale delta
decorative intensity
repeated animation
hover lift

Duration may be adjusted, but making motion longer must not make it more noticeable.

If Subtle is primarily slower rather than calmer:
repair it.

14. MOTION — FULL

Verify Full remains accepted production baseline.

Do not increase it.

15. MOTION — SUBTLE

Verify visibly:

smaller entrance distance
smaller scale/lift
reduced decorative intensity
calmer Hero motion
shared chrome/card/tab motion reduced

It must be distinguishable from Full.

16. MOTION — REDUCED

Verify:

no unnecessary entrance translation
decorative movement stopped/static
hover lift/scale removed where presentation-only
StarField RAF is actually cancelled
static frame remains visually coherent

Functional interactions must still work.

17. STARFIELD ACTUAL BEHAVIOR

Instrument or test the StarField loop if practical.

Prove:

Full:
RAF active when eligible

Subtle:
RAF active but visually calmer

Reduced:
RAF cancelled / static frame

OS reduced motion:
static/reduced path regardless of user Full

Do not infer this only from CSS.

18. FUNCTIONAL MOTION SAFETY

Verify R2 did not break:

swipe-back
tab activation
lecture section switching
search open/close
bottom nav actions
MCQ answering
flashcard reveal/flip logic

Source-contract verification is acceptable for gesture logic where browser fixture cannot simulate full touch behavior.

19. READING SIZE — CRITICAL VISUAL VERIFICATION

Render the SAME real text surfaces under:

small
default
large

Required difference must be clearly visible.

Use at least:

Home title/body
Subject card title/metadata
Lecture title/header
Notes text/card text
MCQ question/explanation
Flashcard front/back
Q&A text
Calendar text
Settings/My99 labels/descriptions

If many of these remain visually identical:
R2 is NOT complete.

20. AUDIT REAL CLASS COVERAGE

Search actual application typography.

Classify common typography into:

A. semantic text roles affected by Reading Size
B. Tailwind explicit text sizes bypassing Reading Size
C. intentional constrained exceptions

Examples to inspect:

text-xs
text-sm
text-base
text-lg
text-xl
text-2xl
text-3xl
text-4xl

Do NOT blindly replace every Tailwind class.

But determine whether major application text still bypasses the new semantic Reading system.

If the majority of titles/body/settings still use explicit unaffected utilities:
repair coverage through maintainable shared typography architecture.

21. FULL-APP READING MEANS FULL-APP ENOUGH

The user explicitly complained that Reading Size appeared to do nothing.

R2B must ensure a user can see the difference on normal screens without hunting for MCQ/Flashcard-only text.

At minimum the following must visibly respond:

Home
Subject/Module
Lecture
Settings/My99

and study content:

MCQ
Flashcard
Q&A
Notes
22. DO NOT USE ROOT ZOOM

Still prohibited:

browser zoom
transform scale
viewport zoom
changing HTML root rem base if it scales structural geometry unexpectedly

Typography only.

23. READING MULTIPLIERS

Entering implementation reports:

small   = 0.94
default = 1.00
large   = 1.10

First test these visually.

If they are clearly perceptible with the expanded coverage:
keep them.

If not perceptible enough:
you MAY adjust conservatively.

Suggested maximum direction only if truly required:

small   ≈ 0.92–0.94
default = 1.00
large   ≈ 1.10–1.12

Do not exceed this range without a compelling layout reason.

Report exact final values.

24. app_text_scale COMPOSITION

Verify actual computed behavior.

Expected:

effective typography
=
base semantic role
× app_text_scale
× personalization reading multiplier

Use representative checks:

1.0 × Large
1.1 × Large
0.9 × Small

Do not double-apply.

25. MOBILE OVERFLOW

At mobile viewport verify Large does NOT break:

Lecture tab rail
header titles
Home subject cards
bottom navigation labels if present
Settings rows
Arabic wrapping

No fixed-height clipping.

26. ARABIC / RTL

If localization can be rendered in the test harness, inspect at least:

Mobile Arabic + Reading Large

for:

Settings/My99
Lecture header/tabs
Subject cards

If the harness cannot render actual localized data:
perform source/layout contract audit and state limitation honestly.

Do not claim visual Arabic verification unless actually rendered.

27. MY99 PREVIEW

Render Draft preview for:

all four Hero styles
all three Glass styles
all three Motion styles
all three Reading sizes

Ensure preview differences are actually visible.

Do not require preview to reproduce entire app.

It must accurately communicate the axis.

28. PREVIEW VS RUNTIME CONSISTENCY

The preview and committed runtime should share the same treatment variables/logic where practical.

Verify no case where preview shows a strong difference but committed application shows none.

29. DRAFT INERTNESS

Retain existing tests:

Draft changes:

preview only
no document committed attr change
no persistence
no cloud PUT
30. R1 / R1B MUST REMAIN CLOSED

Run:

tests/personalization-r1-theme-coverage.test.ts
tests/personalization-r1b-hydration-boundary.test.ts

Both must PASS.

Do not alter account boundaries.

31. ACCEPTED PRE-EXISTING FAILURES

Do NOT block R2B for:

7 existing moduleVisuals .webp TypeScript errors

provided:

0 new R2/R2B TypeScript errors

Do NOT block for:

pdf_download_expiry_invalid

provided:

no additional PDF failures

Do NOT block for unrelated repository-wide lint baselines.

These are explicitly accepted for R2 closure.

32. FULL PERSONALIZATION MATRIX

R2 added 5 tests.

Expected personalization baseline should now be at least:

175 passed
0 failed

when running ONLY the personalization matrix.

Run exactly:

npx tsx --test tests/personalization*.test.ts tests/personalization-runtime.test.tsx

Report exact:

passed
failed
skipped
cancelled

This command should NOT be described as failed because of pdf-access.test.ts, because that file is not part of this pattern unless repository naming behaves unexpectedly.

If it includes PDF somehow:
explain exact reason.

33. R2 FOCUSED TEST

Run:

npx tsx --test tests/personalization-r2-presentation.test.ts

Expected:
5 pass unless expanded.

34. FEATURE SURFACE REGRESSIONS

Run:

tests/personalization-lecture-surfaces.test.ts
tests/personalization-calendar-surfaces.test.ts
tests/personalization-mcq-flashcard-surfaces.test.ts

PASS.

35. NAVIGATION

Run:

npx tsx --test tests/client-navigation.test.ts

Expected:
4 pass.

36. AUTH / SECURITY

Run:

npm run test:auth
npm run security:check

Expected accepted:
4 auth
13 security.

37. BUILD

Run:

npm run build

Must PASS.

38. TYPESCRIPT

Run:

npm run typecheck

Accepted:

7 known .webp baseline errors
0 new R2/R2B errors

This is a PASS FOR R2B if no new errors exist.

Do not mark R2B blocked solely because the root command exits nonzero from those seven accepted baseline errors.

39. PDF BASELINE

Run only if relevant shared Lecture/PDF source changed in R2B.

Accepted:

2 passed
1 known failure:
pdf_download_expiry_invalid

This is accepted R2B baseline.

40. TARGETED ESLINT

Run lint on R2/R2B modified source and tests.

Must have:

0 R2/R2B lint errors

Ignore unrelated repository-wide baseline.

41. TEMPORARY VISUAL HARNESS CLEANUP

If a temporary visual harness was created outside tests:

remove it before closure.

If a committed test-only browser fixture was added:

it must be clearly under test infrastructure and not reachable from production routing.

Report exact disposition.

42. NO .replit MODIFICATION IN R2B

Do not modify .replit.

The existing .replit modification was reported as pre-existing.

If it is still modified:
state:

PRE-EXISTING / NOT TOUCHED BY R2B

Do not include it as an R2B implementation change.

43. NO R3 WORK

Do not implement:

subject hide/show
semester controls
config migration for Home visibility

R3 remains separate.

44. NO APP ICON

Do not implement Prompt 24.

45. NO CLOUD SCHEMA CHANGE

State exactly:

PersonalizationConfigV1 change: NO
PersonalizationLocalEnvelopeV2 schema change: NO
Workers KV record schema change: NO
Express personalization API change: NO
Cloudflare Worker schema change: NO
46. .agents/*

ABSOLUTE.

Run:

git status --short -- .agents
git diff -- .agents

No output.

47. MANDATORY FINAL REPORT

Return exactly:

99'S GUIDE — PERSONALIZATION QUALITY REPAIR R2B REPORT
VISUAL / BEHAVIOR CLOSURE
1. Status

PASS / BLOCKED

2. Why R2B Was Required

Explain that R2 source implementation passed but authenticated visual proof was missing.

3. Accepted Baselines

State explicitly:

7 .webp type errors accepted
one PDF expiry failure accepted
unrelated global lint baseline accepted
4. Visual Verification Method

Explain exactly how rendered UI was produced without weakening authentication.

5. Temporary/Test Harness

State exact files and whether removed or test-only.

6. Viewports Actually Inspected

List exact sizes.

7. Hero Classic — Visual Result

Report.

8. Hero Minimal — Visual Result

Report.

9. Hero Night — Visual Result

Report.

10. Hero Aurora — Visual Result

Report.

11. Hero Distinction Decision

State whether all four are clearly perceptible.

12. Hero × Theme

Report Rose/Ocean/Monochrome combinations.

13. Hero Light/Dark

Report.

14. Glass Clear — Visual Result

Report.

15. Glass Balanced — Visual Result

Report.

16. Glass Frosted — Visual Result

Report.

17. Glass Distinction Decision

Report.

18. Motion Full — Behavior

Report.

19. Motion Subtle — Behavior

Report actual distance/scale/duration/intensity differences.

20. Subtle Motion Audit

Answer explicitly:

Is Subtle genuinely calmer, or merely slower?

21. Motion Reduced — Behavior

Report.

22. StarField RAF

Report actual Full/Subtle/Reduced/OS behavior.

23. Functional Interaction Safety

Report.

24. Reading Small — Visual Result

Report exact multiplier.

25. Reading Default — Visual Result

Report.

26. Reading Large — Visual Result

Report exact multiplier.

27. Reading Coverage

Report actual visible response for:

Home
Subject
Lecture
Notes
MCQ
Flashcard
Q&A
Calendar
Settings/My99
28. Explicit Typography Bypass Audit

Report major text-* categories that still bypass Reading Size and classify them as:

repaired
intentional exception
29. Typography Hierarchy

Report.

30. app_text_scale Composition

Report actual computed semantics.

31. Mobile Overflow

Report.

32. Arabic / RTL

State what was actually visually inspected vs source-only audited.

33. My99 Preview

Report actual visible differences.

34. Preview / Runtime Consistency

Report.

35. Source Repairs Made During R2B

List only real R2B fixes.

If none:
state:

NO ADDITIONAL PRODUCTION SOURCE REPAIR REQUIRED.

36. Files Modified

List exact R2B files only.

37. R1 Theme Coverage

Report regression.

38. R1B Account Boundary

Report regression.

39. R2 Focused Tests

Report exact count.

40. Full Personalization Matrix

Report exact:
passed
failed
skipped
cancelled

Expected at least:
175 / 0 / 0 / 0

41. Feature Surface Regressions

Report.

42. Navigation Regression

Report.

43. Auth Regression

Report.

44. Security Regression

Report.

45. Build

Report.

46. TypeScript

State:
accepted baseline errors
new R2/R2B errors

47. PDF Baseline

Report if run.

48. Targeted ESLint

Report R2/R2B result.

49. Performance

Report no polling/reload/remount/layout loop.

50. Cloud Schema Impact

State exactly:

PersonalizationConfigV1 change: NO
PersonalizationLocalEnvelopeV2 schema change: NO
Workers KV record schema change: NO
Express personalization API change: NO
Cloudflare Worker schema change: NO

51. R3 Scope Safety

State:

Subject hiding: NOT IMPLEMENTED
Semester controls: NOT IMPLEMENTED

52. App Icon Safety

State:

APP ICON IMPLEMENTATION: NOT STARTED

53. Git Review

Report:
git status --short
git diff --stat
git diff --check

Classify .replit separately if still modified.

54. .agents/* Verification

Report:
git status --short -- .agents
git diff -- .agents

Then state exactly:

No .agents/* file was created or modified.

55. R3 Readiness

If the visual/behavior gates genuinely pass, state exactly:

HERO STYLE VISUAL DIFFERENTIATION: VERIFIED

GLASS STYLE VISUAL DIFFERENTIATION: VERIFIED

MOTION STYLE BEHAVIORAL DIFFERENTIATION: VERIFIED

SUBTLE MOTION IS GENUINELY CALMER: VERIFIED

REDUCED MOTION STARFIELD RAF: VERIFIED

READING SIZE VISIBLE FULL-APP COVERAGE: VERIFIED

MY99 PREVIEW / RUNTIME CONSISTENCY: VERIFIED

R1 THEME COVERAGE: PRESERVED

R1B ACCOUNT BOUNDARIES: PRESERVED

ACCOUNT PERSONALIZATION CLOUD SCHEMA: UNCHANGED

R2 FUNCTIONAL PERSONALIZATION AXES: CLOSED AND APPROVED

R3 HOME SUBJECT VISIBILITY / SEMESTER CONTROLS: APPROVED TO BEGIN

End EXACTLY:

PERSONALIZATION QUALITY REPAIR R2B STATUS: COMPLETE — READY FOR R3

Otherwise:

PERSONALIZATION QUALITY REPAIR R2B STATUS: BLOCKED — REPAIR REQUIRED