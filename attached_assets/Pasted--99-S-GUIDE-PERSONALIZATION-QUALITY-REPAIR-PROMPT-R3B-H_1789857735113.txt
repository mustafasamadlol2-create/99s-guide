# 99'S GUIDE — PERSONALIZATION QUALITY REPAIR
# PROMPT R3B
## HOME SUBJECT VISIBILITY + SEMESTER CONTROLS IMPLEMENTATION
### PERSONALIZATION CONFIG V2
### LOCAL V1 → V2 MIGRATION
### FUTURE CLOUD V2 CONTRACT
### HOME-ONLY VISIBILITY
### ATOMIC DRAFT / APPLY / CANCEL / RESET
### SAME-ACCOUNT HOME HYDRATION STABILITY
### DO NOT DEPLOY CLOUD INFRASTRUCTURE
### DO NOT IMPLEMENT APP ICON
### DO NOT BEGIN R4
### DO NOT MODIFY `.agents/*`

R3A is CLOSED AND APPROVED.

The approved architecture is:

```text
PersonalizationConfigV1
→ frozen legacy contract used only for migration

PersonalizationConfigV2
→ new authoritative runtime/write/cloud contract

Entering personalization test baseline:

175 passed
0 failed
0 skipped
0 cancelled

The following prior work is CLOSED and must remain preserved:

R1 full Theme Pack coverage.
R1 Hero theme persistence.
R1B account-switch/logout Classic boundary.
R1B same-account presentation hydration stability.
R2/R2B Hero / Glass / Motion / Reading behavior.
My99 Draft remains globally inert.
Apply remains one atomic account-personalization operation.
Cloud sync infrastructure remains NOT deployed.

This prompt is a PRODUCTION IMPLEMENTATION task.

Implement R3 completely.

1. CANONICAL SUBJECT SET

The exact canonical subject IDs remain:

ID
NT
RM
CA
PHC
ImD
SSC

Repository names verified by R3A:

ID  — Infectious Diseases
NT  — Nutrition
RM  — Research Methodology
CA  — Clinical Attachment
PHC — Public Health Care
ImD — Immune Disturbances
SSC — Student Selected Component

Do not rename IDs.

Do not persist localized subject names.

2. SEMESTER MEMBERSHIP

Exact immutable product mapping:

Semester 1 only
NT
RM
PHC
Semester 2 only
ImD
SSC
Both semesters
ID
CA

This mapping must be represented centrally in code.

Do not infer semester membership from localized labels.

3. PRODUCT BEHAVIOR

R3 adds exactly:

Manual Hide / Show for every canonical subject.
Semester 1 Home visibility toggle.
Semester 2 Home visibility toggle.

These settings affect:

HOME SUBJECT CARDS ONLY

They must NOT disable the underlying subject.

4. EFFECTIVE VISIBILITY

Implement one authoritative pure visibility rule:

effectiveVisible(subject)
=
NOT manuallyHidden(subject)
AND
semesterAllows(subject)

Where:

semesterAllows(ID) = true
semesterAllows(CA) = true

semesterAllows(NT)  = semester1Visible
semesterAllows(RM)  = semester1Visible
semesterAllows(PHC) = semester1Visible

semesterAllows(ImD) = semester2Visible
semesterAllows(SSC) = semester2Visible

Do not duplicate this logic independently inside Home and My99.

5. MANUAL VISIBILITY IS INDEPENDENT

Manual hidden state must never be destroyed by semester toggles.

Required example:

NT manually hidden
Semester 1 = visible
→ NT hidden

Semester 1 = hidden
→ NT/RM/PHC hidden

Semester 1 = visible again
→ RM/PHC return
→ NT remains hidden

Manual Show is required to clear NT's manual hidden state.

6. BOTH-SEMESTER INVARIANT

Semester controls must NEVER hide:

ID
CA

They may be hidden only manually.

Add a dedicated regression for this invariant.

7. ALL SEVEN MAY BE HIDDEN

Allow:

hiddenSubjectIds =
["ID", "NT", "RM", "CA", "PHC", "ImD", "SSC"]

Do not introduce a one-visible minimum rule.

If effective visible subjects = 0:

Home subject grid may be empty.
Do not add a replacement study widget.
Do not add unrelated Home content.
Do not redesign Home in R3.
8. SUBJECT ORDER IS INDEPENDENT

Stored subjectOrder must always contain all seven canonical IDs exactly once.

Visibility never removes IDs from subjectOrder.

Example:

subjectOrder =
["SSC", "ID", "NT", "CA", "RM", "PHC", "ImD"]

If SSC is hidden:

stored order remains exactly the same.

If SSC is later shown:

it returns at its existing position.

Hide/Show and semester toggles must NEVER reorder subjects.

9. CONFIG V1 MUST REMAIN A REAL LEGACY TYPE

Do NOT silently mutate the existing V1 meaning.

Preserve an explicit strict legacy contract conceptually equivalent to:

export interface PersonalizationConfigV1 {
  readonly version: 1;
  readonly themeId: ThemeId;
  readonly heroStyle: HeroStyle;
  readonly glassStyle: GlassStyle;
  readonly motionStyle: MotionStyle;
  readonly readingSize: ReadingSize;
  readonly home: {
    readonly subjectOrder: readonly SubjectId[];
  };
}

It remains needed for migration tests and legacy local-cache parsing.

Do not let the V1 parser accept V2-only Home fields.

10. IMPLEMENT PERSONALIZATION CONFIG V2

Authoritative new configuration:

export interface PersonalizationConfigV2 {
  readonly version: 2;

  readonly themeId: ThemeId;
  readonly heroStyle: HeroStyle;
  readonly glassStyle: GlassStyle;
  readonly motionStyle: MotionStyle;
  readonly readingSize: ReadingSize;

  readonly home: {
    readonly subjectOrder: readonly SubjectId[];

    readonly hiddenSubjectIds: readonly SubjectId[];

    readonly semesterVisibility: {
      readonly semester1: boolean;
      readonly semester2: boolean;
    };
  };
}

The exact structural style may follow repository conventions.

Semantics must match exactly.

11. AUTHORITATIVE CURRENT TYPE

After migration boundaries:

runtime code should use ConfigV2 as the authoritative current config.

If useful:

type PersonalizationConfig = PersonalizationConfigV2;

or repository-equivalent.

Do NOT retain ambiguous runtime unions like:

PersonalizationConfigV1 | PersonalizationConfigV2

throughout the application.

Legacy V1 should be normalized at the boundary.

Runtime should operate on one canonical shape:

ConfigV2
12. FACTORY CONFIG V2

Exact Home defaults:

home: {
  subjectOrder: [
    "ID",
    "NT",
    "RM",
    "CA",
    "PHC",
    "ImD",
    "SSC",
  ],

  hiddenSubjectIds: [],

  semesterVisibility: {
    semester1: true,
    semester2: true,
  },
}

Existing defaults remain:

themeId     = classic-99
heroStyle   = classic
glassStyle  = balanced
motionStyle = full
readingSize = default

Factory V2 must render the exact same default Home subject set as before R3.

13. STRICT CONFIG V2 VALIDATION

V2 must require:

version === 2

Subject order:

exactly seven canonical IDs
no duplicates
no missing canonical IDs
no unknown IDs

Hidden IDs:

zero to seven
canonical SubjectId values only
no persisted unknown IDs
canonical deterministic order
duplicates normalized or rejected according to existing normalizer architecture

Semester visibility:

semester1: boolean
semester2: boolean

No string coercion.

Reject unknown Home keys.

Reject unknown top-level keys according to existing strict contract.

14. HIDDEN SUBJECT CANONICALIZATION

Canonical order is:

ID
NT
RM
CA
PHC
ImD
SSC

Normalize:

["SSC", "NT"]

to:

["NT", "SSC"]

Normalize equivalent hidden sets identically.

Do not allow hidden-array insertion order to create false dirty state.

15. CONFIG NORMALIZATION

Create or extend one canonical normalizer that guarantees a valid runtime V2 config.

It must:

preserve valid existing Theme/Hero/Glass/Motion/Reading values
preserve complete subject order
canonicalize hidden IDs
preserve exact semester booleans
return immutable/fresh structures according to repository conventions

Do not scatter normalization across UI components.

16. V1 → V2 MIGRATION FUNCTION

Implement an explicit pure migration:

ConfigV1
→ ConfigV2

Exact behavior:

preserve:
themeId
heroStyle
glassStyle
motionStyle
readingSize
home.subjectOrder

add:
hiddenSubjectIds = []

semesterVisibility = {
  semester1: true,
  semester2: true
}

Migration must be deterministic.

Migration must not mutate V1 input.

17. LOCAL STORAGE KEY V2

Legacy key:

99s:personalization:v1:<encodeURIComponent(canonicalUserId)>

New authoritative key:

99s:personalization:v2:<encodeURIComponent(canonicalUserId)>

Use the repository's canonical user-ID/key helpers.

Do not construct subtly different encoding logic in multiple places.

18. LOCAL READ ORDER

For an authenticated user:

Read v2 key.
If a valid v2 envelope exists:
use it
do NOT merge v1
ignore legacy v1 ownership
If no valid v2 record exists:
inspect legacy v1 key
perform explicit migration if valid

Do not merge independent v1/v2 states.

V2 is authoritative whenever valid.

19. LEGACY V1 ENVELOPE PARSING

Do not assume the current V2 parser can parse old envelopes.

Maintain a dedicated strict legacy boundary capable of validating:

PersonalizationLocalEnvelopeV2
containing PersonalizationConfigV1

where necessary for migration.

Remember:

cacheVersion: 2

is NOT Config version 2.

20. LOCAL ENVELOPE FORMAT

Keep:

cacheVersion = 2

unless actual source inspection proves that the sync envelope itself must structurally change.

Expected:

interface PersonalizationLocalEnvelopeV2 {
  cacheVersion: 2;
  savedAt: string;

  config: PersonalizationConfigV2;

  sync: {
    knownCloudRevision: string | null;

    pending: {
      intentId: string;
      config: PersonalizationConfigV2;
      createdAt: string;
    } | null;

    lastSyncState:
      | "never"
      | "clean"
      | "retryable-failure"
      | "disabled"
      | "unsupported";

    lastSuccessfulGetAt: string | null;
  };
}

Names must follow actual repository definitions.

21. V1 ENVELOPE → V2 ENVELOPE MIGRATION

When migrating a valid legacy local envelope:

Preserve:

committed V1 personalization via ConfigV1 → ConfigV2 migration
pending intentId
pending createdAt
pending personalization via ConfigV1 → ConfigV2 migration

Do NOT preserve V1 cloud namespace identity as if it belonged to V2.

Reset:

knownCloudRevision = null
lastSuccessfulGetAt = null

Set the migrated V2 sync state to the safe equivalent of:

lastSyncState = "never"

unless existing storage architecture requires an equivalent neutral state.

Do not carry a V1 cloud revision into V2.

22. PENDING INTENT MIGRATION

If legacy envelope contains:

pending.config = ConfigV1

migrate it to a COMPLETE ConfigV2.

Preserve:

intentId
createdAt

Add default R3 fields.

Do not:

drop pending intent
upload a V1 config to V2 namespace
create mixed V1/V2 payloads

The migrated pending config remains authoritative.

23. WRITE V2 BEFORE DELETING V1

Migration durability sequence:

read valid V1
→ create valid V2 envelope
→ successfully persist V2
→ only then attempt removal of V1

If V2 write fails:

V1 remains untouched
migration is not considered successful
do not switch in-memory ownership to an undurable V2 state

If V1 removal fails AFTER V2 write succeeds:

V2 remains authoritative
subsequent reads prefer V2
stale V1 must not overwrite it
24. CORRUPT V2 SAFETY

Explicitly decide and test behavior when:

v2 key exists
but is malformed/corrupt
and a valid v1 key also exists

Preferred safe behavior:

Do NOT silently treat a corrupt authoritative v2 record as if it never existed and resurrect stale V1 state unless that matches existing corruption-recovery architecture.

Audit existing storage safety rules.

Use the safest deterministic behavior and document it.

Do not create accidental rollback from V2 to stale V1.

25. CLOUD V2 CONTRACT

Future cloud configuration becomes:

PersonalizationConfigV2
version: 2

Future cloud KV key:

personalization:v2:<encodeURIComponent(canonicalUserId)>

Do not use the old v1 namespace for V2 writes.

26. CLOUD RECORD V2

Upgrade the cloud record contract conceptually to:

type PersonalizationCloudRecordV2 = {
  recordVersion: 2;
  config: PersonalizationConfigV2;
  revision: string;
  lastIntentId: string;
  updatedAt: string;
};

Preserve existing revision semantics:

revision is identity only
never sorting/order

Do not invent ordering semantics.

27. EXPRESS API

Keep public routes:

GET /api/personalization
PUT /api/personalization

Do not add /v2 endpoints solely for this migration.

Update their current personalization contract to ConfigV2/recordVersion2.

Preserve:

requireUser
canonical req.user.id
no request-controlled user ID
body limits
structured errors
worker secret behavior
no public userId parameter
28. CLOUDFLARE WORKER

Update repository Worker code to future V2 contract:

ConfigV2 validation.
recordVersion: 2.
v2 key namespace.
exact same canonical user-ID rules.
exact same private secret behavior.
existing size limits.
existing intent/revision semantics.
existing confirmation semantics.

DO NOT deploy the Worker.

DO NOT create KV.

DO NOT set secrets.

DO NOT authenticate Wrangler.

This is code-only R3B work.

29. CLOUD DEPLOYMENT REALITY

Entering R3B:

Live personalization KV: NO
Live personalization Worker: NO
Production personalization sync enabled: NO

Do not claim live V2 cloud behavior is verified.

Only source/contracts/tests can be verified.

30. CURRENT CLOUD LIMITS

Preserve:

config:
16,384 UTF-8 bytes

local envelope:
49,152 UTF-8 bytes

public PUT body:
32,768 actual HTTP bytes

cloud record:
32,768 UTF-8 bytes

No limit increase is required.

31. NO REMOTE V1 MIGRATION

Because live V1 KV does not exist:

Do NOT implement:

cloud namespace scan
remote record copy
dual v1/v2 cloud writes
remote migration job
remote fallback lookup into v1 namespace

Future cloud deployment starts directly on v2.

32. CLOUD-DISABLED LOCAL MODE

R3 must work completely when:

PERSONALIZATION_CLOUD_SYNC_ENABLED != "true"

Manual visibility, semesters, and order must persist locally.

Do not require Worker availability for Home customization.

33. EQUALITY

Update config equality.

Must remain ORDER-SENSITIVE for:

home.subjectOrder

Must compare hidden IDs using canonical representation.

Must compare:

semesterVisibility.semester1
semesterVisibility.semester2

Required:

hidden = ["NT", "SSC"]

equals normalized:

["SSC", "NT"]

after canonicalization.

34. DIRTY STATE

Changing any of:

subject order
manual hidden subjects
Semester 1 visibility
Semester 2 visibility

must mark My99 Draft dirty.

Reverting them exactly to committed values must clear dirty state.

No false dirty state due to hidden array ordering.

35. ONE APPLY

Do not add a visibility-specific persistence operation.

Existing combined My99 Apply must write ONE complete ConfigV2 containing:

Theme
Hero
Glass
Motion
Reading
Home order
Hidden IDs
Semester 1
Semester 2

Persist one complete envelope.

Then nonblocking cloud behavior follows existing architecture.

36. APPLY FAILURE

If durable local V2 write fails:

committed state stays unchanged
Home visible list stays unchanged
Draft remains dirty
no cloud PUT
no partial visibility commit

Preserve current structured error flow.

37. CANCEL

Cancel must restore complete committed ConfigV2:

Theme
Hero
Glass
Motion
Reading
Subject order
Hidden IDs
Semester visibility

Real Home remains committed throughout.

38. RESET

Reset Draft to complete V2 factory config.

Specifically Home:

subjectOrder =
ID, NT, RM, CA, PHC, ImD, SSC

hiddenSubjectIds = []

semester1 = true
semester2 = true

Do not globally mutate Home until Apply.

39. PURE HOME VISIBILITY HELPER

Implement a focused pure module such as:

src/features/personalization/homeSubjectVisibility.ts

Use actual repository naming conventions.

Responsibilities:

accept source subjects
accept complete canonical order
accept canonical hidden IDs
accept semester visibility
resolve full ordered source list
derive visible source list
preserve input object identity where practical
not mutate input
expose visibility reason/status helpers if useful for editor UI

Home and My99 preview must share it.

40. UNKNOWN FUTURE SOURCE SUBJECTS

Unknown source subjects must default:

VISIBLE

They must not accidentally disappear because they are absent from V2 canonical IDs.

Do NOT persist unknown IDs into:

subjectOrder
hiddenSubjectIds

For display:

known canonical subjects follow stored order.

Unexpected source subjects should remain available in deterministic source-relative order according to existing homeSubjectOrder safety behavior.

41. HOME RUNTIME

Update Home subject-card rendering only.

Conceptually:

source subjects
→ resolve complete display order
→ apply effective Home visibility
→ render visible subject cards

Do not filter the academic source globally.

Do not alter subject routes/data.

42. CRITICAL NEW R3B HYDRATION RULE

R3A did not fully close this runtime issue.

Home customization reads personalization state directly rather than document CSS attributes.

Therefore R3B MUST prevent SAME-ACCOUNT hydration from causing:

custom order/hidden state
→ temporary factory seven-subject Home
→ custom state again

That visual flash is NOT acceptable.

Required lifecycle:

Same account

If the same authenticated account already owns a valid committed Home personalization and enters an internal hydration/loading transition:

preserve last committed Home order/visibility

until the same account resolves.

No factory Home flash.

Account A → Account B

Immediately cross a safe Home factory boundary:

canonical order
all seven visible
both semesters visible

before B hydration is adopted.

Do NOT display Account A hidden/order state as Account B.

Logout

Immediately return Home customization to factory behavior.

This must parallel the ownership safety principles established by R1B.

Do NOT solve same-account stability by leaking A state into B.

43. HOME OWNERSHIP IMPLEMENTATION

Audit actual Provider/runtime before choosing implementation.

Prefer the smallest lifecycle-aware solution.

Possible valid approaches:

preserve committed config in Provider for same-owner loading while resetting on owner change
a focused ownership-aware Home selector/hook
extending existing account-generation state

Do NOT:

create a second persistent Home config store
use timers
poll
persist extra user identity in CSS
add remount hacks

The authoritative committed ConfigV2 remains the source of truth.

44. HYDRATION TEST — SAME ACCOUNT

Required:

Account A committed:

subjectOrder =
["SSC","ID","NT","CA","RM","PHC","ImD"]

hiddenSubjectIds =
["NT"]

semester2 = false

→ same-account hydration/loading transition

Expected throughout loading:

Home retains effective committed result.

No transient canonical all-seven grid.

45. HYDRATION TEST — A → B

Account A:

hidden = ["NT"]
semester2 = false
custom order

Switch to Account B.

Before B hydration resolves:

expected safe Home boundary:

canonical order
hidden = []
semester1 = true
semester2 = true

Then B resolves with its own config.

At no point may B display A's hidden/order state.

46. LOGOUT TEST

Customized account:

→ logout

Expected Home:

canonical order
all seven visible
both semesters visible

No stale hidden state.

47. HOME-ONLY SCOPE

Manual and semester visibility must NOT affect:

Search.
Subject route access.
Calendar.
Lecture pages.
PDF.
Notes.
Video.
MCQ.
Flashcards.
Q&A.
Admin.
academic data.
permissions.
backend academic queries.

Add source/contract protection where practical.

48. MY99 UI

Extend the existing Home Subject Order editor.

Do not replace the entire My99 Studio.

Recommended hierarchy:

Home Subject Order

Semester Visibility

[ Semester 1 toggle ]
[ Semester 2 toggle ]

Subject rows...

Use existing visual language and R1/R2 semantic surfaces.

49. SEMESTER TOGGLE UI

Use semantic switch/button control matching existing UI system.

Checked/active concept means:

SHOW this semester's exclusive subjects on Home

The control must communicate its state through:

visible text/state
accessibility state
not color alone

Examples of meaning:

Semester 1 subjects: Shown
Semester 1 subjects: Hidden

Use polished product wording rather than destructive confirmation dialogs.

50. SUBJECT ROW UI

Every canonical subject row remains visible in editor.

Include:

localized subject name
order position if existing design exposes it
existing Move Up
existing Move Down
manual Show/Hide control
effective visibility state/reason

Preserve existing order-control behavior.

51. MANUAL TOGGLE WHILE SEMESTER-HIDDEN

Manual visibility remains independently editable.

Example:

Semester 1 hidden
NT manually shown

NT remains effectively hidden because Semester 1 is hidden.

UI must not incorrectly say NT is visible on Home.

Communicate something equivalent to:

Manual: Shown
Effective: Hidden by Semester 1

Do not make manual and effective state indistinguishable.

52. VISIBILITY REASONS

Prefer a pure derived status.

Conceptual statuses:

visible
hidden-manually
hidden-by-semester-1
hidden-by-semester-2

If manually hidden AND semester-hidden:

choose deterministic explanatory UI.

Preferred priority:

hidden-manually

while an additional semester indicator may still be shown.

Do not persist the derived reason.

53. UI ACCESSIBILITY

Manual controls:

semantic button/switch
accessible subject-specific label
aria-pressed, aria-checked, or repository-consistent semantic equivalent
keyboard operable
existing touch-target standard

Semester controls:

proper accessible state
visible descriptive label

Rows:

state not communicated only by color
Move Up/Down disabled semantics preserved
54. I18N

Inspect the ACTUAL repository localization architecture.

Do not assume the exact file path from R3A if source differs.

Add English and Arabic translations for concepts including:

Semester Visibility
Semester 1
Semester 2
Show semester subjects
Hide semester subjects
Show subject
Hide subject
Shown on Home
Hidden manually
Hidden by Semester 1
Hidden by Semester 2

Use existing translation conventions.

Do not persist translated text.

55. RTL

Ensure:

row layout remains valid RTL
canonical order is NOT reversed in storage
Move Up/Down remains logical stored order
eye/visibility icon does not need directional mirroring
semester toggle placement remains usable
Arabic labels may wrap safely

Do not create fixed-height clipping.

56. MY99 DRAFT PREVIEW

Preview must use:

Draft ConfigV2

and the SAME Home visibility helper used by runtime.

Example:

hiddenSubjectIds = ["NT"]
semester2 = false

default order preview must show:

ID
RM
CA
PHC

and omit:

NT
ImD
SSC
57. DRAFT GLOBAL INERTNESS

Before Apply:

changing:

order
Hide/Show
Semester 1
Semester 2

must produce:

preview changes only

real Home:
NO CHANGE

local committed persistence:
NO CHANGE

cloud pending intent:
NO CHANGE

cloud PUT:
NO CHANGE

Protect this with tests.

58. REAL HOME AFTER APPLY

After successful Apply:

committed ConfigV2 updates
Home reflects new effective visibility/order
no page reload
no forced remount
no scroll reset unless existing navigation naturally does so
59. MOTION

Do not add a new Home animation engine.

Existing card/layout presentation may update naturally.

If presentation animation is used:

Full may use existing behavior.
Subtle remains calmer.
Reduced suppresses unnecessary decorative entrance.

Do not animate hidden cards in a way that delays semantic disappearance.

60. CONFIG SIZE

Keep all existing limits.

Add tests showing representative ConfigV2 remains far below size limits.

No limit constant changes.

61. PUBLIC PUT BODY LIMIT

Preserve R3-independent route-specific:

express.json({ limit: "32kb" })

ordering that was previously repaired.

Do not accidentally regress it while changing route validators.

62. ERROR CONTRACT

If API receives:

V1 config after V2 contract is authoritative
malformed hidden IDs
invalid semester value
unknown Home key
wrong record version

return existing structured validation/error semantics.

Do not leak implementation details.

63. STALE CLOUD INTENT SAFETY

Existing pending intent remains authoritative.

V2 implementation must retain:

C1 pending
→ C2 Apply
→ durable C2 supersedes C1

No acknowledgement of C1 may clear C2.

Test this with visibility differences.

Example:

C1: hidden NT
C2: hidden NT + semester2 false

Late C1 acknowledgement must not clear C2.

64. CLOUD CONFIRMATION SEMANTICS

Preserve:

known revision identity semantics
confirmed-empty rules
pending local state wins
same-config/different-revision identity adoption
differing remote config requires existing confirmation behavior

Update comparison to full ConfigV2.

Do not weaken conflict rules.

65. UNKNOWN V1 CLOUD DATA

Do not add production fallback to old cloud v1 namespace.

If somehow a V1-shaped response reaches the V2 client:

reject as unsupported/invalid according to contract.

Local migration is the only approved V1 migration path.

66. APP ICON

Absolutely no App Icon work.

No Swift.

No Xcode.

No icon assets.

No Prompt 24.

67. NO R4

Do not perform the final whole-app personalization QA sweep.

R4 comes only after R3 closure.

68. TEST — CONFIG V1 FROZEN

Add a regression proving:

valid legacy V1 still parses through migration parser
V1 rejects V2-only Home fields
V1 contract was not silently broadened
69. TEST — CONFIG V2

Test:

exact version 2
complete order
hidden IDs
semester booleans
strict unknown keys
invalid IDs
duplicates
malformed values
70. TEST — MIGRATION

Test exact V1 → V2 preservation of:

theme
hero
glass
motion
reading
subjectOrder

and defaults:

hidden = []
semester1 = true
semester2 = true
71. TEST — LOCAL KEY MIGRATION

Cover:

v2 valid → v2 wins
v2 absent + v1 valid → migrate
V2 persisted before V1 deletion
V2 write failure → keep V1 authoritative
V1 deletion failure after V2 success → V2 still authoritative
both keys → V2 wins
migration is idempotent

Also cover the selected corrupt-V2 safety behavior explicitly.

72. TEST — PENDING MIGRATION

Legacy envelope with pending V1:

intentId preserved
createdAt preserved
pending config upgraded
knownCloudRevision reset
lastSuccessfulGetAt reset

No mixed version.

73. TEST — VISIBILITY HELPER

Exhaustive critical combinations:

all visible
manual NT hidden
semester1 hidden
semester2 hidden
both semesters hidden
manual ID hidden
manual CA hidden
all seven manually hidden
manual + semester interactions

Both-semester invariant required.

74. TEST — ORDER PRESERVATION

For custom order:

SSC, ID, NT, CA, RM, PHC, ImD

hide/show SSC.

Expected:

stored order unchanged.

Visible list filters only.

75. TEST — UNKNOWN SOURCE SUBJECT

Add an unknown synthetic source subject in helper tests.

It must remain visible.

It must not enter persisted V2 canonical arrays.

76. TEST — IMMUTABILITY

Visibility/order resolution must not mutate:

source subjects
subjectOrder
hidden IDs
semester object
77. TEST — DIRTY EQUALITY

Test:

["SSC","NT"]

versus normalized:

["NT","SSC"]

does not create dirty difference.

Test real hidden/semester changes do.

78. TEST — DRAFT

Changing R3 fields before Apply:

Draft changes
preview changes
committed unchanged
Home runtime unchanged
no persistence
no cloud PUT
79. TEST — APPLY

Successful Apply writes one complete V2 envelope.

Pending config must include:

all existing axes
order
hidden IDs
semester visibility

No fragmented writes.

80. TEST — APPLY FAILURE

Simulate local persistence failure.

Expected:

committed config unchanged
Draft still dirty
no runtime Home mutation
no cloud PUT
81. TEST — CANCEL / RESET

Cancel:

restores all committed V2 fields.

Reset:

Draft factory only.

Apply Reset:

commits canonical all-visible Home.

82. TEST — SAME-ACCOUNT HOME HYDRATION

Mandatory.

Prove no Home order/visibility flash to factory during same-account loading.

This test is separate from R1B document-attribute tests.

83. TEST — ACCOUNT SWITCH HOME BOUNDARY

Mandatory.

A custom Home:

→ switch B
→ factory Home boundary
→ B config

No A state visible as B.

84. TEST — LOGOUT HOME BOUNDARY

Mandatory.

Logout produces:

canonical order
all seven visible
both semesters visible
85. TEST — CLOUD DISABLED

With cloud disabled:

V2 Apply persists locally
reload/hydration restores Home visibility
no Worker required
86. TEST — EXPRESS V2

Update dedicated personalization API tests.

Cover:

valid V2 GET/PUT path
V1 rejected on V2 PUT
malformed hidden IDs
invalid semesters
oversized body unchanged
auth unchanged
no user-controlled target ID
87. TEST — WORKER V2

Cover:

recordVersion = 2
config.version = 2
personalization:v2:<encodedUser>

and strict rejection of wrong version.

Preserve secret/auth behavior.

88. TEST — INTENT SUPERSESSION

Visibility-specific scenario:

C1:
hidden = ["NT"]
semester2 = true

C2:
hidden = ["NT"]
semester2 = false

Late C1 success/ack must not clear C2.

89. TEST — SIZE LIMITS

V2 must remain within:

16 KiB config
32 KiB HTTP/record
49,152 local envelope

Do not weaken byte-count implementation.

90. TEST — HOME-ONLY SCOPE

Protect against accidental filtering outside Home.

At minimum source contracts should establish that visibility helper is consumed by:

HomeDashboard
My99 Draft preview

and not inserted into global subject source/search/calendar logic.

91. TEST — UI ACCESSIBILITY

Test where practical:

Semester controls expose state.
Subject visibility controls have subject-specific accessible names.
Hidden-by-semester state has readable text.
controls are not color-only.
all seven rows remain rendered in editor.
92. TEST — I18N

Verify all new English and Arabic keys exist.

No raw user-visible English strings on Arabic path where existing architecture expects translation.

93. R1 / R1B REGRESSION

Must continue passing:

tests/personalization-r1-theme-coverage.test.ts
tests/personalization-r1b-hydration-boundary.test.ts
94. R2 / R2B REGRESSION

Must continue passing:

tests/personalization-r2-presentation.test.ts

Preserve:

Hero
Glass
Motion
Reading
95. FULL PERSONALIZATION MATRIX

Entering baseline:

175 passed
0 failed
0 skipped
0 cancelled

Run exactly:

npx tsx --test tests/personalization*.test.ts tests/personalization-runtime.test.tsx

Report exact totals.

No R3 personalization failure accepted.

96. PERSONALIZATION API REGRESSION

Run dedicated personalization API tests.

Entering previous baseline:

16 passed
0 failed

The total should increase if V2 tests are added.

No failure accepted.

97. AUTH / SECURITY

Run:

npm run test:auth
npm run security:check

Accepted:

Auth: 4 passed
Security: 13 passed

No regression.

98. NAVIGATION

Run:

npx tsx --test tests/client-navigation.test.ts

Accepted:

4 passed
0 failed
99. FEATURE SURFACES

Run existing personalization surface suites:

Lecture
Calendar
MCQ / Flashcard
Home where available

No visibility filtering outside Home.

100. BUILD

Run:

npm run build

Must PASS.

101. TYPESCRIPT

Run:

npm run typecheck

Accepted unrelated baseline only:

7 missing `.webp` declarations
src/features/modules/moduleVisuals.ts

No new R3B TypeScript error.

Do not mark R3B blocked solely by those seven accepted errors.

102. PDF BASELINE

Only rerun if R3B touches shared PDF code unexpectedly.

Accepted historical baseline:

2 passed
1 known failure
pdf_download_expiry_invalid

R3B should not need PDF changes.

103. TARGETED ESLINT

Run targeted lint across all R3B modified TS/TSX/test files.

Required:

0 R3B lint errors

Unrelated repository-wide lint baseline is accepted.

104. VISUAL VERIFICATION

Use safe test/browser fixture if normal authenticated preview cannot be entered.

Do not weaken authentication.

Inspect My99 Home customization at minimum on:

mobile
tablet
desktop

Use exact rendered sizes and report them.

Verify:

semester controls readable
seven rows always present
manual hide/show readable
hidden-by-semester state clear
Move Up/Down still usable
Draft preview reflects changes
no overflow

Also verify real Home-equivalent rendered list before/after committed R3 config.

Temporary fixtures must be removed before closure unless test-only infrastructure is intentionally retained.

105. ARABIC VISUAL CHECK

If test fixture can safely render localization:

inspect Arabic/RTL at least at mobile or tablet size.

Verify:

labels wrap
controls do not overlap
row order remains logical
no fixed-height clipping

If Arabic cannot actually be rendered:

state source/test-only verification honestly.

106. NO DEPLOYMENT

Do NOT:

deploy Worker
create KV namespace
set Wrangler secret
modify Render production env
modify Cloudflare production settings
run authenticated production deployment

R3B is repository implementation only.

107. NO DATABASE MIGRATION

No:

Prisma schema
PostgreSQL migration
Supabase table
D1 table
R2 object

for personalization.

108. GIT REVIEW

Run:

git status --short
git diff --stat
git diff --check

Report all R3B modifications.

Do not silently include unrelated files.

109. .agents/* ABSOLUTE PROHIBITION

Run:

git status --short -- .agents
git diff -- .agents

Expected no output.

No .agents/* creation or modification.

110. MANDATORY FINAL IMPLEMENTATION REPORT

Return exactly:

99'S GUIDE — PERSONALIZATION QUALITY REPAIR R3B REPORT
HOME SUBJECT VISIBILITY / SEMESTER IMPLEMENTATION + CONFIG V2
1. Status

PASS / BLOCKED

2. User-Visible Result

Explain exact Home/My99 behavior.

3. Files Modified

| File | Exact Change | Why |

4. Canonical Subject Mapping

Print Semester 1 / Semester 2 / Both.

5. Config V1 Preservation

Explain how V1 remains frozen and migration-only.

6. Config V2 Final Type

Print exact implemented shape.

7. Factory Config V2

Print exact Home defaults.

8. Config V2 Validation

Report strict rules.

9. Hidden Subject Canonicalization

Report deterministic behavior.

10. V1 → V2 Config Migration

Report exact preservation/defaulting.

11. Local Storage V2 Key

Print exact key format.

12. Local Migration Read Order

Report.

13. Corrupt V2 Behavior

Report exact selected behavior.

14. Local Envelope Version

Report cacheVersion and explain why.

15. Pending Intent Migration

Report exact fields preserved/reset.

16. V1 Cleanup

Report write-before-delete behavior and deletion failure handling.

17. Cloud V2 Contract

Report ConfigV2 / recordVersion2.

18. Cloud KV Namespace

Print exact future key.

19. Express API

Report V2 contract changes.

20. Cloudflare Worker

Report V2 contract changes.

21. Cloud Deployment State

State exactly:

Live personalization KV: NO
Live personalization Worker: NO
Production personalization sync: DISABLED

Then state:

NO CLOUD DEPLOYMENT WAS PERFORMED IN R3B.

22. Config / Record Size Limits

Report unchanged limits.

23. Home Visibility Helper

Explain pure helper architecture.

24. Manual Hide / Show

Report behavior.

25. Semester 1

Report behavior.

26. Semester 2

Report behavior.

27. ID / CA Both-Semester Invariant

Report.

28. Manual + Semester Interaction

Report NT example.

29. Subject Order Preservation

Report.

30. All Subjects Hidden

Report behavior.

31. Unknown Future Source Subjects

Report.

32. Home Runtime Integration

Report exact filtering point.

33. Home-Only Scope

Confirm Search/Calendar/routes/study content unaffected.

34. My99 UI

Describe final UI.

35. Semester Controls

Report semantics/accessibility.

36. Subject Row Controls

Report semantics/accessibility.

37. Hidden-By-Semester State

Report visual/derived status behavior.

38. Draft Preview

Report exact use of shared helper.

39. Draft Global Inertness

Confirm real Home/persistence/cloud unchanged before Apply.

40. Apply

Report one complete V2 write.

41. Apply Failure

Report atomic failure behavior.

42. Cancel

Report.

43. Reset

Report.

44. Equality / Dirty State

Report order sensitivity and hidden-set canonical equality.

45. Same-Account Home Hydration

Report whether custom Home state remains stable without factory flash.

46. Account A → B Home Boundary

Report:

A custom state
→ factory Home boundary
→ B state

47. Logout Home Boundary

Report all-seven factory state.

48. Stale Hydration / Sync Protection

Report.

49. Cloud-Disabled Local Behavior

Report reload/local persistence result.

50. Intent Supersession

Report C1/C2 visibility test.

51. English / Arabic Localization

Report exact integration.

52. RTL

Report.

53. Accessibility

Report.

54. Motion

Report Home card behavior under Full/Subtle/Reduced.

55. Config V1 Frozen Tests

Report exact count/results.

56. Config V2 Tests

Report exact count/results.

57. Migration Tests

Report exact count/results.

58. Visibility Helper Tests

Report exact count/results.

59. Order / Unknown Subject Tests

Report exact count/results.

60. Draft / Apply / Cancel / Reset Tests

Report exact count/results.

61. Home Hydration Boundary Tests

Report exact count/results.

62. Local Key / Pending Migration Tests

Report exact count/results.

63. Express V2 Tests

Report exact count/results.

64. Worker V2 Tests

Report exact count/results.

65. Intent Supersession Tests

Report.

66. R1 Regression

Report.

67. R1B Regression

Report.

68. R2/R2B Regression

Report.

69. Full Personalization Matrix

Report exact:

passed
failed
skipped
cancelled

Entering baseline:

175 / 0 / 0 / 0

70. Personalization API Regression

Report exact totals.

71. Feature Surface Regressions

Report.

72. Navigation Regression

Report.

73. Auth Regression

Report.

74. Security Regression

Report.

75. Build

Report.

76. TypeScript

State:

accepted .webp baseline errors
new R3B errors

77. Targeted ESLint

Report.

78. Visual Verification

Report only actual rendered viewport sizes and actual observations.

79. Arabic Visual Verification

State actual rendered vs source-only verification honestly.

80. Performance

Confirm:

no polling
no reload
no remount loop
no duplicate Home data source
no new network request
no heavy animation framework
81. Database Impact

State exactly:

No SQL personalization storage.
No D1 personalization storage.
No R2 personalization storage.

82. App Icon Impact

State exactly:

APP ICON IMPLEMENTATION: NOT STARTED.

83. R4 Scope Safety

State:

FINAL R4 PERSONALIZATION QA: NOT STARTED.

84. Git Review

Report:

git status --short
git diff --stat
git diff --check

85. .agents/* Verification

Report:

git status --short -- .agents
git diff -- .agents

Then state exactly:

No .agents/* file was created or modified.

86. R4 Readiness

If every R3B gate genuinely passes, state exactly:

PERSONALIZATION CONFIG V2: IMPLEMENTED AND VERIFIED

V1 → V2 LOCAL MIGRATION: VERIFIED

PENDING INTENT V1 → V2 MIGRATION: VERIFIED

FUTURE CLOUD V2 CONTRACT: IMPLEMENTED IN SOURCE

HOME MANUAL SUBJECT VISIBILITY: VERIFIED

SEMESTER 1 VISIBILITY: VERIFIED

SEMESTER 2 VISIBILITY: VERIFIED

ID / CA BOTH-SEMESTER INVARIANT: VERIFIED

HOME SUBJECT ORDER PRESERVATION: VERIFIED

MY99 DRAFT / APPLY / CANCEL / RESET: VERIFIED

SAME-ACCOUNT HOME HYDRATION FLICKER: ELIMINATED

ACCOUNT-SWITCH HOME FACTORY BOUNDARY: VERIFIED

LOGOUT HOME FACTORY BOUNDARY: VERIFIED

HOME-ONLY VISIBILITY SCOPE: VERIFIED

R1 / R1B / R2 PERSONALIZATION BEHAVIOR: PRESERVED

NO LIVE CLOUD DEPLOYMENT PERFORMED

R4 FINAL FULL-APP PERSONALIZATION QA: APPROVED TO BEGIN

End EXACTLY:

PERSONALIZATION QUALITY REPAIR R3B STATUS: COMPLETE — READY FOR R4

Otherwise:

PERSONALIZATION QUALITY REPAIR R3B STATUS: BLOCKED — REPAIR REQUIRED