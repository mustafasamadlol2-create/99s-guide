# 99'S GUIDE — PERSONALIZATION QUALITY REPAIR
# PROMPT R4C
## FINAL SUBJECT-DATA INTEGRITY + `.agents/*` CLEANLINESS CLOSURE
### RESOLVE TWO OBJECTIVE R4B REPORT CONTRADICTIONS
### VERIFY VISUAL HARNESS USED REAL PRODUCTION SUBJECT IDS/NAMES
### REMOVE ALL PERSONALIZATION-GENERATED `.agents/*` REPOSITORY DIFF
### DO NOT REDESIGN PERSONALIZATION
### DO NOT ADD FEATURES
### DO NOT DEPLOY CLOUD
### DO NOT IMPLEMENT APP ICON

R4B reported PASS, but its own evidence contains TWO objective contradictions that prevent final production closure.

This prompt is the final narrow closure gate.

Do not redo R1–R4.

Do not redesign ConfigV2.

Do not add features.

Repair only if actual production data or repository cleanliness is wrong.

---

# 1. BLOCKER A — SUBJECT DATA MISMATCH IN THE R4B VISUAL REPORT

The approved R3 architecture reported the canonical subjects as:

```text
ID  = Infectious Diseases
NT  = Nutrition
RM  = Research Methodology
CA  = Clinical Attachment
PHC = Public Health Care
ImD = Immune Disturbances
SSC = Student Selected Component

R4B State B used:

subjectOrder:
SSC ID NT CA RM PHC ImD

hiddenSubjectIds:
NT

semester1 = true
semester2 = false

Therefore expected effective IDs are:

ID
CA
RM
PHC

If the above canonical metadata is correct, expected labels are:

Infectious Diseases
Clinical Attachment
Research Methodology
Public Health Care

However R4B reported the ACTUAL rendered result as:

Infectious Diseases
Cardiology
Respiratory Medicine
Public Health & Community

R4B also reported:

Cardiology manually hidden

for the CA scenario.

This is inconsistent with the previously approved canonical subject mapping.

You MUST determine the exact reason.

2. DO NOT ASSUME WHICH REPORT IS CORRECT

Inspect ACTUAL production source.

Find the authoritative source for the seven Home subjects.

Trace:

canonical SubjectId
subject metadata
English labels
Arabic labels
HomeDashboard source
My99 subject editor source
any subject icon/catalog mapping
test fixture subject metadata
visual harness subject metadata used in R4B

Do NOT infer labels from abbreviations.

Do NOT use R3A report wording as authority if actual repository source contradicts it.

The production repository is authoritative.

3. PRINT THE ACTUAL PRODUCTION SUBJECT MAP

Return the exact production mapping:

ID  = ?
NT  = ?
RM  = ?
CA  = ?
PHC = ?
ImD = ?
SSC = ?

For each ID cite the exact production source file internally in the report.

Also report Arabic label if available.

4. DETERMINE WHICH FAILURE OCCURRED

Choose exactly one:

CASE A — R4B HARNESS MOCK DATA WAS WRONG

Production source matches the R3 canonical map, but the disposable visual harness used incorrect labels such as:

Cardiology
Respiratory Medicine
Public Health & Community

If so:

personalization production logic may still be correct
R4B Home visual proof was partially invalid
rebuild the visual fixture using ACTUAL production subject metadata/components
rerun the relevant Home visual checks
CASE B — R3 CANONICAL MAP WAS WRONG

Actual production source maps IDs differently from the R3 report.

If so:

STOP before changing product semantics.

Audit whether semester mapping:

Semester 1:
NT RM PHC

Semester 2:
ImD SSC

Both:
ID CA

still corresponds to the intended real subjects.

Do NOT silently reinterpret IDs.

If the semester architecture is wrong for production data:

return BLOCKED and explain the exact mismatch.

CASE C — PRODUCTION SOURCE ITSELF IS INCONSISTENT

Different application modules map the same ID to different subjects.

If so:

return BLOCKED.

Do not mask it in the harness.

5. VISUAL HARNESS MUST NOT INVENT SUBJECTS

Any R4C visual harness must obtain production subject data by one of these methods:

Preferred:

import the actual production subject catalog/data source

or:

render the actual production Home component with deterministic mocked backend records whose IDs/names match the production contract

Do NOT manually invent:

Cardiology
Respiratory Medicine
...

unless those are truly the production labels for those IDs.

6. RECHECK STATE B USING REAL DATA

After the authoritative mapping is known, render:

subjectOrder:
SSC ID NT CA RM PHC ImD

hidden:
NT

semester1:
true

semester2:
false

Verify effective IDs exactly:

ID
CA
RM
PHC

The rendered labels must correspond to the ACTUAL authoritative production labels for those IDs.

Report both:

visible IDs
visible labels

Do not report labels alone.

7. RECHECK STATE D

Render:

hiddenSubjectIds:
CA

Verify the UI says the ACTUAL production label corresponding to CA is manually hidden.

Do not substitute another subject name.

8. VERIFY UI AND PERSISTENCE USE IDS, NOT LABELS

Critical invariant:

Personalization must persist only:

ID
NT
RM
CA
PHC
ImD
SSC

not:

Cardiology
Clinical Attachment
...

Verify:

subjectOrder
hiddenSubjectIds
semester membership
visibility helper

all operate on IDs.

Localized/display labels may change without changing persisted personalization semantics.

9. VERIFY SEMESTER MEMBERSHIP AGAINST ACTUAL SUBJECTS

After resolving authoritative labels, print:

Semester 1 only:
NT = <actual label>
RM = <actual label>
PHC = <actual label>

Semester 2 only:
ImD = <actual label>
SSC = <actual label>

Both:
ID = <actual label>
CA = <actual label>

Then answer:

Does this actual subject mapping still match the approved product semester requirement?
YES / NO

If NO:

R4C must be BLOCKED.

Do not silently change semester logic.

10. HOME VISIBILITY LOGIC MUST REMAIN ID-BASED

Do not change:

effectiveVisible(subject)
=
!hiddenSubjectIds.includes(subject.id)
&& semesterAllows(subject.id)

or repository-equivalent unless a real bug is discovered.

Do not make labels participate in business logic.

11. R4B CHIP FIX

Preserve the legitimate R4B production repair in:

src/features/personalization/components/My99Studio.tsx

reported as:

min-w-0
max-w-full
break-words
whitespace-normal

Verify it remains layout-only.

It must not alter subject IDs or metadata.

12. BLOCKER B — .agents/* HARD REQUIREMENT

R4B final git state was:

M .agents/memory/MEMORY.md
?? .agents/memory/r4b-visual-harness-tailwind.md

Therefore the statement:

.agents/* PERSONALIZATION DIFF: NONE

was factually incorrect.

For this project, the requirement is based on repository state, not the stated purpose of the files.

Final requirement:

git status --short -- .agents

must return:

NO OUTPUT
13. REMOVE ONLY R4/R4B-GENERATED .agents CHANGES

Restore:

.agents/memory/MEMORY.md

to its entering tracked state by removing only the R4B-added visual-harness memory line.

Remove the R4B-generated untracked file:

.agents/memory/r4b-visual-harness-tailwind.md

Do NOT remove legitimate pre-existing .agents content.

Do NOT rewrite unrelated agent memory.

Do NOT add another memory file explaining the removal.

The final repository must simply have zero R4/R4B .agents/* diff.

14. VERIFY ALL THREE GIT VIEWS

Run:

git status --short -- .agents
git diff -- .agents
git diff --cached -- .agents

Required:

all three produce no output

If the coding platform itself insists on writing .agents/* again:

do not falsely report PASS.

Return BLOCKED and show the exact files.

15. PERSONALIZATION MATRIX

Run:

npx tsx --test tests/personalization*.test.ts tests/personalization-runtime.test.tsx

Entering:

181 passed
0 failed

Required:

0 failed
16. HOME SUBJECT TEST

Run focused Home personalization tests.

Ensure tests explicitly verify IDs, not only display labels.

At minimum assert:

State B visible IDs:
["ID", "CA", "RM", "PHC"]

according to the custom stored order filtering semantics.

If order resolution produces another ordering, print and justify the exact ID sequence.

Do not infer correctness from labels.

17. MIGRATION DURABILITY

Rerun:

tests/personalization-storage.test.ts
tests/personalization-r4-integration.test.ts

R4 migration durability repair must remain intact.

18. TYPESCRIPT

Run:

npm run typecheck

Required:

PASS
0 errors
19. BUILD

Run:

npm run build

Required:

PASS
20. TARGETED ESLINT

If any production/test source is modified:

run targeted ESLint.

Required:

0 errors
21. AUTH / SECURITY / NAVIGATION

If only .agents cleanup occurs and no production code changes beyond the already-tested My99 chip fix:

existing R4B results may stand.

If production subject metadata/business logic changes:

rerun:

npm run test:auth
npm run security:check
npx tsx --test tests/client-navigation.test.ts
22. VISUAL RERUN SCOPE

If CASE A occurred:

rerun only the Home/My99 visual states whose subject labels depended on wrong harness data.

At minimum:

State B
State D

Use actual production metadata.

Report:

viewport
visible IDs
visible labels
zero overflow

No need to redo every R2 Hero/Glass visual test.

23. ARABIC LABEL INTEGRITY

For the same actual IDs, confirm Arabic UI resolves the corresponding actual Arabic subject labels.

Do not create a separate English-vs-Arabic subject mapping.

Both languages must be display projections of the same stable IDs.

24. NO CLOUD DEPLOYMENT

Still:

Live personalization KV: NO
Live personalization Worker: NO
Production personalization sync: DISABLED

Do not deploy anything.

25. NO APP ICON

Do not begin Prompt 24.

26. TEMPORARY HARNESS

Remove any R4C visual harness/screenshots before final closure.

No permanent debug route.

27. FINAL GIT REVIEW

Run:

git status --short
git diff --stat
git diff --check

Expected legitimate personalization source diff may include the previously accepted My99 chip wrapping repair if not committed.

But:

.agents/*

must not appear.

28. MANDATORY FINAL REPORT

Return exactly:

99'S GUIDE — PERSONALIZATION QUALITY REPAIR R4C REPORT
SUBJECT-DATA INTEGRITY + .agents/* FINAL CLEANLINESS
1. Status

PASS / BLOCKED

2. Why R4C Was Required

Explain both contradictions:

rendered subject names did not match the previously approved canonical map
.agents/* still had repository changes
3. Authoritative Production Subject Source

List exact production source file(s).

4. Actual Production Subject Map

Print:

ID = ...
NT = ...
RM = ...
CA = ...
PHC = ...
ImD = ...
SSC = ...

5. Arabic Subject Map

Print actual Arabic labels where available.

6. R3 Mapping Comparison

State whether the R3 reported mapping was:
CORRECT
or
INCORRECT

Explain exact discrepancy.

7. R4B Harness Root Cause

Choose:

HARNESS MOCK DATA ERROR
R3 ARCHITECTURE DATA ERROR
PRODUCTION SOURCE INCONSISTENCY

Explain.

8. Semester Mapping Against Actual Subjects

Print:

Semester 1:
NT = ...
RM = ...
PHC = ...

Semester 2:
ImD = ...
SSC = ...

Both:
ID = ...
CA = ...

Then state:

APPROVED SEMESTER PRODUCT MAPPING STILL VALID: YES / NO

9. ID-Based Persistence

Confirm subjectOrder/hiddenSubjectIds store IDs only.

10. State B Reverification

Print:

stored order
manual hidden IDs
semester values
effective visible IDs
effective visible labels

11. State D Reverification

Print CA actual label and manual-hidden rendered result.

12. Visual Rerun

Report exact viewport(s) actually rerendered.

13. R4B Chip Repair

Confirm wrapping repair remains layout-only.

14. .agents/* Cleanup

List exact R4/R4B-generated .agents changes removed.

15. .agents/* Final Commands

Print results for:

git status --short -- .agents
git diff -- .agents
git diff --cached -- .agents

Required result:
NO OUTPUT for all three.

16. Personalization Matrix

Report:
passed
failed
skipped
cancelled

17. Home Visibility Tests

Report ID-based assertions/results.

18. Migration Durability

Report.

19. TypeScript

Report.

20. Build

Report.

21. ESLint

Report if applicable.

22. Auth / Security / Navigation

Report whether rerun or unchanged from R4B.

23. Temporary Harness Cleanup

Report.

24. Cloud

State exactly:

Live personalization KV: NO
Live personalization Worker: NO
Production personalization sync: DISABLED
NO PERSONALIZATION CLOUD DEPLOYMENT PERFORMED.

25. App Icon

State exactly:

APP ICON IMPLEMENTATION: NOT STARTED.

26. Git Review

Report:

git status --short
git diff --stat
git diff --check

Explicitly confirm .agents/* does not appear.

27. Final Closure

PASS only if:

actual production subject map is resolved
semester mapping remains product-correct
visual harness uses real subject data
ID-based persistence is verified
.agents/* has zero repository diff
personalization matrix has zero failures
typecheck passes
build passes

If PASS, state exactly:

AUTHORITATIVE SUBJECT DATA: VERIFIED

VISUAL HARNESS SUBJECT DATA: VERIFIED AGAINST PRODUCTION

SEMESTER MAPPING: VERIFIED AGAINST ACTUAL SUBJECTS

HOME VISIBILITY: VERIFIED BY STABLE SUBJECT IDS

R4B CHIP WRAPPING REPAIR: PRESERVED

.agents/* REPOSITORY STATE: CLEAN

PERSONALIZATION MATRIX: PASS

TYPESCRIPT: PASS

PRODUCTION BUILD: PASS

R4 MIGRATION DURABILITY: PRESERVED

99'S GUIDE MY99 PERSONALIZATION QUALITY REPAIR: CLOSED AND APPROVED

End EXACTLY:

PERSONALIZATION QUALITY REPAIR R4C STATUS: COMPLETE — PRODUCTION CLOSURE APPROVED

Otherwise end:

PERSONALIZATION QUALITY REPAIR R4C STATUS: BLOCKED — FINAL CLOSURE NOT YET APPROVED