# 99'S GUIDE — PERSONALIZATION SYSTEM
# PHASE 0 / PROMPT 1
## PRODUCTION ARCHITECTURE AUDIT & CUSTOMIZATION FREEZE
### READ-ONLY AUDIT — DO NOT MODIFY RUNTIME CODE

You are auditing the CURRENT production repository of 99's Guide before implementing a new personalization/customization system.

This is PHASE 0.

This prompt is AUDIT ONLY.

DO NOT implement the personalization feature yet.

DO NOT modify runtime application behavior.

DO NOT redesign anything.

DO NOT add UI.

DO NOT add database tables.

DO NOT add Cloudflare KV.

DO NOT add themes.

DO NOT add icons.

DO NOT add settings.

DO NOT add new Home widgets.

DO NOT alter existing functionality.

The purpose of this audit is to establish a precise production baseline before any customization code is written.

---

# 1. PRODUCT RULE — ABSOLUTE SCOPE BOUNDARY

The future personalization system may ONLY customize how EXISTING 99's Guide functionality:

- looks
- is visually styled
- is visually arranged where safe
- presents already-existing Home sections
- applies motion / glass / reading-size preferences
- displays an alternate bundled app icon

It must NEVER create new academic/product functionality.

Future personalization must NOT introduce:

- new study widgets
- new academic data
- new recommendations
- new exam systems
- new "upcoming lectures" unless that exact feature already exists today
- new "continue studying" functionality unless it already exists today
- new Quick Actions area unless such an area already exists
- new Profile features
- new Calendar functionality
- new navigation destinations
- new algorithms
- new content types
- new progress logic
- new points logic
- new notification behavior
- new AI functionality
- new role/permission behavior

This audit must identify what already exists rather than inventing anything.

---

# 2. CORE SAFETY PRINCIPLE

The CURRENT production appearance must become:

CLASSIC_99

in the future theme engine.

Classic 99 must visually reproduce the current application as closely as technically possible.

The future personalization engine must always have a safe fallback:

invalid/missing preferences
→ Classic 99 defaults
→ application remains fully usable

The personalization system must never become required for core application functionality.

---

# 3. READ-ONLY REQUIREMENT

This phase should make ZERO intentional runtime product changes.

Do NOT modify:

src runtime code
server runtime code
Prisma schema
migrations
Cloudflare worker runtime
D1 schema
R2
Capacitor native configuration
Info.plist
App icons
CSS tokens
Tailwind theme
Settings UI
Home UI

If documentation is absolutely required for the audit, prefer returning the complete report in your final response rather than creating repository artifacts.

Do NOT create:

.agents/*
temporary architecture files
audit markdown files
screenshots committed to repository

Expected git diff after this phase:

NO CHANGES

If the repository was already dirty before this task, identify pre-existing diffs but do not modify them.

---

# 4. AUDIT CURRENT APPLICATION ARCHITECTURE

Identify the exact architecture currently used for:

Frontend:
- React
- TypeScript
- Vite
- Tailwind
- CSS files
- theme-related context/providers
- app bootstrap
- Settings
- Home
- navigation
- sidebar
- mobile bottom bar
- iPad layout
- Console
- Modules
- MCQ
- Flashcards
- Calendar
- Profile
- Notifications
- Auth screens

Native:
- Capacitor configuration
- iOS project
- current app icon configuration
- alternate icon support, if any
- splash assets
- native appearance/status bar behavior

Backend:
- authentication identity source
- user ID format
- any preference/settings endpoints
- any existing per-user non-academic settings persistence

Cloudflare:
- existing Workers
- D1
- R2
- Pages/Workers frontend deployment
- whether KV is already configured anywhere
- naming conventions for bindings

Do not modify any of them.

---

# 5. AUDIT THE CURRENT LIGHT/DARK SYSTEM

Determine exactly how the current application handles:

- light mode
- dark mode
- system mode, if supported
- initial appearance before React mounts
- theme persistence
- localStorage keys
- document classes
- `data-theme`
- CSS variables
- hardcoded theme classes
- browser/system preference listeners
- Capacitor/native appearance integration

Identify:

A. current source of truth
B. startup sequence
C. where flashes can occur
D. what would need to remain compatible with future personalization

Do not redesign it yet.

---

# 6. INITIAL PAINT / FLASH AUDIT

Inspect:

index.html
theme preload logic
main.tsx
App.tsx
global CSS

Determine what happens before React mounts.

We need future personalization to avoid:

white flash
wrong theme flash
Classic → custom theme flash

Report exact initialization sequence.

Do not change it.

---

# 7. GLOBAL COLOR INVENTORY

Audit the frontend for colors that are currently:

- hardcoded Tailwind utility colors
- hex colors
- rgba colors
- CSS variables
- inline styles
- gradient stops

Examples to search for:

rose-*
indigo-*
blue-*
amber-*
emerald-*
red-*
green-*
black
white

and literal:

#...
rgb(...)
rgba(...)

Group findings by intent.

Do NOT simply count strings.

Classify each color usage.

---

# 8. COLOR CLASSIFICATION

Every relevant color should be classified into one of these categories:

## A. BRAND / CUSTOMIZABLE

Examples:

primary accent
active navigation
selected tabs
focus accent
hero accents
decorative gradients
selected chips
profile accent
glass tint

These are likely future theme tokens.

## B. STRUCTURAL / SURFACE

Examples:

background
surface
elevated surface
border
text primary
text secondary
glass surface

These may have Light/Dark variants.

## C. SEMANTIC — MUST NOT FOLLOW THEME

Examples:

error
danger
success
warning
offline
moderation status

These should remain semantic.

## D. ACADEMIC / DOMAIN SEMANTIC

Examples:

MCQ difficulty:
Easy green
Normal amber
Hard red

These must NOT be recolored by arbitrary themes.

## E. SUBJECT / MODULE IDENTITY

If modules/subjects have fixed identity colors:

identify them separately.

Do not assume they should become theme-dependent.

---

# 9. DETERMINE SAFE FUTURE THEME TOKEN BOUNDARIES

Based on current usage, propose — but DO NOT implement — a minimal semantic theme token architecture.

Example conceptual token candidates:

--app-background
--app-surface
--app-surface-elevated
--app-surface-muted
--app-text
--app-text-muted
--app-border
--app-accent
--app-accent-hover
--app-accent-soft
--app-focus-ring
--app-nav-active
--app-nav-active-soft
--app-glass
--app-glass-border
--app-hero-from
--app-hero-to
--app-hero-accent

Do not blindly adopt this exact list.

Derive the recommended token list from THIS repository.

Report which existing UI areas each proposed token would control.

No CSS changes yet.

---

# 10. AUDIT CURRENT HOME PAGE

This is critical.

Find the actual current Home / Welcome / main landing UI.

List EVERY section/component currently present.

For each existing section determine:

- component/file
- visible name
- current order
- mobile behavior
- iPad behavior
- desktop behavior
- whether it is navigation-critical
- whether it may safely be hidden
- whether it may safely be reordered
- whether it must remain fixed

DO NOT invent any Home section.

---

# 11. HOME CUSTOMIZATION CLASSIFICATION

Classify every ACTUAL current Home section into:

LOCKED_CORE

or

CUSTOMIZABLE_EXISTING

Definitions:

LOCKED_CORE:
Cannot safely be removed/reordered because it is essential to navigation, orientation, branding, or app operation.

CUSTOMIZABLE_EXISTING:
Already-existing optional/presentation section that could safely support:
- show/hide
- reorder

without changing product functionality.

If an existing section should support reorder but not hide, state so.

Do not create a third-party-style dashboard builder.

---

# 12. HOME LAYOUT STATE

Identify whether current Home layout/order is:

- static JSX
- array-driven
- configuration-driven
- responsive CSS-driven

Estimate how invasive future reorder/show-hide support would be.

Recommend the least invasive architecture.

Do not implement.

---

# 13. HERO AUDIT

Identify the CURRENT Hero area.

Report:

- file/component
- current background
- particles
- animations
- images
- gradients
- branding
- text
- buttons
- responsive behavior
- light/dark differences

Determine what could safely be visually customized without changing content/functionality.

Potential future customization may include:

- background treatment
- gradient
- particle intensity
- accent

But DO NOT add new hero content.

---

# 14. GLASSMORPHISM AUDIT

Identify existing glass/glass-like patterns:

- backdrop blur
- transparent surfaces
- border opacity
- shadow
- background opacity

Determine whether these are centralized or repeated.

Identify which components could safely respond to future:

Clear
Balanced
Frosted

styles.

No implementation yet.

---

# 15. MOTION AUDIT

Inventory current motion/animation systems.

Include:

- Motion / Framer successor usage
- CSS animations
- transitions
- hero particles
- tab transitions
- swipe transitions
- sidebar animations
- bottom bar animations
- text transitions
- loading states

Classify them:

A. structural navigation motion
B. decorative motion
C. feedback animation
D. accessibility-sensitive motion

Determine how future:

Full
Subtle
Reduced

could safely affect them.

Very important:

Reduced Motion must NOT break navigation/swipe mechanics.

Do not implement.

---

# 16. READING SIZE AUDIT

Identify text/content areas where a future:

Small
Default
Large

reading-size preference could safely apply.

Examples may include existing:

lecture text
MCQ text
flashcards
module information
content descriptions

Do NOT automatically scale:

navigation
bottom tabs
fixed controls
critical responsive geometry

Report recommended scope.

No implementation.

---

# 17. SETTINGS AUDIT

Inspect the current Settings architecture.

Report:

- Settings entry point
- routes/views
- mobile full-page behavior
- iPad behavior
- current appearance controls
- language controls
- notification controls
- blocked users/reports
- any customization-related settings already present

Recommend where future:

My 99 / Personalization

should be inserted.

Do NOT add it yet.

---

# 18. CURRENT USER-PREFERENCE PERSISTENCE AUDIT

Search for existing per-user preferences.

Examples:

language
appearance
notification preferences
sidebar state
layout preferences
local UI state

For each, identify whether it is stored in:

localStorage
IndexedDB
PostgreSQL
D1
server session
Capacitor preferences
other

We want future personalization to avoid high SQL usage.

Report what can be reused.

---

# 19. SQL IMPACT RULE

Future personalization should NOT add routine preference writes to Supabase/PostgreSQL.

Do NOT create a UserCustomization SQL table in this audit.

The intended architecture is:

Cloudflare Workers KV
+
local device cache

unless this audit uncovers a compelling blocker.

Analyze compatibility of KV with the existing deployment.

---

# 20. CLOUDFLARE KV AUDIT — NO IMPLEMENTATION

Inspect current Cloudflare configuration files.

Determine:

- existing Workers
- wrangler configs
- existing bindings
- whether KV is already used
- best Worker location for future personalization endpoints
- whether existing authenticated API architecture can safely proxy personalization requests

The future design should conceptually use:

one small JSON document per user

key concept:

personalization:<opaque-user-id>

Do not use email as key.

Do not put academic data in KV.

Do not create KV namespace yet.

---

# 21. KV READ/WRITE STRATEGY REVIEW

Assess the proposed future strategy:

Startup:
1. load local cached personalization immediately
2. apply visual preference before/at React bootstrap
3. application starts normally
4. after authenticated session resolves, fetch KV preferences
5. reconcile silently
6. update local cache only if necessary

Editing:
1. user edits draft locally
2. no KV write for every click
3. user presses Apply
4. ONE KV write

Failure:
KV unavailable
→ local preferences still work
→ Classic 99 fallback always exists

Report whether this fits current architecture.

Do not implement.

---

# 22. PERSONALIZATION SECURITY BOUNDARY

Determine future endpoint security requirements.

Preferences must be scoped to authenticated user's own user ID.

Client must NEVER be allowed to send:

arbitrary target userId

for preference read/write.

Backend/Worker should derive identity from trusted authenticated application context.

Audit whether existing Render auth tokens/cookies can be used directly with Cloudflare Worker safely, or whether Render should proxy KV access.

Recommend one architecture.

Do not implement it.

---

# 23. APP ICON AUDIT

Inspect:

public icons
PWA manifest
Capacitor iOS assets
Xcode asset catalogs
Info.plist
CFBundleIcons
alternate icon configuration

Report:

- current Light icon
- current Dark icon
- current PWA icon setup
- native iOS icon setup
- whether alternate app icons are currently configured
- technical work required to support multiple icons

Do NOT add icons.

---

# 24. APP ICON FUTURE ARCHITECTURE

Evaluate future support for approximately 10–12 bundled variants.

Important:

Icon images themselves should be bundled assets.

Preference storage should contain only:

iconId

Native icon preference should be device-local.

Do not sync icon selection through account KV unless there is a strong reason.

Theme and icon may support:

Match Theme

but implementation is later.

No code changes.

---

# 25. PWA ICON LIMITATIONS

Audit what is realistically possible for installed PWA icons.

Clearly distinguish:

- native iOS alternate icons
- PWA manifest icons
- in-app brand mark

Do not promise dynamic OS home-screen PWA icon behavior that browsers do not reliably support.

Recommend realistic behavior.

---

# 26. IN-APP BRAND MARK AUDIT

Identify where the 99 logo/brand mark appears in-app.

Examples:

sidebar
auth
welcome
loading
profile/navigation

Determine whether changing in-app brand mark would be safe.

Future implementation should preserve 99 branding.

No arbitrary user-uploaded logos.

---

# 27. THEME PACK CONSTRAINTS

Future Theme Packs must be curated.

Do NOT recommend arbitrary RGB/color picker in V1.

Audit whether eight curated themes are technically reasonable.

Proposed conceptual themes:

Classic 99
Midnight
Ocean
Emerald
Rose
Amber
Violet
Monochrome

Do not implement them.

Report whether any current hardcoded design makes a specific theme risky.

---

# 28. LIGHT/DARK VARIANTS

Future theme packs should support:

Theme ID
+
Appearance Mode

Example:

Emerald + Light
Emerald + Dark

Do NOT model them as 16 separate unrelated themes unless there is a compelling reason.

Audit whether current CSS architecture supports this cleanly.

---

# 29. SEMANTIC COLORS MUST REMAIN FIXED

Identify every place future theming must NOT override:

error red
success
warnings
MCQ Easy
MCQ Normal
MCQ Hard
moderation status
online/offline indicators
destructive actions

Include file locations.

---

# 30. RESPONSIVE AUDIT

Inspect personalization-relevant components at current responsive breakpoints.

Focus on:

Mobile
iPad portrait
iPad landscape
desktop/laptop

Determine risks if:

- accents change
- reading size increases
- Home sections reorder
- Hero style changes
- glass opacity changes

Do not render new layouts.

---

# 31. RTL / ARABIC AUDIT

Identify personalization-related RTL risks:

Settings
segmented controls
theme cards
icon grids
Home reordering
reading size
hero
presets

Determine existing RTL utilities/patterns to reuse.

No new translations yet.

---

# 32. ACCESSIBILITY AUDIT

Report existing support for:

prefers-reduced-motion
contrast
focus states
keyboard navigation
screen reader labels

Determine how future customization must preserve:

WCAG-style contrast
visible focus
not-color-only status
reduced motion safety

Do not change anything.

---

# 33. CONSOLE SCOPE

Console is NOT a customization target for functionality.

Future themes may visually skin existing Console surfaces through global tokens.

But personalization must NOT:

- reorder Console navigation
- hide admin tools
- alter permissions
- alter data
- alter moderation

Confirm how Console currently inherits global colors.

---

# 34. MODULES SCOPE

Future customization may visually skin module page surfaces only where safe.

Do not alter:

module colors if they are academically/identity significant
module content
module resources
resource PDFs
resource upload

Identify module colors that should remain fixed versus global-theme-responsive.

---

# 35. MCQ SCOPE

Future theme may change generic page surfaces/accent.

Must NOT change:

category semantics
difficulty colors
answer correctness colors
selected answer semantics
quiz logic
points

Identify exact MCQ semantic colors that must remain fixed.

---

# 36. FLASHCARD SCOPE

Theme may change generic surface treatment.

Must NOT change:

card content
flip behavior
review state semantics
progress behavior

Identify any visual dependencies.

---

# 37. CALENDAR SCOPE

Theme may affect accents/surfaces only.

Do not alter:

schedule data
day/week/month logic
calendar navigation
event color semantics if meaningful

Audit current calendar colors.

---

# 38. PROFILE SCOPE

Theme may affect:

generic surface
accent ring
buttons

Do not add new profile customization functionality in V1 unless it is purely a consequence of global theme.

No new cover/avatar systems.

---

# 39. NOTIFICATIONS SCOPE

Theme may affect generic visuals only.

Do not alter:

read/unread logic
swipe behavior
filters
notification semantics

---

# 40. NAVIGATION SAFETY

Audit:

bottom navigation
sidebar
swipe navigation
back gestures
route transitions

Future theming must not modify geometry or pointer behavior unless absolutely necessary.

Identify which classes are visual-only and can become tokens.

---

# 41. CLASSIC 99 VISUAL PARITY REQUIREMENT

Create an explicit inventory of the current production values that would define Classic 99.

Examples:

backgrounds
surfaces
accent
nav active state
hero
glass
borders
text
focus
buttons

This will be the visual baseline for Phase 2.

Do not change these values.

---

# 42. DEFAULT PERSONALIZATION CONTRACT — PROPOSAL ONLY

Based on audit, propose the future TypeScript shape.

Conceptually:

type PersonalizationConfigV1 = {
  version: 1;
  appearance: ...;
  themeId: ...;
  heroStyle: ...;
  glassStyle: ...;
  motionStyle: ...;
  readingSize: ...;
  home?: {
    order: ...;
    hidden: ...;
  };
};

App icon may be stored separately device-local.

Do NOT implement.

Only propose the minimal fields actually justified by current app.

---

# 43. DO NOT STORE FULL THEME TOKENS PER USER

Future preference store must save IDs/options only.

Example:

themeId = "emerald"

NOT:

{
  accent: "#...",
  border: "#...",
  ...
}

Theme definitions belong in app code.

Confirm this recommendation.

---

# 44. PERSONALIZATION VERSIONING

Propose a simple preference version strategy.

Example:

version: 1

Future incompatible preference shapes can migrate locally.

Invalid/unknown fields should be ignored safely.

Classic 99 fallback must remain.

Do not implement.

---

# 45. DEVICE LOCAL VS ACCOUNT SYNC MATRIX

Recommend which settings should be:

ACCOUNT-SYNCED through KV

versus:

DEVICE-LOCAL

Expected direction:

ACCOUNT:
theme
appearance if appropriate
hero style
glass
motion
reading size
existing Home presentation preferences

DEVICE LOCAL:
native app icon

But base the final matrix on repository audit.

---

# 46. APPEARANCE SYNC QUESTION

Specifically evaluate whether:

System / Light / Dark

should sync across devices or remain device-local.

Example:
User may want iPhone Dark but laptop System.

Recommend the best UX for 99's Guide.

Do not implement.

---

# 47. HOME ORDER SYNC

If current Home has safe customizable sections:

future order/visibility may sync via KV.

But verify component IDs can remain stable across app versions.

Recommend canonical IDs.

Do not implement.

---

# 48. PERFORMANCE

Future personalization must not:

- cause additional SQL traffic
- refetch academic content on theme changes
- cause full React app remount
- reload page
- recreate socket connection
- reset current route
- reset scroll position
- reset current MCQ session
- reset Calendar state

Audit current architecture for risks.

---

# 49. THEME CHANGE EXPECTED BEHAVIOR

Future Theme Apply should ideally:

update CSS/customization state
→ no page reload
→ no navigation reset
→ no data refetch

Assess feasibility.

---

# 50. OFFLINE BEHAVIOR

Because personalization is noncritical:

offline startup should use local cached configuration.

No KV:
→ app still starts.

Corrupted cache:
→ Classic 99.

No internet:
→ customization still applies locally.

Assess which local storage mechanism fits current application best:

localStorage
IndexedDB
Capacitor Preferences
existing settings utility

Recommend one.

No implementation.

---

# 51. DATA PRIVACY

Future KV document should contain visual preferences only.

No:

name
email
academic scores
health data
messages
MCQ history
lecture progress
roles

Only customization identifiers.

Confirm.

---

# 52. CLOUDFARE COST / USAGE DESIGN

Do not calculate speculative exact billing.

Architect for low usage:

- one small KV read during authenticated reconciliation
- local cache for startup
- one KV write per explicit Apply
- avoid writes per click
- avoid academic SQL
- avoid polling KV

Confirm that this access pattern is appropriate.

---

# 53. PRESET ARCHITECTURE

Future presets such as:

Classic
Night Study
Minimal
Fresh

must only set existing personalization fields.

Presets are NOT separate stored theme documents.

They are bundled code definitions.

No new functionality.

Assess feasibility.

---

# 54. LIVE PREVIEW ARCHITECTURE

Future Settings preview should render a small representative preview.

It should NOT mount a second full copy of the app.

Do not duplicate complex components.

Recommend a lightweight preview component using tokens.

No implementation.

---

# 55. APPLY / CANCEL MODEL

Evaluate future UX:

editing creates draft state

Apply:
→ commit locally
→ persist/sync

Cancel/back without Apply:
→ discard draft

Reset:
→ draft Classic 99 defaults
→ Apply required to commit

Recommend exact behavior.

---

# 56. FAILURE MODEL

Future failures must not make app unusable.

Examples:

KV 500
invalid themeId
unknown iconId
old preference version
offline

Expected safe behavior:

- keep local valid preference
- otherwise Classic 99
- no fatal screen
- no auth failure

Report.

---

# 57. NO DATABASE MIGRATION EXPECTED

Personalization system itself should ideally require:

No Prisma schema change.
No PostgreSQL migration.

Cloudflare KV is intended for account-synced preferences.

Confirm whether repository architecture supports this.

If you find a genuine blocker, explain it.

Do not create a schema migration.

---

# 58. NO D1 REQUIRED

Personalization does not need D1.

Do not propose D1 unless audit reveals a compelling architectural reason.

KV is preferred for preferences.

---

# 59. NO R2 REQUIRED

Themes and app icons are bundled assets.

Do not upload theme/icon assets to R2.

Only IDs are preferences.

---

# 60. NO AI

Personalization requires zero AI/provider calls.

Do not involve Cloudflare Workers AI.

---

# 61. NO NEW NPM DEPENDENCIES

Phase 0 should add none.

Future implementation should prefer current libraries.

Audit whether any new dependency would even be necessary.

Expected:
none.

---

# 62. IDENTIFY HIGH-RISK FILES

Produce a list of files where future personalization changes have elevated regression risk.

Examples may include:

App.tsx
index.css
main.tsx
Settings
Home
navigation
theme-preload
Capacitor config
Info.plist

Rank risk:

HIGH
MEDIUM
LOW

Explain why.

---

# 63. IDENTIFY SAFE MODULARIZATION POINTS

Recommend future new directories/modules, e.g.:

src/features/personalization/
shared/personalization.ts

But only if they fit current repository architecture.

Do not create them now.

Avoid dumping personalization logic into App.tsx.

---

# 64. FUTURE PHASE VALIDATION STRATEGY

Recommend regression checks after each future phase.

At minimum include:

build
focused TypeScript
focused ESLint
client navigation
auth
MCQ
Flashcards
Notes
Module Resources
Calendar where applicable
Capacitor sync

Do not execute unnecessary destructive tests during audit.

---

# 65. INSPECT CURRENT TEST INFRASTRUCTURE

List relevant existing tests that can protect personalization work.

Identify missing test coverage that future phases may need.

Do not add tests yet in this audit.

---

# 66. GIT WORKTREE SAFETY

Run:

git status --short
git diff --check

Record existing dirty files before audit.

Do not clean/reset user work.

Do not modify existing files.

At end, confirm this audit introduced no new diff.

---

# 67. FORBIDDEN CHANGES

Absolutely do not modify:

auth
OAuth
user roles
owner/admin hierarchy
lecture data
material upload
Notes
Module Resources
R2
MCQ business logic
MCQ categories
MCQ difficulty
Flashcards
Calendar data
notifications behavior
Study Hall
points
moderation
database schema
Prisma migrations
D1
AI providers
Cloudflare AI
PDF security
swipe navigation
gesture mechanics

No feature work in Phase 0.

---

# 68. NO `.agents/*`

Do not create or modify:

.agents/*

No agent artifacts.

---

# 69. AUDIT SUCCESS CRITERIA

Phase 0 is complete only when we know:

- exact current appearance architecture
- exact current Light/Dark mechanism
- exact current Home content
- what existing Home content is safely customizable
- current color/visual architecture
- semantic colors that must remain fixed
- current motion architecture
- current Hero architecture
- current glass architecture
- current Settings architecture
- current preference storage
- Cloudflare KV integration point
- app icon/native structure
- PWA icon limitations
- initial paint sequence
- RTL concerns
- responsive risks
- safe future module boundaries
- Classic 99 visual baseline
- files most at risk

And ZERO product behavior was changed.

---

# 70. MANDATORY FINAL REPORT

Return a comprehensive report.

Use this exact title:

# 99'S GUIDE PERSONALIZATION — PHASE 0 AUDIT REPORT

## 1. Phase Status

State:

PASS

or:

BLOCKED

This is an audit status only.

## 2. Runtime Changes

Expected:

NONE

State clearly whether any runtime source was modified.

## 3. Repository Baseline

Summarize current frontend/backend/native/theme architecture.

## 4. Current Appearance System

Explain exact Light/Dark/System implementation.

Include:

- source of truth
- persistence
- startup sequence
- relevant files

## 5. Initial Paint Sequence

Explain from index.html to React mount.

Identify flash risk.

## 6. Current Visual Architecture

Describe:

- CSS variables
- Tailwind
- hardcoded colors
- inline styles
- global CSS

## 7. Customizable Color Inventory

Table:

| Visual Role | Current Implementation | Files/Areas | Future Token Candidate |
|---|---|---|---|

Only customizable roles.

## 8. Semantic Colors That Must Remain Fixed

Table:

| Semantic Role | Current Color/Classes | Area |
|---|---|---|

Include MCQ difficulty.

## 9. Proposed Theme Token Set

Proposal only.

List minimal tokens and intended consumers.

## 10. Classic 99 Baseline

Describe the current production visual values that must become Classic 99.

## 11. Current Home Sections

CRITICAL.

Table:

| Existing Section | File/Component | Current Order | Mobile/iPad/Desktop | Classification |
|---|---|---:|---|---|

Classification only:

LOCKED_CORE
CUSTOMIZABLE_EXISTING

Do not include nonexistent sections.

## 12. Home Customization Recommendation

For each CUSTOMIZABLE_EXISTING section state whether future system should allow:

- reorder
- hide/show
- both
- neither

## 13. Hero Audit

Exact current behavior and safe visual customization scope.

## 14. Glass Audit

Current glass patterns and consolidation recommendation.

## 15. Motion Audit

Table:

| Motion | Type | File | Can Full/Subtle/Reduced Affect It? | Risk |
|---|---|---|---|---|

## 16. Reading Size Scope

List exact existing content areas suitable for reading-size customization.

## 17. Settings Architecture

Where My 99 should eventually live.

## 18. Current Preference Storage

Table:

| Preference | Current Store | Key/Mechanism | Account or Device |
|---|---|---|---|

## 19. Cloudflare Architecture

List current relevant Cloudflare services/configuration.

## 20. KV Suitability

State whether Workers KV is suitable.

Explain recommended integration point.

Do NOT create KV.

## 21. Recommended KV Security Architecture

Choose ONE preferred future model:

A. frontend → Render authenticated backend → KV

or

B. frontend → authenticated Cloudflare Worker → KV

or another justified model.

Explain why.

## 22. Future KV Data Shape

Proposal only.

Show compact example JSON using IDs/options only.

No personal data.

## 23. Local Cache Recommendation

Choose:

localStorage
IndexedDB
Capacitor Preferences
existing abstraction

Explain.

## 24. Startup / Sync Recommendation

Exact future local-first sequence.

## 25. Apply / Draft Recommendation

Define:

draft
apply
cancel
reset

## 26. Account-Synced vs Device-Local

Table:

| Preference | Account Sync | Device Local | Reason |
|---|---:|---:|---|

Include app icon.

## 27. Appearance Sync Recommendation

Should System/Light/Dark sync or remain per-device?

Give one recommendation.

## 28. App Icon Audit

Report:

- current icon assets
- PWA setup
- iOS setup
- alternate icon readiness

## 29. App Icon Future Architecture

Proposal for 10–12 bundled variants.

No implementation.

## 30. PWA Icon Limitation

State realistic behavior.

## 31. In-App Brand Mark

List occurrences and future safe handling.

## 32. Theme Pack Feasibility

Assess:

Classic 99
Midnight
Ocean
Emerald
Rose
Amber
Violet
Monochrome

No implementation.

## 33. Responsive Risks

Table:

| Area | Mobile | iPad Portrait | iPad Landscape | Desktop |
|---|---|---|---|---|

## 34. RTL / Arabic Risks

List concrete findings.

## 35. Accessibility Requirements

List constraints for future themes.

## 36. Navigation Safety

Identify visual-only vs behavior-critical navigation styles.

## 37. Console Scope

Explain exactly what future personalization may and may not affect.

## 38. Modules Scope

Same.

## 39. MCQ Scope

Same.

## 40. Flashcard Scope

Same.

## 41. Calendar Scope

Same.

## 42. Profile Scope

Same.

## 43. Notifications Scope

Same.

## 44. SQL Impact

State expected future personalization SQL usage.

Expected:

0 dedicated personalization SQL writes.

## 45. D1 Impact

Expected:

Not required.

## 46. R2 Impact

Expected:

Not required.

## 47. AI Impact

Expected:

None.

## 48. Performance Risks

List potential remount/refetch/layout risks and mitigation strategy.

## 49. High-Risk Files

Table:

| File | Risk | Why |
|---|---|---|

## 50. Recommended Future Module Structure

Proposal only.

## 51. Existing Tests We Can Reuse

List them.

## 52. Missing Tests Needed Later

List them.

## 53. Existing Dirty Git State

Report pre-existing changes.

Do not alter them.

## 54. Git Diff After Audit

Confirm this audit introduced:

0 files changed

if true.

## 55. Agent Artifact Verification

State exactly:

No `.agents/*` file was created or modified.

## 56. Phase 1 Readiness

List any blockers.

If no blockers:

PERSONALIZATION PHASE 0: APPROVED FOR FOUNDATION DESIGN

If blocked:

PERSONALIZATION PHASE 0: BLOCKED

End EXACTLY with one of:

PERSONALIZATION PHASE 0 STATUS: AUDIT COMPLETE — READY FOR PHASE 1

or

PERSONALIZATION PHASE 0 STATUS: BLOCKED — DO NOT BEGIN IMPLEMENTATION