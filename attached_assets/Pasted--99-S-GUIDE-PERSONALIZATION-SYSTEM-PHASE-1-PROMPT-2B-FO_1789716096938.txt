# 99'S GUIDE — PERSONALIZATION SYSTEM
# PHASE 1 / PROMPT 2B
## FOUNDATION CONTRACT REPAIR & VERIFICATION
### SMALL CORRECTION ONLY — NO RUNTIME INTEGRATION

The Phase 1 foundation implementation is mostly correct.

Do NOT begin Prompt 3.

Do NOT add runtime personalization.

Do NOT modify UI, Settings, App.tsx, Home, index.css, theme preload, KV, SQL, D1, R2, native icons, or application behavior.

This task is ONLY to correct and fully verify the existing personalization foundation.

Current files include:

shared/personalization.ts
tests/personalization.test.ts

Keep changes limited to these files unless a tiny neutral shared type import is absolutely necessary.

---

# 1. CRITICAL CORRECTION — SUBJECT ORDER

The current report states:

- subject-order deduplication and completion
- partial subject-order normalization

That is NOT the required behavior.

The required V1 behavior is strict and deterministic:

A valid personalized subject order must:

- contain every canonical subject ID
- contain each canonical subject ID exactly once
- contain no duplicates
- contain no unknown IDs
- contain no missing IDs

ONLY a complete valid permutation is accepted.

If `home.subjectOrder` is:

- missing
- partial
- duplicated
- contains an unknown ID
- contains a missing canonical ID
- contains an extra ID
- wrong type

then the normalized result must use the COMPLETE canonical production subject order.

Do NOT deduplicate-and-complete a partial user order.

Do NOT attempt best-effort repair.

Rationale:

Customization must be deterministic and must never invent an ordering the user did not explicitly save.

---

# 2. CANONICAL PRODUCTION SUBJECT ORDER

Read the actual current Home subjects from the repository.

Export one immutable canonical order.

Report the exact subject IDs and their order.

Do NOT invent IDs.

Do NOT reorder current production Home.

This constant is the fallback for invalid subject order.

---

# 3. IMMUTABILITY

Ensure defaults cannot be accidentally mutated.

This must be safe:

```ts
const a = createDefaultPersonalization();
const b = createDefaultPersonalization();

Mutating nested arrays in a must not mutate:

b
canonical constants
DEFAULT_PERSONALIZATION_V1

Use readonly constants and fresh copies where required.

Add a test proving this.

4. ACCOUNT CONFIG MUST EXCLUDE APPEARANCE

The account-synced V1 contract must NOT contain:

appearance
themeMode
colorScheme
light
dark
system

as an account field.

Light / Dark / System remains device-local.

For STRICT server-style validation:

input containing an explicit unknown account field such as:

{
  "appearance": "dark"
}

must fail strict validation.

For SAFE local normalization:

unknown keys may be stripped, but they must never appear in normalized PersonalizationConfigV1.

Add explicit tests.

5. ACCOUNT CONFIG MUST EXCLUDE APP ICON

The account-synced V1 contract must NOT contain:

appIconId
iconId
nativeIcon

Strict validation of a payload containing appIconId must fail.

Safe normalization may ignore the unknown field.

Normalized account config must never contain it.

Add explicit tests.

6. EXACT V1 CONTRACT

Verify the final account-synced shape remains only:

type PersonalizationConfigV1 = {
  version: 1;
  themeId: ThemeId;
  heroStyle: HeroStyle;
  glassStyle: GlassStyle;
  motionStyle: MotionStyle;
  readingSize: ReadingSize;
  home: {
    subjectOrder: readonly SubjectId[]; // or equivalent safe type
  };
};

Do NOT add unrelated fields.

If exact syntax differs because of readonly/Zod inference, report the actual final type.

7. CANONICAL THEME IDS

Verify exactly these IDs:

classic-99
midnight
ocean
emerald
rose
amber
violet
monochrome

No other user theme IDs.

No arbitrary colors.

8. OTHER CANONICAL IDS

Hero:

classic
minimal
night
aurora

Default:
classic

Glass:

clear
balanced
frosted

Default:
balanced

Motion:

full
subtle
reduced

Default:
full

Reading:

small
default
large

Default:
default

Confirm these exactly.

9. CLASSIC 99 DEFAULT

Default must be:

version = 1
themeId = "classic-99"
heroStyle = "classic"
glassStyle = "balanced"
motionStyle = "full"
readingSize = "default"
home.subjectOrder = exact production order

No runtime style values are required yet.

10. SAFE NORMALIZATION VS STRICT VALIDATION

Maintain two clearly different behaviors.

Strict validation

For future server/KV writes:

requires valid complete V1
rejects unknown keys
rejects invalid enums
rejects invalid/incomplete subject order
rejects appearance
rejects appIconId

No silent correction for server writes.

Safe normalization

For local/historical/corrupted cached data:

must NEVER throw
valid V1 → use normalized value
invalid/unsupported V1 → Classic 99 defaults
unknown irrelevant fields may be ignored ONLY if the remaining document is otherwise valid
invalid subject order → complete canonical default order

Document exact behavior.

11. VERSION BEHAVIOR

Verify:

valid version 1
→ accepted

missing version
→ defaults

version 0
→ defaults unless a real V0 migration exists (it should not)

version 2
→ defaults

version 99
→ defaults

string version
→ defaults

Do NOT guess future migrations.

12. PAYLOAD SIZE LIMIT

The Phase 1 specification required a small account payload limit.

Add/export:

MAX_PERSONALIZATION_PAYLOAD_BYTES

Use:

16 KiB = 16384 bytes

unless the current implementation already chose another defensible small value.

Prefer exactly:

16384

for consistency.

Implement a pure UTF-8 byte-size helper using an environment-safe approach compatible with frontend and backend.

Do NOT depend solely on Node Buffer in the shared module if that would make the browser bundle unsafe.

A TextEncoder-compatible or similarly neutral strategy is preferred.

Provide a helper such as:

isPersonalizationPayloadWithinLimit(...)

or equivalent.

This does not create KV or HTTP handling yet.

13. PAYLOAD TESTS

Test:

small normal V1 document → within limit
Unicode/Arabic counts as UTF-8 bytes correctly
payload below 16384 → accepted by size helper
payload >16384 → rejected

Do not allocate giant files.

In-memory strings are sufficient.

14. MALFORMED INPUT MATRIX

Add/verify tests for at least:

undefined
null
""
"hello"
0
123
[]
{}
wrong version
unknown theme
unknown hero
unknown glass
unknown motion
unknown reading size
subjectOrder missing
subjectOrder partial
subjectOrder duplicate
subjectOrder unknown ID
subjectOrder extra ID
appearance field in strict payload
appIconId field in strict payload
large irrelevant object / oversized serialized payload

No uncaught exception.

15. UNKNOWN FIELD POLICY TEST

Explicitly prove:

Safe normalization:
valid V1 + unrelated unknown key
→ produces clean V1 without unknown key

Strict validation:
valid V1 + unrelated unknown key
→ fails

This intentional distinction must be documented.

16. PERSONAL DATA BOUNDARY

Verify there are NO contract fields for:

name
email
avatar
role
academicGroup
scores
progress
MCQ history
notification data
messages
health data

No need to enumerate every impossible key in Zod; confirm final shape contains none.

17. ACCESSIBILITY CONTRACT COMMENTS

Retain/document the future rule:

OS accessibility always overrides personalization in the safer direction.

Specifically:

prefers-reduced-motion: reduce
must never be overridden by:
motionStyle = full

Reduced-transparency accessibility preference must similarly take precedence over user glass preference.

Do NOT wire runtime behavior yet.

18. NO RUNTIME INTEGRATION

Mandatory:

Do NOT modify:

src/App.tsx
src/index.css
src/main.tsx
public/theme-preload.js
index.html
SettingsView
HomeDashboard
server.ts
Prisma
Cloudflare worker
Capacitor
Info.plist
icons

No new runtime import of shared personalization contract.

19. TESTS

Run the complete personalization foundation suite.

Expected test count will now be greater than the previous 7.

Also run:

npm run build
npm run test:auth
npx tsx --test tests/client-navigation.test.ts
git diff --check

Run focused MCQ metadata tests and a focused Notes policy test if available.

Do not repair unrelated baseline failures.

20. DIFF SCOPE

Expected changed files:

shared/personalization.ts
tests/personalization.test.ts

Nothing else.

If any other file changes, explain exactly why.

No .agents/*.

21. MANDATORY FINAL REPORT

Return exactly:

99'S GUIDE PERSONALIZATION — PHASE 1 FOUNDATION REPAIR REPORT
1. Status

PASS / FAIL

2. Files Changed

Exact list.

3. Final V1 Type

Print exact TypeScript shape.

4. Canonical Subject IDs

List exact IDs in production order.

5. Subject Order Semantics

Confirm:

valid complete permutation accepted
partial falls back completely
duplicate falls back completely
unknown falls back completely
no deduplicate-and-complete behavior remains
6. Classic 99 Defaults

Exact object values.

7. Theme IDs

Exact eight.

8. Hero / Glass / Motion / Reading IDs

Exact values/defaults.

9. Appearance Boundary

Confirm device-local and absent from account contract.

10. App Icon Boundary

Confirm device-local and absent from account contract.

11. Strict Validation

Explain exact unknown-field behavior.

12. Safe Normalization

Explain exact fallback behavior.

13. Versioning

Exact supported/future behavior.

14. Payload Limit

Report:

MAX_PERSONALIZATION_PAYLOAD_BYTES = 16384

or exact chosen value and reason.

Explain UTF-8 measurement.

15. Immutability

Explain test proving defaults cannot be mutated through returned objects.

16. Personal Data Boundary

Confirm account document contains presentation preferences only.

17. Tests

List every personalization test.

18. Test Results
Command	Result
19. Runtime Impact

State exactly:

NO RUNTIME VISUAL OR BEHAVIOR CHANGE

20. SQL / D1 / R2 / KV

Expected:

No SQL change.
No D1 change.
No R2 change.
No KV created.

21. Forbidden File Verification

Confirm no changes to:

App.tsx
index.css
theme-preload
Settings
Home
server
Prisma
Cloudflare Worker
native files

22. Agent Artifact Verification

State exactly:

No .agents/* file was created or modified.

23. Prompt 3 Readiness

If all checks pass, state:

PERSONALIZATION FOUNDATION CONTRACTS: APPROVED FOR LOCAL-FIRST INTEGRATION

End exactly:

PERSONALIZATION PHASE 1 PROMPT 2B STATUS: COMPLETE — READY FOR PROMPT 3

or:

PERSONALIZATION PHASE 1 PROMPT 2B STATUS: BLOCKED — REPAIR REQUIRED