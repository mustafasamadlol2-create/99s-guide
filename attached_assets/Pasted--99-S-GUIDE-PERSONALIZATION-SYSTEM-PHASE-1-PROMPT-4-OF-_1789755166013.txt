# 99'S GUIDE — PERSONALIZATION SYSTEM
# PHASE 1 / PROMPT 4 OF ~22
## RUNTIME PERSONALIZATION PROVIDER + ACCOUNT HYDRATION
### First Runtime Integration
### ZERO VISUAL CHANGE
### NO KV / NO SQL / NO THEME TOKENS YET

Phase 0 audit is APPROVED.

Phase 1 / Prompt 2B foundation contracts are APPROVED.

Phase 1 / Prompt 3 local-first storage/state foundation is APPROVED.

Current approved modules include:

shared/personalization.ts

src/features/personalization/personalizationStorage.ts
src/features/personalization/personalizationState.ts

tests/personalization.test.ts
tests/personalization-storage.test.ts
tests/personalization-state.test.ts

We are now performing the FIRST runtime integration of personalization.

This prompt must mount a safe personalization runtime/provider into the actual application and connect it to the CURRENT authenticated user identity.

However:

THE APPLICATION MUST LOOK AND BEHAVE EXACTLY THE SAME AFTER THIS PROMPT.

No theme rendering is allowed yet.

No CSS tokens are allowed yet.

No Settings UI is allowed yet.

No Home customization is allowed yet.

No Cloudflare KV is allowed yet.

The personalization runtime will exist and hydrate state, but NOTHING outside the personalization architecture should visually consume those preferences yet.

---

# 1. PRIMARY GOAL

Implement:

1. Personalization runtime/provider.
2. Safe authenticated-user hydration.
3. Account-switch isolation.
4. Logout in-memory reset.
5. Stale async hydration protection.
6. Draft operations exposed through the runtime.
7. Local Apply orchestration.
8. Structured hydration/apply status.
9. Runtime tests.
10. Minimal application mounting.

Do NOT implement visual theming.

---

# 2. ABSOLUTE PRODUCT BOUNDARY

Personalization remains PRESENTATION ONLY.

This prompt must NOT add or alter:

- academic content
- lectures
- modules
- module resources
- MCQ behavior
- MCQ categories
- MCQ difficulty
- Flashcards
- Calendar
- Profile functionality
- notifications behavior
- Study Hall
- points
- roles
- admin permissions
- moderation
- AI
- PDF behavior
- navigation destinations
- gestures
- swipe mechanics
- Home content

No new product feature is permitted.

---

# 3. ZERO VISUAL CHANGE REQUIREMENT

Mandatory:

After this prompt:

- Classic 99 looks identical.
- Light mode looks identical.
- Dark mode looks identical.
- System mode behaves identically.
- Hero looks identical.
- Home order remains identical.
- Sidebar looks identical.
- Bottom bar looks identical.
- Console looks identical.
- Modules look identical.
- Calendar looks identical.
- MCQ looks identical.
- Flashcards look identical.
- Notifications look identical.
- Profile looks identical.

The new personalization provider MUST NOT:

- add classes to `<html>`
- add `data-theme`
- set CSS variables
- change colors
- change font size
- change glass
- change motion
- change Hero
- reorder subjects
- change app icon

That belongs to later prompts.

---

# 4. AUDIT CURRENT AUTH OWNERSHIP BEFORE WIRING

Before modifying runtime integration, inspect how the authenticated current user is currently resolved.

Find the canonical existing source for:

- current authenticated user
- current user ID
- auth loading state
- logout
- account switching/session restoration

DO NOT create a second auth system.

DO NOT decode JWT manually if the application already exposes authenticated user state.

DO NOT read arbitrary IDs from localStorage if trusted current-user state already exists.

Use the closest existing trusted runtime source.

Report exactly what was used.

---

# 5. PERSONALIZATION PROVIDER

Create a focused provider, likely under:

src/features/personalization/

Recommended:

PersonalizationProvider.tsx
usePersonalization.ts

or equivalent small structure.

Do NOT put the implementation into:

App.tsx
SettingsView.tsx
HomeDashboard.tsx

A tiny integration import/wrapper in an existing root file is acceptable.

Keep logic modular.

---

# 6. PROVIDER INPUT

Preferred architecture:

The provider receives or derives the CURRENT authenticated opaque user ID from existing trusted auth state.

Conceptually:

```tsx
<PersonalizationProvider userId={currentUser?.id ?? null}>
  ...
</PersonalizationProvider>