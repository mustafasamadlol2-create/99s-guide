# 99'S GUIDE — PERSONALIZATION SYSTEM
# PHASE 2 / PROMPT 10 OF ~22
## MCQ & FLASHCARD CLASSIC 99 SEMANTIC TOKENIZATION
### Neutral Presentation Shell Only
### CORRECTNESS / DIFFICULTY / CATEGORY / REVIEW / SUBJECT IDENTITY PROTECTED
### NO ALTERNATE THEMES YET

Phase 0 is APPROVED.

Phase 1 is CLOSED AND APPROVED.

Phase 2 / Prompt 5B:
CLASSIC 99 TOKEN FOUNDATION — CLOSED AND APPROVED.

Phase 2 / Prompt 6B:
CORE CHROME CLASSIC 99 TOKENIZATION — CLOSED AND APPROVED.

Phase 2 / Prompt 7:
LOW-RISK FEATURE-SURFACE CLASSIC 99 TOKENIZATION — CLOSED AND APPROVED.

Phase 2 / Prompt 8:
LECTURE & CONTENT-SURFACE CLASSIC 99 TOKENIZATION — CLOSED AND APPROVED.

Phase 2 / Prompt 9:
CALENDAR CLASSIC 99 TOKENIZATION — CLOSED AND APPROVED.

We are now tokenizing the student-facing MCQ and Flashcard presentation layers.

This is HIGH RISK because these interfaces contain colors that convey:

- correctness
- incorrectness
- selected answers
- difficulty
- category
- review state
- progress
- bookmarks
- subject identity
- clinical/review context

Therefore Prompt 10 is strictly:

GENERIC NEUTRAL PRESENTATION
→ CLASSIC 99 SEMANTIC TOKENS

while preserving every academic and interaction semantic exactly.

Required result:

BEFORE
=
AFTER
=
CLASSIC 99

No intentional redesign.

No business-logic modification.

No alternate theme rendering yet.

---

# 1. EXACT PROMPT 10 SCOPE

Audit and selectively token-convert GENERIC presentation colors in:

A. Student-facing MCQ UI.

B. Student-facing Flashcard / Anki UI.

Potential safe targets:

- outer page/workspace neutral surfaces
- generic card shell where it is truly neutral
- generic primary question/concept text
- generic secondary/supporting text
- neutral separators
- neutral borders
- generic nonsemantic icon/button surfaces
- empty states
- neutral loading shells
- neutral explanatory containers ONLY if not correctness/review-state colored

Do not assume every neutral-looking color is themeable.

Audit first.

---

# 2. EXPLICITLY OUT OF SCOPE

Do NOT token-convert or behaviorally modify:

- MCQ correctness
- incorrect answers
- selected-answer state
- submitted-answer state
- Easy difficulty
- Normal difficulty
- Hard difficulty
- MCQ categories
- Previous Year identity
- AI Generated identity
- Resource identity
- bookmark semantic state
- hint state if color carries meaning
- explanation semantic state
- progress/completion semantics
- points
- scoring
- Flashcard subject identity
- Flashcard review-state colors
- Flashcard difficulty/review buttons
- card flip behavior
- card progress
- captured/review tags
- Clinical Concept / Explanation state semantics

Only generic presentation.

---

# 3. NO ALTERNATE THEMES

Do NOT implement visual palettes for:

- midnight
- ocean
- emerald
- rose
- amber
- violet
- monochrome

Only Classic 99 remains visually implemented.

`themeId` stays visually inert.

---

# 4. NO RUNTIME THEME SWITCHING

Still forbidden:

- `data-theme`
- theme DOM classes
- `style.setProperty`
- React palette objects
- dynamic CSS variable writes
- theme-based component keys
- page reload
- app remount

No runtime alternate-theme behavior in Prompt 10.

---

# 5. AUDIT ACTUAL MCQ ARCHITECTURE FIRST

Locate all actual STUDENT-FACING MCQ components and rendering paths.

Likely sources may include:

- `LectureDetailView.tsx`
- MCQ-specific feature components
- MCQ question component
- options
- result/explanation components
- category/filter tabs
- progress/header components

But repository source is authoritative.

Do NOT broaden into Admin/Console MCQ creation unless a genuinely shared neutral component is involved.

Report exact files audited.

---

# 6. ADMIN MCQ CREATION OUT OF SCOPE

Do NOT directly refactor:

- `CreateMCQ.tsx`
- AI Review Studio
- AI Import
- Admin Console MCQ tools
- import/check flows
- metadata persistence

unless a tiny shared purely-presentational component is already reused by the student MCQ UI and exact parity requires it.

Expected:
no direct admin MCQ changes.

---

# 7. MCQ COLOR CLASSIFICATION

Every MCQ color must be classified before migration:

A. GENERIC THEMEABLE PRESENTATION
B. CORRECTNESS SEMANTIC
C. DIFFICULTY SEMANTIC
D. CATEGORY / SOURCE IDENTITY
E. SELECTION / ANSWER STATE
F. PROGRESS / COMPLETION
G. BOOKMARK / SAVED STATE
H. WARNING / ERROR / SUCCESS
I. BRAND / DECORATIVE
J. UNKNOWN / HIGH RISK

Only A should normally migrate.

---

# 8. MCQ DIFFICULTY — ABSOLUTELY FIXED

Persistent difficulty values:

- Easy
- Medium internally / Normal in UI
- Hard

Current intended visual semantics:

Easy
→ green

Normal
→ amber

Hard
→ red

These MUST remain independent of user theme.

Do NOT map any of them to:

- semantic-action-accent
- semantic-navigation-tab-active
- future ThemeId
- generic themed surface

Future Emerald theme must not make Hard emerald.

Future Rose theme must not make Easy rose.

---

# 9. MCQ CORRECTNESS — ABSOLUTELY FIXED

Correct answer:
retain success/green semantics.

Incorrect answer:
retain danger/red semantics.

Do NOT theme these.

This applies to:

- option background
- option border
- icons
- text
- explanation/result indicators
- submitted-state indicators

where current code uses correctness semantics.

---

# 10. SELECTED ANSWER STATE

Audit selected-but-not-yet-judged option styling separately from:

correct
incorrect

If selection uses a generic interaction accent:
it may eventually be themeable.

BUT Prompt 10 should migrate it only if:

- it is definitely not correctness semantic
- exact Light/Dark parity exists
- behavior remains unchanged

When uncertain:
DEFER.

Do not alter answer-selection logic.

---

# 11. MCQ CATEGORY SEMANTICS

Canonical categories include:

- ALL — virtual UI filter only
- PREVIOUS_YEAR
- AI_GENERATED
- RESOURCE

Do NOT alter:

- persisted category values
- filtering
- default category
- labels
- category routing
- category order
- source metadata

If category chips/tabs have differentiated identity colors:
preserve them for Prompt 10.

If they are purely generic selected/unselected tabs:
migration is allowed only with proven exact parity.

Document classification.

---

# 12. MCQ ALL FILTER

`ALL` is a virtual filter and not persisted.

Do not accidentally introduce it into database/metadata.

Prompt 10 is visual-only.

---

# 13. MCQ SOURCE HIDING IN ALL

Current product behavior where All can mix questions without exposing origin must remain unchanged.

No source-label behavior changes.

---

# 14. MCQ QUESTION TEXT

Generic question text may move to approved semantic primary/chrome text token if exact.

Do not alter:

- font size
- font weight
- line height
- wrapping
- RTL
- selection
- animation

---

# 15. MCQ OPTION NEUTRAL STATE

Neutral/unanswered option surface may be themeable if it is purely generic.

Possible migration targets:

- default neutral card background
- neutral border
- generic text

ONLY if exact existing token values match.

Do NOT affect:

selected
correct
incorrect
disabled/submitted semantic styles.

---

# 16. MCQ HINT

Audit Hint presentation.

If hint uses amber/yellow because it conveys hint/warning/assistance semantics:
do not genericize it.

If only its surrounding neutral card is generic:
that surrounding shell may migrate.

No hint logic changes.

---

# 17. MCQ EXPLANATION

Audit explanation presentation.

Generic explanation text/card may be themeable.

But preserve any:

correctness
source
clinical
warning
success

semantic colors.

No explanation behavior changes.

---

# 18. MCQ BOOKMARK

Selected bookmark color/state is semantic user state.

Do NOT automatically theme it.

Bookmark logic and persistence untouched.

---

# 19. MCQ PROGRESS

Progress/completion may carry semantic meaning.

Do not convert progress colors into generic user theme accent unless existing implementation is purely decorative and exact classification proves it.

Conservative default:
leave progress semantics unchanged.

---

# 20. MCQ POINTS / SCORE

Do NOT modify:

- points calculation
- score
- answered count
- progress
- completion
- server writes
- state transitions

No functionality changes.

---

# 21. MCQ FILTERS

Do not change:

- filtering behavior
- bookmarks filter
- difficulty filter
- category filter
- navigation
- shuffle
- stable ordering

Only neutral visual shell where exact.

---

# 22. MCQ STABLE SHUFFLE

Do NOT alter the current stable shuffle/mixed All behavior.

No randomization refactor.

---

# 23. AUDIT ACTUAL FLASHCARD ARCHITECTURE

Identify actual student-facing Flashcard components/rendering.

Potentially:

- `LectureDetailView.tsx`
- Flashcard-specific components
- question/front view
- explanation/back view
- progress/review controls
- subject theme helper

Repository source is authoritative.

Report exact audited files.

---

# 24. FLASHCARD COLOR CLASSIFICATION

Classify:

A. GENERIC THEMEABLE PRESENTATION
B. SUBJECT IDENTITY
C. FRONT/BACK SEMANTIC STATE
D. REVIEW STATE
E. DIFFICULTY / CONFIDENCE
F. PROGRESS
G. BRAND / DECORATIVE
H. UNKNOWN / HIGH RISK

Only A should migrate.

---

# 25. FLASHCARD SUBJECT IDENTITY — FIXED

Current Flashcards use subject-specific identity via logic such as:

`getFlashcardTheme(lecture.subjectId)`

Preserve this.

Do NOT replace:

teal
violet
sky
rose
amber
indigo
slate
or other subject-specific accents

with generic theme accent.

A future Ocean theme must not make every Flashcard subject Ocean.

---

# 26. FLASHCARD FRONT/BACK BEHAVIOR

The current card behavior where:

Clinical Concept
→ Explanation

and content changes on tap/space must remain unchanged.

Do NOT modify:

- flip/toggle state
- tap handling
- keyboard Space behavior
- title transition
- content transition
- progress display
- tags
- review controls
- animation timing

Color/token only.

---

# 27. FLASHCARD CARD SURFACE

Audit whether the card itself is:

A. generic neutral surface

or:

B. intentionally subject-colored / identity-bearing.

If B:
leave it.

If A:
generic neutral background/text/border may migrate if exact.

Do not flatten subject identity.

---

# 28. FLASHCARD REVIEW STATES

Any controls representing:

Easy
Medium/Normal
Hard
Again
Known
Review
or equivalent current review states

must preserve their current semantic colors.

Do NOT make them generic theme accent.

---

# 29. FLASHCARD PROGRESS

Do not change:

progress counts
review state
card index
session logic
persistence
keyboard navigation

If progress bar has semantic/subject identity:
leave its color.

---

# 30. CAPTURE / REVIEW TAGS

The user previously required these to remain visible across the question/explanation transition.

Do NOT change:

- visibility
- layout
- labels
- persistence
- transition

If their color encodes status:
preserve it.

---

# 31. GENERIC FLASHCARD TEXT

Pure neutral:

- page title
- helper text
- generic metadata

may use semantic text tokens if exact.

Do not change typography.

---

# 32. SHARED LECTUREDETAILVIEW — HIGH RISK

Prompt 8 already touched generic Lecture surfaces.

If Prompt 10 modifies `LectureDetailView.tsx` again:

every Prompt 10 edit must be limited specifically to MCQ/Flashcard presentation classes.

Do NOT reopen Prompt 8 areas.

Do NOT alter:

PDF
Notes
Video
Q&A

unless a single shared neutral token class necessarily affects all tabs and is already approved.

Expected:
no need to re-edit their behavior.

---

# 33. LECTURE TAB BAR REMAINS FROZEN

Do NOT modify:

PDF / Notes / MCQ / Anki / Video / Q&A tab geometry or behavior.

Prompt 8 already preserved it.

No new tab-bar color work unless absolutely necessary, which is not expected.

---

# 34. EXISTING APPROVED TOKENS FIRST

Reuse exact existing tokens:

- semantic-background-page
- semantic-background-grouped
- semantic-surface-primary
- semantic-surface-elevated
- semantic-surface-muted
- semantic-content-primary
- semantic-content-secondary
- semantic-content-subtle
- semantic-chrome-content-primary
- semantic-chrome-content-secondary
- semantic-chrome-content-subtle
- semantic-chrome-content-muted
- semantic-border-default
- semantic-border-strong
- semantic-action-accent
- semantic-action-accent-soft
- semantic-focus-ring
- semantic-chrome-surface-active
- semantic-chrome-surface-hover
- semantic-navigation-tab-active
- semantic-navigation-tab-inactive
- semantic-glass-surface
- semantic-glass-border

Use actual source names.

Do not invent duplicates.

---

# 35. NEW TOKEN LIMIT

New token role allowed only if:

- generic presentation
- exact Light/Dark pair known
- no approved existing token matches
- needed repeatedly or architecturally justified
- future themes should reasonably control it

Expected:
0–6 new roles.

If more:
defer.

Do not create:

--mcq-option-third-gray
--flashcard-label-two-muted

micro-tokens.

---

# 36. NO SEMANTIC COLORS IN THEMEABLE TOKEN FAMILY

Do NOT add themeable tokens for:

--mcq-easy
--mcq-hard
--mcq-correct
--mcq-incorrect

These are fixed/domain semantic roles.

If central aliases already exist or become useful:
they must remain FIXED semantic tokens, not themeable ones.

---

# 37. FIXED TOKEN OPTION

If the repository currently duplicates exact correctness/difficulty values heavily and a tiny fixed semantic alias is clearly beneficial, it MAY be centralized.

But:

- no visual change
- not required
- do not broaden scope
- must be explicitly classified fixed/non-themeable
- tests must enforce independence from theme accent

Conservative preference:
leave existing semantic classes intact.

---

# 38. NO HARD-CODED COLOR PURGE

Hardcoded colors are expected to remain.

Do NOT remove every:

green
red
amber
rose
violet
teal
hex
rgba

Many are intentional semantics/identity.

---

# 39. LIGHT MODE PARITY

Every migrated generic value must prove:

previous Light
=
new Classic token Light

exactly.

---

# 40. DARK MODE PARITY

Same for Dark.

No approximate substitution.

---

# 41. SYSTEM MODE

Unchanged:

`app_theme`
→ OS preference
→ `html.dark`
→ semantic tokens

No provider involvement.

---

# 42. RTL

MCQ and Flashcards contain Arabic/RTL-sensitive content.

Do NOT alter:

- `dir`
- text alignment
- option ordering
- icon ordering
- chevron direction
- padding direction
- keyboard navigation
- swipe direction

Color only.

---

# 43. KEYBOARD SAFETY

Do NOT alter:

- number/option shortcuts
- Enter/Space
- Flashcard Space reveal
- arrow keys
- desktop navigation
- focus behavior

No event-handler edits.

---

# 44. TOUCH SAFETY

Do NOT alter:

- tap behavior
- swipe
- drag
- pointer events
- hit targets
- gesture exclusions

---

# 45. IPHONE SAFETY

No geometry changes to:

- question cards
- option rows
- Flashcards
- bottom controls
- safe-area relationship
- Lecture tab relationship

---

# 46. IPAD SAFETY

No changes to:

- Lecture tab sizing
- card width
- option width
- landscape/portrait breakpoints
- margins/gaps
- keyboard shortcuts
- swipe-back layers

---

# 47. DESKTOP SAFETY

No changes to:

- keyboard interactions
- content width
- hover geometry
- sidebar relationship
- focus order

---

# 48. ACCESSIBILITY

Preserve:

- focus-visible
- ARIA
- keyboard controls
- reduced motion
- reduced transparency
- forced colors
- non-color-only correctness communication

Correct/incorrect must not become color-only if currently supplemented by icons/text.

Do not remove those indicators.

---

# 49. NO MOTION STYLE WIRING

`motionStyle` remains unwired.

Do NOT use Prompt 10 to alter:

- Flashcard transitions
- MCQ transitions
- option animation
- progress animation

---

# 50. NO READING SIZE WIRING

`readingSize` remains unwired.

Do not modify:

`app_text_scale`

No typography size changes.

---

# 51. NO GLASS / HERO / HOME WORK

All remain unwired/out of scope.

---

# 52. NO SETTINGS / PROFILE / NOTIFICATIONS / CALENDAR WORK

Those areas are closed.

Do not reopen them.

---

# 53. NO MODULE WORK

No Module changes.

---

# 54. NO CONSOLE WORK

No Admin/Console feature tokenization.

---

# 55. NO AI WORK

Do NOT modify:

AI Generate
AI Extract
AI Enhance
AI Import
review studio
Cloudflare AI
Gemini fallback
AI metadata generation

---

# 56. NO BACKEND WORK

Do not modify:

server.ts
MCQ APIs
Flashcard APIs
Prisma
D1 sync
points logic
dedupe
AI import
metadata persistence

Prompt 10 is frontend presentation only.

---

# 57. NO KV / SQL / D1 / R2

No personalization backend yet.

ZERO new network calls.

---

# 58. TOKEN SOURCE OF TRUTH

CSS remains the only source of actual visual values.

If metadata changes are needed:

`classic99Tokens.ts`

contains names/categories only.

Do not add palette values to TypeScript.

---

# 59. EXPECTED FILE SCOPE

Audit first.

Likely runtime files may include:

- `src/features/lectures/components/LectureDetailView.tsx`
- actual MCQ-specific student components
- actual Flashcard student components
- possibly shared neutral MCQ/Flashcard presentation helpers

plus:

- `src/index.css`
- `classic99Tokens.ts`
- tests

only if genuinely needed.

Aim:

2–8 runtime files.

Do not spread into a broad feature refactor.

---

# 60. LECTUREDETAILVIEW DIFF SAFETY

If touched:

every Prompt 10 modification must be:

CLASS / COLOR / TOKEN ONLY.

No:

state
hooks
effects
callbacks
conditionals
array logic
question logic
review logic
DOM restructuring
tab changes

---

# 61. MCQ SOURCE CONTRACT TEST

Add:

`tests/personalization-mcq-flashcard-surfaces.test.ts`

or equivalent.

Verify actual repository behavior.

At minimum test:

- generic MCQ shell/text uses semantic token where migrated
- difficulty remains fixed semantic
- correct remains fixed success
- incorrect remains fixed danger
- category/source identity remains independent
- no theme ID branching
- no business logic touched

---

# 62. FLASHCARD SOURCE CONTRACT TEST

Same test file may verify:

- generic Flashcard neutral shell/text uses semantic token where migrated
- subject identity remains direct
- review states remain direct
- Clinical Concept / Explanation behavior paths remain present
- capture/review tags remain present
- keyboard/tap transition path remains intact
- no theme branching

---

# 63. TEST DIFFICULTY ISOLATION

Explicitly ensure:

Easy
Normal
Hard

do not resolve through:

- semantic-action-accent
- semantic-navigation-tab-active
- themeId

---

# 64. TEST CORRECTNESS ISOLATION

Explicitly ensure:

correct
incorrect

remain independent from generic theme accent.

---

# 65. TEST FLASHCARD IDENTITY ISOLATION

Protect:

`getFlashcardTheme(...)`

or actual current identity helper/source.

No replacement by generic theme pack.

---

# 66. TEST NO RUNTIME THEME MUTATION

Existing global tests may already cover this.

Ensure Prompt 10 introduces none.

---

# 67. TEST NO ADMIN MCQ SCOPE EXPANSION

Verify Prompt 10 did not alter:

CreateMCQ
AI review/import
Console MCQ workflows

unless a shared file was genuinely involved.

Expected:
unchanged.

---

# 68. EXISTING MCQ METADATA TEST

Run:

`npx tsx --test tests/mcq-metadata.test.ts`

Mandatory.

---

# 69. MCQ AI REGRESSION

Run:

`npm run test:ai-mcq`

Mandatory.

Prompt 10 must not affect MCQ contracts or AI generation/import.

---

# 70. FLASHCARD AI REGRESSION

Run:

`npm run test:ai-flashcard`

Mandatory.

---

# 71. CLIENT NAVIGATION

Run:

`npx tsx --test tests/client-navigation.test.ts`

because LectureDetail is shared.

---

# 72. NOTES POLICY

Run:

`npx tsx --test tests/material-note-policy.test.ts`

because LectureDetail may be touched.

---

# 73. PDF REGRESSION

If `LectureDetailView.tsx` is touched, run the existing PDF access test.

Report the known expired-token fixture separately if it still fails.

Do not fix it.

---

# 74. AUTH

Run:

`npm run test:auth`

---

# 75. CALENDAR PROMPT 9 TEST

Run the new Calendar personalization suite to prove Prompt 10 did not reopen Calendar.

---

# 76. PROMPT 8 LECTURE TEST

Run:

`tests/personalization-lecture-surfaces.test.ts`

Mandatory if LectureDetailView changes.

---

# 77. ALL PERSONALIZATION TESTS

Run every personalization suite, including:

- personalization.test.ts
- personalization-state.test.ts
- personalization-storage.test.ts
- personalization-runtime.test.tsx
- personalization-tokens.test.ts
- Prompt 7 feature surfaces
- Prompt 8 lecture surfaces
- Prompt 9 Calendar surfaces
- Prompt 10 MCQ/Flashcard surfaces

Report total passing count.

---

# 78. BUILD

Run:

`npm run build`

Must pass.

---

# 79. TYPESCRIPT / ESLINT

Run targeted TS/ESLint on touched files.

Do not repair known unrelated:

- seven `.webp` imports
- `.local` lint issues
- supabaseStorage lint issue
- SmoothAutoHeight warning

---

# 80. VISUAL VERIFICATION

If authenticated preview is available, inspect:

MCQ LIGHT:
- neutral question shell
- unanswered options
- answered correct state
- answered incorrect state
- Easy/Normal/Hard badge
- explanation/hint
- category/filter area

MCQ DARK:
same where practical.

FLASHCARD LIGHT:
- front state
- explanation state
- progress
- subject identity
- review controls

FLASHCARD DARK:
same where practical.

Do NOT change anything simply because you prefer another appearance.

Only detect regressions.

---

# 81. VISUAL SEMANTIC CHECK

If authenticated preview exists, explicitly verify:

- Easy still visually distinct
- Normal still visually distinct
- Hard still visually distinct
- Correct still success-coded
- Incorrect still danger-coded
- Flashcard subject identity still visible
- review states still distinguishable

If preview unavailable:
state gate.

---

# 82. IPHONE VERIFICATION

If available:

- MCQ option rows
- question wrapping
- Flashcard flip area
- bottom controls
- touch targets

No geometry changes expected.

---

# 83. IPAD VERIFICATION

If available:

- option widths
- card widths
- Lecture tab relationship
- portrait
- landscape

No geometry changes.

---

# 84. RTL VERIFICATION

If Arabic view is available:

check:

- question alignment
- options
- badges
- explanation
- Flashcard front/back
- review controls

No RTL logic changes expected.

---

# 85. DEFERRED MCQ VALUES

Report important MCQ colors left unchanged because they are:

- difficulty
- correctness
- category/source
- selected state
- progress
- bookmark
- hint
- explanation semantics
- exact token unavailable

Do not force migration.

---

# 86. DEFERRED FLASHCARD VALUES

Report values left unchanged because they are:

- subject identity
- review state
- progress
- front/back state
- tags
- exact token unavailable

---

# 87. HIGH-RISK DIFF REVIEW

For each touched runtime file:

every Prompt 10 modification must be categorized.

Expected:
COLOR/TOKEN ONLY.

If any business or interaction logic changed:
revert.

---

# 88. NO FIRST-PAINT WORK

Do not modify:

- theme-preload
- index.html
- LaunchScreen

Known mismatch remains deferred.

---

# 89. GIT REVIEW

Run:

git status --short
git diff --stat
git diff --check

No unrelated modifications.

---

# 90. NO `.agents/*`

No `.agents/*` file may be created or modified.

---

# 91. DEFINITION OF DONE

Prompt 10 passes only if:

- Generic MCQ presentation is selectively tokenized.
- Generic Flashcard presentation is selectively tokenized.
- Classic Light parity exact.
- Classic Dark parity exact.
- Easy remains green semantic.
- Normal remains amber semantic.
- Hard remains red semantic.
- Correct remains success semantic.
- Incorrect remains danger semantic.
- selected-answer behavior unchanged.
- category/filter behavior unchanged.
- All virtual filter behavior unchanged.
- bookmark unchanged.
- hint/explanation logic unchanged.
- points/scoring unchanged.
- Flashcard subject identity unchanged.
- Clinical Concept/Explanation behavior unchanged.
- review states unchanged.
- card progress unchanged.
- capture/review tags unchanged.
- keyboard behavior unchanged.
- touch behavior unchanged.
- Lecture tab bar unchanged.
- no alternate themes.
- themeId visually inert.
- provider unchanged.
- motion/reading/glass/hero unwired.
- no admin MCQ/AI scope expansion.
- no backend change.
- no KV.
- no SQL.
- no D1.
- no R2.
- no network change.
- build passes.
- tests pass.
- diff scoped and clean.

---

# 92. MANDATORY FINAL IMPLEMENTATION REPORT

The task is NOT complete without this report.

Return exactly:

# 99'S GUIDE PERSONALIZATION — PHASE 2 PROMPT 10 REPORT

## 1. Status

PASS / FAIL

## 2. Visual Impact

State exactly:

NO INTENTIONAL VISUAL CHANGE

Then report exact visual verification performed.

## 3. MCQ Architecture Audit

List actual student-facing:

- question
- option
- header/progress
- filter/category
- hint
- explanation
- result

files/components.

## 4. Flashcard Architecture Audit

List actual:

- front
- explanation/back
- progress
- review controls
- subject-theme source

files/components.

## 5. Files Modified

| File | Area | Exact Tokenization | Why Safe |
|---|---|---|---|

## 6. Files Created

| File | Purpose |
|---|---|

## 7. MCQ Color Classification

Summarize:

- generic presentation
- correctness
- difficulty
- category/source
- selected-answer
- progress
- bookmark
- hint/explanation semantics

## 8. MCQ Generic Migration

| Previous Value/Class | Semantic Token | Light Parity | Dark Parity |
|---|---|---|---|

## 9. MCQ Neutral Option Safety

Explain exact treatment of unanswered/default option.

## 10. MCQ Selected-Answer Safety

Explain whether it was migrated or deferred and why.

## 11. Difficulty Safety

Report exact sources for:

Easy
Normal
Hard

Confirm independent from user theme.

## 12. Correctness Safety

Report correct/incorrect styling and confirm fixed semantics.

## 13. Category Safety

Report:

ALL
PREVIOUS_YEAR
AI_GENERATED
RESOURCE

and confirm persistence/filtering unchanged.

## 14. Hint Safety

Explain unchanged logic and semantic styling.

## 15. Explanation Safety

Explain unchanged logic and semantic styling.

## 16. Bookmark Safety

Confirm unchanged.

## 17. Progress / Score / Points Safety

Confirm unchanged.

## 18. Flashcard Color Classification

Summarize:

- generic presentation
- subject identity
- front/back state
- review state
- progress
- tags

## 19. Flashcard Generic Migration

| Previous Value/Class | Semantic Token | Light Parity | Dark Parity |
|---|---|---|---|

## 20. Subject Identity Safety

List current identity source/helper and confirm unchanged.

## 21. Clinical Concept / Explanation Safety

Confirm header/content behavior and transitions unchanged.

## 22. Review-State Safety

List preserved semantic review colors/states.

## 23. Flashcard Progress Safety

Confirm unchanged.

## 24. Capture / Review Tags

Confirm unchanged.

## 25. Keyboard Safety

Confirm MCQ/Flashcard keyboard handling unchanged.

## 26. Touch Safety

Confirm tap/swipe/pointer behavior unchanged.

## 27. New Tokens

If any:

| Token | Light | Dark | Previous Source | Why Needed |
|---|---|---|---|---|

If none:
state NONE.

## 28. Token Proliferation

State number of new roles.

## 29. Deferred MCQ Values

List exact deferred areas/reasons.

## 30. Deferred Flashcard Values

List exact deferred areas/reasons.

## 31. Fixed Semantic Safety

Confirm:

error
success
warning
destructive
validation

remain theme-independent.

## 32. Lecture Shell / Tab Safety

Confirm Prompt 8 shell and tab geometry remain unchanged.

## 33. Calendar Safety

Confirm Prompt 9 files untouched.

## 34. Module Safety

Confirm module identity/resources untouched.

## 35. Other Closed Features

Confirm no direct edits to:

Home
Settings
Profile
Notifications
Calendar
Modules
Auth
Console

## 36. Light/Dark/System

Confirm appearance architecture unchanged.

## 37. Theme Runtime

Confirm themeId remains visually inert.

## 38. Hero / Glass / Motion / Reading

Confirm all remain unwired.

## 39. Provider

Confirm unchanged.

## 40. RTL Safety

Confirm no direction/layout changes.

## 41. Accessibility

Confirm focus, keyboard, reduced-motion, forced-colors, non-color-only feedback preserved.

## 42. Initial Paint

Confirm preload/index/LaunchScreen untouched.

## 43. Tests Added/Updated

List exact Prompt 10 invariants.

## 44. MCQ Metadata Regression

Report exact result.

## 45. AI MCQ Regression

Report exact result.

## 46. AI Flashcard Regression

Report exact result.

## 47. Lecture/Navigation/Notes/PDF Regression

Report exact results.

## 48. Personalization Test Results

Report total pass count.

## 49. Build / Static Checks

| Command | Result |
|---|---|

## 50. Visual Verification

State exactly:

- auth state
- viewport
- Light/Dark
- MCQ states actually observed
- Flashcard states actually observed

Do not overclaim.

## 51. Phone Verification

State result or gate.

## 52. iPad Verification

State result or gate.

## 53. RTL Verification

State result or gate.

## 54. Existing Baseline Issues

List separately.

## 55. Git Diff Summary

Exact runtime/test files and stats.

## 56. High-Risk Diff Review

Confirm every runtime Prompt 10 modification is class/color/token-only.

## 57. Backend / Business Logic Verification

Confirm no changes to:

MCQ APIs
Flashcard APIs
metadata
categories
difficulty persistence
AI import
points
scoring
dedupe
D1 sync
server
Prisma

## 58. Network / Persistence

State exactly:

ZERO new personalization network calls.
No SQL change.
No D1 change.
No R2 change.
No KV yet.

## 59. Agent Artifact Verification

State exactly:

No `.agents/*` file was created or modified.

## 60. Phase 2 Closure Readiness

If all requirements pass, state exactly:

MCQ & FLASHCARD CLASSIC 99 TOKENIZATION: CLOSED AND APPROVED

CLASSIC 99 APPLICATION TOKENIZATION: READY FOR FINAL PHASE-2 AUDIT

End EXACTLY:

PERSONALIZATION PHASE 2 PROMPT 10 STATUS: COMPLETE — READY FOR PHASE 2 CLOSURE AUDIT

or:

PERSONALIZATION PHASE 2 PROMPT 10 STATUS: BLOCKED — REPAIR REQUIRED