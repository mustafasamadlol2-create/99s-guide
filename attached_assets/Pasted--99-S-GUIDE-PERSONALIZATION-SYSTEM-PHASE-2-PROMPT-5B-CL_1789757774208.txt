# 99'S GUIDE — PERSONALIZATION SYSTEM
# PHASE 2 / PROMPT 5B
## CLASSIC 99 TOKEN FOUNDATION — SOURCE-TRUTH & PARITY CLOSURE
### VERIFY FIRST — REPAIR ONLY IF REQUIRED
### NO NEW THEME / NO REDESIGN

Prompt 5 has implemented the Classic 99 semantic token foundation.

DO NOT BEGIN PROMPT 6.

This task is a focused closure audit of Prompt 5.

The purpose is to prove that:

CURRENT PRODUCTION VISUAL VALUES
=
CLASSIC 99 TOKEN VALUES

and that semantic/academic colors remain isolated from future user themes.

Do not expand tokenization into additional feature components.

Do not add Midnight/Ocean/Emerald/etc palettes.

Do not add Settings UI.

Do not wire themeId visually.

Do not add KV.

Do not change product behavior.

---

# 1. INSPECT ACTUAL PROMPT 5 DIFF

Inspect all files changed by Prompt 5.

Expected likely files include:

src/index.css
src/features/personalization/classic99Tokens.ts
tests/personalization-tokens.test.ts

and any explicitly justified minimal stylesheet/import file.

Record exact diff.

Do not assume the prior summary is sufficient.

---

# 2. VERIFY CLASSIC 99 SOURCE TRUTH

For EVERY user-themeable semantic token added in Prompt 5:

identify:

- token name
- exact light value or alias
- exact dark value or alias
- exact previous production source
- whether replacement preserves behavior exactly

Create a complete mapping.

Example:

| Token | Light | Dark | Previous source | Parity |
|---|---|---|---|---|

Do not use approximate language.

---

# 3. NO VISUAL CLEANUP

If Prompt 5 accidentally normalized different existing production values into one new value, repair it.

Example:

If production previously used:

#FBFBFD

for one role and:

#F5F7F8

for another distinct role,

do NOT collapse them merely because they look similar.

Preserve existing distinctions where they affect actual rendering.

---

# 4. THEMEABLE TOKENS

List the exact tokens currently intended to respond to future theme packs.

They may include roles such as:

background
grouped background
surface
elevated surface
muted surface
primary text
secondary text
subtle text
generic border
strong border
generic accent
soft accent
focus
navigation active
glass
generic shadow
generic Hero roles

Do not add new tokens just to make this list longer.

Use only what Prompt 5 actually implemented.

---

# 5. FIXED SEMANTIC TOKENS

Verify fixed semantic/status tokens are NOT derived from user theme accent.

For each:

error
success
warning
offline
online
destructive
validation error

verify there is no dependency chain such as:

--status-error
→ --app-accent

or:

--status-success
→ theme pack accent

These must remain independent.

Repair only if violated.

---

# 6. ACADEMIC GOLD AUDIT

Prompt 5 summary mentions:

"Academic gold identity"

Audit exactly what this represents.

Determine whether it is:

A. fixed academic/domain identity

or

B. Classic 99 brand accent

These concepts must NOT be confused.

If the value is actually generic 99 branding:

it belongs in the Classic themeable/brand token family.

If it is genuinely fixed academic semantic identity:

it may remain fixed.

Do NOT classify a brand color as immutable academic semantics simply because it is gold.

Report the exact usage locations before deciding.

Repair classification if necessary.

---

# 7. MCQ COLOR ISOLATION

Verify the following current meanings remain independent from future theme accent:

Easy
Normal
Hard
Correct
Incorrect

Search actual CSS/TS/TSX usage.

Confirm none now resolve through:

--app-accent
--app-nav-active
--app-focus-ring

If Prompt 5 did not change MCQ components, state that clearly.

---

# 8. MODULE IDENTITY ISOLATION

Verify:

CA
ID
RM
NT
ImD
PHC
SSC

continue using their existing module identity source.

Expected:

src/features/modules/moduleVisuals.ts

or exact current source.

They must not resolve through generic theme tokens.

No module color redesign.

---

# 9. FLASHCARD IDENTITY ISOLATION

Verify existing subject/flashcard identity colors remain untouched.

No generic theme accent substitution.

---

# 10. TAILWIND SAFETY

Prompt 5 added "Tailwind-compatible aliases".

Audit exactly what this means.

Mandatory:

Do NOT redefine global Tailwind palette names such as:

red
green
blue
amber
orange
rose
emerald
violet
slate

Do NOT redefine standard utility semantics globally.

The new themeable colors must have dedicated app-specific aliases/names.

Report every Tailwind alias added.

---

# 11. NO ACCIDENTAL GLOBAL RECOLOR

Search for any Prompt 5 change that could cause existing classes such as:

text-blue-*
bg-red-*
border-emerald-*
text-amber-*

to resolve differently than before.

Expected:

none.

If found, repair.

---

# 12. LIGHT/DARK ARCHITECTURE

Confirm:

Light semantic values resolve through the existing default/root architecture.

Dark semantic values resolve through the existing:

html.dark

architecture.

Do NOT add a second dark-mode mechanism.

Do NOT add:

data-theme="dark"

for appearance.

---

# 13. SYSTEM MODE

Verify System mode remains:

prefers-color-scheme
→ current appearance code
→ html.dark

No PersonalizationProvider involvement.

---

# 14. THEME IDs MUST STILL BE VISUALLY INERT

The following IDs exist in the contract:

midnight
ocean
emerald
rose
amber
violet
monochrome

Prompt 5 must NOT contain complete or partial palettes for them.

Search actual source.

Expected:

only Classic 99 has visual token values.

If any alternate palette was added prematurely:

remove it.

---

# 15. UNIMPLEMENTED THEME SAFETY

Confirm that the current runtime does NOT attempt to activate an alternate token set when:

committed.themeId = "emerald"

or any other future ID.

At this point:

PersonalizationProvider may hold the ID,
but visual CSS remains Classic 99.

No undefined token state.

---

# 16. PROVIDER MUST REMAIN VISUALLY INERT

Search for runtime consumption of:

committed.themeId
draft.themeId

outside personalization state/provider logic.

There must be no DOM styling effect yet.

No:

document.documentElement.dataset
classList theme updates
CSS-variable JS writes
style.setProperty
theme-based key/remount

---

# 17. CSS IMPORT ORDER

If Prompt 5 added a dedicated CSS file or new import:

verify its position relative to:

Tailwind
existing global styles
dark-mode selectors
responsive overrides

It must not accidentally override later component styles due to cascade order.

Report actual import chain.

---

# 18. INDEX.CSS CHANGE REVIEW

If `src/index.css` changed:

report each logical edit category.

Confirm it was limited to:

- token definitions
- aliases
- minimal central references

No unrelated cleanup.

No responsive layout modifications.

No gesture changes.

No animation changes.

---

# 19. HERO TOKENS

If Prompt 5 introduced Hero token roles:

confirm they reproduce current Hero values only.

Do NOT connect:

heroStyle

Do NOT create:

minimal
night
aurora

visual definitions.

---

# 20. GLASS TOKENS

If Prompt 5 introduced glass roles:

confirm they represent current production glass only.

Do NOT connect:

clear
balanced
frosted

No blur amount behavior changes.

---

# 21. SHADOW TOKEN

If a generic shadow token exists:

verify no shadow geometry changed.

Only color/semantic aliasing is acceptable if parity is exact.

If complex shadow declarations were rewritten unnecessarily:

revert to minimal architecture.

---

# 22. FOCUS STATE

Verify focus ring remains visible in:

Light
Dark

and its Classic value matches current production behavior.

Do not recolor error/validation focus states to generic accent.

---

# 23. ACCESSIBILITY RULES

Verify Prompt 5 did NOT break or remove:

prefers-reduced-motion
reduced transparency handling
forced-colors
focus visibility

No personalization theme may override these yet.

---

# 24. INITIAL PAINT BASELINE

Confirm untouched:

public/theme-preload.js
index.html
LaunchScreen.tsx

The previously documented first-paint mismatch remains a known baseline issue.

Do NOT fix it in 5B.

---

# 25. CLASSIC TOKEN SOURCE OF TRUTH

State clearly whether visual values live in:

CSS only

or:

CSS + TypeScript metadata

The actual color values must have ONE authoritative source.

`classic99Tokens.ts` must NOT duplicate all CSS color values if that creates two independent sources of truth.

If TypeScript file currently duplicates token values:

evaluate whether it is necessary.

Preferred:

TypeScript defines names/categories/metadata.

CSS defines actual render values.

If duplication exists without a strong reason, reduce it.

---

# 26. TOKEN METADATA

If `classic99Tokens.ts` exists, ideal responsibilities are things like:

THEMEABLE_TOKEN_NAMES
FIXED_SEMANTIC_TOKEN_NAMES
IMPLEMENTED_THEME_IDS
type metadata

not a second manually maintained full palette.

Report exact design.

---

# 27. IMPLEMENTED THEME IDS

If useful, expose/test:

IMPLEMENTED_THEME_IDS = ["classic-99"]

The contract may still contain all 8 valid future ThemeIds.

Do not confuse:

valid preference ID

with:

currently visually implemented theme.

---

# 28. NO APP.TSX CHANGE

Prompt 5B should not modify App.tsx.

If Prompt 5 unexpectedly modified it:

audit why.

Theme token foundation should not require App.tsx runtime changes.

---

# 29. NO HOME / SETTINGS CHANGE

Confirm untouched:

HomeDashboard.tsx
SettingsView.tsx

No subject-order consumption.

No customization UI.

---

# 30. NO PRODUCT FEATURE CHANGE

Confirm no changes to:

auth
navigation
swipe
MCQ
Flashcards
Calendar
Modules
Notes
Module Resources
Notifications
Profile
Console
AI

---

# 31. TOKEN TEST IMPROVEMENT

Ensure tests cover at least:

- canonical themeable token names
- Classic 99 only visually implemented
- Light token block exists
- Dark token block exists
- status semantic tokens independent
- MCQ/domain semantics not mapped to app accent
- standard Tailwind palette names not redefined
- no alternate theme palette blocks
- no runtime theme DOM mutation
- exact token names unique
- no duplicate semantic token declarations in conflicting scopes

Use maintainable tests.

Avoid brittle full-file snapshots.

---

# 32. SOURCE PARITY TEST

Where feasible, add a focused parity assertion for values moved from existing centralized variables.

Do not attempt to snapshot the entire app stylesheet.

The goal is to prevent accidental token drift.

---

# 33. BUILD & TESTS

Run ALL personalization tests:

- personalization.test.ts
- personalization-state.test.ts
- personalization-storage.test.ts
- personalization-runtime.test.tsx
- personalization-tokens.test.ts

Then run:

npm run build

npm run test:auth

npx tsx --test tests/client-navigation.test.ts

npx tsx --test tests/mcq-metadata.test.ts

npx tsx --test tests/material-note-policy.test.ts

npm run test:ai-mcq

npm run test:ai-flashcard

git diff --check

Run targeted ESLint/TypeScript for touched/new files.

Do not repair unrelated baseline failures.

---

# 34. VISUAL VERIFICATION

Perform actual browser preview verification where available.

At minimum inspect:

LIGHT:
- Home/root canvas
- major generic surface
- text
- focus/active state

DARK:
- same areas

If possible also inspect:

- sidebar or bottom navigation
- one Settings/card surface

Report exactly what was visually inspected.

Do NOT claim pixel-perfect comparison unless an automated before/after image comparison was actually performed.

---

# 35. NO SCREENSHOT REPOSITORY ARTIFACTS

Do not commit screenshots.

Temporary visual verification files must not remain in repository.

---

# 36. EXPECTED REPAIR SCOPE

If implementation is already correct:

no runtime repair may be necessary.

Only improve tests/reporting if needed.

If repair is required:

keep it confined to Prompt 5 token files.

Do not expand tokenization.

---

# 37. GIT REVIEW

Run:

git status --short
git diff --stat
git diff --check

Separate pre-existing dirty state from this task.

No unrelated modifications.

---

# 38. NO `.agents/*`

No `.agents/*` file may be created or modified.

---

# 39. DEFINITION OF DONE

Prompt 5B passes only when:

- every Classic themeable token has traceable production source
- Light/Dark mapping verified
- status colors are theme-independent
- MCQ semantics independent
- module identities independent
- flashcard identities independent
- academic gold is classified correctly
- Tailwind standard palette is untouched
- no alternate theme palette exists
- provider remains visually inert
- CSS has one source of truth for actual values
- no Home/Settings behavior changed
- no product logic changed
- build passes
- personalization tests pass
- regression tests pass
- visual preview has been checked
- diff is clean and scoped

---

# 40. MANDATORY FINAL REPORT

Return exactly:

# 99'S GUIDE PERSONALIZATION — PHASE 2 PROMPT 5B CLOSURE REPORT

## 1. Status

PASS / FAIL

## 2. Files Changed

Exact list.

## 3. Prompt 5 Diff Audit

Summarize what Prompt 5 actually changed.

## 4. Themeable Token Inventory

Table:

| Token | Light Value/Alias | Dark Value/Alias | Previous Production Source | Parity Result |
|---|---|---|---|---|

Include every themeable token.

## 5. Fixed Semantic Inventory

Table:

| Token/Role | Value/Source | Theme Independent? |
|---|---|---|

## 6. Academic Gold Classification

Explain exact current usage and final classification.

## 7. MCQ Semantic Isolation

Report findings.

## 8. Module Identity Isolation

Report findings.

## 9. Flashcard Identity Isolation

Report findings.

## 10. Tailwind Alias Audit

List every app-specific alias added.

Confirm standard Tailwind palettes are untouched.

## 11. Classic 99 Light

Confirm production parity.

## 12. Classic 99 Dark

Confirm production parity.

## 13. Light/Dark/System Architecture

Confirm unchanged.

## 14. Current Visual Source of Truth

Explain where actual token values live.

## 15. TypeScript Token Metadata

Explain exact purpose of classic99Tokens.ts.

## 16. Implemented Theme IDs

Expected:

classic-99 only

## 17. Future Theme IDs

Confirm no visual palettes exist yet for:

midnight
ocean
emerald
rose
amber
violet
monochrome

## 18. Runtime Theme Consumption

Confirm committed themeId still does not alter DOM/CSS.

## 19. Hero

Confirm unchanged.

## 20. Glass

Confirm unchanged.

## 21. Motion

Confirm unchanged.

## 22. Reading Size

Confirm unchanged.

## 23. Home / Settings

Confirm unchanged.

## 24. Initial Paint

Confirm preload/index/LaunchScreen untouched and baseline mismatch intentionally remains.

## 25. Accessibility

Confirm reduced motion/transparency/forced colors remain intact.

## 26. Token Tests

List all invariant/parity tests.

## 27. Personalization Test Results

Report total passed.

## 28. Regression Results

| Command | Result |
|---|---|

## 29. Visual Verification

State exactly what was manually/automatically checked.

Do not overclaim.

## 30. Existing Baseline Failures

List unrelated typecheck/lint issues separately.

## 31. Git Diff Summary

Exact scope.

## 32. Forbidden File Verification

Confirm no unauthorized changes to:

App.tsx
Home
Settings
server
Prisma
Cloudflare
wrangler
Capacitor
Info.plist
icons

## 33. Network / Persistence

State exactly:

ZERO new personalization network calls.
No SQL change.
No D1 change.
No R2 change.
No KV yet.

## 34. Agent Artifact Verification

State exactly:

No `.agents/*` file was created or modified.

## 35. Prompt 6 Readiness

If all requirements pass, state exactly:

CLASSIC 99 TOKEN FOUNDATION: CLOSED AND APPROVED

CORE CHROME TOKENIZATION: APPROVED TO BEGIN

End exactly:

PERSONALIZATION PHASE 2 PROMPT 5B STATUS: COMPLETE — READY FOR PROMPT 6

or:

PERSONALIZATION PHASE 2 PROMPT 5B STATUS: BLOCKED — REPAIR REQUIRED