# 99'S GUIDE — PERSONALIZATION SYSTEM
# PHASE 4 / PROMPT 18 OF ~22
## HOME SUBJECT ORDER CUSTOMIZATION
### Reorder Existing Seven Home Subjects Only
### NO HIDING — NO ADDING — NO NEW HOME CONTENT
### Draft Preview + Single Apply Workflow
### NO APP ICON / KV / BACKEND SYNC YET

Phase 0 is CLOSED AND APPROVED.

Phase 1 is CLOSED AND APPROVED.

Phase 2 is CLOSED AND APPROVED.

Phase 3 is CLOSED AND APPROVED.

Phase 4 approved work:

- Prompt 15 / 15B
  My 99 Studio + Theme Pack
  CLOSED AND APPROVED

- Prompt 16 / 16B
  Hero Style + Glass Style
  CLOSED AND APPROVED

- Prompt 17 / 17B
  Motion Style + Reading Size
  CLOSED AND APPROVED

Current My 99 customizable axes:

1. Theme Pack
2. Hero Style
3. Glass Style
4. Motion Style
5. Reading Size

Current personalization contract already includes:

```ts
home: {
  subjectOrder: readonly SubjectId[];
}

The approved canonical subject order is:

ID
NT
RM
CA
PHC
ImD
SSC

The existing contract already requires:

every valid SubjectId
exactly once
complete permutation
no duplicates
no missing subjects
no unknown IDs

Current default:

home.subjectOrder =
[ID, NT, RM, CA, PHC, ImD, SSC]

This field is currently unwired.

Prompt 18 must wire it safely to:

the existing Home subject presentation
the My 99 Draft workflow
the contained personalization preview

This is REORDER ONLY.

NO subject hiding.
NO subject disabling.
NO new subject.
NO Home widgets.
NO recommendations.
NO quick actions.
NO algorithmic sorting.
NO backend changes.

1. PRIMARY GOAL

Allow the user to choose the display order of the EXISTING seven Home subject cards.

User may reorder:

ID
NT
RM
CA
PHC
ImD
SSC

The resulting order must affect ONLY the intended Home subject-card area.

It must NOT affect:

database ordering
lecture ordering
subject IDs
subject names
lecture data
Library ordering unless Library actually shares the exact intended Home list and the audit proves it should not
Calendar
search
exams
MCQ
Flashcards
notifications
academic logic
2. HARD PRODUCT BOUNDARY

Prompt 18 is presentation ordering only.

Absolutely do NOT add:

subject visibility toggles
hide subject
archive subject
favorite subject
pin subject
custom subject
custom section
folders
recommendations
"continue studying"
quick actions
recent subjects
progress sorting
smart sorting
most-used sorting
automatic ordering
AI ordering

Only manual ordering of the seven existing Home subjects.

3. HOME AUDIT FIRST

Before editing anything, audit the ACTUAL current Home architecture.

Identify:

exact Home component(s)
exact source of Home subjects
exact subject-card component
exact list/array currently rendered
exact current order
exact SubjectId values
whether the order is hardcoded, imported, mapped, derived, or API-driven
whether the list contains exactly seven subjects
whether Home shares this array with other features
whether filtering/search/grouping occurs before rendering
whether Hero/Today/Schedule/Search/Upcoming sections are separate

Report exact files and relationships.

Do NOT assume HomeDashboard.tsx owns the subject list merely because it owns Hero presentation.

4. CURRENT HOME CONTENT FREEZE

Prompt 18 must not modify the existence, logic, or placement of:

Welcome/Hero
Today's Message
upcoming event alert
inline search
Today/Schedule area
existing academic notices
any existing Home content outside the subject-card collection

The ONLY Home content behavior allowed to change is:

ORDER OF EXISTING SUBJECT CARDS.

5. SUBJECT SET IS IMMUTABLE

The final rendered Home subject set must remain exactly:

ID
NT
RM
CA
PHC
ImD
SSC

No matter what personalization order is stored.

Count:

7

Always.

6. CANONICAL DEFAULT ORDER

The factory/default order remains exactly:

ID
NT
RM
CA
PHC
ImD
SSC

Do not derive the default from current object iteration.

Use the approved canonical SubjectId order already present in the personalization contract/shared constants.

Prefer ONE authoritative canonical source.

Do not create a second drifting canonical array if one already exists.

7. COMMITTED VS DRAFT RULE

Production Home must follow:

committed.home.subjectOrder

ONLY.

My 99 reorder UI and contained preview follow:

draft.home.subjectOrder

ONLY.

Changing Draft order must NOT immediately reorder the real Home page.

Only successful Apply may update Committed and therefore production Home order.

8. NO GLOBAL DOM ATTRIBUTE FOR SUBJECT ORDER

Do NOT create:

data-app-subject-order

Subject ordering is structured data, not CSS presentation.

Do NOT serialize order into:

DOM attributes
CSS variables
body classes
query parameters

Use the committed personalization data directly at the Home subject rendering boundary.

9. HOME ORDER HELPER

Prefer a small pure helper.

Example architecture:

orderHomeSubjects(subjects, subjectOrder)

or equivalent.

Responsibilities:

accept current Home subject entities
accept validated SubjectId order
return same entities reordered
preserve every entity exactly once
preserve object identity where practical
never filter
never clone/modify subject domain data unnecessarily

Keep it deterministic.

10. DEFENSIVE ORDERING

Even though PersonalizationConfigV1 strictly validates order, the Home presentation helper should remain fail-safe.

If unexpected runtime data reaches the ordering boundary:

unknown ID
duplicate
missing ID
partial order

the Home must NOT lose subjects.

Preferred defensive result:

canonical production order

or another deterministic complete fallback using the approved canonical set.

Do NOT silently render fewer than seven subjects.

11. NO FILTER-BASED IMPLEMENTATION

Avoid dangerous logic such as:

order.map(id => subjects.find(...)).filter(Boolean)

without proving completeness.

That pattern can silently drop cards.

Use a defensive implementation that guarantees the full subject set.

12. UNKNOWN SUBJECT SAFETY

The current V1 contract is exactly the seven known subjects.

Do not invent future-subject behavior in persisted config.

If the runtime Home source unexpectedly contains an extra domain subject not represented by the V1 contract:

STOP and report the architecture mismatch rather than silently hiding it.

Expected current audit:

exact seven subjects.

13. NO DOMAIN DATA MUTATION

Do NOT mutate:

subject ID
subject name
description
icon
color
module identity
progress
lecture counts
completion state
links
routes

Order only.

14. HOME SOURCE ORDER

The raw/domain source may remain in its canonical/business order.

Do NOT rewrite the domain source array simply to personalize one user.

Personalized order should be applied at presentation time.

15. NO SERVER ORDER

Do NOT write subject order into:

PostgreSQL
Prisma
Supabase
D1
R2
backend config
subject records

It remains inside the existing account-scoped personalization config.

16. NO NEW NETWORK CALL

Production Home must NOT fetch subject order separately.

No personalization endpoint.

No network request.

Use already-hydrated committed config.

17. PROVIDER ACCESS AT HOME

Use the existing personalization provider/context safely.

Do not create another provider.

Do not trigger hydration from Home.

Do not access personalization storage directly from Home.

Home should read the existing committed config through the approved runtime/context abstraction.

18. PROVIDER AVAILABILITY / FALLBACK

If Home can render before personalization hydration completes:

it must safely show the canonical/default order during that boundary.

After committed personalization hydrates:

it may render the user's committed order.

Do not show another user's order during account switching.

Existing account boundary remains authoritative.

19. ACCOUNT SWITCH

Representative:

Account A order:

SSC
PHC
CA
RM
NT
ID
ImD

Account B order:

ID
CA
ImD
NT
PHC
RM
SSC

Required behavior:

A personalized order
→ account boundary/default canonical order
→ B personalized order after hydration

No A-order leakage into B.

20. LOGOUT

Logout from a customized Home order:

→ anonymous/default personalization
→ canonical subject order

No prior account order remains visible.

21. STALE HYDRATION

Existing generation guard must prevent a stale Account A order from replacing Account B after switch.

No new race-management system.

22. HOME RENDER STABILITY

Reordering Home subjects must not:

remount the whole Home page
reset Home scroll position
recreate Hero unnecessarily
reset Today/Schedule state
reload
refetch subject data solely because order changed

Only the subject-card ordering changes after Committed updates.

23. REACT KEY SAFETY

Subject cards must remain keyed by stable:

SubjectId

or existing stable subject identifier.

Do NOT key by array index.

This is critical when order changes.

Stable keys preserve component identity and avoid unnecessary remounts.

24. HOME APPLY BEHAVIOR

User is inside My 99 and changes subject order.

Before Apply:

real Home remains committed order.

After successful Apply:

when Home is next visible—or if shared state causes an already-mounted Home to update—the subject cards use the new committed order.

No reload.

No forced navigation.

No route reset.

25. NO AUTOMATIC HOME NAVIGATION

Applying subject order in My 99 must NOT automatically send the user to Home.

Stay in My 99.

26. MY 99 SECTION

Add one new section:

Home Subject Order

Professional Arabic localization required.

Suggested description concept:

Choose the order your subjects appear on Home.

Arabic should communicate:

اختر ترتيب ظهور المواد في الصفحة الرئيسية.

Use professional existing app tone.

27. SECTION POSITION

Final My 99 visible order should become:

Main Draft Preview
Theme Pack
Hero Style
Glass Style
Motion Style
Reading Size
Home Subject Order
Reset / Cancel / Apply

No App Icon placeholder.

No future placeholder.

28. SUBJECT ORDER UI — CORE REQUIREMENT

Display all seven subjects in the Draft order.

The user must be able to move subjects.

Minimum production requirement:

touch usable
mouse usable
keyboard accessible
screen-reader understandable
no hidden subject
29. REORDER UX — PREFERRED MODEL

Audit existing dependencies first.

If the already-installed Motion library provides a stable reorder primitive compatible with the current React stack, a vertical reorder list MAY be used.

However:

dragging must NOT be the only way to reorder.

Accessible explicit move controls are mandatory.

Each row should have:

subject identity
drag handle if implemented
Move Up control
Move Down control

The first row disables Move Up.
The last row disables Move Down.

This guarantees keyboard and assistive-access functionality.

30. DO NOT ADD A NEW DND LIBRARY

Do NOT install:

dnd-kit
react-beautiful-dnd
sortablejs
another drag library

solely for seven subjects.

Use:

existing Motion dependency if genuinely safe
or
explicit move controls only

No dependency expansion is necessary.

31. TOUCH DRAG SAFETY

If drag is implemented:

drag must begin from a dedicated handle.

Do not make the whole row draggable on mobile.

This protects:

vertical page scrolling
Settings swipe-back
tap targets
text selection/accessibility
32. DRAG AXIS

If implemented:

vertical axis only.

No horizontal reorder.

No free-form grid dragging.

33. DRAG VS SWIPE-BACK

Critical mobile behavior:

subject reorder dragging must not trigger or break:

global swipe-back
My 99 page back gesture
page scrolling

Use the existing event architecture carefully.

If safe coexistence cannot be proven:

omit drag and rely on explicit move controls.

Correctness is more important than drag flair.

34. DO NOT MODIFY GLOBAL GESTURE SYSTEM

No Prompt 18 changes to:

swipe-back engine
page pager
global touch handlers
pointer navigation
App route transitions

The reorder UI must adapt to existing gestures.

35. REORDER CARD CONTENT

Each row/card may show existing subject identity:

subject short ID
existing localized/display name if available
existing subject icon/identity presentation if safely reusable

Do NOT duplicate academic metadata.

Do NOT add:

progress
grades
lecture counts
recommendations

unless that exact information is already required merely to identify the subject—and preferably avoid it.

Keep reorder rows compact.

36. SUBJECT IDENTITY COLORS

Subject identity colors remain domain semantics.

Do not recolor them based on reorder position.

Do not assign rainbow colors to ranks.

37. POSITION NUMBERS

A small position number:

1–7

is acceptable.

It must update as Draft order changes.

Do not persist the number separately.

It is derived from array index.

38. ACCESSIBLE REORDER LABELS

Each subject row needs accessible controls such as:

Move Infectious Diseases up
Move Infectious Diseases down

Arabic equivalents must be localized.

Do not expose raw internal IDs alone if a human-readable subject name exists.

39. SCREEN READER ORDER

DOM order should reflect Draft order.

Do not visually reorder only with CSS order while leaving semantic DOM order unchanged.

The actual React list order should follow Draft.

40. KEYBOARD REORDER

At minimum:

Tab reaches Move Up/Down buttons.
Enter/Space activates them.
Disabled boundary buttons expose actual disabled state.

Optional keyboard shortcuts are not required.

Do NOT hijack arrow keys globally.

41. FOCUS AFTER MOVE

After pressing Move Up/Down:

prefer keeping focus on the same subject's control/row where practical.

Do not remount the whole list.

Stable SubjectId keys are required.

42. ANNOUNCEMENT AFTER MOVE

If the current accessibility architecture supports a lightweight aria-live region:

it is acceptable to announce:

Moved Infectious Diseases to position 2.

English + Arabic.

Do not add a complex accessibility subsystem solely for this.

If not implemented:
the updated DOM order + controls must still remain accessible.

43. DRAFT ONLY

Every reorder action changes:

draft.home.subjectOrder

ONLY.

No reorder action may:

Apply automatically
write storage
change committed
reorder actual Home
send network calls
44. UPDATE DRAFT API

Use the existing personalization reducer/API.

If a typed action already exists for subject order:
use it.

If not but updateDraft supports a safe home-order operation:
use that.

Do NOT bypass provider state by mutating Draft arrays directly.

45. IMMUTABILITY

Every reorder produces a new array.

Do not mutate:

draft.home.subjectOrder

in place.

No .splice() against provider-owned array without first creating a copy.

46. COMPLETE PERMUTATION AFTER EVERY MOVE

After every UI move:

Draft subject order must remain:

length 7
all seven canonical IDs
no duplicate
no missing ID

This should be guaranteed structurally.

47. NO "REMOVE" ACTION

There must be no:

X button
trash icon
hide eye
disable toggle

This is order only.

48. NO "ADD" ACTION

No plus button.

No subject picker.

No custom entry.

49. RESET ORDER

The existing Studio Reset resets the WHOLE Draft config.

Therefore Home order returns to canonical:

ID
NT
RM
CA
PHC
ImD
SSC

Do NOT add a separate persisted Reset Order action.

50. OPTIONAL LOCAL "RESTORE ORDER" CONTROL

Prefer NOT to add a separate section-level reset in Prompt 18.

The existing global Reset already handles it.

Avoid duplicated semantics.

51. CANCEL ORDER

If committed Home order is custom and Draft is rearranged:

Cancel restores Draft order exactly to committed order.

Zero persistence writes.

52. APPLY ORDER

One existing Apply button persists all personalization fields together.

Example:

User changes:

Theme
Hero
Glass
Motion
Reading
Home Subject Order

Then Apply:

ONE persistence write.

No subject-specific write.

53. SIX-AXIS CONFIG

After Prompt 18 the user-facing config axes become:

Theme
Hero
Glass
Motion
Reading
Home Subject Order

The underlying contract remains the same V1 config.

No schema change.

54. SIX-AXIS APPLY SUCCESS TEST

Committed defaults:

Theme: Classic
Hero: Classic
Glass: Balanced
Motion: Full
Reading: Default
Home: ID, NT, RM, CA, PHC, ImD, SSC

Draft:

Theme: Ocean
Hero: Aurora
Glass: Frosted
Motion: Subtle
Reading: Large
Home: SSC, ImD, PHC, CA, RM, NT, ID

Before Apply:

Global presentation remains committed defaults.

Production Home remains canonical order.

My 99 preview reflects Draft presentation and Draft Home order.

Successful Apply:

Committed becomes entire Draft.

Exactly ONE persistence write.

Production Home order becomes:

SSC
ImD
PHC
CA
RM
NT
ID

No reload.

55. SIX-AXIS APPLY FAILURE

Same Draft, persistence fails.

Expected:

Committed remains previous config.

Production Home remains previous committed order.

Draft remains custom order.

isDirty remains true.

No partial commit.

No partial Home reorder.

56. CANCEL SIX-AXIS

Committed Home:

CA
ID
NT
RM
PHC
ImD
SSC

Draft Home:

SSC
ImD
PHC
RM
NT
ID
CA

Cancel:

Draft returns exactly to committed sequence.

Zero writes.

57. RESET SIX-AXIS

Reset Draft becomes canonical full config including:

home.subjectOrder =
ID
NT
RM
CA
PHC
ImD
SSC

No write until Apply.

Production Home remains committed until Apply.

58. MAIN DRAFT PREVIEW

Extend the existing contained My 99 Draft Preview to show subject order.

Do NOT mount the real Home subject grid.

Use a lightweight abstract/compact subject-order specimen.

It should render the seven subject identities in:

draft.home.subjectOrder

order.

59. PREVIEW CONTENT RULE

The Home-order preview may show:

subject short IDs
small subject identity pills/tiles

Do NOT show fake:

lecture counts
progress
grades
recommendations
60. PREVIEW MUST REMAIN LIGHTWEIGHT

Seven small items only.

No real SubjectView components.

No data fetch.

No timers.

No APIs.

61. PREVIEW ORDER

The main preview should make it visually clear that Draft order changes.

If the user moves SSC to first:

the preview immediately displays SSC first.

The production Home still remains committed until Apply.

62. NO NEW PREVIEW ATTRIBUTE NEEDED

Do NOT create:

data-personalization-preview-subject-order

The order is React data.

Render the Draft order directly.

Existing preview attributes remain:

Theme
Hero
Glass
Motion
Reading
63. PREVIEW THEME IDENTITY

Subject preview items may use existing subject/domain identity safely.

Do NOT map order position to Theme Pack accent.

Subject identity remains semantic/domain-specific.

64. HOME PRODUCTION READ PATH

Production Home should consume:

committed.home.subjectOrder

through a minimal stable hook/context read.

Avoid making every subject card individually subscribe to personalization.

Prefer ordering once at the list boundary.

65. MEMOIZATION

If useful, compute ordered subjects with:

useMemo(...)

based on:

source subject array
committed.home.subjectOrder

Do not add unnecessary memoization complexity.

Correctness first.

66. HYDRATION FLICKER

Because account personalization is local-first/hydrated asynchronously, Home may initially show canonical order before committed account config is available.

This is accepted architecture.

Do NOT solve it with:

global unscoped last-order cache
localStorage key outside personalization storage
user-independent subject-order cache

That could leak account personalization.

67. NO ACCOUNT-UNSCOPED HOME ORDER CACHE

Forbidden:

home_subject_order
last_subject_order
subject_order

outside the existing account-scoped PersonalizationConfig cache.

68. SUBJECT ORDER STORAGE

Persist only as existing:

PersonalizationConfigV1.home.subjectOrder

No new key.

No new envelope.

No version bump.

69. HOME SUBJECT CARD GEOMETRY

Reordering must not alter:

card size
grid columns
radius
icon size
text size
gaps
responsive breakpoints
animation behavior

Order only.

70. HOME RESPONSIVE GRID

Existing iPhone/iPad/desktop Home subject-grid geometry remains unchanged.

Do not redesign the grid.

71. HOME RTL

Reordering is a logical sequence.

Do NOT manually reverse persisted order in Arabic.

The persisted array means the user's chosen logical order.

Let the existing CSS/grid/RTL behavior render appropriately.

Audit actual Home layout behavior.

72. RTL PERSISTENCE SEMANTICS

A user selecting:

ID, CA, RM, ...

should have the SAME stored SubjectId sequence regardless of language.

Do not store different arrays for Arabic and English.

73. LANGUAGE SWITCH

Changing language must not modify subject order.

No sort by translated name.

No locale sort.

74. SUBJECT NAME LOCALIZATION

The reorder UI should use the existing localized subject names if available.

Do not create a second translation mapping if the project already has one.

If subject display names are canonical English only in current product:
follow actual architecture.

Do not invent inconsistent names.

75. MY 99 LOCALIZATION

Add English + Arabic for:

Home Subject Order
section description
Move up
Move down
position, if shown
optional accessibility move announcement
Home-order preview heading if visible

No English leak in Arabic.

76. ACCESSIBLE GROUP SEMANTICS

Home Subject Order is NOT a radiogroup.

Do not mark it as radio selection.

Use a semantic ordered/list structure.

Preferred:

<ol>
  <li>...</li>
</ol>

or equivalent accessible list.

Reorder controls are buttons.

77. DRAG HANDLE ACCESSIBILITY

If drag handle exists:

it must have accessible label
it must not be the only control
keyboard users still have Move Up/Down

No decorative inaccessible drag-only UX.

78. TOUCH TARGETS

Move controls and drag handle should meet practical mobile touch sizing consistent with the existing UI.

Do not create tiny 16px-only tap targets.

79. VISUAL PREMIUM QUALITY

The reorder list should feel consistent with My 99:

compact
clear hierarchy
restrained drag handle
current Draft order obvious
no unnecessary borders/noise

Do not redesign the Studio.

80. MOTION STYLE INTERACTION

The reorder UI must not depend on Motion Style for functionality.

If visual reorder animation is used:

Full may show normal layout transition
Subtle/Reduced may reduce decoration if the existing presentation architecture naturally supports it

BUT functionality must remain identical.

Do NOT expand Prompt 17 Motion architecture just for reorder animation.

No requirement for animated reorder.

Static reorder is acceptable.

81. PREFERS-REDUCED-MOTION

If reorder animation exists:

OS reduced-motion must win.

Simpler:
avoid nonessential reorder animation if unnecessary.

82. NO HOME HERO CHANGE

Prompt 18 must not change Hero.

83. NO GLASS CHANGE

No Glass behavior change.

84. NO THEME CHANGE

No palette changes.

All eight themes frozen.

85. NO READING CHANGE

No typography changes beyond new Home-order UI text using normal Studio styles.

No study Reading hooks in Home order.

86. NO MOTION CHANGE

No changes to global Motion Style implementation.

87. APP ICON

Still unimplemented.

No App Icon control or placeholder.

88. NO KV

Still no KV.

89. NO SQL / D1 / R2

No backend personalization storage.

90. NO SERVER CHANGE EXPECTED

Expected backend/server diff:

ZERO.

If backend change seems necessary:

STOP.

The architecture is wrong.

91. NO SHARED CONTRACT CHANGE EXPECTED

The contract already contains home.subjectOrder.

Expected shared contract diff:

ZERO.

No version bump.

92. PROVIDER CHANGE EXPECTED

Expected:

ZERO or extremely small only if a typed Draft reorder action is genuinely missing.

Audit first.

Prefer existing reducer/updateDraft semantics.

Do NOT rewrite provider.

93. ORDERING PURE HELPER TESTS

Add dedicated tests for the production ordering helper.

Required:

Canonical input + canonical preference
→ canonical.

Canonical input + reversed preference
→ reversed.

Arbitrary valid permutation
→ exact permutation.

Input subject objects retain correct corresponding IDs/data.

94. INVALID ORDER FALLBACK TEST

Even if normal validation prevents invalid config, directly test defensive helper behavior.

Cases:

duplicate ID
missing ID
unknown ID
too short
too long if helper accepts unknown runtime arrays

Expected:

complete deterministic safe order.

No missing subject.

95. NO MUTATION TEST

Verify ordering helper does not mutate:

input subjects array
preference array
96. STABLE IDENTITY TEST

If practical:

verify returned subject objects are the same object references, only reordered.

No unnecessary data cloning.

97. HOME RENDER SOURCE TEST

Add a focused source/runtime invariant proving production Home ordering uses:

committed.home.subjectOrder

not Draft.

98. NO DRAFT HOME LEAK

Verify:

My 99 Draft changes cannot directly affect Home render path.

No Home component reads Draft order.

99. STUDIO REORDER TEST

Test:

Start:

ID, NT, RM, CA, PHC, ImD, SSC

Move SSC up once:

ID, NT, RM, CA, PHC, SSC, ImD

Move SSC to top via repeated operations or helper:

SSC, ID, NT, RM, CA, PHC, ImD

All seven remain.

100. BOUNDARY BUTTON TEST

At first row:

Move Up disabled.

At last row:

Move Down disabled.

No wrap-around unless explicitly designed—which is NOT required.

Preferred:
no wrap-around.

101. MOVE HELPER

Create/use a small pure reorder helper for Draft UI.

Example:

moveSubject(order, subjectId, direction)

or:

moveSubjectToIndex(...)

Must:

preserve all IDs
be immutable
no-op safely at boundaries

Do not duplicate movement logic across buttons and drag.

102. DRAG RESULT VALIDATION

If drag is implemented:

the final reorder callback must still produce a complete valid permutation.

Do not trust DOM position alone.

103. RESET TEST

Custom Draft order
→ Reset

Expected canonical order exactly.

No write.

104. CANCEL TEST

Committed custom order A.
Draft custom order B.
Cancel.

Expected Draft exactly A.

Zero writes.

105. APPLY WRITE COUNT

Multiple subject moves before Apply:

0 writes.

Apply success:

1 write.

Apply failure:

no committed change.

106. SIX-AXIS APPLY TEST

Test representative all-field Draft:

themeId = ocean
heroStyle = aurora
glassStyle = frosted
motionStyle = subtle
readingSize = large
subjectOrder = [SSC, ImD, PHC, CA, RM, NT, ID]

Successful Apply:

one write.

All six axes committed atomically.

107. ACCOUNT ISOLATION TEST

A Draft order must not appear in B.

A committed order must not leak into B during hydration boundary.

108. LOGOUT TEST

Logout clears personalized Home order to default anonymous state.

109. LOCALIZATION TEST

Every new visible Prompt 18 string exists in:

English
Arabic

110. RTL SOURCE TEST

Verify Prompt 18 does not add inappropriate hardcoded:

left
right

for reorder-control placement where logical alignment is expected.

Do not write a brittle whole-file test.

111. ACCESSIBILITY TEST

Protect:

semantic list
Move Up buttons
Move Down buttons
disabled boundary states
accessible subject names
no drag-only interaction

If drag exists:
protect accessible drag handle too.

112. HOME CONTENT FREEZE TEST

Add maintainable invariant showing Prompt 18 does NOT add new Home sections/widgets.

Do not snapshot entire Home.

Protect the subject-order boundary specifically.

113. SUBJECT COUNT TEST

Production ordering output count must always equal:

7

for valid current Home source.

114. UNIQUE ID TEST

Output contains seven unique SubjectIds.

115. CANONICAL SET TEST

Set equality exactly:

ID
NT
RM
CA
PHC
ImD
SSC

Order may vary.

Set may not.

116. HOME ROUTING SAFETY

Subject card click/navigation destination must remain tied to SubjectId.

Reorder must not alter route mapping.

Test/source audit representative card behavior.

117. SUBJECT VISUAL IDENTITY SAFETY

Existing subject icon/color/identity helper remains keyed by SubjectId.

Order position must not affect identity.

118. HOME SEARCH SAFETY

If Home has inline search independent of subject cards:

do not change it.

If the search actually filters these cards:
audit carefully.

Personalized order should remain the baseline ordering of the same cards when no search/filter applies.

Do not change search semantics.

119. HOME TODAY/SCHEDULE SAFETY

No edits.

120. HOME UPCOMING EVENT SAFETY

No edits.

121. MAIN PREVIEW TEST

Verify My 99 contained preview renders Draft subject IDs in exact Draft order.

No fake data.

122. PREVIEW GLOBAL ISOLATION

Representative:

Committed Home:
canonical

Draft Home:
reversed

Expected:

Production Home:
canonical

My 99 preview:
reversed

until Apply.

123. PREVIEW FIVE STYLE AXES STILL WORK

Adding Home order preview must not break existing:

Theme preview
Hero preview
Glass preview
Motion preview
Reading preview

Main preview now represents all six personalization dimensions, though Home order itself is data order rather than a CSS attribute.

124. STUDIO PERFORMANCE

Seven reorder rows and seven preview chips are trivial.

Do NOT add:

virtualization
complex observers
network calls
expensive layout calculation
125. HOME PERFORMANCE

Ordering seven items should be O(7) / trivial.

No sorting on every card separately.

No expensive deep cloning.

126. NO LARGE FILE GROWTH

Keep Prompt 18 modular.

Likely useful files:

src/features/personalization/homeSubjectOrder.ts

or similar pure helper,

and perhaps:

src/features/personalization/components/HomeSubjectOrderEditor.tsx

Do not add hundreds of lines to My99Studio if a focused component is cleaner.

127. FILE SIZE DISCIPLINE

My99Studio should orchestrate.

HomeSubjectOrderEditor should own reorder UI.

Pure helpers should remain outside component code.

Do not create a monolith.

128. EXPECTED HOME FILE CHANGE

Only the actual Home subject list boundary should require a production Home edit.

Target:

1 Home runtime file if possible.

If multiple unrelated Home files need changes:

STOP and audit architecture.

129. EXPECTED STUDIO FILES

Likely:

My99Studio
personalization catalog/i18n
HomeSubjectOrderEditor
order helper
tests

No broad feature changes.

130. NO APP.TSX CHANGE EXPECTED

My 99 route already exists.

Prompt 18 should not require App routing changes.

If App.tsx needs editing merely to access subject order:
STOP and inspect provider placement first.

Expected App.tsx product diff:

ZERO.

131. PRESENTATION BRIDGE CHANGE EXPECTED

ZERO.

Subject order is not a global presentation attribute.

Do not add it to PresentationBridge.

132. THEME BRIDGE CHANGE EXPECTED

ZERO.

133. INDEX.CSS

May receive small Studio reorder styles if needed.

Do not change Theme/Hero/Glass/Motion/Reading runtime definitions.

134. DIRTY STATE

Existing deep config equality should automatically handle reordered arrays.

Test:

Committed canonical.
Draft swapped two subjects.

Expected:
isDirty = true.

Reorder back exactly to canonical.

Expected:
isDirty = false if all other fields equal.

No manual dirty flag.

135. ORDER ARRAY EQUALITY

Order matters.

The deep config comparison must treat:

[ID, NT, ...]

and:

[NT, ID, ...]

as different.

Audit existing implementation.

Do not replace with Set equality.

136. APPLY DISABLED STATE

Apply enabled when order differs.

Apply disabled when order matches committed and no other field differs.

137. CURRENT VS DRAFT HOME ORDER

Unlike single-value cards, do not add Current badges to each row.

Keep reorder UI simple.

If useful, a subtle label may state:

Unsaved order

using existing overall Studio dirty state.

Do not clutter each subject.

138. CURRENT ORDER REFERENCE

Do NOT render a second full committed-order list beside Draft.

The Draft reorder list plus existing Cancel behavior is sufficient.

Main preview is Draft.

Production Home remains committed.

139. RESET COMMUNICATION

The existing global Reset action is sufficient.

No modal needed.

Reset remains reversible with Cancel until Apply.

140. DRAG VISUALS

If drag exists:

subtle elevation while dragging
no large scale/bounce
no spring spectacle
no custom drag preview outside list
no haptics requirement

Respect reduced motion.

141. NO AUTO-SCROLL COMPLEXITY

With only seven items, avoid implementing elaborate drag auto-scroll unless an existing library provides it safely.

Explicit move controls guarantee usability.

142. IOS TOUCH SAFETY

Ensure reorder controls work in Capacitor iOS without:

page zoom
long-press text selection interfering
horizontal swipe conflict

Do not globally disable gestures.

143. PWA / DESKTOP SAFETY

Mouse and keyboard controls must work.

Do not make the feature touch-only.

144. VISUAL VERIFICATION — AUTHENTICATED IF AVAILABLE

If authenticated preview is available:

test My 99 reorder list.

At minimum:

canonical order
move one subject up/down
move subject to first
Reset
Cancel
Apply

Then inspect Home after Apply.

145. MOBILE VISUAL

If available at phone width:

verify:

seven rows fit/scroll naturally
move controls are tappable
drag handle if present is isolated
sticky action area does not cover rows
no horizontal overflow
146. IPAD VISUAL

Verify:

reorder section width is appropriate
not excessively stretched
controls remain clear
no giant empty gaps
147. DESKTOP VISUAL

Keep reorder editor within Studio content width.

Do not stretch each row edge-to-edge across a huge viewport unnecessarily.

148. ARABIC VISUAL

If available:

verify subject names/control labels in RTL.

Do not reverse the persisted sequence based on language.

149. HOME VISUAL AFTER APPLY

If authenticated preview available:

apply a noncanonical order.

Navigate Home.

Verify:

exactly seven cards
only order changed
card geometry unchanged
Hero unchanged
Today/Schedule unchanged
search unchanged
subject card click targets unchanged
150. VISUAL GATE HONESTY

If authenticated access is unavailable:

do NOT claim live Home reorder verification.

Report source/test/build verification only.

151. ALL PERSONALIZATION TESTS

Run every current personalization test including:

contract
state
storage
runtime
tokens
Phase-2 closure
feature surfaces
Lecture
Calendar
MCQ/Flashcards
Theme runtime
Theme library
Phase-3 closure
Studio
Presentation
Reading composition
new Prompt 18 subject-order suite

Previous accepted baseline:

114 passed
0 failed

New total should be >=114.

Report exact total.

152. STUDIO REGRESSION

Run:

npx tsx --test tests/personalization-studio.test.ts
153. PRESENTATION REGRESSION

Run:

npx tsx --test tests/personalization-presentation.test.ts
154. READING COMPOSITION REGRESSION

Run:

npx tsx --test tests/personalization-reading-composition.test.ts
155. THEME ENGINE REGRESSION

Run:

tests/personalization-theme-runtime.test.ts
tests/personalization-theme-library.test.ts
tests/personalization-closure.test.ts
tests/personalization-tokens.test.ts
156. HOME / NAVIGATION REGRESSION

Run existing Home/navigation tests if present.

Mandatory:

npx tsx --test tests/client-navigation.test.ts

Also run any existing Home/subject-focused test discovered during audit.

Report exact names.

157. AUTH

Run:

npm run test:auth
158. STUDY REGRESSIONS

Run:

npx tsx --test tests/material-note-policy.test.ts tests/mcq-metadata.test.ts
npm run test:ai-mcq
npm run test:ai-flashcard
159. FEATURE PERSONALIZATION REGRESSIONS

Run:

Lecture personalization suite
Calendar personalization suite
MCQ/Flashcard personalization suite
160. BUILD

Run:

npm run build

Must pass.

161. TYPESCRIPT

Run targeted TypeScript checks on Prompt 18 files.

Full project typecheck may still contain only the known seven .webp errors.

No new error.

162. ESLINT

Run targeted ESLint.

Do not fix unrelated baseline lint.

163. PDF BASELINE

No PDF change expected.

If suite is run:

accepted baseline:

2 pass
1 known pdf_download_expiry_invalid

Do not repair here.

164. NO .agents/*

ABSOLUTE.

Do not create, modify, or delete:

.agents/*

No memory note.

No exception.

165. .agents/* CHECK

Run:

git status --short -- .agents
git diff -- .agents

Expected:
no output.

166. NO DOCUMENTATION ARTIFACTS

Do not create:

prompt summary markdown
implementation report file
planning file
agent memory

The final response report is sufficient.

167. GIT REVIEW

Run:

git status --short
git diff --stat
git diff --check

Classify every changed/untracked file.

No unrelated implementation changes.

168. DEFINITION OF DONE

Prompt 18 passes only if:

Production Home reads committed subject order.
Draft order does not affect Home before Apply.
exactly seven subjects remain.
canonical subject set remains unchanged.
no subject can be hidden.
no subject can be added.
no subject can be duplicated.
no subject can disappear.
default order remains ID, NT, RM, CA, PHC, ImD, SSC.
reorder helper is deterministic and defensive.
input arrays are not mutated.
stable SubjectId keys remain.
subject click/navigation remains unchanged.
subject identity remains unchanged.
Hero remains unchanged.
Search remains unchanged.
Today/Schedule remains unchanged.
Upcoming event remains unchanged.
Home grid geometry remains unchanged.
account boundary uses canonical default.
logout uses canonical default.
stale hydration remains guarded.
My 99 exposes Home Subject Order.
all seven subjects visible in editor.
Draft order editor is accessible.
Move Up/Down controls exist.
drag is optional, never sole interaction.
no new drag dependency.
drag does not modify global gestures if implemented.
section uses Draft only.
zero writes during reordering.
one write on successful Apply.
failure is atomic.
Cancel restores committed order.
Reset restores canonical Draft order.
six-axis Apply works.
preview reflects Draft order.
production Home remains committed before Apply.
no DOM subject-order attribute.
no PresentationBridge change.
no schema change.
no provider rewrite.
no backend/server change.
no new network calls.
no global order cache.
no Home widgets.
no subject hiding.
no App Icon work.
no KV.
no SQL.
no D1.
no R2.
Theme/Hero/Glass/Motion/Reading behavior preserved.
29 Theme Pack tokens unchanged.
app_text_scale composition preserved.
iOS 16 Glass compatibility preserved.
tests pass.
build passes.
no .agents/*.
diff remains scoped.
169. MANDATORY FINAL IMPLEMENTATION REPORT

The task is incomplete without this report.

Return exactly:

99'S GUIDE PERSONALIZATION — PHASE 4 PROMPT 18 REPORT
1. Status

PASS / FAIL

2. User-Facing Result

Explain exactly how Home Subject Order works.

3. Home Architecture Audit

List:

exact Home file(s)
exact subject source
exact card component
exact current subject IDs
exact pre-Prompt-18 order
whether source is shared with other features
4. Files Created
File	Purpose
5. Files Modified
File	Exact Change	Why
6. Canonical Subject Set

Print exact seven IDs.

7. Canonical Default Order

Print exact order.

8. Production Order Architecture

Explain:

committed.home.subjectOrder
→ ordering helper
→ Home subject list

9. Draft Order Architecture

Explain:

draft.home.subjectOrder
→ My 99 reorder editor
→ contained preview

10. Defensive Fallback

Explain behavior for invalid/partial/duplicate runtime order.

11. Ordering Helper

Explain exact algorithm and complexity.

12. Immutability

Confirm inputs are not mutated.

13. Object / Subject Identity

Confirm reordered subjects retain correct IDs/data and stable references where applicable.

14. React Key Safety

State exact subject-card key.

Confirm index keys are not used.

15. Home Content Freeze

Confirm no changes to:

Hero
Search
Today/Schedule
Upcoming event
other Home content
16. Home Geometry

Confirm card/grid layout unchanged.

17. Subject Navigation Safety

Confirm routes/click behavior remain keyed to SubjectId.

18. Subject Visual Identity

Confirm color/icon/identity remain SubjectId-based.

19. My 99 Section

Explain placement and structure.

20. Reorder UX

State whether implementation uses:

Move Up/Down
drag handle
both

Explain why.

21. Drag Safety

If drag exists:
report touch/swipe/scroll handling.

If no drag:
state NOT IMPLEMENTED — explicit accessible controls are authoritative.

22. Accessibility

Report:

list semantics
control labels
keyboard behavior
disabled boundaries
focus behavior
drag accessibility if relevant
23. Localization

List new English/Arabic keys.

24. RTL

Explain persistence semantics and layout behavior.

25. Draft-Only Behavior

Confirm reordering performs zero global/Home changes before Apply.

26. Reorder Write Count

Report multiple moves before Apply.

Expected:
0 writes.

27. Apply Success

Report:

one write
committed order
production Home result
no reload
28. Apply Failure

Report atomic behavior.

29. Cancel

Report exact order restoration and writes.

30. Reset

Report canonical Draft order and writes.

31. Reset + Apply

Report resulting canonical committed Home order.

32. Dirty State

Explain array-order equality behavior.

33. Six-Axis Apply

Report representative:

Theme
Hero
Glass
Motion
Reading
Home Order

and exact write count.

34. Main Draft Preview

Explain how subject order is represented.

35. Preview Isolation

Report:

Committed canonical
Draft reversed

→ production Home vs preview behavior.

36. Account Boundary

Report representative A → canonical boundary → B order.

37. Logout

Confirm canonical anonymous/default order.

38. Stale Hydration

Confirm existing generation guard remains authoritative.

39. Storage Architecture

Confirm subject order remains only inside existing PersonalizationConfigV1.

40. Global Cache Safety

Confirm no unscoped Home-order key exists.

41. Provider / Contract

Confirm whether either changed.

Expected:
no architecture rewrite.

42. Presentation Bridges

Confirm:

ThemeBridge unchanged.
PresentationBridge unchanged.
No subject-order DOM attribute.

43. Theme / Hero / Glass / Motion / Reading Integrity

Confirm all prior customization behavior remains unchanged.

44. Home Subject Order Tests

List exact new invariants.

45. Ordering Helper Tests

Report valid/reverse/arbitrary/invalid/no-mutation results.

46. Studio Test Results

Report.

47. Presentation Test Results

Report.

48. Reading Composition Results

Report.

49. Theme Engine Results

Report.

50. Personalization Test Results

Report exact total passed/failed.

51. Navigation / Home Regression

Report exact commands/results.

52. Auth Regression

Report.

53. Study Regression

Report Notes/MCQ metadata/AI MCQ/AI Flashcard.

54. Feature Personalization Regression

Report Lecture/Calendar/MCQ-Flashcard suites.

55. Build

Report.

56. TypeScript

Report targeted result and known baseline.

57. ESLint

Report targeted result and unrelated baseline.

58. PDF Baseline

Report if run, otherwise state NOT RUN.

59. Visual Verification

State exactly:

auth state
viewport(s)
language(s)
Home actually observed or not
My 99 reorder editor actually observed or not
move controls actually exercised or not
Apply actually exercised or not
Home post-Apply actually observed or not

Do not overclaim.

60. Responsive Review

Report phone/iPad/desktop or visual gate.

61. Performance

Report ordering cost and render impact.

62. Feature Diff Verification

Confirm no unrelated feature edits.

63. Business Logic Verification

Confirm academic/domain logic unchanged.

64. Network / Persistence

State exactly:

ZERO new personalization network calls.
No KV.
No SQL personalization storage.
No D1 personalization storage.
No R2 personalization storage.

65. Future Feature Safety

Confirm:

App Icons remain unimplemented.
No subject hiding.
No new Home widgets/content.

66. Git Diff Summary

Exact files/stat.

Confirm:

git diff --check: PASS

67. .agents/* Verification

Report:

git status --short -- .agents
git diff -- .agents

Then state exactly:

No .agents/* file was created or modified.

68. Phase 4 Closure Readiness

If everything passes, state exactly:

HOME SUBJECT ORDER CUSTOMIZATION: CLOSED AND APPROVED

SEVEN-SUBJECT COMPLETENESS: VERIFIED

HOME BUSINESS CONTENT: UNCHANGED

MY 99 SIX-AXIS DRAFT/APPLY WORKFLOW: VERIFIED

PHASE 4 PERSONALIZATION STUDIO: READY FOR FINAL CLOSURE AUDIT

End EXACTLY:

PERSONALIZATION PHASE 4 PROMPT 18 STATUS: COMPLETE — READY FOR PHASE 4 CLOSURE AUDIT

or:

PERSONALIZATION PHASE 4 PROMPT 18 STATUS: BLOCKED — REPAIR REQUIRED