# 99'S GUIDE — PERSONALIZATION SYSTEM
# PHASE 5 / PROMPT 21B
## CLOUD SYNC V1 IMPLEMENTATION CLOSURE AUDIT
### VERIFY PROMPT 21 AGAINST THE FINAL 20D CONTRACT
### REPAIR ONLY REAL IMPLEMENTATION DEFECTS
### NO LIVE CLOUDFLARE DEPLOYMENT
### NO PRODUCTION FEATURE ENABLEMENT
### DO NOT BEGIN PROMPT 22 UNTIL THIS PASSES

Prompt 21 implementation is complete provisionally.

The implementation summary reported:

- Local Envelope V2
- V1 → V2 migration
- durable pending intents
- 49,152-byte local envelope limit
- cloud record validation
- reconciliation logic
- initial GET
- pending PUT recovery
- confirmation window
- foreground/network lifecycle
- 15-minute discovery
- dedicated Cloudflare Personalization Worker
- Workers KV
- internal secret authentication
- GET /api/personalization
- PUT /api/personalization
- feature flag
- production build PASS
- Worker TypeScript PASS
- 29 personalization tests PASS
- 13 security contract checks PASS
- no live deployment
- no `.agents/*`

This is NOT enough evidence to close Prompt 21.

Prompt 21B must inspect ACTUAL SOURCE and run the complete mandatory verification matrix.

REPAIR ONLY real Prompt-21 defects found.

DO NOT:

- deploy Worker
- create live KV namespace
- configure live Render secrets
- enable production cloud sync
- implement App Icons
- add SQL/D1/R2 personalization persistence
- modify `.agents/*`

---

# 1. FIRST AUDIT THE ACTUAL DIFF

Run:

```bash
git status --short
git diff --stat
git diff --check
git diff

Classify every changed/untracked file.

Do not rely on the prior summary.

Report exactly:

frontend files
shared files
backend files
Worker files
config/env files
test files

Flag unrelated changes.

2. VERIFY LOCAL ENVELOPE V2 EXACTLY

Inspect actual storage implementation.

Required conceptual type:

type PersonalizationLocalEnvelopeV2 = {
  cacheVersion: 2;
  savedAt: string;
  config: PersonalizationConfigV1;
  sync: {
    knownCloudRevision: string | null;
    pending: {
      intentId: string;
      config: PersonalizationConfigV1;
      createdAt: string;
    } | null;
    lastSyncState:
      | "never"
      | "clean"
      | "retryable-failure"
      | "disabled"
      | "unsupported";
    lastSuccessfulGetAt: string | null;
  };
};

If actual implementation differs:
report exact differences and justify or repair them.

3. VERIFY EXACT LOCAL KEY

Must remain:

99s:personalization:v1:<encodeURIComponent(userId)>

No new active:

99s:personalization-sync:...
99s:personalization-outbox:...

unless implementation discovered a genuine unavoidable constraint.

If extra active keys were added:
STOP and audit against Prompt 20D.

4. VERIFY ONE-WRITE LOCAL APPLY

Successful explicit Apply must perform ONE local envelope persistence operation representing together:

new committed config
pending config snapshot
pending intent ID

Then and only then:

committed updates
dirty clears
cloud sync may begin

Prove with tests.

Do not count cloud PUT as a second LOCAL persistence write.

5. VERIFY LOCAL APPLY FAILURE

Simulate local V2 persistence failure.

Expected:

old committed remains
Draft remains dirty
no pending new intent becomes authoritative
ZERO cloud PUTs
previous stored envelope remains valid
6. CRITICAL V1 → V2 LEGACY PROVENANCE TEST

This is mandatory.

Test this exact sequence:

Existing valid cacheVersion:1 customized config
→ load application
→ normalize/migrate to cacheVersion:2
→ migration rewrite succeeds
→ app/provider is destroyed/restarted
→ restart reads ONLY the V2 record
→ knownCloudRevision = null
→ pending = null
→ initial cloud GET = empty
→ confirmation GET = empty

Expected:

the persisted customized local config is STILL recognized
as an existing pre-cloud local personalization candidate

Then:

create durable migration pending intent
PUT that LOCAL config to cloud

It must NOT be mistaken for:

no local personalization / factory-only state

If the current V2 record cannot preserve or infer this after restart:
THIS IS A REAL DEFECT.

Repair using the smallest safe mechanism.

A small explicit provenance field is allowed if necessary.

Do not rely on transient in-memory migration state.

7. NO-CACHE FACTORY CONTRAST TEST

Test separately:

no local cache ever existed
→ factory defaults only in memory
→ first GET empty
→ confirmation GET empty

Expected:

no V2 record created merely because defaults rendered
no migration intent
no PUT

This must remain distinguishable from Section 6.

8. V1 MIGRATION WRITE FAILURE

Valid V1 record.

Opportunistic V2 rewrite fails.

Expected:

validated V1 config still used in memory
no data loss
no false cloud state
next natural persistence can retry migration
9. C1 → C2 DURABILITY

Test:

C1 is committed + pending.

Then C2 Apply.

Success:

config = C2
pending.config = C2
pending.intentId = C2 intent

C1 disappears only after the atomic envelope replacement succeeds.

If C2 local write fails:

C1 remains durable
C2 is not uploaded
committed remains C1
Draft remains C2 and dirty
10. PUT SOURCE AUDIT

Every PUT must use durable:

envelope.sync.pending.config
envelope.sync.pending.intentId

Never:

Draft
UI state
independently reconstructed current config

Add/retain test.

11. PUT ACK DURABILITY

Remote PUT succeeds with revision R.

Local acknowledgement must persist in ONE V2 write:

knownCloudRevision = R
pending = null
lastSyncState = clean

Only after durable acknowledgement should sync state be considered locally complete.

12. PUT ACK WRITE FAILURE

Simulate:

Worker accepted request
→ response received
→ local acknowledgement write fails

Expected:

pending intent remains durable
current config remains correct
next startup/retry reuses SAME intentId
no data loss
13. LOST PUT RESPONSE

Simulate Worker write success but client timeout/lost response.

Expected:

pending remains
same intent retried
visible Worker record with same lastIntentId
→ success without another KV write

Also simulate stale Worker read where intent is not visible:

duplicate KV write is permitted
client remains safe
no exactly-once claim
14. CLOUD RECORD EXACT SHAPE

Verify actual:

type PersonalizationCloudRecordV1 = {
  recordVersion: 1;
  config: PersonalizationConfigV1;
  revision: string;
  lastIntentId: string;
  updatedAt: string;
};

No counter.

No writerId.

No ordering timestamp.

15. REVISION MUST NEVER BE ORDERED

Inspect implementation.

There must be NO logic conceptually equivalent to:

remoteRevision > localRevision
remoteRevision < localRevision
localeCompare(revision)
parseInt(revision)

Revision may only be compared for equality/inequality.

Run behavior tests using deliberately unordered strings.

16. WORKER KV KEY

Verify exact output:

personalization:v1:<encodedUserId>

No slash.

No email.

No client-selected identity.

17. USER ID VALIDATION

Run focused tests:

PASS:

valid usr_...
valid UUID

FAIL:

email
empty
malformed UUID
arbitrary words
path traversal-like strings
oversized IDs

Backend identity comes only from authenticated req.user.id.

18. SUBJECT ORDER CLOUD VALIDATION

Accept arbitrary valid permutations of:

ID
NT
RM
CA
PHC
ImD
SSC

Reject:

duplicate
missing
unknown
wrong length

Do not regress to fixed tuple ordering.

19. FIRST EMPTY GENERAL RULE

Test both:

A.
knownCloudRevision = null
+
local customized config

B.
knownCloudRevision = null
+
no local config

First empty GET:

MUST NOT immediately:

seed cloud
conclude final absence

Both require confirmation.

20. CONFIRMATION EMPTY — LEGACY LOCAL

Second empty after valid persisted local personalization:

durable migration intent created FIRST
V2 saved
only then PUT
21. CONFIRMATION EMPTY — NO LOCAL

Second empty with no persisted local personalization:

remain factory defaults
no PUT
no V2 record created solely for sync
22. KNOWN REVISION + EMPTY

Test:

knownCloudRevision != null
→ GET empty

Expected:

local state retained
confirmation required

Confirmation also empty:

still no auto-delete
still no automatic resurrection PUT
unresolved/retryable state
23. SAME CONFIG / DIFFERENT REVISION

Test:

remote revision different
remote config exactly equals local committed

Expected:

revision identity can be persisted immediately
no config replacement
no confirmation delay

If Draft dirty:
Draft remains unchanged.

If local pending exists:
pending must NOT be cleared merely due config equality.

24. DIFFERENT CONFIG / DIFFERENT REVISION

First GET:
different config + different revision

Expected:
NO immediate adoption.

Confirmation required.

Confirmation same candidate:
adopt only when:

no pending
Draft clean
account generation valid

Confirmation candidate changes:
adopt neither.

25. VERIFY cacheTtl EXACTLY

The implementation summary mentioned a 45-second confirmation window but did not prove Worker read settings.

Inspect Worker source.

Every reconciliation KV GET should intentionally use:

cacheTtl: 30

using the actual supported Workers KV API.

Report exact source location.

If missing:
repair.

26. VERIFY CONFIRMATION DELAY CONTRACT

Final approved contract was:

random delay 40,000–50,000 ms

The summary says:

45-second confirmation window

Determine actual implementation.

If it is FIXED 45,000 ms:
repair to randomized 40–50 seconds unless a concrete testability/runtime reason justifies the deviation.

Use an injectable/random helper so fake-timer tests remain deterministic.

Do NOT use recurring interval.

27. CONFIRMATION TIMER CANCELLATION

Verify timer/candidate cancels on:

account switch
logout
Provider unmount
explicit local Apply
candidate replacement

No stale candidate may later commit.

28. BACKGROUND CONFIRMATION

If background/suspension prevents timer execution:

next valid foreground/session trigger handles unresolved confirmation.

No background-worker complexity.

29. CLEAN FOREGROUND 15-MINUTE DISCOVERY

Test:

last successful GET = 16 minutes ago
→ foreground
→ exactly 1 GET.

5 minutes ago
→ 0 GET.

Repeated foreground events inside window
→ 0 extra GET.

No polling timer.

30. INITIAL SESSION GET

A newly created authenticated Provider/account session must run one initial GET after local hydration.

The previous persisted lastSuccessfulGetAt must NOT suppress the initial account-session probe if that is the approved implementation.

Report exact implementation.

31. DEVICE FOREGROUND FOREVER

Verify no periodic GET timer exists.

Accepted limitation:

clean permanently foregrounded device does not have real-time sync guarantee.

32. NETWORK RESTORATION

Test:

pending intent
→ PUT priority.

retryable reconciliation
→ GET/confirmation.

clean + <15 min
→ no GET.

clean + >=15 min
→ one GET.

33. FEATURE DISABLED

Server:

PERSONALIZATION_CLOUD_SYNC_ENABLED=false

GET:
200 disabled.

PUT:
200 disabled.

Frontend:

local works
pending survives
no retry storm
future session may probe again
34. OLD SERVER 404

Verify:

classified unsupported-server
local works
pending durable
no repeated same-session probing
new session allowed to probe again

Persistent lastSyncState="unsupported" must not permanently suppress future server support.

35. WORKER SECRET

Verify actual internal header.

Expected:

X-Personalization-Sync-Secret

or explicitly documented repository-equivalent.

Check:

missing secret rejected
wrong secret rejected
correct secret accepted
no secret value in logs
no secret value in frontend
36. WORKER CORS

Worker must not expose permissive browser CORS.

Specifically search for:

Access-Control-Allow-Origin: *

or frontend-origin CORS behavior.

Expected:
none for Personalization Worker.

37. WORKER BEST-EFFORT IDEMPOTENCY

Visible current record with:

lastIntentId === incoming.intentId

→ return current record
→ ZERO KV write.

Test exact KV write count.

38. KV 429

Test simulated same-key throttling.

Expected:

429 classification
local Apply still success
pending remains
Retry-After honored if available
otherwise bounded backoff >1 sec
no immediate retry loop
39. C1 429 → C2 APPLY

Test:

C1 gets 429.

Before C1 retry:
C2 Apply succeeds.

Later retry:
C2 only.

Never upload superseded C1.

40. RETRY BOUNDS

Report exact implemented retry schedule/count.

No infinite foreground retry.

After bounded immediate retry budget:
leave pending/retryable for lifecycle trigger.

41. CLOUD CONFIG SIZE

Verify serialized config limit:

16,384 bytes

at appropriate boundaries.

42. PUBLIC HTTP BODY SIZE

Verify:

32,768 bytes

enforced specifically for personalization PUT.

Do not merely cite global Express 20 MB parser.

43. CLOUD RECORD SIZE

Verify:

32,768 bytes

at Worker boundary.

44. LOCAL V2 SIZE

Verify:

49,152 bytes

on read/write.

Oversized/corrupt envelope:
safe rejection/fallback.

45. EXPRESS AUTHENTICATION

Verify actual routes use:

requireUser

or exact existing authenticated middleware.

No admin/owner requirement.

46. PUBLIC API IDENTITY

Search route/controller.

No:

req.body.userId
req.params.userId
req.query.userId

may determine target account.

Target is:

req.user.id

only.

47. UNAUTHENTICATED API TEST

GET → 401.

PUT → 401.

48. SESSION SECURITY

Using existing auth test helpers where practical, verify invalid session conditions cannot sync:

expired/revoked where modeled
sessionVersion invalid
banned/disabled user if middleware already covers it

Do not rebuild auth system.

49. CORS / CSRF

Verify personalization route reuses existing centralized API CORS.

No new wildcard.

PUT follows current mutation protection such as existing X-Requested-With and origin behavior.

Native bearer path must remain valid.

50. PUBLIC PUT STRICT BODY

Preferred request:

{
  config;
  intentId;
  knownRevision;
}

No userId.

Test extra userId.

Preferred:
400 strict rejection.

If repository convention strips unknowns:
prove identity still cannot be influenced.

51. WORKER URL / SECRET SERVER-ONLY

Search all frontend/client sources for:

PERSONALIZATION_WORKER_URL
PERSONALIZATION_SYNC_SECRET
X-Personalization-Sync-Secret

No client runtime usage.

Environment variable NAMES may appear in server/config only.

52. FRONTEND BUNDLE AUDIT

Build frontend.

Search generated frontend bundle where practical.

Confirm no:

Worker internal URL
sync secret value
internal auth header logic

Public /api/personalization path is expected.

53. MY99STUDIO CLOUD-UNAWARE

Audit:

My99Studio.tsx

must not import:

cloud client
Worker
revision helpers
sync metadata
intent generator

It interacts through Provider only.

54. ZERO-NETWORK DRAFT TEST

Explicitly instrument personalization client calls.

Perform:

Theme Draft selection
Hero
Glass
Motion
Reading
Home reorder
Cancel
Reset

Expected:
0 GET/PUT triggered by those actions themselves.

Apply is the mutation boundary.

55. CLOUD FAILURE MUST NOT FAIL LOCAL APPLY

Simulate local V2 persistence PASS + cloud failure.

Expected external Apply result:
local success.

Do NOT return:

persistence-failed

because Worker/KV failed.

Dirty clears because local Apply succeeded.

Pending remains.

56. ACCOUNT SWITCH DURING GET

A GET response returns after B active.

Expected:

no B config mutation
no B V2 write
no B candidate
no B revision change
57. ACCOUNT SWITCH DURING PUT

A PUT may finish remotely after switch.

Expected:

no B mutation
no B V2 write
no B success state

A's durable pending state must remain logically recoverable unless A acknowledgement was durably persisted before switch.

58. LOGOUT DURING GET

Late response cannot hydrate anonymous state.

59. LOGOUT DURING PUT

Late response cannot reapply signed-out user's presentation anonymously.

Stored account envelope may remain for future sign-in.

60. STRICTMODE DEDUPLICATION

Simulate duplicate initialization effects.

Expected:
1 initial GET.

One Apply:
1 active PUT operation for that intent.

No duplicate Provider commit.

61. PROVIDER SIZE / MODULARITY

Inspect final Provider.

Report line-count change.

Ensure:

reconciliation is extracted
network client extracted
storage parsing extracted
lifecycle orchestration is not one huge Provider block

If Provider became an obvious monolith:
refactor narrowly.

62. SERVER MODULARITY

Do not bury all Prompt 21 logic in historical large server.ts.

Expected:
focused routes/service/client modules,
minimal registration only.

Report.

63. WORKER MODULARITY

Worker entrypoint should not contain all validation/storage/testing logic in one massive file if easily separable.

Keep reasonable structure.

64. NO DATABASE PERSONALIZATION

Run searches/diff.

Expected ZERO Prompt-21 personalization changes to:

Prisma schema
Prisma migrations
Supabase SQL
D1 schema
R2 storage services

Any such diff blocks closure.

65. APP ICON

Expected ZERO.

66. app_theme / app_text_scale

Verify cloud serializers never include these.

Existing device-local behavior unchanged.

67. FULL PERSONALIZATION MATRIX

CRITICAL.

The implementation summary reported:

29 passed

but accepted Phase-4 baseline before Prompt 21 was:

130 passed
0 failed

Determine what the 29 represented.

Run the COMPLETE current personalization suite, including ALL Phase 1–4 suites plus new Phase 5 tests.

Final count MUST be at least the pre-Prompt-21 baseline unless files/tests were legitimately consolidated.

Any reduction must be explained test-by-test.

Report exact:

passed
failed
skipped
cancelled

Do not call 29 tests the full personalization matrix if they are only new Prompt-21 tests.

68. PHASE-4 CLOSURE TEST

Run:

npx tsx --test tests/personalization-phase4-closure.test.ts

Report exact result.

69. STORAGE TESTS

Run current personalization storage tests.

Must cover both:

cacheVersion 1 read/migration
cacheVersion 2 read/write

Report exact count.

70. LOCAL DURABILITY TEST SUITE

Run dedicated Prompt-21 durability suite.

Report exact count and test file names.

71. RECONCILIATION SUITE

Run dedicated empty/divergent/confirmation/foreground suite.

Report exact count.

72. WORKER TESTS

Run dedicated Personalization Worker tests.

Report exact command/count.

No live Cloudflare required.

73. BACKEND API TESTS

Run dedicated Express personalization API tests.

Report exact command/count.

Must include:

auth
body validation
user isolation
disabled
upstream error mapping
74. PROVIDER / LIFECYCLE TESTS

Run dedicated provider/lifecycle/race suite.

Report exact count.

75. SECURITY CONTRACT TESTS

The summary reported 13 passed.

Identify exact file/command.

List what those 13 protect.

Do not report only an aggregate label.

76. NAVIGATION REGRESSION

Run:

npx tsx --test tests/client-navigation.test.ts

Expected baseline:
4 pass.

77. AUTH REGRESSION

Run:

npm run test:auth

Expected baseline:
4 pass.

78. NOTES / MCQ METADATA

Run:

npx tsx --test tests/material-note-policy.test.ts tests/mcq-metadata.test.ts

Expected:
5 pass.

79. AI MCQ

Run:

npm run test:ai-mcq

Expected:
18 pass.

80. AI FLASHCARD

Run:

npm run test:ai-flashcard

Expected:
17 pass.

81. FEATURE PERSONALIZATION REGRESSION

Run:

tests/personalization-lecture-surfaces.test.ts
tests/personalization-calendar-surfaces.test.ts
tests/personalization-mcq-flashcard-surfaces.test.ts

Accepted combined baseline:
20 pass.

82. STUDIO / PRESENTATION / READING / HOME REGRESSIONS

Run explicitly:

tests/personalization-studio.test.ts
tests/personalization-presentation.test.ts
tests/personalization-reading-composition.test.ts
tests/personalization-home-subject-order.test.ts

Report each.

83. THEME ENGINE REGRESSION

Run actual current:

tests/personalization-theme-runtime.test.ts
tests/personalization-theme-library.test.ts
tests/personalization-tokens.test.ts
tests/personalization-closure.test.ts

Report actual counts.

84. PDF BASELINE

Run:

npx tsx --test tests/pdf-access.test.ts

Accepted only:

2 passed
1 known failure
pdf_download_expiry_invalid

Anything else blocks closure.

85. ROOT BUILD

Run:

npm run build

Must pass.

86. WORKER BUILD / TYPECHECK

Run the actual Worker package typecheck/build command.

Report exact command/result.

Do not substitute root build if Worker is outside root TS graph.

87. ROOT TYPECHECK

Run:

npm run typecheck

Accepted only known baseline:

7 missing .webp errors in:

src/features/modules/moduleVisuals.ts

Any additional Prompt-21 error blocks closure.

88. TARGETED TYPESCRIPT

Run focused validation for:

shared cloud contracts
frontend sync
Provider/storage
backend
Worker

All Prompt-21 code must pass.

89. TARGETED ESLINT

Run all Prompt-21 files.

No new issue.

Report unrelated repository baseline separately.

90. SECRET SEARCH

Search repository excluding irrelevant generated/vendor content for:

PERSONALIZATION_SYNC_SECRET
X-Personalization-Sync-Secret
PERSONALIZATION_WORKER_URL

Classify each occurrence.

Expected:

server config/service
Worker config/source
.env.example variable names if used
tests

No secret VALUE committed.

No frontend secret use.

91. WORKER CONFIG AUDIT

Report exact Worker configuration.

Confirm:

KV binding name PERSONALIZATION_KV
no D1
no R2
no real secret
no fabricated live namespace ID presented as deployed
92. FEATURE FLAG DEFAULT SAFETY

Confirm source does NOT automatically enable sync.

Missing/false env:
local-only.

No application startup crash when cloud env absent and feature disabled.

93. DEVELOPMENT LOCAL-ONLY TEST

Start/run application without Worker configuration.

Expected:

app boots
local personalization works
no mandatory cloud setup
no crash
94. IN-MEMORY CROSS-DEVICE INTEGRATION

Using mocks/in-memory KV:

simulate:

Device A
→ Apply Ocean/etc
→ local V2
→ Express
→ Worker
→ KV

Device B
→ initial GET
→ cloud record
→ local V2 adoption

Then Device B applies another config
→ Device A later foreground reconciliation
→ divergent confirmation
→ adoption

Report exact result.

No live cloud required.

95. DEPLOYMENT STATUS

Must remain honest.

Prompt 21B must NOT deploy.

Report:

KV namespace live? YES/NO
Worker deployed? YES/NO
Render production env configured? YES/NO
feature enabled? YES/NO

Expected currently:

NO / NO / NO / NO

unless user independently changed deployment outside this task.

96. NO .agents/*

ABSOLUTE.

Run:

git status --short -- .agents
git diff -- .agents

No output.

Do not modify.

97. NO REPORT ARTIFACT

Do not create closure Markdown.

Final response only.

98. GIT REVIEW

Run:

git status --short
git diff --stat
git diff --check

Classify every final changed/untracked file.

git diff --check must PASS.

99. DEFINITION OF DONE

Prompt 21B passes only if:

unified V2 matches final architecture
V1 backward compatibility proven
migrated V1 local provenance survives restart
no-cache defaults remain distinguishable
one-write local Apply proven
local failure atomic
C1→C2 durable
PUT source durable
PUT acknowledgement durable
lost response safe
remote adoption durable
revision equality-only
exact Worker key
exact user-ID security
exact subject permutation validation
unknown empty confirmed
known+empty protected
same-config revision optimization correct
different config confirmed
Worker uses cacheTtl 30
confirmation is randomized 40–50 seconds
foreground discovery is 15 minutes
no polling
feature disabled safe
old server safe
429 safe
bounded retries
Express authentication correct
no client target user
CORS/CSRF reused
Worker secret private
no Worker CORS
no frontend secret/direct Worker access
Draft zero-network behavior preserved
cloud failure does not fail local Apply
account/logout races safe
StrictMode dedupe proven
FULL personalization matrix passes
Phase-4 closure passes
regressions pass
build passes
Worker typecheck/build passes
only accepted seven-webp TS baseline remains
PDF baseline unchanged
no SQL/D1/R2 personalization
App Icon absent
no .agents/*
no production rollout
100. MANDATORY FINAL REPORT

Return exactly:

99'S GUIDE PERSONALIZATION — PHASE 5 PROMPT 21B IMPLEMENTATION CLOSURE REPORT
1. Status

PASS / FAIL

2. Why Prompt 21 Needed Closure Verification

Explain the incomplete evidence from the first summary.

3. Prompt 21 Final Diff

List every created/modified file by layer.

4. Local Envelope V2

Print exact actual type.

5. Local Storage Key

Print exact key.

6. V1 Migration

Report.

7. Legacy Provenance After Migration + Restart

Report the exact mandatory scenario and result.

8. No-Cache Factory Distinction

Report.

9. Local Size Limit

Report.

10. One-Write Apply

Report exact storage-write count.

11. Local Apply Failure

Report.

12. Crash Recovery

Report.

13. C1 → C2

Report.

14. PUT Durable Source

Report.

15. PUT Acknowledgement

Report.

16. PUT Acknowledgement Failure

Report.

17. Lost PUT Response

Report.

18. Remote Adoption Durability

Report.

19. Cloud Record Shape

Print exact actual type.

20. Revision Semantics

Confirm equality-only.

21. KV Binding / Exact Key

Report.

22. Canonical User-ID Validation

Report regex/formats.

23. Subject Order Validation

Report.

24. Worker cacheTtl

State exact actual value/source.

25. Confirmation Delay

State actual implementation.

Confirm randomized 40–50 seconds or explain/fix deviation.

26. First Empty Rule

Report.

27. Confirmed Empty — Legacy Local

Report.

28. Confirmed Empty — No Local

Report.

29. Known Revision + Empty

Report.

30. Same Config / Different Revision

Report.

31. Different Config / Different Revision

Report.

32. Dirty Draft

Report.

33. Pending Local Intent

Report.

34. Clean Foreground Discovery

Report exact 15-minute behavior.

35. Initial Session GET

Report.

36. Network Restoration

Report.

37. Device-Stays-Foreground Limitation

Report.

38. Worker Authentication

Report.

39. Worker CORS

Report.

40. Worker Idempotency

Report exact write-count behavior.

41. 429 Handling

Report.

42. Retry Bounds

Report exact policy.

43. Config / HTTP / Cloud / Local Size Limits

Report all four.

44. Express Endpoints

Print exact paths.

45. Express Authentication

Report.

46. No Client User ID

Report.

47. CORS / CSRF

Report.

48. Feature Flag

Report exact disabled behavior.

49. Old Server

Report.

50. Frontend Cloud Client

Report.

51. Provider Integration

Report.

52. My99Studio Cloud Boundary

Confirm no cloud ownership.

53. Zero-Network Draft Actions

Report measured/tested result.

54. Cloud Failure vs Local Apply

Report.

55. Account-Switch Races

Report GET/PUT results.

56. Logout Races

Report.

57. StrictMode

Report.

58. Secret Search / Bundle Audit

Report every relevant occurrence category and confirm no leak.

59. Worker Configuration

Report binding and absence of D1/R2.

60. Database Diff

State exactly:

No SQL personalization storage.
No D1 personalization storage.
No R2 personalization storage.

61. App Icon / Device-Local Settings

Confirm App Icon absent and app_theme/app_text_scale excluded.

62. Local Durability Tests

Exact file(s), command(s), counts.

63. Reconciliation Tests

Exact file(s), command(s), counts.

64. Worker Tests

Exact command/count.

65. Backend API Tests

Exact command/count.

66. Provider / Lifecycle Tests

Exact command/count.

67. Security Contract Tests

Identify exact 13 or current count and invariants.

68. FULL Personalization Matrix

Report exact:
passed
failed
skipped
cancelled

Explain relation to Phase-4 baseline of 130.

69. Phase-4 Closure

Report.

70. Studio Regression

Report.

71. Presentation Regression

Report.

72. Reading Regression

Report.

73. Home Order Regression

Report.

74. Theme Engine Regression

Report.

75. Feature Personalization Regression

Report.

76. Navigation Regression

Report.

77. Auth Regression

Report.

78. Notes / MCQ Metadata

Report.

79. AI MCQ

Report.

80. AI Flashcard

Report.

81. PDF Baseline

Report exact result.

82. Root Build

Report.

83. Worker Build / TypeScript

Report.

84. Root TypeScript

Report accepted baseline/no new errors.

85. Targeted ESLint

Report.

86. Local-Only Development Boot

Report.

87. In-Memory Cross-Device Integration

Report Device A → cloud → Device B → later reconciliation.

88. Performance

Report:

no polling
GET frequency
local write count
request dedupe
KV write behavior
89. Modularity

Report Provider/server/Worker structure and relevant file sizes.

90. Deployment Status

State exactly:

KV namespace live: YES / NO
Worker deployed: YES / NO
Render production env configured: YES / NO
Cloud sync feature enabled: YES / NO

Do not infer.

91. Git Diff Summary

Report exact final files/stat.

Confirm:

git diff --check: PASS

92. .agents/* Verification

Report:

git status --short -- .agents
git diff -- .agents

Then state exactly:

No .agents/* file was created or modified.

93. Prompt 22 Readiness

If all gates pass, state exactly:

ACCOUNT PERSONALIZATION CLOUD SYNC V1: CLOSED AND APPROVED

UNIFIED LOCAL ENVELOPE V2: VERIFIED

V1 → V2 MIGRATION PROVENANCE: VERIFIED

CRASH-SAFE LOCAL APPLY: VERIFIED

BEST-EFFORT KV RECONCILIATION: VERIFIED

WORKERS KV STALE-READ CONFIRMATION: VERIFIED

AUTHENTICATED USER ISOLATION: VERIFIED

CROSS-DEVICE DISCOVERY LIFECYCLE: VERIFIED

SECRET / NETWORK BOUNDARIES: VERIFIED

PHASE-4 PERSONALIZATION REGRESSIONS: VERIFIED

PRODUCTION CLOUD ROLLOUT: NOT YET PERFORMED

PROMPT 22 STAGING / DEPLOYMENT VERIFICATION: APPROVED TO BEGIN

End EXACTLY:

PERSONALIZATION PHASE 5 PROMPT 21 STATUS: COMPLETE — READY FOR PROMPT 22

or:

PERSONALIZATION PHASE 5 PROMPT 21 STATUS: BLOCKED — REPAIR REQUIRED