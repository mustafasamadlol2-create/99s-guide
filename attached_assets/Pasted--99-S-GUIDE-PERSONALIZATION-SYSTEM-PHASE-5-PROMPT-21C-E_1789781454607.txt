# 99'S GUIDE — PERSONALIZATION SYSTEM
# PHASE 5 / PROMPT 21C
## EXPRESS PERSONALIZATION API BOUNDARY CLOSURE
### AUTHENTICATED ROUTE INTEGRATION TESTS + TRUE 32 KiB REQUEST LIMIT
### VERIFY FIRST — REPAIR ONLY REAL DEFECTS
### NO CLOUDFLARE DEPLOYMENT
### NO KV NAMESPACE CREATION
### NO PRODUCTION FEATURE ENABLEMENT
### DO NOT BEGIN PROMPT 22 UNTIL THIS PASSES

Prompt 21 implementation is provisionally approved.

Prompt 21B verified:

- 141 / 141 complete personalization matrix
- Unified Local Envelope V2
- V1 → V2 migration provenance
- one-write local Apply
- crash-safe pending intent
- C1 → C2 supersession
- PUT acknowledgement recovery
- opaque revision equality only
- Worker cacheTtl 30
- randomized 40–50 second confirmation
- 15-minute clean foreground discovery
- Worker secret isolation
- KV key correctness
- no D1 / R2 / SQL personalization storage
- no App Icon work
- build PASS
- Worker TypeScript PASS
- targeted ESLint PASS
- accepted PDF baseline unchanged
- no `.agents/*`

DO NOT redesign those systems.

Prompt 21C exists for ONE remaining closure gap:

THE PUBLIC EXPRESS PERSONALIZATION API BOUNDARY DOES NOT YET HAVE SUFFICIENT AUTOMATED INTEGRATION EVIDENCE.

The Prompt 21B report showed only:

GET /api/personalization → 401
PUT /api/personalization → 401

as live local smoke tests.

Authenticated success, request validation, authenticated user isolation, disabled behavior, upstream mapping, CSRF/mutation behavior, and actual 32 KiB HTTP request-size enforcement were described from source inspection rather than proven through dedicated route tests.

Prompt 21C must close that gap.

---

# 1. PRIMARY GOAL

Create/complete focused automated integration tests for:

GET /api/personalization

PUT /api/personalization

using the actual Express route/middleware architecture as far as practical.

The tests must prove:

- authentication
- canonical identity derivation
- user isolation
- strict body validation
- request-size enforcement
- feature-disabled behavior
- Worker-service success mapping
- empty-state mapping
- 429 mapping
- 502 mapping
- 503 mapping
- mutation protection
- no user-controlled target account

Do not deploy anything.

---

# 2. AUDIT ACTUAL ROUTE IMPLEMENTATION FIRST

Inspect actual current source.

At minimum inspect:

- `server.ts`
- `server/services/personalizationSync.ts`
- current auth middleware
- `requireUser`
- existing CORS middleware
- existing CSRF / `X-Requested-With` mutation protection
- Express body parser configuration
- existing API test helpers
- existing authenticated route-test patterns

Do NOT infer behavior from the prior report.

---

# 3. ROUTE MODULARITY

If the personalization route is currently embedded directly in `server.ts` in a way that makes safe automated testing impractical:

you MAY extract only the personalization route into a small focused router/module.

Example conceptually:

```text
server/routes/personalization.ts

or repository-standard equivalent.

Requirements:

no business redesign
same public routes
same middleware
minimal server.ts registration
no unrelated server refactor

Do not refactor the historical server broadly.

4. PUBLIC ROUTES — FROZEN

Keep exactly:

GET /api/personalization
PUT /api/personalization

No user-ID path.

No query identity.

No alternate public Worker route.

5. AUTHENTICATION — AUTOMATED TEST REQUIRED

Prove:

Unauthenticated:

GET /api/personalization → 401
PUT /api/personalization → 401

Then prove at least one AUTHENTICATED request actually reaches the personalization route successfully.

Do not consider only unauthenticated smoke sufficient.

6. AUTHENTICATED IDENTITY

For authenticated User A:

the backend target identity MUST come from:

req.user.id

Prove through an automated test that the personalization Worker service receives User A's canonical ID.

7. CLIENT-SUPPLIED userId ATTACK

Authenticated User A sends:

{
  "userId": "<USER-B-ID>",
  "config": { ... },
  "intentId": "...",
  "knownRevision": null
}

Preferred result:

400

because body is strict.

If current repository-wide validation convention intentionally strips unknown fields instead:

prove Worker service still receives User A, NEVER User B.

Report exact final behavior.

No test may rely only on source inspection.

8. QUERY / PATH IDENTITY

Confirm no route exists such as:

/api/personalization/:userId
/api/personalization?userId=...

If query userId is supplied to the current route:

it must not influence target identity.

Add a focused test if practical.

9. FEATURE FLAG — DISABLED GET

With:

PERSONALIZATION_CLOUD_SYNC_ENABLED=false

authenticated GET must produce exactly the implemented disabled contract.

Expected:

200
{
  "status": "disabled",
  "record": null
}

Worker client must NOT be called.

Prove call count = 0.

10. FEATURE FLAG — DISABLED PUT

Authenticated valid PUT while feature disabled:

Expected:

200

with disabled status.

Worker client call count:

0

This is a server boundary test.

The frontend local pending behavior is already covered elsewhere and does not need redesign here.

11. ENABLED GET — CLOUD RECORD

Mock/inject the backend Worker service.

For authenticated User A:

Worker service returns valid cloud record.

Public GET must return:

status = ok
record = validated record

and expected success HTTP status.

Prove user ID passed to the Worker service is User A.

12. ENABLED GET — EMPTY

Worker service returns no cloud record.

Public API must return:

{
  "status": "empty",
  "record": null
}

with expected success status.

13. ENABLED GET — MALFORMED UPSTREAM RECORD

Worker service returns malformed/invalid cloud record or an equivalent invalid-upstream result.

Public API must map this to the approved controlled error.

Expected architecture:

502

Do not expose raw Worker payload.

14. ENABLED GET — UPSTREAM UNAVAILABLE

Simulate:

timeout
network failure
or equivalent upstream-unavailable service classification.

Public API should map to:

503

with retryable classification according to actual implemented API shape.

15. VALID AUTHENTICATED PUT

Use a valid arbitrary Home subject permutation, not only canonical order.

Example:

SSC
ImD
PHC
CA
RM
NT
ID

Authenticated User A submits valid:

{
  config,
  intentId,
  knownRevision
}

Expected:

route accepts it
Worker service receives User A ID
Worker service receives exact validated config
Worker service receives intentId
knownRevision remains contextual only
public API returns validated success record
16. INVALID CONFIG — ENUM

Test invalid:

themeId
heroStyle
glassStyle
motionStyle
readingSize

At least representative invalid enum cases must return:

400

before Worker service call.

Do not require one test per enum if shared validation already has focused unit coverage.

17. INVALID HOME ORDER

At public route boundary, reject representative:

duplicate subject
missing subject
unknown subject
wrong length

Expected:

400

Worker service:

0 calls
18. INVALID CONFIG VERSION
config.version != 1

must be rejected before forwarding.

Expected:

400

unless exact current API distinguishes unsupported schema differently.

Report exact contract.

19. INVALID intentId

Test:

empty
too long
invalid format if implementation constrains format

Expected:

400

No Worker service call.

20. INVALID knownRevision

Test malformed/too-long revision.

Expected validation behavior.

knownRevision = null remains valid.

Remember:

knownRevision is NOT CAS authority.

21. UNKNOWN BODY FIELDS

Test extra unexpected fields.

At minimum:

{
  "userId": "..."
}

Preferred:

strict rejection.

Report actual implementation.

22. CONFIG PAYLOAD SIZE

Retain semantic config maximum:

16,384 UTF-8 bytes

Automated test:

serialized config beyond this bound
→ reject.

Do not confuse this with the full HTTP request size.

23. TRUE HTTP REQUEST SIZE — CRITICAL

Final approved HTTP personalization PUT maximum:

32,768 bytes

Prompt 21C must determine HOW the current implementation enforces this.

Report exact mechanism.

A check only equivalent to:

Buffer.byteLength(JSON.stringify(req.body))

after the global Express JSON parser has already accepted a much larger body is NOT a complete enforcement of the literal HTTP-body limit.

24. AUDIT GLOBAL BODY PARSER

Report current global body parser:

location
configured limit
whether raw body is retained
whether route-specific parsing currently occurs
whether Content-Length is available/reliable in tests
whether chunked requests are supported by the current test/server path

Do not guess.

25. ENFORCE ACTUAL 32 KiB BOUNDARY

Select the smallest safe implementation consistent with the current Express architecture.

Preferred options, in order:

A. Route-specific JSON parser with:

32 KiB

mounted BEFORE a broader global parser for the personalization route.

or

B. Existing raw-body verification support that can enforce actual received byte length.

or

C. Another repository-native mechanism that measures actual request bytes before/while parsing.

Do NOT perform a broad server parser redesign.

26. CONTENT-LENGTH ALONE

Do not rely solely on trusting a client-supplied Content-Length header if the architecture can enforce bytes directly.

Content-Length may be used as an early rejection optimization, but it should not be the sole security boundary if chunked transfer/body parsing can bypass it.

27. TRUE OVERSIZED REQUEST TEST

Construct a syntactically valid JSON PUT request whose actual HTTP body is:

> 32,768 bytes

Expected:

413

The Worker service must receive:

0 calls

The test must exercise the actual Express HTTP request path.

28. WHITESPACE / WRAPPER SIZE CASE

Where practical, test an HTTP JSON body whose semantically parsed config is small but whose actual request bytes exceed 32 KiB through harmless JSON whitespace or wrapper expansion.

Expected:

413

This proves the HTTP limit is not merely reserialized object size.

If framework/test-client normalization makes this impossible:
state exactly why and prove the selected byte-limiting middleware independently.

Do not overclaim.

29. WORKER 429 MAPPING

Mock Worker service throttling.

Public PUT must map to:

429

and preserve/forward approved retry guidance such as:

Retry-After

where implemented.

Test actual status/header/body classification.

30. WORKER 502 MAPPING

Malformed Worker response / invalid cloud record:

expected:

502

No raw internal response leak.

31. WORKER 503 MAPPING

Worker unavailable/timeout:

expected:

503

with retryable public classification.

32. WORKER INTERNAL 401/403 MUST NOT BECOME USER AUTH IDENTITY

If backend-to-Worker private-secret auth fails:

map it as an upstream/server configuration failure according to current service architecture.

Do not incorrectly tell an authenticated end user:

"your session is invalid"

when the actual problem is backend → Worker credentials.

Test/report exact mapping.

33. PUBLIC USER 401 REMAINS AUTH 401

Separately:

invalid/missing user session
→ 401

Do not conflate with Worker internal auth.

34. MUTATION PROTECTION — COOKIE PATH

Using actual existing test helpers if practical:

authenticated cookie-backed PUT WITHOUT required mutation protection header/origin requirements:

must be rejected according to current server security contract.

Then valid protected cookie PUT:

must reach route.

Do not weaken global security for testing.

35. NATIVE / BEARER PATH

If existing architecture allows bearer-authenticated native mutations without cookie-CSRF semantics:

add/retain a representative test proving the current valid bearer route still works.

Do not create a new auth mode.

36. CORS

Do not add route-specific wildcard CORS.

Source/test verification should prove personalization routes use centralized CORS.

No:

Access-Control-Allow-Origin: *

repair.

37. ROUTE SERVICE INJECTION / TESTABILITY

Prefer dependency injection or existing service mocking pattern.

Tests must NOT call the real Cloudflare Worker.

No live KV.

No external internet dependency.

38. BACKEND API TEST FILE

Create or complete a dedicated test file.

Preferred:

tests/personalization-api.test.ts

or repository-standard equivalent.

Do not hide all API tests inside unrelated security tests.

39. REQUIRED BACKEND API TEST CATEGORIES

The dedicated suite must cover at minimum:

unauth GET
unauth PUT
authenticated disabled GET
authenticated disabled PUT
authenticated GET ok
authenticated GET empty
authenticated PUT ok
userId injection protection
invalid config
invalid Home order
invalid intent/revision metadata
true oversized HTTP request
Worker 429 mapping
Worker 502 mapping
Worker 503 mapping
authenticated identity passed to Worker service
mutation protection behavior

Tests may be combined where appropriate.

40. NO CLOUD ARCHITECTURE REDESIGN

Do not modify:

local envelope V2
KV reconciliation
confirmation timing
foreground discovery
Worker key model
opaque revision model
App Icon scope

unless the API audit exposes a directly related defect.

41. NO LIVE DEPLOYMENT

Do not:

create Cloudflare KV namespace
run production Wrangler deploy
configure Render production variables
enable feature flag
test against production accounts

Prompt 22 remains the deployment/staging phase.

42. PERSONALIZATION MATRIX AFTER ANY SOURCE REPAIR

If Prompt 21C modifies production source:

rerun complete:

npx tsx --test tests/personalization*.test.ts tests/personalization-runtime.test.tsx

Accepted current baseline entering Prompt 21C:

141 passed
0 failed
0 skipped
0 cancelled

Final result must not regress.

If additional API tests match personalization*.test.ts, total should increase.

43. AUTH REGRESSION

Run:

npm run test:auth

Accepted baseline:

4 passed
0 failed
44. SECURITY CHECK

Run:

npm run security:check

Accepted current baseline:

13 passed

No regression.

45. BUILD

Run:

npm run build

Must PASS.

46. ROOT TYPECHECK

Run:

npm run typecheck

Accepted only:

7 known missing .webp errors in:

src/features/modules/moduleVisuals.ts

No new errors.

47. WORKER TYPECHECK

Rerun actual Worker TypeScript command.

Must PASS.

48. TARGETED ESLINT

Run targeted ESLint for:

personalization route/module if extracted
server.ts registration if changed
server/services/personalizationSync.ts
new API test
any size-limit middleware/helper

No new issue.

49. PDF BASELINE IF PRODUCTION SERVER SOURCE CHANGED

If route/server production source changes, rerun:

npx tsx --test tests/pdf-access.test.ts

Accepted only:

2 passed
1 known failure
pdf_download_expiry_invalid

No new failure.

50. GIT DIFF

Run:

git status --short
git diff --stat
git diff --check

Classify Prompt 21C changes separately from existing Prompt 21 changes.

git diff --check must PASS.

51. .agents/* ABSOLUTE PROHIBITION

Run:

git status --short -- .agents
git diff -- .agents

Expected:
no output.

No .agents/* file may be created, modified, or deleted.

52. NO REPORT ARTIFACT

Do not create Markdown closure report in repository.

Return report in final response only.

53. DEFINITION OF DONE

Prompt 21C passes only if:

dedicated automated Express personalization API suite exists
unauthenticated GET/PUT proven
authenticated GET/PUT success proven
req.user.id identity derivation proven
client userId cannot target another account
feature-disabled GET/PUT proven
Worker service not called when disabled
empty GET mapping proven
malformed upstream → 502 proven
unavailable upstream → 503 proven
Worker throttle → 429 proven
Retry-After behavior proven where implemented
invalid config rejected before Worker call
invalid Home order rejected
invalid intent/revision rejected
strict unknown body behavior documented/tested
actual 32,768-byte HTTP request limit is truly enforced
oversized actual HTTP request → 413
config 16,384-byte semantic limit remains
auth 401 and internal Worker auth failures are not conflated
cookie mutation protection remains
bearer/native auth remains compatible
no wildcard CORS
no live Worker/KV dependency in tests
full personalization matrix remains >=141 pass / 0 fail
auth regression passes
security check passes
build passes
no new TypeScript errors
Worker typecheck passes
targeted lint passes
no SQL/D1/R2 personalization
no App Icon changes
no live deployment
no .agents/*
54. MANDATORY FINAL REPORT

Return exactly:

99'S GUIDE PERSONALIZATION — PHASE 5 PROMPT 21C API BOUNDARY CLOSURE REPORT
1. Status

PASS / FAIL

2. Why Prompt 21C Was Required

Explain the missing automated Express-boundary evidence.

3. API Production Files Changed
File	Exact Reason

If no production repair:
state:

NO API PRODUCTION REPAIR REQUIRED.

4. API Test Files

List exact dedicated files.

5. Route Architecture

Explain exact route registration/module.

6. GET Route

Print exact path/middleware.

7. PUT Route

Print exact path/middleware.

8. Authenticated Identity

Explain req.user.id usage.

9. userId Injection Test

Report exact result.

10. Unauthenticated GET

Report.

11. Unauthenticated PUT

Report.

12. Disabled GET

Report HTTP/body + Worker call count.

13. Disabled PUT

Report HTTP/body + Worker call count.

14. Authenticated GET Success

Report.

15. Authenticated GET Empty

Report.

16. Authenticated PUT Success

Report valid arbitrary subject permutation.

17. Invalid Config

Report.

18. Invalid Home Order

Report.

19. Invalid intentId / knownRevision

Report.

20. Unknown Body Fields

Report strict/strip behavior and identity safety.

21. Config Size Limit

Report exact 16,384-byte enforcement.

22. Global Body Parser Audit

Report exact current parser/location/limit.

23. Actual HTTP Request Size Mechanism

Explain exact byte-enforcement implementation.

24. 32 KiB Oversized Request Test

Report actual request size/status/Worker call count.

25. Whitespace / Parsed-Size Bypass Test

Report result or exact framework limitation.

26. Worker 429 Mapping

Report status/header/body.

27. Worker 502 Mapping

Report.

28. Worker 503 Mapping

Report.

29. Internal Worker Auth Failure Mapping

Report.

30. Public Session 401 Separation

Report.

31. Cookie Mutation Protection

Report.

32. Native / Bearer Compatibility

Report.

33. CORS

Confirm centralized behavior/no wildcard.

34. Dedicated API Test Results

Print exact command:

and exact:
passed
failed
skipped
cancelled

35. FULL Personalization Matrix

Report exact total.

Baseline entering Prompt 21C:
141 passed / 0 failed.

36. Auth Regression

Report.

37. Security Check

Report.

38. Build

Report.

39. Root TypeScript

Report accepted seven-webp baseline/no new errors.

40. Worker TypeScript

Report.

41. Targeted ESLint

Report.

42. PDF Baseline

Report if rerun.

43. Database / App Icon Safety

State exactly:

No SQL personalization storage.
No D1 personalization storage.
No R2 personalization storage.
No App Icon implementation.

44. Deployment Status

State exactly:

KV namespace live: YES / NO
Worker deployed: YES / NO
Render production env configured: YES / NO
Cloud sync feature enabled: YES / NO

No inference.

45. Git Review

Report:

git status --short
git diff --stat
git diff --check

46. .agents/* Verification

Report:

git status --short -- .agents
git diff -- .agents

Then state exactly:

No .agents/* file was created or modified.

47. Final Prompt 21 Decision

If all gates pass, state exactly:

ACCOUNT PERSONALIZATION CLOUD SYNC V1: CLOSED AND APPROVED

PUBLIC EXPRESS PERSONALIZATION API BOUNDARY: VERIFIED

AUTHENTICATED USER ISOLATION: VERIFIED

32 KiB HTTP REQUEST LIMIT: VERIFIED

WORKER ERROR MAPPING: VERIFIED

COOKIE / BEARER SECURITY BOUNDARY: VERIFIED

FULL PERSONALIZATION REGRESSION MATRIX: VERIFIED

PRODUCTION CLOUD ROLLOUT: NOT YET PERFORMED

PROMPT 22 STAGING / DEPLOYMENT VERIFICATION: APPROVED TO BEGIN

End EXACTLY:

PERSONALIZATION PHASE 5 PROMPT 21 STATUS: COMPLETE — READY FOR PROMPT 22

Otherwise:

PERSONALIZATION PHASE 5 PROMPT 21 STATUS: BLOCKED — REPAIR REQUIRED