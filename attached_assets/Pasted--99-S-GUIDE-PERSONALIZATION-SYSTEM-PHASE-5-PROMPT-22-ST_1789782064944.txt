# 99'S GUIDE — PERSONALIZATION SYSTEM
# PHASE 5 / PROMPT 22
## STAGING INFRASTRUCTURE DEPLOYMENT & LIVE WORKERS KV VERIFICATION
### CREATE STAGING KV ONLY
### DEPLOY STAGING PERSONALIZATION WORKER ONLY
### VERIFY LIVE WORKER + BACKEND-TO-WORKER PATH
### PRODUCTION RENDER MUST REMAIN UNCHANGED
### PRODUCTION CLOUD SYNC MUST REMAIN DISABLED
### NO PRODUCTION KV NAMESPACE
### NO PRODUCTION WORKER DEPLOYMENT
### NO `.agents/*`

Prompt 21 is CLOSED AND APPROVED.

Verified entering Prompt 22:

- Personalization cloud-sync V1 implemented.
- Unified Local Envelope V2 verified.
- V1 → V2 migration provenance verified.
- Crash-safe local Apply verified.
- Best-effort KV reconciliation verified.
- Worker cacheTtl = 30 seconds.
- Confirmation = randomized 40–50 seconds.
- Clean foreground discovery = 15 minutes.
- Authenticated Express API verified.
- Real 32 KiB HTTP request limit verified.
- Worker secret/browser boundary verified.
- Full personalization matrix = 157 passed / 0 failed.
- Root build PASS.
- Worker TypeScript PASS.
- Cloud rollout has NOT occurred.

Prompt 22 is the FIRST task allowed to create real Cloudflare resources.

It is STAGING ONLY.

---

# 1. PRIMARY GOAL

Create an isolated, non-production Cloudflare staging environment for the Personalization Worker and verify the real deployed Worker against a real Workers KV namespace.

The staging system must prove:

Cloudflare authentication
→ staging KV namespace
→ staging Worker deployment
→ private Worker secret
→ real KV GET/PUT
→ exact record validation
→ backend service → deployed Worker
→ cleanup of smoke-test keys

WITHOUT:

- deploying production Worker
- creating production KV
- changing Render production env
- enabling production sync
- writing personalization for real application users
- changing SQL/D1/R2
- implementing App Icons

---

# 2. ABSOLUTE PRODUCTION SAFETY RULE

Every Cloudflare mutating command must explicitly target STAGING.

NEVER run:

```bash
wrangler deploy
wrangler secret put ...
wrangler kv ...

against the default/production environment by accident.

Where applicable require:

--env staging

or another explicitly verified staging target.

If the installed Wrangler version uses different current syntax:
inspect wrangler --help / official current docs and adapt safely.

DO NOT guess.

3. IMPORTANT SECRET-PUT BEHAVIOR

Current Wrangler behavior must be audited before use.

wrangler secret put can create/deploy a Worker version.

Therefore:

NEVER execute:

wrangler secret put PERSONALIZATION_SYNC_SECRET

without explicit staging targeting.

Use the staging environment explicitly.

Report the exact command used, but NEVER report the secret value.

4. CLOUDFLARE AUTH GATE

From:

cloudflare-personalization-api

run the current equivalent of:

npx wrangler whoami

Report:

authenticated: yes/no
account name/id only if normally exposed and safe
Wrangler version

If not authenticated:

STOP.

Do not attempt production credentials.

Do not weaken security.

Return:

PROMPT 22 STATUS: BLOCKED — CLOUDFLARE AUTHENTICATION REQUIRED

Do not continue.

5. INSPECT CURRENT WRANGLER CONFIG

Before modification, inspect:

cloudflare-personalization-api/wrangler.jsonc

Report:

Worker base name
main entrypoint
compatibility date
current PERSONALIZATION_KV placeholder
existing environments
any production resource IDs currently present

Expected entering Prompt 22:

production KV ID is NOT live/configured.

If a production ID unexpectedly exists:
STOP and investigate before mutation.

6. STAGING ENVIRONMENT DESIGN

Use Wrangler environment:

staging

The intended deployed Worker identity should clearly resolve to a staging Worker such as:

99s-personalization-api-staging

Use Wrangler's actual environment naming behavior.

Do not deploy the base Worker as production.

7. ENVIRONMENT BINDINGS

Wrangler environment bindings are environment-specific.

The staging environment must explicitly bind:

PERSONALIZATION_KV

to the STAGING KV namespace.

Do not assume a top-level production binding is inherited.

8. CREATE ONE STAGING KV NAMESPACE

Create exactly ONE non-production namespace for this feature.

Preferred logical purpose/name:

99s personalization staging

or repository/Wrangler-generated equivalent.

Use current Wrangler KV namespace syntax.

Current modern command family is conceptually:

npx wrangler kv namespace create ...

Target staging explicitly where supported.

Capture:

generated namespace name/title
namespace ID

Do not create production namespace.

Do not create D1.

Do not create R2.

9. KV NAMESPACE ID SECURITY

A KV namespace ID is not treated as a secret.

It may be placed in the staging Wrangler binding if required.

However:

Worker secret remains secret.
Auth tokens remain secret.
Cloudflare API tokens remain secret.

Do not print secrets.

10. UPDATE WRANGLER STAGING CONFIG ONLY

Add/configure:

env.staging

with:

PERSONALIZATION_KV

bound to the real staging namespace.

Do NOT replace a production placeholder with the staging ID.

The staging ID must live in staging-specific configuration.

Production environment remains unconfigured/unusable.

11. NO AUTO-PROVISION PRODUCTION RESOURCES

If current Wrangler supports automatic provisioning:

ensure the deployment command cannot silently auto-create a production KV resource.

Use explicit staging binding/resource configuration.

If Wrangler proposes creating extra resources:
STOP and inspect.

Do not accept unrelated automatic resources.

12. STAGING SECRET GENERATION

Generate one cryptographically strong staging-only shared secret.

Requirements:

random
high entropy
staging only
never printed
never committed
never written to frontend
never written to .env.example
never written under .agents/*

Keep it only in:

staging Worker secret
current secure local shell/session for staging verification

If a temporary local secret file is absolutely required:

keep it outside repository or confirmed ignored
restrict access where practical
delete it after verification
prove it is absent from Git

Prefer process environment instead.

13. STAGING WORKER SECRET

Set:

PERSONALIZATION_SYNC_SECRET

for:

--env staging

only.

The HTTP header remains:

X-Personalization-Sync-Secret

Do not configure any production secret.

14. SECRET COMMAND SAFETY

Before executing the actual secret mutation:

verify that the command targets the staging Worker/environment.

The report must state:

Production secret mutation performed: NO

Never include the secret value in command output/report.

15. PRE-DEPLOY BUILD

Before live deployment rerun:

npx tsc --noEmit \
  --target ES2022 \
  --module ESNext \
  --moduleResolution Bundler \
  --skipLibCheck \
  src/index.ts

from Worker directory.

Must PASS.

Also run:

npx tsx --test ../tests/personalization-worker.test.ts

or the correct repository-root equivalent.

Must PASS.

16. OPTIONAL WRANGLER DRY-RUN

If current Wrangler supports a safe dry-run for deployment:

use it first for the staging environment.

Verify:

intended Worker name
intended KV binding
no D1
no R2
no production target

If dry-run is unavailable:
state so and continue only after config inspection.

17. DEPLOY STAGING WORKER ONLY

Deploy:

staging

environment only.

Never invoke an ambiguous production deploy.

Capture:

deployed Worker name
staging URL
deployment/version identifier if shown
deployment timestamp

Do not expose secrets.

18. VERIFY STAGING URL

The URL must correspond to the staging Worker, not the production/base Worker.

Record the exact hostname in the closure report.

Do NOT add this URL to frontend code.

Do NOT add it to a VITE_ environment variable.

19. DIRECT WORKER — NO SECRET

Use a unique synthetic valid staging user ID.

Example conceptually:

usr_personalization-staging-smoke-<random>

Use the ACTUAL validated ID grammar.

Call the deployed staging Worker WITHOUT the secret.

Expected:

401

No record leakage.

No CORS bypass.

20. DIRECT WORKER — WRONG SECRET

Call with a deliberately incorrect secret.

Expected:

401

Never use a real user ID.

21. DIRECT WORKER — NO CORS

Inspect response headers.

Expected:

NO permissive browser CORS contract.

Specifically no:

Access-Control-Allow-Origin: *

Do not add CORS merely for smoke testing.

22. UNIQUE STAGING SMOKE USER

Generate a unique synthetic ID satisfying canonical server rules.

Use only this synthetic identity for live staging tests.

Do NOT use:

Mustafa's real account ID
production user IDs
reviewer real user IDs
emails

Record only the synthetic ID in the report.

23. INITIAL LIVE GET

Authenticated internal Worker GET using correct staging secret.

For a fresh smoke ID:

expected normally:

empty

If an old staging smoke record unexpectedly exists:
use a new unique smoke identity.

Do not delete unknown keys.

24. LIVE PUT CONFIG

Use a valid non-default six-axis config, for example conceptually:

Theme: Ocean
Hero: Aurora
Glass: Clear
Motion: Subtle
Reading: Large
Home:
SSC → ImD → PHC → CA → RM → NT → ID

Generate a valid opaque intent ID using the actual validator contract.

PUT through deployed staging Worker using correct internal secret.

Expected:

200
status = ok

Validate returned:

recordVersion = 1
exact config
opaque revision
matching lastIntentId
valid server-generated updatedAt
25. LIVE KV WRITE PROOF

Do not rely solely on HTTP success.

Use current Wrangler KV key tooling against the STAGING binding/namespace where practical.

Conceptual current syntax:

npx wrangler kv key get "<KEY>" \
  --binding PERSONALIZATION_KV \
  --env staging \
  --remote

Use actual supported syntax from installed Wrangler.

Verify key:

personalization:v1:<encodedSyntheticId>

contains a valid V1 cloud record.

Do not inspect other users/keys.

26. STALE NEGATIVE POSSIBILITY

Because the first staging GET may have cached an empty result:

an immediate GET after PUT is NOT required to show the new value.

If it still returns empty/old:

this is not automatically a failure.

Wait one approved confirmation interval:

40–50 seconds

then retry once.

Report what was actually observed.

Do not overclaim KV consistency.

27. LIVE GET AFTER WRITE

Eventually perform another Worker GET.

If record becomes visible:

validate it.

If still stale after the single controlled confirmation:

preserve PUT success evidence
inspect exact remote KV value through Wrangler
report the weak-consistency observation accurately
do not loop indefinitely
28. LIVE BEST-EFFORT IDEMPOTENCY

After the first PUT is visible to the same staging path, retry the SAME:

intentId
config

Prefer waiting enough to avoid confusing the 1-write/sec platform limit.

Observe returned revision.

Expected ideal path:

same visible lastIntentId
→ same existing record
→ no new write

If a duplicate write occurs because Worker observes stale KV:

do NOT call architecture failed.

Report:

best-effort idempotency not observed on this attempt due to KV visibility

The code/unit test already proves the intended no-write branch.

29. DO NOT INTENTIONALLY STRESS KV

Do NOT spam same-key writes.

Do NOT run load tests.

Do NOT intentionally consume free-plan write quota.

Do NOT repeatedly provoke 429.

429 behavior is already covered by unit/integration tests.

Staging should use only a handful of writes.

30. SERVER-TO-WORKER LIVE VERIFICATION

After direct Worker verification, validate the REAL backend service module against the deployed staging Worker.

Use:

server/services/personalizationSync.ts

with process-local staging env:

PERSONALIZATION_CLOUD_SYNC_ENABLED=true
PERSONALIZATION_WORKER_URL=<STAGING WORKER URL>
PERSONALIZATION_SYNC_SECRET=<STAGING SECRET>

Do not persist these into production configuration.

31. SECRET MUST NOT APPEAR IN COMMAND REPORT

If environment variables are set in the shell:

report only:

PERSONALIZATION_SYNC_SECRET=<redacted>

Never echo actual value.

32. BACKEND SERVICE LIVE GET

Using a SECOND unique synthetic valid user ID:

invoke the actual backend personalization Worker client/service.

The request must travel:

server service
→ deployed staging Worker
→ staging KV

Initial GET should safely classify:

empty

or the actual expected internal shape.

33. BACKEND SERVICE LIVE PUT

Using the real server service:

PUT a valid config with:

synthetic canonical user ID
valid intent
knownRevision = null

Expected:

service receives valid ok
record passes shared validation
no direct frontend call
no auth secret leak

This verifies server-to-Worker networking with real Cloudflare infrastructure.

34. BACKEND SERVICE LIVE GET AFTER PUT

Perform a later GET through the same service.

Respect KV eventual consistency.

If first read is stale:
perform at most the approved controlled confirmation.

Verify returned record when visible.

35. DO NOT MODIFY RENDER PRODUCTION

Absolutely NO:

Render production environment variables
Render production restart
Render production deploy
PERSONALIZATION_CLOUD_SYNC_ENABLED=true in production
production Worker URL on Render
production sync secret on Render

Prompt 22 is NOT a Render production rollout.

36. PUBLIC EXPRESS LIVE-AUTH TEST — OPTIONAL GATE

If a secure existing NON-PRODUCTION authenticated test account/session is already available:

you MAY test:

local Express
→ authenticated /api/personalization
→ staging Worker
→ staging KV

using the existing auth flow.

Do not print credentials.

Do not use a real normal-user production account for the staging test.

Do not create an auth bypass.

If no secure non-production authenticated session is available:

state:

LIVE AUTHENTICATED EXPRESS → STAGING WORKER TEST: NOT RUN

This is NOT a Prompt 22 blocker because the Express auth boundary is already covered by the 16-test Prompt 21C API suite.

The mandatory live staging boundary is:

backend service → deployed Worker → real staging KV.

37. NO DATABASE MUTATION REQUIRED

Staging infrastructure verification must not require:

creating a database user
changing Prisma
changing Supabase rows
creating D1 rows

Use synthetic Worker identities for infrastructure verification.

38. STAGING RECORD CLEANUP

After live tests, delete ONLY the synthetic smoke keys created by Prompt 22.

Use current Wrangler KV key deletion against:

--env staging
--remote

or exact namespace ID.

Conceptual:

npx wrangler kv key delete "<SMOKE_KEY>" \
  --binding PERSONALIZATION_KV \
  --env staging \
  --remote

Adapt to installed Wrangler syntax.

Do not delete namespace.

Do not delete unknown keys.

39. CLEANUP PROPAGATION

KV deletion is also eventually propagated.

An immediate GET may briefly observe old data.

Do not loop indefinitely.

Report:

delete command success
any observed delayed visibility
40. KEEP STAGING INFRASTRUCTURE

After Prompt 22 PASS:

KEEP:

staging KV namespace
staging Worker
staging Worker secret

They are useful for future verification.

Do NOT delete the entire staging Worker/namespace unless deployment failed and cleanup is explicitly necessary.

41. PRODUCTION RESOURCES MUST REMAIN ABSENT

At closure verify:

Production personalization KV created: NO
Production personalization Worker deployed: NO
Production Render sync env configured: NO
Production feature enabled: NO

Do not infer.

Inspect actual Cloudflare/Wrangler state where practical.

42. FEATURE FLAG SAFETY

Repository default must still be:

cloud sync OFF unless explicitly enabled.

Prompt 22 staging changes must not make production frontend/backend automatically enable sync.

No hardcoded:

PERSONALIZATION_CLOUD_SYNC_ENABLED=true

in production source.

43. WRANGLER PRODUCTION BINDING SAFETY

If top-level Wrangler config still contains:

replace-with-personalization-kv-id

or another production placeholder:

leave it clearly non-live.

The real staging namespace ID belongs only to staging config.

Do not accidentally make:

wrangler deploy

a viable production deployment using staging KV.

If needed, make production deployment fail safely until production KV is explicitly configured later.

44. STAGING RESOURCE CONFIG DIFF

Production source modifications during Prompt 22 should be minimal.

Allowed:

cloudflare-personalization-api/wrangler.jsonc

for explicit staging environment binding.

Possibly package scripts if strictly useful for safe staging deployment.

Do not modify personalization behavior unless live staging exposes a real defect.

45. LIVE DEFECT POLICY

If staging reveals a REAL code defect:

STOP deployment progression.

Repair only the defect.

Then rerun:

focused tests
full personalization matrix
Worker TypeScript
build
staging smoke again

Do not hide staging defects.

Do not proceed to production readiness until fixed.

46. NO ARCHITECTURE REDESIGN FOR EXPECTED KV STALENESS

Expected examples:

immediate GET after PUT still empty
deletion not immediately visible

are NOT automatically defects.

Evaluate against the accepted KV model.

47. WRANGLER AUTH / ACCOUNT SAFETY

Report:

Wrangler version
authenticated account
staging resource names

Do NOT report:

API token
secret
session credential
48. CLOUDFLARE PLAN

If Wrangler/dashboard/API makes the account plan clearly available without risky access:

report it.

If not:

state:

Cloudflare plan: NOT VERIFIED BY PROMPT 22

Do not guess.

The mass-rollout capacity gate remains separate.

49. FREE-PLAN WRITE DISCIPLINE

Regardless of plan:

use minimal writes.

Do not generate hundreds/thousands of staging records.

One or two synthetic identities are sufficient.

50. LOCAL SECRET CLEANUP

After staging verification:

remove the secret from temporary local files if any.

Shell/session variable may be unset when no longer needed.

Do NOT delete the deployed staging Worker secret.

Verify no secret file is tracked.

51. SECRET REPOSITORY SEARCH

Search repository after staging setup for the actual staging secret if safely possible without printing it.

At minimum verify:

git diff
git status

contain no secret.

Search expected variable names:

PERSONALIZATION_SYNC_SECRET
X-Personalization-Sync-Secret

Variable names are allowed.

Secret VALUE must not be present.

52. FRONTEND SAFETY

Rerun/search enough to confirm:

no staging Worker URL in frontend bundle
no staging secret
no direct Worker calls
frontend still uses /api/personalization

Do not point PWA directly to staging Worker.

53. DATABASE SAFETY

Confirm:

No SQL personalization storage.
No D1 personalization storage.
No R2 personalization storage.

Prompt 22 must not alter database schemas.

54. APP ICON

No App Icon implementation.

55. .agents/*

ABSOLUTE prohibition.

Run:

git status --short -- .agents
git diff -- .agents

Expected:
no output.

56. POST-STAGING TEST MATRIX

After any repository config change run at minimum:

npx tsx --test tests/personalization*.test.ts tests/personalization-runtime.test.tsx

Entering baseline:

157 passed
0 failed
0 skipped
0 cancelled

Must not regress.

57. API BOUNDARY REGRESSION

Run:

npx tsx --test tests/personalization-api.test.ts

Entering baseline:

16 passed
0 failed
58. WORKER TEST REGRESSION

Run:

npx tsx --test tests/personalization-worker.test.ts

Entering baseline:

3 passed
0 failed
59. CROSS-DEVICE REGRESSION

Run:

npx tsx --test tests/personalization-cross-device.test.ts

Entering baseline:

1 passed
0 failed
60. SECURITY REGRESSION

Run:

npm run security:check

Entering baseline:

13 passed
61. AUTH REGRESSION

Run:

npm run test:auth

Entering baseline:

4 passed
0 failed
62. BUILD

Run:

npm run build

Must PASS.

63. ROOT TYPECHECK

Run:

npm run typecheck

Accepted only:

7 known missing .webp errors
0 new Prompt-22 errors
64. WORKER TYPECHECK

Run the verified Worker TypeScript command again.

Must PASS.

65. TARGETED ESLINT

Run targeted lint on any Prompt 22 repository files changed.

Must PASS.

66. GIT DIFF CHECK

Run:

git status --short
git diff --stat
git diff --check

git diff --check must PASS.

Classify staging config changes separately from prior Prompt 21 code.

67. NO SOURCE REPORT ARTIFACT

Do not create a Markdown report in the repository.

Return report in final response only.

68. DEPLOYMENT PASS CRITERIA

Prompt 22 PASS requires:

Wrangler authenticated
staging KV created
staging KV bound correctly
staging Worker deployed
staging secret configured
production environment untouched
direct Worker unauthenticated rejection verified
direct Worker correct-secret path verified
real staging KV PUT verified
KV record shape verified
backend service → deployed Worker verified
eventual-read behavior observed/handled honestly
smoke keys cleaned
staging resources retained
no secret leak
frontend does not know Worker
production feature remains OFF
complete regression matrix passes
no .agents/*
69. DO NOT CALL THIS PRODUCTION-READY YET

Prompt 22 proves:

STAGING INFRASTRUCTURE WORKS.

It does NOT yet prove:

production KV namespace exists
production Worker is deployed
Render production backend is configured
App Store/PWA users are syncing
production capacity is sufficient
live real-user rollout is safe

Those require a separate production-canary decision.

70. MANDATORY FINAL REPORT

Return exactly:

99'S GUIDE PERSONALIZATION — PHASE 5 PROMPT 22 STAGING DEPLOYMENT REPORT
1. Status

PASS / BLOCKED / FAIL

2. Staging Goal

State what was deployed and what was intentionally not deployed.

3. Wrangler Version

Report exact version.

4. Cloudflare Authentication

Authenticated: YES / NO

Do not report tokens.

5. Cloudflare Account

Report safe account identifier/name if Wrangler exposes it.

6. Existing Worker Config Before Staging

Report base Worker name, placeholder/live bindings, and existing environments.

7. Staging Environment

Report exact Wrangler environment.

8. Staging Worker Name

Report exact deployed name.

9. Staging Worker URL

Report exact staging URL.

10. Staging KV Namespace

Report:

namespace name/title
namespace ID
binding name

Namespace ID is not a secret.

11. Production KV Safety

State exactly:

Production personalization KV created: YES / NO

Expected:
NO

12. Staging Secret

State:
configured / not configured

Do NOT print value.

13. Production Secret Safety

State exactly:

Production personalization Worker secret configured: YES / NO

Expected:
NO

14. Worker Pre-Deploy Verification

Report typecheck/tests.

15. Staging Deployment

Report exact deployment command and result.

Redact no secret because secret must not be in command.

16. No-Secret Worker Test

Report status/result.

17. Wrong-Secret Worker Test

Report status/result.

18. Worker CORS Test

Report headers/result.

19. Synthetic Smoke Identity

Report the synthetic staging ID only.

20. Initial Worker GET

Report exact observed result.

21. Live Worker PUT

Report status.

22. Live Cloud Record

Report:

recordVersion
themeId
heroStyle
glassStyle
motionStyle
readingSize
Home order
revision present
lastIntentId matched
updatedAt valid

Do not print secrets.

23. KV Direct Verification

Report Wrangler KV get command/result classification.

Do not dump unnecessary user data.

24. Immediate Post-PUT GET

Report actual observation:

current record
stale empty
stale previous value
other
25. Confirmation GET

If needed:
report delay and actual observation.

26. Best-Effort Idempotency Live Observation

Report:

observed same-record/no-write behavior
or
duplicate allowed due to KV staleness

Do not overclaim.

27. Same-Key Write Discipline

Report approximate number of staging PUTs.

Confirm no load testing.

28. Backend Service Staging Env

Report names only:

PERSONALIZATION_CLOUD_SYNC_ENABLED
PERSONALIZATION_WORKER_URL
PERSONALIZATION_SYNC_SECRET

Secret value must be redacted.

29. Backend Service → Worker GET

Report.

30. Backend Service → Worker PUT

Report.

31. Backend Service → Worker Post-PUT GET

Report eventual-consistency behavior.

32. Live Authenticated Express Test

State:
RUN / NOT RUN

If run:
report test-account classification and result without credentials.

If not run:
state why.

33. Staging Smoke-Key Cleanup

List only synthetic keys deleted.

Report command success.

34. Cleanup Propagation

Report any expected delayed visibility.

35. Staging Resources Retained

State whether staging Worker/KV remain available.

Expected:
YES

36. Production Render Safety

State exactly:

Render production env modified: YES / NO
Render production restarted: YES / NO
Render production deployed: YES / NO

Expected:
NO / NO / NO

37. Production Feature Flag

State exactly:

Production cloud sync enabled: YES / NO

Expected:
NO

38. Cloudflare Plan

Report verified plan or:

Cloudflare plan: NOT VERIFIED BY PROMPT 22

39. Secret Repository Audit

Confirm:

actual secret absent from Git
no secret file tracked
only environment/header names in source
40. Frontend Boundary Audit

Confirm:

no staging Worker URL in frontend bundle
no Worker secret
no direct Worker fetch
frontend still uses Express /api/personalization
41. Database Safety

State exactly:

No SQL personalization storage.
No D1 personalization storage.
No R2 personalization storage.

42. App Icon

State:

No App Icon implementation.

43. Personalization Matrix

Report exact:
passed
failed
skipped
cancelled

Baseline entering Prompt 22:
157 / 0 / 0 / 0

44. API Regression

Report exact result.

45. Worker Regression

Report exact result.

46. Cross-Device Regression

Report exact result.

47. Security Regression

Report.

48. Auth Regression

Report.

49. Build

Report.

50. Root TypeScript

Report accepted baseline/no new errors.

51. Worker TypeScript

Report.

52. Targeted ESLint

Report.

53. Git Review

Report:

git status --short
git diff --stat
git diff --check

54. .agents/* Verification

Report:

git status --short -- .agents
git diff -- .agents

Then state exactly:

No .agents/* file was created or modified.

55. Staging Decision

If all staging gates pass, state exactly:

STAGING PERSONALIZATION KV: DEPLOYED AND VERIFIED

STAGING PERSONALIZATION WORKER: DEPLOYED AND VERIFIED

PRIVATE WORKER AUTHENTICATION: VERIFIED LIVE

REAL WORKERS KV READ/WRITE PATH: VERIFIED LIVE

BACKEND → STAGING WORKER PATH: VERIFIED LIVE

EVENTUAL-CONSISTENCY BEHAVIOR: ACCEPTED AS DESIGNED

PRODUCTION PERSONALIZATION KV: NOT CREATED

PRODUCTION WORKER: NOT DEPLOYED

PRODUCTION RENDER CONFIG: UNCHANGED

PRODUCTION CLOUD SYNC: DISABLED

STAGING VERIFICATION: CLOSED AND APPROVED

PRODUCTION CANARY ROLLOUT: READY FOR SEPARATE APPROVAL

End EXACTLY:

PERSONALIZATION PHASE 5 PROMPT 22 STATUS: COMPLETE — STAGING VERIFIED, PRODUCTION NOT ENABLED

If Cloudflare authentication/resources prevent completion:

PERSONALIZATION PHASE 5 PROMPT 22 STATUS: BLOCKED — STAGING INFRASTRUCTURE ACTION REQUIRED

If a real defect is found:

PERSONALIZATION PHASE 5 PROMPT 22 STATUS: BLOCKED — REPAIR REQUIRED