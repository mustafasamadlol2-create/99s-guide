# 99'S GUIDE — PERSONALIZATION SYSTEM
# PHASE 5 / PROMPT 22R
## REPLIT-ONLY DEPLOYMENT DEFERRAL & LOCAL-EXPORT READINESS CLOSURE
### DO NOT AUTHENTICATE CLOUDFLARE
### DO NOT DEPLOY ANYTHING
### DO NOT CREATE KV
### DO NOT MODIFY RENDER
### PREPARE THE CODEBASE FOR LATER LOCAL DEPLOYMENT ONLY
### CONTINUE THE DEVELOPMENT WORKFLOW AFTER THIS PROMPT

IMPORTANT ENVIRONMENT CLARIFICATION:

The project is currently being developed inside REPLIT.

The user will NOT perform Cloudflare authentication, Wrangler deployment, Render production configuration, PowerShell, CMD, or local-machine deployment operations at this stage.

After all coding prompts are complete, the user will:

1. Download/export the full project from Replit.
2. Extract it on their Windows machine.
3. Open PowerShell/CMD locally.
4. Authenticate Wrangler locally.
5. Create/deploy staging infrastructure.
6. Configure production infrastructure only after staging passes.

Therefore Prompt 22's live Cloudflare staging deployment is intentionally DEFERRED.

This is NOT a project failure.

This is NOT an implementation blocker.

Do NOT attempt another Wrangler login.

Do NOT ask for Cloudflare credentials.

Do NOT create tokens.

Do NOT deploy anything.

Do NOT modify production infrastructure.

---

# 1. PROMPT 22 STATUS RECLASSIFICATION

The previous Prompt 22 result:

PROMPT 22 STATUS: BLOCKED — CLOUDFLARE AUTHENTICATION REQUIRED

must now be interpreted as:

PROMPT 22 LIVE DEPLOYMENT: DEFERRED UNTIL POST-REPLIT LOCAL EXPORT

The reason is environmental only:

Wrangler authentication requires the user's real local/authenticated deployment environment.

The application implementation itself remains approved from Prompt 21C.

---

# 2. DO NOT REOPEN PROMPT 21

Prompt 21 / 21B / 21C remain CLOSED AND APPROVED.

Accepted implementation baseline includes:

- 157 personalization tests passed
- 0 failed
- Unified Local Envelope V2
- V1 → V2 migration provenance
- crash-safe Apply
- Cloudflare Worker implementation
- Express API boundary
- real 32 KiB HTTP request limit
- KV weak-consistency handling
- randomized 40–50s confirmation
- 15-minute clean foreground discovery
- private Worker secret boundary
- authenticated user isolation
- no SQL/D1/R2 personalization storage
- no App Icon implementation
- no production deployment

Do not reimplement these.

---

# 3. REPLIT-ONLY RULE

From this point until the user explicitly says the project has been downloaded locally:

ALLOWED:

- source-code changes
- TypeScript
- React
- Node/Express
- Worker source
- Wrangler configuration templates
- unit tests
- integration tests
- local/in-memory KV mocks
- build validation
- lint
- static deployment-readiness checks
- configuration validation
- code cleanup
- code modularization
- future feature prompts

NOT ALLOWED:

- wrangler login
- Cloudflare OAuth/device login
- creating real KV namespaces
- deploying real Workers
- setting real Worker secrets
- configuring Render production
- restarting Render
- modifying production Cloudflare resources
- enabling production sync
- using real user accounts for infrastructure tests
- requiring PowerShell/CMD on the user's local machine

---

# 4. STAGING DEPLOYMENT REMAINS A LATER OPERATIONAL STEP

The live staging workflow defined in Prompt 22 is retained conceptually.

It will be executed AFTER project export.

Do not discard its requirements.

Later local deployment must still include:

- `wrangler whoami`
- staging KV creation
- staging-only binding
- staging Worker secret
- staging Worker deployment
- synthetic user smoke tests
- backend → Worker → KV verification
- cleanup of synthetic records
- production untouched until staging passes

But none of those operations occur now.

---

# 5. AUDIT CURRENT DEPLOYMENT CONFIG FOR PORTABILITY

Without authenticating or deploying:

inspect:

```text
cloudflare-personalization-api/
cloudflare-personalization-api/wrangler.jsonc
cloudflare-personalization-api/package.json
.env.example
server/services/personalizationSync.ts
server/routes/personalization.ts

Verify they are portable after project download.

Check that no Replit-specific absolute path, hostname, token, or secret is required.

6. WRANGLER CONFIG MUST REMAIN SAFE

Audit wrangler.jsonc.

Requirements:

no real production namespace ID
no real staging namespace ID required yet
no real secret
no Cloudflare API token
no account credential
PERSONALIZATION_KV binding architecture preserved
deployment cannot accidentally use a Replit-specific resource

If a placeholder exists, keep it clearly non-live.

Do NOT fabricate an ID.

7. DO NOT ADD REAL STAGING ID

No:

kv_namespaces.id = <invented-id>

No fake production-looking namespace identifier.

Configuration should clearly require later local setup.

8. ENVIRONMENT TEMPLATE AUDIT

Verify .env.example contains only variable NAMES/placeholders such as:

PERSONALIZATION_CLOUD_SYNC_ENABLED=
PERSONALIZATION_WORKER_URL=
PERSONALIZATION_SYNC_SECRET=

No real values.

No VITE_ Worker secret.

No Replit secret reference that would prevent local Windows use.

9. FEATURE FLAG SAFE DEFAULT

Verify:

if:

PERSONALIZATION_CLOUD_SYNC_ENABLED

is absent or not exactly enabled,

the application remains local-only.

Application startup must not require:

Worker URL
Worker secret
Cloudflare authentication
KV

This is essential for downloaded-project portability.

10. WINDOWS PORTABILITY AUDIT

Source/config/scripts needed for later deployment must not depend on Unix-only shell behavior unless unavoidable.

Audit package scripts relevant to:

Worker typecheck
Worker deployment
build
tests

If a deployment command is shell-specific, prefer cross-platform npm scripts or document the direct Wrangler command in the final report.

DO NOT create a deployment documentation file.

Final report only.

11. NODE / WRANGLER PORTABILITY

Report the required:

Node version
npm/pnpm requirement if applicable
Wrangler package/version source

Do not globally install anything in the user's future environment.

Prefer:

npx wrangler

or package-local Wrangler.

12. WORKER BUILD MUST BE SELF-CONTAINED

Verify the Worker can typecheck/build from the exported repository without importing unavailable Replit-only modules.

Run the existing Worker typecheck.

Must PASS.

13. ROOT BUILD

Run:

npm run build

Must PASS.

14. PERSONALIZATION MATRIX

Run:

npx tsx --test tests/personalization*.test.ts tests/personalization-runtime.test.tsx

Accepted entering baseline:

157 passed
0 failed
0 skipped
0 cancelled

No regression.

15. API SUITE

Run:

npx tsx --test tests/personalization-api.test.ts

Accepted baseline:

16 passed
0 failed

16. WORKER SUITE

Run:

npx tsx --test tests/personalization-worker.test.ts

Accepted baseline:

3 passed
0 failed

17. CROSS-DEVICE SUITE

Run:

npx tsx --test tests/personalization-cross-device.test.ts

Accepted baseline:

1 passed
0 failed

18. SECURITY

Run:

npm run security:check

Accepted baseline:

13 passed

19. AUTH

Run:

npm run test:auth

Accepted baseline:

4 passed
0 failed

20. TYPESCRIPT

Run root typecheck.

Only accepted unrelated baseline:

7 missing .webp declarations in:

src/features/modules/moduleVisuals.ts

No new error.

Run Worker TypeScript separately.

Worker must PASS.

21. TARGETED ESLINT

Run targeted lint on Phase-5 personalization implementation.

No new issue.

22. SECRET AUDIT

Search repository.

Confirm no actual:

Cloudflare API token
Worker secret
Render secret
staging secret
production secret

is committed.

Environment/header NAMES are allowed.

Do not output any secret if one is found.

If a real secret is found:

report BLOCKED and identify only the file/location, not the value.

23. REPLIT-SPECIFIC SECRET DEPENDENCY

Audit whether source directly calls:

process.env.REPLIT_...

or otherwise requires Replit secrets specifically for personalization deployment.

Expected:

NO.

Normal process environment variables are portable.

24. FRONTEND BOUNDARY

Verify again:

frontend knows only:

/api/personalization

No:

Worker URL
Worker secret
KV namespace ID
Cloudflare account ID

in frontend runtime.

25. DATABASE SAFETY

Confirm exactly:

No SQL personalization storage.
No D1 personalization storage.
No R2 personalization storage.

No DB deployment step is needed for this sync feature.

26. APP ICON SAFETY

No App Icon implementation.

Do not start App Icon work in this prompt.

27. EXPORT SAFETY

Audit whether all Prompt-21 source files are ordinary repository files that will be included when the user downloads/exports the project from Replit.

No implementation may exist only in:

temporary filesystem
ignored scratch directory
Replit runtime-only storage
untracked generated runtime state

Untracked SOURCE files are acceptable only if they are visibly present in repository working tree and will be included in project export.

Report them explicitly.

28. GIT STATUS

Run:

git status --short
git diff --stat
git diff --check

Do not commit unless user explicitly requests.

Report all untracked source files that must be preserved in the downloaded project.

29. .agents/*

Run:

git status --short -- .agents
git diff -- .agents

No output.

No changes.

30. DO NOT CREATE DEPLOYMENT DOCUMENTATION

Do NOT create:

DEPLOY.md
CLOUDFLARE_SETUP.md
README deployment section
staging report file

The future local commands will be provided later in chat.

31. LOCAL-EXPORT DEPLOYMENT CHECKPOINT

At the end of this prompt, classify the feature as:

CODE COMPLETE
STAGING DEPLOYMENT DEFERRED
PRODUCTION DEPLOYMENT DEFERRED

Do NOT call staging verified.

Do NOT call production ready-live.

32. FUTURE LOCAL WORKFLOW

The future local-machine sequence will be:

Download/export project from Replit.
Extract on Windows.
Open project in PowerShell/CMD.
Install dependencies.
Run tests/build.
npx wrangler login --device or current supported auth method.
npx wrangler whoami.
Resume Prompt 22 live staging operations.
Create staging KV.
Set staging Worker secret.
Deploy staging Worker.
Run staging smoke verification.
Only after staging PASS consider production canary.

Do NOT execute these now.

33. CONTINUATION RULE

This deferred deployment MUST NOT block subsequent CODE-ONLY prompts.

After Prompt 22R passes:

the workflow may continue to later development prompts inside Replit.

Any later prompt requiring real external deployment must also be deferred until local export unless the user explicitly changes this rule.

34. MANDATORY FINAL REPORT

Return exactly:

99'S GUIDE PERSONALIZATION — PHASE 5 PROMPT 22R REPLIT DEFERRAL CLOSURE REPORT
1. Status

PASS / BLOCKED

2. Environment Classification

State:

Current development environment: REPLIT

3. Why Live Prompt 22 Was Deferred

Explain Wrangler authentication/local deployment requirement.

4. Prompt 21 Status

Confirm Prompt 21 remains closed and approved.

5. Live Cloudflare Actions Performed

State exactly:

Wrangler login performed: NO
KV namespace created: NO
Worker deployed: NO
Worker secret configured: NO

6. Production Actions Performed

State exactly:

Production KV created: NO
Production Worker deployed: NO
Render production env modified: NO
Production cloud sync enabled: NO

7. Worker Config Portability

Report.

8. Environment Variable Portability

Report.

9. Windows / Local CLI Portability

Report Node/package/Wrangler expectations.

10. Worker TypeScript

Report.

11. Root Build

Report.

12. Personalization Matrix

Report exact counts.

13. API Regression

Report.

14. Worker Regression

Report.

15. Cross-Device Regression

Report.

16. Security Regression

Report.

17. Auth Regression

Report.

18. Root TypeScript

Report accepted baseline/no new errors.

19. Targeted ESLint

Report.

20. Secret Audit

Confirm no live credentials committed.

21. Frontend Boundary

Confirm frontend uses Express only.

22. Database Safety

State exactly:

No SQL personalization storage.
No D1 personalization storage.
No R2 personalization storage.

23. App Icon

State:

No App Icon implementation.

24. Export Safety

List any untracked/new source files that MUST be present in the downloaded project.

25. Git Review

Report:
git status --short
git diff --stat
git diff --check

26. .agents/* Verification

Report:
git status --short -- .agents
git diff -- .agents

Then state exactly:

No .agents/* file was created or modified.

27. Deferred Local Deployment Checkpoint

State exactly:

PERSONALIZATION CLOUD SYNC CODE: COMPLETE

LIVE STAGING DEPLOYMENT: DEFERRED UNTIL LOCAL PROJECT EXPORT

PRODUCTION DEPLOYMENT: NOT STARTED

REPLIT CODE-ONLY WORKFLOW: MAY CONTINUE

28. Final Decision

If all code-readiness gates pass, state exactly:

PROMPT 22 LIVE INFRASTRUCTURE DEPLOYMENT: SAFELY DEFERRED

PROMPT 21 IMPLEMENTATION: REMAINS CLOSED AND APPROVED

PROJECT EXPORT PORTABILITY: VERIFIED

CLOUDFLARE AUTHENTICATION: NOT REQUIRED INSIDE REPLIT

SUBSEQUENT CODE-ONLY PROMPTS: APPROVED TO CONTINUE

End EXACTLY:

PERSONALIZATION PHASE 5 PROMPT 22R STATUS: COMPLETE — LIVE DEPLOYMENT DEFERRED, CODE WORKFLOW MAY CONTINUE

Otherwise:

PERSONALIZATION PHASE 5 PROMPT 22R STATUS: BLOCKED — CODE READINESS REPAIR REQUIRED