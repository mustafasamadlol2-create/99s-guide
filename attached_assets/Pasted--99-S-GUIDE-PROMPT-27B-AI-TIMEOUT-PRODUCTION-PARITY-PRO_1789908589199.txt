# 99'S GUIDE — PROMPT 27B
## AI TIMEOUT PRODUCTION PARITY + PROMPT 27 CLOSURE
### RESTORE THE PREVIOUSLY ACCEPTED LONG-RUNNING AI TIMEOUT CONTRACT
### VERIFY SCANNED PDF / GEMINI FILES API DOES NOT FAIL PREMATURELY
### CORRECT RESOURCE-vs-AI IMAGE CAPABILITY REPORTING
### NO CALENDAR IMPLEMENTATION YET
### NO NEW FEATURE
### DO NOT MODIFY `.agents/*`

Prompt 27 implementation is provisionally accepted.

The following Prompt 27 repairs are already accepted and MUST be preserved:

- backend-routed Resource PDF upload
- PDF binary signature validation
- AI PDF/image staging
- scanned/image-only PDF support
- Gemini Files API PDF transport
- `markdown_conversion` provider metadata contract
- nested API error-message normalization
- review-before-persistence
- resource/AI authorization
- all Prompt 27 tests

However Prompt 27 CANNOT close yet because its final report exposed one release-critical timeout mismatch and one capability-reporting contradiction.

Do not redesign the upload architecture.

Do not begin Prompt 28.

---

# 1. RELEASE-CRITICAL TIMEOUT MISMATCH

Prompt 27 reported:

```text
Frontend AI preview timeout: 180 seconds
Frontend resource upload timeout: 180 seconds
Server/provider default AI timeout: 30 seconds
Cloudflare provider timeout: 30 seconds
Gemini provider timeout: 30 seconds
Gemini Files API processing timeout: 30 seconds
Gemini cleanup timeout: 2 seconds

This does NOT match the previously accepted long-running AI production behavior.

The previously accepted target architecture was approximately:

Frontend AI operation timeout:
600 seconds

Server AI default timeout:
240 seconds
with environment override support

Gemini Files/media processing timeout:
180 seconds
with environment override support

The exact final implementation must be derived from the ACTUAL current timeout architecture, but a scanned PDF / Files API request must not be killed by a generic 30-second default.

2. AUDIT EVERY TIMEOUT SOURCE

Search actual source for all relevant timeout definitions.

At minimum audit:

frontend AI request timeout

frontend resource upload timeout

server global/request timeout affecting AI routes

AI service timeout

Gemini generation timeout

Gemini Files API upload/ACTIVE polling timeout

Cloudflare document conversion timeout

provider cleanup timeout

AbortController timers

environment-variable overrides

Print the exact files/constants/env names.

Do not rely on the Prompt 27 report.

3. TIMEOUT OWNERSHIP

There must be a clear hierarchy.

The outer client timeout MUST be longer than the maximum legitimate server-side AI operation.

A request must not have:

server permitted to run for 240s
but frontend aborts at 180s

Likewise a Files API PDF must not have a 30-second polling timeout if normal provider processing can legitimately take longer.

4. REQUIRED TARGET CONTRACT

Unless actual provider constraints require a slightly different value, implement/restore this production contract:

Frontend AI request timeout:
600 seconds

Server AI operation default:
240 seconds

Gemini Files API processing timeout:
180 seconds

All should preserve environment override support where the architecture already supports it.

Do not hardcode duplicate constants across many files.

Centralize/reuse existing configuration where practical.

5. RESOURCE UPLOAD TIMEOUT

Resource upload is not the same operation as AI generation.

Audit the existing resource upload timeout independently.

If the currently accepted resource-upload timeout is:

180 seconds

and it safely supports the 50 MiB resource limit, it may remain.

Do NOT automatically increase every upload to 600 seconds.

Report the decision separately.

6. GEMINI GENERATION TIMEOUT

Do not confuse:

Gemini Files API processing timeout

with:

Gemini model generation timeout

Audit both.

The model-generation request must have enough budget to operate inside the server's overall AI operation budget.

Avoid an internal provider timer that expires substantially before the server-level budget for normal large-PDF generation.

7. CLOUDFLARE PROVIDER TIMEOUT

Cloudflare Markdown Conversion may have its own timeout.

Audit it separately.

Do not blindly force it to equal Gemini Files API.

Choose a value appropriate to the current document-conversion path while remaining within the server operation budget.

Report exact final value.

8. CLEANUP TIMEOUT

A short best-effort cleanup timeout such as:

2 seconds

may remain if that is intentional.

Cleanup failure must not incorrectly convert a successful generation into failure unless existing architecture explicitly requires it.

9. ABORT PROPAGATION

Verify timeout/cancellation remains:

frontend abort
→ server request abort
→ AI operation abort
→ provider abort
→ temporary cleanup

No orphaned model call.

No orphaned Gemini File where cleanup is possible.

10. SCANNED PDF LONG-RUNNING TEST

Add a deterministic mocked test proving a scanned PDF operation can remain pending beyond the old 30-second threshold without being rejected by the configured production timeout contract.

Do NOT actually sleep for minutes.

Use:

fake timers
injected timeout configuration
mocked polling clock

or the repository's existing deterministic timeout-test approach.

Required assertion:

the configured Gemini Files processing budget is not 30 seconds
and supports the accepted long-running PDF path
11. ENV OVERRIDE TEST

If timeout environment variables exist, test:

default value
valid override
invalid override fallback

Do not allow:

0
negative
NaN
unbounded absurd value

unless the existing config system already safely handles these.

Use reasonable clamping if the architecture already uses it.

Do not invent a complex configuration framework.

12. FRONTEND / SERVER BUDGET TEST

Add a contract test verifying:

frontend AI timeout
>
server default AI operation timeout

and that the Files API budget is compatible with the server operation budget.

This prevents the same regression later.

13. PRESERVE PROMPT 27 TRANSPORT REPAIR

Do not regress the accepted transport enum:

inline
files_api
markdown_conversion

The shared response schema must continue to accept the provider's actual metadata.

14. PRESERVE NESTED ERROR REPAIR

Keep:

error.message
legacy string error
top-level message

normalization.

No [object Object].

15. RESOURCE IMAGE REPORTING CONTRADICTION

Prompt 27 Section 9 stated:

Resource uploads are PDF-only.
Resource types remain PDF and NOTE.
Resource image files are not newly accepted.

This is acceptable if it reflects the actual existing product contract.

But Section 53 incorrectly stated:

RESOURCE IMAGE UPLOAD: VERIFIED

Do NOT implement Resource-image support merely to satisfy that line.

Instead verify and report the capability accurately.

The final distinction must be:

RESOURCE PDF UPLOAD: VERIFIED

RESOURCE IMAGE UPLOAD:
NOT A CURRENT PRODUCT CAPABILITY / OUT OF SCOPE

if the production Resource system is genuinely PDF-only.

Separately:

AI IMAGE INPUT: VERIFIED

must remain true.

Do not conflate Resource storage with temporary AI image input.

16. RESOURCE PRODUCT CONTRACT

Audit actual Resource UI/API types one last time.

If image Resources were historically supported and Prompt 27 accidentally removed them, that is a defect and must be repaired.

If Resources have always been PDF/NOTE only, preserve that behavior and correct only the report/readiness language.

Do not guess.

17. RELEVANT AI TESTS

Rerun all Prompt 27 AI/upload tests, including:

tests/ai-input.test.ts
tests/ai-gemini-media.test.ts
tests/ai-admin-http.test.ts
tests/cloudflare-ai-provider.test.ts
tests/material-upload-validation.test.ts
tests/api-client-error.test.ts

and existing:

npm run test:ai
npm run test:ai-mcq
npm run test:ai-flashcard

No failure accepted.

18. SCANNED PDF REGRESSION

Reconfirm:

valid image-only PDF
→ accepted
→ original binary preserved
→ Gemini Files API
→ structured AI result

No OCR requirement.

No empty-text rejection.

19. TYPECHECK

Run:

npm run typecheck

Required:

PASS
0 errors
20. BUILD

Run:

npm run build

Required:

PASS
21. PERSONALIZATION

Run:

npx tsx --test tests/personalization*.test.ts tests/personalization-runtime.test.tsx

Expected entering baseline:

183 passed
0 failed
22. APP ICON / PWA

Run:

npx tsx --test tests/app-icon.test.ts tests/app-icon-pwa-assets.test.ts

Expected:

15 passed
0 failed
23. AUTH / SECURITY

Run:

npm run test:auth
npm run security:check

Expected:

Auth:
4 passed
0 failed

Security:
13 passed
24. TARGETED ESLINT

Run ESLint on every Prompt 27B modified TS/TSX/test file.

Required:

0 errors
25. DO NOT BEGIN CALENDAR AI

Do not implement:

Calendar AI route
schedule PDF import UI
event extraction
Prisma Calendar changes
allDay field
source page persistence
event bulk import

Prompt 28 comes only after Prompt 27B closes.

26. .agents/*

Run:

git status --short -- .agents
git diff -- .agents
git diff --cached -- .agents

Required:

NO OUTPUT

Do not create memory files.

27. GIT REVIEW

Run:

git status --short --untracked-files=all
git diff --stat
git diff --check

No Git add/commit/push.

28. MANDATORY FINAL REPORT

Return exactly:

99'S GUIDE — PROMPT 27B REPORT
AI TIMEOUT PRODUCTION PARITY + PROMPT 27 CLOSURE
1. Status

PASS / BLOCKED

2. Timeout Regression Found

Explain why the 30-second budgets were unsafe or whether the previous report was inaccurate.

3. Timeout Source Inventory

Use:

| Layer | File / Config | Previous Value | Final Value | Override |

4. Frontend AI Timeout

Report exact final value.

5. Resource Upload Timeout

Report exact final value and why.

6. Server AI Timeout

Report exact final default and environment override.

7. Gemini Generation Timeout

Report exact final value.

8. Gemini Files API Processing Timeout

Report exact final value.

9. Cloudflare Conversion Timeout

Report exact final value.

10. Cleanup Timeout

Report.

11. Timeout Hierarchy

Print the final relationship.

12. Abort Propagation

Report.

13. Long-Running Scanned PDF Test

Report exact test and assertion.

14. Timeout Config Tests

Report.

15. Frontend / Server Budget Contract Test

Report.

16. Scanned PDF Regression

Report.

17. Transport Metadata Regression

Confirm:

inline
files_api
markdown_conversion

18. Nested Error Regression

Report.

19. Resource Upload Capability

State exactly whether production Resources support:

PDF
images

Do not conflate AI input.

20. Resource PDF Upload

Report.

21. AI Image Input

Report separately.

22. Prompt 27 AI / Upload Test Matrix

Report exact passed/failed.

23. MCQ Regression

Report.

24. Flashcard Regression

Report.

25. Personalization Regression

Report.

26. App Icon / PWA Regression

Report.

27. TypeScript

Report.

28. Build

Report.

29. Auth

Report.

30. Security

Report.

31. ESLint

Report.

32. Calendar Architecture

State:

UNCHANGED FROM PROMPT 27 READ-ONLY AUDIT.

33. Database / Cloud

State exactly:

NO DATABASE MIGRATION PERFORMED.
NO CLOUD DEPLOYMENT PERFORMED.

34. Git Review

Report:
git status --short --untracked-files=all
git diff --stat
git diff --check

35. .agents/*

Report exact three commands/results.

Then:

No .agents/* file was created or modified.

36. Prompt 28 Readiness

If all gates pass, state exactly:

RESOURCE PDF UPLOAD: VERIFIED

RESOURCE IMAGE UPLOAD: NOT A CURRENT PRODUCT CAPABILITY
(or the exact verified existing capability if production source proves otherwise)

AI PDF INPUT: VERIFIED

AI IMAGE INPUT: VERIFIED

SCANNED / IMAGE-ONLY PDF INPUT: VERIFIED

LONG-RUNNING AI TIMEOUT CONTRACT: VERIFIED

FRONTEND / SERVER AI TIMEOUT HIERARCHY: VERIFIED

GEMINI FILES PROCESSING BUDGET: VERIFIED

TRANSPORT METADATA CONTRACT: VERIFIED

NESTED ERROR CONTRACT: VERIFIED

AI REVIEW-BEFORE-PERSISTENCE: PRESERVED

PERSONALIZATION REGRESSION: PASS

APP ICON / PWA REGRESSION: PASS

TYPESCRIPT: PASS

PRODUCTION BUILD: PASS

.agents/*: CLEAN

PROMPT 27: CLOSED

PROMPT 28 AI SCHEDULE / CALENDAR EVENT IMPORT: APPROVED TO BEGIN

End EXACTLY:

99'S GUIDE PROMPT 27B STATUS: COMPLETE — READY FOR PROMPT 28

Otherwise:

99'S GUIDE PROMPT 27B STATUS: BLOCKED — REPAIR REQUIRED