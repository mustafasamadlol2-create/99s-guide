# 99'S GUIDE — PROMPT 29
## FINAL REGRESSION + EXPORT RE-CLOSURE
### FINAL AND LAST SOURCE GATE BEFORE DOWNLOADING THE APPLICATION
### REVALIDATE THE COMPLETE RELEASE CANDIDATE AFTER PROMPTS 27 / 27B / 28 / 28B
### CLEAN INSTALL
### FULL TEST SUITE
### PRODUCTION BUILD
### BACKEND STARTUP
### PWA / SERVICE WORKER
### DATABASE MIGRATION EXPORT SAFETY
### SECURITY / SECRET / LINT / GIT INVENTORY
### NO NEW FEATURES
### NO REDESIGN
### NO CLOUD DEPLOYMENT
### NO PRODUCTION DATABASE MIGRATION
### NO GIT COMMIT / PUSH
### DO NOT MODIFY `.agents/*`
### DO NOT STOP FOR INTERMEDIATE PROGRESS UPDATES
### RETURN ONE COMPLETE FINAL REPORT

This is the LAST planned implementation/audit prompt before the user downloads the final source tree.

Prompt 28 / 28B is CLOSED AND APPROVED.

Do NOT reopen its architecture unless an actual regression or release blocker is found.

The currently approved feature set includes:

- existing 99's Guide application
- My99 Personalization ConfigV2
- Home subject order/visibility
- native iOS alternate App Icons
- repaired PWA install icons
- repaired Resource PDF upload
- repaired AI PDF/image ingestion
- scanned/image-only PDF AI support
- long-running AI timeout contract
- MCQ/Flashcard AI workflows
- AI Schedule / Calendar Import
- human review-before-persistence
- all-day Calendar events
- source grounding/provenance
- duplicate/conflict validation
- transactional/idempotent Calendar import

Prompt 29 must verify that the COMPLETE CURRENT REPOSITORY is safe to export.

---

# 1. FINAL RELEASE DECISION

At the end there must be exactly one source-release decision:

```text
EXPORT READINESS: APPROVED

or:

EXPORT READINESS: BLOCKED

If APPROVED, the user will download this repository and move to:

local machine
→ GitHub
→ real PWA deployment/testing
→ production migration/backend deployment when appropriate
→ later Mac/Xcode native validation

Do not create another implementation phase.

2. NO NEW PRODUCT WORK

Do NOT add:

more Calendar features
recurrence
more App Icons
Android alternate icons
personalization features
new AI modes
new storage providers
new deployment platforms
analytics
notification redesign
new database fields unless an actual release-breaking schema defect is proven

Prompt 29 is closure only.

3. FIRST — COMPLETE WORKING-TREE INVENTORY

Before edits, run:

git status --short --untracked-files=all
git diff --stat
git diff --check

Do not rely on previous reports.

Inventory the ACTUAL CURRENT repository.

Classify every modified/untracked file as:

production source
database migration/schema
test
required asset
configuration/lockfile
user/reference attachment
generated output
temporary artifact
unrelated/pre-existing
4. VERIFY PROMPT 27 / 28 FILES ACTUALLY EXIST

Prompt 29 must independently verify the final Schedule Import and upload implementation files exist.

At minimum locate the actual equivalents of:

server/services/materialUploadValidation.ts

server/services/ai/*
server/services/calendarImport/*

src/features/lectures/ai/*
src/features/calendar/import/*

shared/calendarContracts.ts

prisma/schema.prisma
prisma/migrations/20260920000000_add_calendar_schedule_import/

tests/calendar-import.test.ts
tests/calendar-import-http.test.ts
tests/ai-timeouts.test.ts
tests/material-upload-validation.test.ts
tests/api-client-error.test.ts

Do not infer existence from prior reports.

5. REQUIRED FILE GIT / EXPORT SAFETY

For every newly required production/test/migration file determine:

exists?
tracked?
modified?
untracked?
ignored?

No required file may be accidentally ignored.

Use:

git status --short --untracked-files=all
git ls-files
git check-ignore -v <relevant-path>

as appropriate.

Do NOT run git add.

6. HARD GATE — DATABASE MIGRATION MUST BE PRESENT

Verify this migration is physically present and exportable:

prisma/migrations/20260920000000_add_calendar_schedule_import/migration.sql

Verify it contains the final approved schema changes.

A migration existing only in a prior checkpoint/report but missing from the downloadable source is a release blocker.

7. FINAL PRISMA SCHEMA

Verify final CalendarEvent contains the approved additions:

allDay
importSource
sourceDocumentName
sourceDocumentSha256
sourcePage
importFingerprint

and the approved CalendarImportJob model exists.

Use actual final names as authority.

No duplicate/obsolete alternate model should remain.

8. SUBJECT CATALOG FINAL CONTRACT

Verify Schedule Import uses exactly:

ID
NT
RM
CA
PHC
ImD
SSC

from the canonical production source.

No second six-subject list.

No omission of:

ID = Infectious Diseases
9. TARGET GROUP FINAL CONTRACT

Verify manual Calendar and AI import share the approved target-group contract.

Expected from Prompt 28B:

A
B
C
D
E
ALL

Use actual current production source as authority.

Verify:

ALL overlaps A/B/C/D/E

No importer-only duplicate contract.

10. EVENT TYPE SAFETY

Verify generic:

TEST
ASSESSMENT
EVALUATION

does NOT silently become EXAM.

Required ambiguity behavior remains human review.

No regression.

11. PAGE COUNT FINAL PORTABILITY

Verify production code uses a portable Node PDF parser:

pdf-lib / PDFDocument.getPageCount()

or actual approved equivalent.

Required:

pdfinfo NOT REQUIRED
naive /Type /Page counting NOT authoritative

No production OS dependency on Poppler.

12. TIMEOUT FINAL CONTRACT

Verify exact approved defaults:

Frontend AI:          600s
Server/provider AI:   240s
Gemini Files:         180s
Cloudflare conversion:180s
Cleanup:                2s

Resource upload remains independently:

180s

unless source proves a safe intentional change.

13. NO AI DIRECT CALENDAR WRITES

Reconfirm architectural invariant:

upload/extract/verify/review
→ ZERO CalendarEvent writes

Only explicit commit may persist Calendar events.

This is a hard release gate.

14. PRODUCTION MIGRATION STATE

The Prompt 28 migration has NOT been applied to production.

DO NOT apply it in Prompt 29.

State clearly:

SOURCE MIGRATION: PRESENT
DISPOSABLE VALIDATION: COMPLETED
PRODUCTION MIGRATION: NOT APPLIED
15. DEPLOYMENT ORDER — CRITICAL

Determine whether the NEW backend code requires the Prompt 28 schema immediately at runtime.

Assume it does unless inspection proves otherwise.

The final report must establish the safe production deployment order.

Strong expected order:

1. Backup/check production database.
2. Apply Prompt 28 Prisma migration to production.
3. Verify schema/migration status.
4. Deploy new backend.
5. Verify backend health.
6. Deploy/test new PWA frontend.

Do NOT deploy now.

Do NOT apply migration now.

This is report-only.

16. DO NOT DEPLOY FRONTEND FEATURE AGAINST OLD BACKEND AS FULL ACCEPTANCE

A static PWA deployment can be tested for general UI/build behavior.

But the new Calendar AI importer cannot be considered end-to-end production-tested until:

new migration
+
new backend routes
+
new frontend

are deployed compatibly.

Document this clearly.

Do not claim the importer works against the current old production backend if it has not been updated.

17. PACKAGE / LOCKFILE FINAL AUDIT

Prompt 26 previously repaired Replit-internal registry URLs and applied safe dependency security patches.

Audit final:

package.json
package-lock.json
.npmrc

Search for:

replit package firewall
internal registry
localhost registry
temporary private registry

Required:

0 external-install-blocking internal registry URLs
18. DEPENDENCY VERSIONS

Report actual final versions of any packages changed during release work, including if present:

nodemailer
sharp
@capacitor/cli
pdf-lib

Do NOT upgrade anything merely because a newer version exists.

19. CLEAN EXTERNAL INSTALL

Perform a true final clean install using:

npm ci

Prefer a disposable clean copy if necessary.

Required:

PASS

The final lockfile must work outside Replit.

20. LOCKFILE STABILITY

Verify final npm ci does not rewrite:

package-lock.json

Unexpected lockfile mutation is a blocker requiring investigation.

21. PRODUCTION NPM AUDIT

Run:

npm audit --omit=dev

Do not run:

npm audit fix --force

For EVERY remaining high/moderate advisory report:

package
severity
direct/transitive
production reachability
fix available
major/breaking required?
final disposition

A genuinely reachable unresolved high/critical production vulnerability may block export.

Do not block purely on advisory count without reachability analysis.

22. FRESH DIST

Remove/move stale dist safely.

Build from source.

Required:

source
→ fresh dist

No source behavior may depend on pre-existing build output.

23. TYPECHECK

Run:

npm run typecheck

Required:

PASS
0 errors
24. PRODUCTION BUILD

Run the authoritative build command.

Expected:

npm run build

Required:

PASS

Verify actual frontend and backend artifacts.

25. BACKEND ARTIFACT

Verify the current production backend bundle exists.

Historically:

dist/server.cjs

Use actual scripts as authority.

Confirm it can resolve runtime dependencies.

26. PRODUCTION SERVER START SMOKE TEST

Start the built backend using safe local/test configuration.

Use:

PRISMA_DB_PUSH=false

or current safe equivalent if appropriate.

Do not trigger production migration.

Verify:

server starts
does not crash immediately
configuration loads
health check succeeds if available

Then stop it.

27. OLD-SCHEMA STARTUP BEHAVIOR

Because production migration is not applied yet, determine whether simply starting the new backend against an OLD production schema would fail immediately.

Do NOT connect to production.

Use source/schema inspection or disposable test DBs.

Report one:

NEW BACKEND REQUIRES MIGRATION BEFORE STARTUP

or:

NEW BACKEND CAN START BEFORE MIGRATION, BUT IMPORT FEATURE REQUIRES MIGRATION

Do not guess.

This determines deployment order.

28. PRISMA VALIDATION

Run:

npx prisma validate
npx prisma generate

using safe required environment placeholders/config.

Required:

PASS
29. MIGRATION HISTORY VALIDATION

Reconfirm the Prompt 28 migration is structurally valid.

If disposable PostgreSQL remains available, perform a final non-production migration validation.

Do not modify production.

If historical chain needs the known primary-owner fixture:
report that truthfully.

Do not rewrite old migrations.

30. MIGRATION DRIFT / SOURCE CONSISTENCY

Verify:

final prisma/schema.prisma
↔ migration SQL

are consistent for the Prompt 28 additions.

No schema-only field missing from migration.

No migration-only obsolete field.

31. FULL TEST SUITE — HARD GATE

Run the authoritative FULL repository test suite.

Do NOT report only selected suites.

Collect exact:

passed
failed
skipped
cancelled

Required strong target:

failed = 0
32. PDF ACCESS TEST CLOSURE

Prompt 26 historically had:

pdf_download_expiry_invalid

Audit final state.

There must not be a normal unexplained red test in the final suite.

If the previously known fixture defect remains:
repair the fixture, not secure production expiry validation.

Report final focused PDF result.

33. CALENDAR IMPORT SUITE

Run final Calendar import tests.

Required current baseline is at least:

calendar import:
25 passed
0 failed

calendar import HTTP:
4 passed
0 failed

If totals increased due Prompt 28B tests:
report actual counts.

No failure accepted.

34. AI INPUT / TIMEOUT SUITES

Run final Prompt 27/27B relevant tests, including actual equivalents of:

tests/ai-timeouts.test.ts
tests/ai-input.test.ts
tests/ai-gemini-media.test.ts
tests/ai-admin-http.test.ts
tests/cloudflare-ai-provider.test.ts
tests/material-upload-validation.test.ts
tests/api-client-error.test.ts

No failures.

35. MCQ

Run:

npm run test:ai-mcq

Expected entering baseline:

18 passed
0 failed
36. FLASHCARD

Run:

npm run test:ai-flashcard

Expected:

17 passed
0 failed
37. RESOURCE CAPABILITY

Preserve exact distinction:

RESOURCE PDF: SUPPORTED
RESOURCE IMAGE: NOT A CURRENT PRODUCT CAPABILITY
AI PDF: SUPPORTED
AI IMAGE: SUPPORTED
SCANNED PDF: SUPPORTED

Do not accidentally widen the Resource file product contract.

38. PERSONALIZATION HARD GATE

Run EXACT:

npx tsx --test tests/personalization*.test.ts tests/personalization-runtime.test.tsx

Required:

183 passed
0 failed
0 skipped
0 cancelled

No 181-count ambiguity accepted.

39. APP ICON + PWA TESTS

Run:

npx tsx --test tests/app-icon.test.ts tests/app-icon-pwa-assets.test.ts

Expected:

15 passed
0 failed
40. AUTH

Run:

npm run test:auth

Expected:

4 passed
0 failed
41. SECURITY

Run:

npm run security:check

Expected:

13 passed

Also run final Calendar import HTTP authorization tests.

42. NAVIGATION

Run:

npx tsx --test tests/client-navigation.test.ts

Expected:

4 passed
0 failed
43. PRODUCTION LINT

Run the repository lint process.

Classify:

production frontend
production backend
shared
tests
.local/skills

Hard requirement:

production application/server/shared ESLint errors = 0

Do not rewrite .local/skills merely for repository-global cleanliness.

44. PROMPT 29 MODIFIED FILE LINT

Any Prompt-29 repair file must receive targeted ESLint.

Required:

0 errors
45. PWA FINAL SOURCE

Audit final:

manifest
service worker
index.html
install icons
maskable icon
apple-touch icon
favicon
App Icon preview assets

No Prompt 27/28 work may regress Prompt 25.

46. PWA MANIFEST

Verify built manifest remains valid and all icon URLs resolve.

Expected install assets include current equivalents of:

/icon-192.png
/icon-512.png
/icon-512-maskable.png

Declared dimensions must equal actual dimensions.

47. SERVICE WORKER PRECACHE — HARD GATE

Serve fresh dist.

Parse every explicit precache URL.

Request every one.

Report:

requested: N
successful: N
failed: 0

Any missing precache asset is a blocker.

48. INDEX.HTML

Verify:

viewport
manifest
favicon
apple-touch-icon
theme metadata
entry script

No duplicate conflicting metadata.

No old broken icon path.

49. PWA ROUTING

Verify source/client SPA navigation.

Report separately:

CLIENT ROUTING: VERIFIED
HOST SPA FALLBACK: DEPLOYMENT-SPECIFIC

Do not invent Azure/ASP.NET/Cloudflare routing configuration before the real deployment environment is selected/configured.

50. PWA REAL HOST BOUNDARY

Static/source checks cannot prove:

HTTPS
real browser install prompt
real service-worker scope
host cache headers
provider SPA rewrites

State:

SOURCE PWA CONTRACT: VERIFIED
REAL PWA HOST VALIDATION: POST-EXPORT

if all source checks pass.

51. API BASE URL

Audit current:

getApiBaseUrl()
VITE_API_BASE_URL
native API fallback
PWA/web API behavior

No Replit preview URL may become production authority.

No accidental localhost production base.

Do not change intentional production backend URL without evidence.

52. NEW PWA HOST — CORS

Inspect current backend CORS allowlist/configuration.

Determine whether the future PWA hostname must be added.

Do not invent the hostname.

Report:

NEW PWA HOST CORS ACTION: REQUIRED / NOT REQUIRED

and exact configuration/code location.

Do not use wildcard CORS.

53. GOOGLE OAUTH HOST ACTION

Determine what must be updated after the new PWA hostname exists.

Report actual required provider-side items, such as:

authorized JavaScript origin
redirect URI

only if relevant to current Google auth flow.

Do not modify provider console.

54. APPLE AUTH HOST ACTION

Determine what must be updated after the hostname exists.

Potentially:

Service ID domain
return URL

Use actual architecture.

Do not change Apple configuration now.

55. CAPACITOR FINAL CHECK

Verify current:

appId
appName
webDir
native server settings

Expected bundle identifier remains:

com.nine9sguide.app

No temporary QA/replit server embedded.

56. CAPACITOR SYNC FINAL SAFETY

Because Capacitor CLI was patched during release work, run:

npx cap sync ios

if safe in this environment.

Then verify AGAIN:

AppIcon.swift preserved
primary AppIcon preserved
dark primary asset preserved
Midnight preserved
Rose preserved
Monochrome preserved
alternate build settings preserved
AppIcon.swift target membership preserved

Do not claim Xcode compile success.

57. NATIVE APP ICON FINAL CONTRACT

Re-run static App Icon tests after final sync.

No regression.

58. NO XCODE CLAIM

Current environment is Linux unless actually changed.

State:

NATIVE XCODE BUILD: DEFERRED
REAL IPHONE TEST: DEFERRED
REAL IPAD TEST: DEFERRED
ARCHIVE / VALIDATE: DEFERRED

This does not block source export.

59. CALENDAR IMPORT FRONTEND BUILD SAFETY

Inspect fresh production build for Calendar Schedule Import frontend chunks/assets.

No missing module/import.

No dev harness dependency.

No debug provider dependency.

60. FINAL RESPONSIVE SMOKE

If browser tooling is available, perform a fresh production-build visual smoke of:

normal Calendar management
Schedule Import entry UI
web/PWA App Icon unsupported state
Home

At least one narrow mobile and one desktop/tablet viewport.

Do not bypass production auth in committed code.

Temporary QA harness must be removed.

61. SECRET SCAN — TRACKED SOURCE

Scan export-relevant source for:

DB passwords
service-role keys
JWT secrets
session secrets
Gemini API keys
Google client secrets
Apple private keys
Cloudflare tokens
Render tokens
private certificates

Do NOT print values.

Report exactly:

TRACKED PRODUCTION SECRET FOUND: YES / NO
62. ENV FILES

Inspect:

.env
.env.*
.env.example

and Git state.

Real secret env files must NOT be intended for GitHub.

Sanitized example files may remain.

Do not print values.

63. FRONTEND BUNDLE SECRET SCAN

Inspect final frontend JS/text output for server-only secret markers.

Report:

SERVER SECRET IN CLIENT BUNDLE: YES / NO

Do not print a matched secret value.

64. VITE ENV AUDIT

List client-visible:

VITE_*

names and purpose only.

Remember all VITE values are public.

Flag any variable whose purpose should be server-secret.

Do not print values.

65. AI SECRET SAFETY

Verify:

Gemini/provider API keys remain server-side
Calendar import provider secrets remain server-side
no AI credential embedded in frontend

No live Gemini call is required in Prompt 29.

66. TEMP SOURCE PATH SAFETY

Reconfirm CalendarImportJob sourcePath:

not exposed to frontend
not client-settable
cleared when no longer needed
restricted to owned temp root

No arbitrary filesystem deletion route.

67. DEBUG / TEST ROUTE SCAN

Search runtime source for accidental:

debug
test-only route
auth bypass
visual harness
QA route
mock provider route
temporary Calendar importer bypass

Legitimate test files do not count.

No release QA harness may remain.

68. TEMPORARY ARTIFACT SCAN

Search for:

screenshots
contact sheets
scratch PDFs
temporary import JSON
browser profiles
logs
local DB dumps
Prompt reports

Classify rather than blindly delete user-owned content.

Remove Prompt-generated temporary runtime artifacts.

69. CONSOLE DEBUG SCAN

Find accidental production:

console.log
console.debug
temporary tracing

Remove only unintended debugging.

Preserve intentional structured logging.

Never log tokens or source-document private content unnecessarily.

70. TODO / FIXME

Scan production code for:

TODO
FIXME
HACK
TEMP

Do not fix every future note.

Report only release-critical unfinished behavior.

71. LARGE FILE SCAN

Inspect modified/untracked files for accidental large artifacts:

database dumps
videos
scratch PDFs
screenshots
provider responses
raw generated media

Do not classify legitimate App Icon/PWA assets as accidental.

72. IMPORT SOURCE RETENTION

Verify successful/failed/cancelled AI Schedule imports do not leave temporary source PDFs/images indefinitely.

No cleanup regression.

73. CALENDAR JOB STALE CLEANUP

Reconfirm stale PROCESSING jobs have bounded failure behavior and source cleanup.

No permanent processing state.

74. CONTENT SYNC

Verify final imported Calendar event includes approved fields through the current sync architecture.

Do not require external/live sync testing.

Static/tests sufficient here.

75. SOCKET BATCH

Verify one coherent:

batch-upsert

or approved equivalent is used per import commit, not hundreds of refresh emissions.

76. NOTIFICATION STORM

Verify Calendar bulk import does NOT send one notification per imported event.

No regression.

77. IMPORT LIMITS

Report final:

max source files/images
max upload bytes
max candidate count
page chunk size
verification batch size

No unbounded input.

78. IMPORT HTTP SECURITY

Re-run HTTP Calendar importer security tests.

Hard requirements:

anonymous rejected
ordinary user rejected
admin permitted
job ownership enforced
malformed request rejected
sourcePath hidden
candidate injection rejected
79. COMMIT IDEMPOTENCY

Reconfirm:

retry commit
→ zero duplicate Calendar rows

Fingerprint must remain server-generated.

80. DATABASE RUNTIME DEPLOYMENT NOTE

Because the new frontend feature references new backend routes and the backend references new schema:

Prompt 29 report must explicitly distinguish:

SOURCE EXPORT READY

from:

PRODUCTION FEATURE DEPLOYED

Do not conflate them.

81. CURRENT PRODUCTION DATA MUST NOT BE TOUCHED

Do NOT:

prisma migrate deploy against production
prisma db push production
seed production
delete production rows
write test import jobs to production

Prompt 29 is source/export closure only.

82. TEST DB ONLY

Any database integration test must use:

disposable/local/test database

Never production.

83. VERSION — DO NOT BUMP

Report current:

application version
package version
Capacitor version
iOS minimum version
bundle identifier

Do not increment build/version yet.

App Store version/build bump happens after real PWA/native validation.

84. TOOLCHAIN

Report actual:

Node
npm
React
Vite
Capacitor
Prisma

versions.

85. NODE COMPATIBILITY

Inspect:

engines
.nvmrc
deployment config

where present.

State recommended/supported Node version for the downloaded source.

86. CASE SENSITIVITY

Linux is authoritative for filename-case sanity.

Audit relevant recent files/imports:

calendarImport
calendar/import
AppIcon*
app-icons
manifest/icon files
migration directory

No Windows-only case mismatch.

87. GIT DIFF CHECK

Run:

git diff --check

Required:

NO OUTPUT
88. .agents/* ABSOLUTE HARD GATE

Before final report run:

git status --short -- .agents
git diff -- .agents
git diff --cached -- .agents

Required:

NO OUTPUT

Never create Prompt 29 memory files.

89. DO NOT CREATE REPORT ARTIFACTS

Do NOT add:

PROMPT29.md
FINAL_RELEASE_REPORT.md
EXPORT_READINESS.md

The report belongs in chat only.

90. DO NOT CREATE ZIP INSIDE REPOSITORY

Do NOT create a ZIP yet.

The user will download/export after this report is accepted.

91. DO NOT GIT ADD / COMMIT / PUSH

Do not mutate Git staging/history.

No:

git add
git commit
git push
git reset --hard
92. REQUIRED FUTURE-GIT INCLUDE INVENTORY

Final report must explicitly list currently untracked REQUIRED files that the user must include in the future Git commit.

This is critical.

A passing source implementation is useless if required files are omitted from GitHub.

93. REQUIRED FUTURE-GIT EXCLUDE INVENTORY

Explicitly identify categories not to commit, including as applicable:

node_modules/
dist/
real .env files
temporary logs
temporary browser artifacts
Prompt/reference attached text files
local test DB/dumps
scratch PDFs/images

Do NOT exclude:

ios/
tests/
prisma/migrations/
public production assets
94. IOS MUST BE EXPORTED

The final downloaded source MUST retain:

ios/

including App Icon native work.

Even though first validation is PWA-focused.

95. TESTS MUST BE EXPORTED

Do not strip tests.

They belong in the GitHub source repository.

96. MIGRATIONS MUST BE EXPORTED

The full:

prisma/migrations/

history, including Prompt 28 migration, must be included.

97. POST-EXPORT REAL PWA TEST PLAN

Final report should give a concise ordered test plan after the user downloads/uploads:

1. Commit required files to GitHub.
2. Configure PWA environment.
3. Configure the real PWA hostname in backend CORS.
4. Configure Google/Apple web hostname/redirect settings if required.
5. Deploy static/PWA frontend.
6. Smoke-test general app against compatible backend.
7. Before enabling/testing Schedule Import end-to-end, apply Prompt 28 production migration.
8. Deploy compatible new backend.
9. Test Schedule Import with a real timetable PDF but review before commit.
10. Test normal Resource PDF + AI MCQ/Flashcard input again.
11. Test PWA install/service worker/session restoration.
12. After PWA acceptance, move to Mac/Xcode native validation.

Order migration/backend correctly based on actual startup analysis.

98. SCHEDULE IMPORT REAL-PDF TEST EXPECTATION

Do NOT claim AI schedule accuracy based only on mocked provider tests.

After deployment, the user must test with an actual timetable PDF.

Required real workflow:

upload real PDF
→ inspect every extracted candidate
→ compare dates/times/pages
→ correct ambiguities
→ test with a SMALL selected subset first
→ confirm Calendar result
→ only then use large import

This is a post-export validation requirement, not a source blocker.

99. SOURCE RELEASE PASS DEFINITION

Prompt 29 may PASS only if:

clean external npm ci passes
no internal Replit registry blocks install
lockfile stable
TypeScript 0 errors
production build passes
backend artifact valid
backend starts under safe test config
full test suite has 0 unexplained failures
PDF known failure is closed
Calendar import tests green
Calendar HTTP security tests green
AI input/timeouts green
MCQ green
Flashcard green
Resource PDF green
Personalization exactly 183/0/0/0
App Icon/PWA green
Auth green
Security green
Navigation green
production source lint errors = 0
final Prisma schema validates
Prompt 28 migration exists
migration SQL matches schema additions
production migration remains unapplied
deployment ordering documented
PWA manifest valid
every service-worker precache asset resolves
no server secret in frontend bundle
no tracked real production secret
no required source file ignored
no temp/debug harness
App Icon native source survives final Capacitor sync
.agents/* is clean
exact Git include/exclude inventory exists
100. MANDATORY FINAL REPORT

Return exactly:

99'S GUIDE — PROMPT 29 REPORT
FINAL REGRESSION + EXPORT RE-CLOSURE
1. Status

PASS / BLOCKED

2. Final Decision

State exactly:

EXPORT READINESS: APPROVED

or:

EXPORT READINESS: BLOCKED

3. Prompt 29 Repairs

List only actual repairs performed.

If none:

NO PROMPT 29 PRODUCTION REPAIR REQUIRED.

4. Working Tree Classification

Summarize current production/test/migration/config/reference items.

5. Prompt 27 / 28 Required File Audit

Report whether every required implementation file exists.

6. Required File Git Safety

Report tracked/untracked/ignored state.

7. Required Untracked Files to Include in Future Git Commit

List exact paths.

8. Files / Categories Not to Commit

List exact categories/reference files.

9. Prompt 28 Migration Presence

Print exact path.

10. Final CalendarEvent Schema Additions

Report.

11. CalendarImportJob

Report.

12. Subject Contract

Print:

ID
NT
RM
CA
PHC
ImD
SSC

13. Target Group Contract

Report actual final values/source.

14. Event-Type Ambiguity Safety

Confirm generic TEST is not auto-EXAM.

15. PDF Page Count

Report portable implementation.

16. AI Timeout Contract

Print exact:
frontend
server/provider
Gemini Files
Cloudflare
cleanup
resource upload

17. No-Direct-Persistence Invariant

State:

AI SCHEDULE PREVIEW CREATES ZERO CALENDAR EVENTS.

18. Production Migration State

State exactly:

SOURCE MIGRATION: PRESENT
PRODUCTION MIGRATION: NOT APPLIED

19. Safe Production Deployment Order

Print the exact ordered migration/backend/frontend deployment sequence.

20. New Backend vs Old Schema

State one:

NEW BACKEND REQUIRES MIGRATION BEFORE STARTUP

or:

NEW BACKEND CAN START BEFORE MIGRATION, BUT SCHEDULE IMPORT REQUIRES MIGRATION

with evidence.

21. npm Registry Audit

Report Replit/internal registry reference count.

22. Final Relevant Dependency Versions

Report nodemailer/sharp/Capacitor CLI/pdf-lib if present.

23. Clean npm ci

Report.

24. Lockfile Stability

Report.

25. Production Dependency Audit

Report final severity totals and disposition of each remaining high/moderate advisory.

26. TypeScript

Report.

27. Fresh Production Build

Report.

28. Frontend Artifact

Report.

29. Backend Artifact

Report.

30. Production Server Smoke Test

Report.

31. Prisma Validate / Generate

Report.

32. Migration / Schema Consistency

Report.

33. Disposable Migration Verification

Report actual final state.

34. Full Test Suite

Print exact:

passed
failed
skipped
cancelled

35. PDF Test Closure

Report exact focused result and whether previous failure is closed.

36. Calendar Import Tests

Report exact count.

37. Calendar Import HTTP Security Tests

Report exact count.

38. AI Input / Timeout Regression

Report.

39. MCQ

Report.

40. Flashcard

Report.

41. Resource Capabilities

State exactly:

RESOURCE PDF: SUPPORTED
RESOURCE IMAGE: NOT A CURRENT PRODUCT CAPABILITY
AI PDF: SUPPORTED
AI IMAGE: SUPPORTED
SCANNED PDF: SUPPORTED

42. Personalization

Required:

183 passed
0 failed
0 skipped
0 cancelled

43. App Icon / PWA Tests

Report.

44. Auth

Report.

45. Security

Report.

46. Navigation

Report.

47. Production Lint

Report production frontend/backend/shared errors/warnings.

48. .local/skills Lint

Classify separately.

49. PWA Manifest

Report.

50. PWA Install Icons

Report.

51. Service Worker

Report.

52. Precache HTTP Verification

Print:

requested
successful
failed

53. index.html

Report.

54. SPA / Deep-Link

State:

CLIENT ROUTING: VERIFIED

and host fallback state.

55. PWA Source Contract

State:

SOURCE PWA CONTRACT: VERIFIED / NOT VERIFIED

56. Real PWA Host

State:

REAL PWA HOST VALIDATION: DEFERRED TO POST-EXPORT DEPLOYMENT

57. API Base URL

Report.

58. New PWA Host CORS Action

State:

REQUIRED / NOT REQUIRED

and exact config location.

59. Google OAuth Host Actions

Report.

60. Apple OAuth Host Actions

Report.

61. Capacitor Config

Report:
appId
appName
webDir

62. Final Cap Sync

RUN / NOT RUN

Report result.

63. Native App Icon Integrity

Report Original/Midnight/Rose/Monochrome.

64. Native Runtime Validation

State:

NATIVE XCODE BUILD: DEFERRED
REAL IPHONE TEST: DEFERRED
REAL IPAD TEST: DEFERRED
ARCHIVE / VALIDATE: DEFERRED

65. Calendar Import Frontend Build

Report.

66. Responsive Smoke QA

Report actual verification level.

67. Tracked Secret Scan

State exactly:

TRACKED PRODUCTION SECRET FOUND: YES / NO

68. Environment Files

Report status without values.

69. Frontend Bundle Secret Scan

State:

SERVER SECRET IN CLIENT BUNDLE: YES / NO

70. VITE Environment Variables

List names and purposes only.

71. AI Secret Safety

Report.

72. Calendar Import sourcePath Safety

Report.

73. Debug / Test Route Scan

Report.

74. Temporary Artifact Scan

Report.

75. Console Debug Scan

Report.

76. TODO / FIXME Release Blockers

Report.

77. Large / Accidental File Scan

Report.

78. Calendar Temporary Source Cleanup

Report.

79. Stale Import Job Handling

Report.

80. Calendar Content Sync

Report.

81. Calendar Socket Batch

Report.

82. Notification Storm Safety

Report.

83. Import Limits

Print final:
files/images
total bytes
candidate cap
PDF page chunk
verification batch

84. Import HTTP Authorization

Report anonymous/user/admin/ownership/tampering state.

85. Commit Idempotency

Report.

86. Source Export vs Production Deployment

Explain distinction.

87. Version Information

Report:
app/package version
Capacitor
iOS minimum
bundle identifier

Do not bump.

88. Toolchain

Report:
Node
npm
React
Vite
Capacitor
Prisma

89. Node Compatibility

Report.

90. Case Sensitivity

Report.

91. Git Status

Print:

git status --short --untracked-files=all

92. Git Diff Stat

Print:

git diff --stat

93. Git Diff Check

Required:

NO OUTPUT

94. .agents/*

Print exact results:

git status --short -- .agents
git diff -- .agents
git diff --cached -- .agents

Then state exactly:

No .agents/* file was created or modified.

95. Export Include Inventory

List source categories that MUST be downloaded/committed, including:

frontend
backend
shared
package files
public
tests
prisma/schema
prisma/migrations
ios
Capacitor config
configuration templates

96. Export Exclude Inventory

List categories such as:

node_modules
dist
real .env secrets
temporary files
logs
browser profiles
prompt/reference attachments where not project source

97. Tests Must Remain

Confirm:

TESTS MUST BE INCLUDED IN GITHUB SOURCE.

98. iOS Must Remain

Confirm:

IOS PROJECT MUST BE INCLUDED IN THE FINAL SOURCE EXPORT.

99. Migrations Must Remain

Confirm:

PRISMA MIGRATION HISTORY, INCLUDING PROMPT 28 MIGRATION, MUST BE INCLUDED.

100. Post-Export PWA / Backend Test Order

Give exact ordered steps after download.

101. Real Schedule PDF Validation

State that mocked AI tests do not replace real-PDF review.

Give safe first real test:

real schedule PDF
→ review every extracted field
→ import a small subset
→ verify Calendar
→ then test a larger batch
102. Native Validation After PWA Acceptance

List:

Mac
Xcode
npx cap sync ios
native build
iPhone
iPad
Original icon
Midnight
Rose
Monochrome
icon persistence
Archive
Validate
TestFlight
App Store update

103. Final Release Candidate Gate

If every gate passes, state exactly:

CLEAN EXTERNAL NPM INSTALL: VERIFIED

NO REPLIT-INTERNAL REGISTRY BLOCKER: VERIFIED

TYPESCRIPT: PASS

PRODUCTION FRONTEND BUILD: PASS

PRODUCTION BACKEND BUILD: PASS

PRODUCTION SERVER STARTUP: VERIFIED

FULL TEST SUITE: PASS

PDF ACCESS REGRESSION: PASS

CALENDAR AI IMPORT: PASS

CALENDAR AI HTTP SECURITY: PASS

AI INPUT + TIMEOUTS: PASS

MCQ: PASS

FLASHCARD: PASS

RESOURCE PDF UPLOAD: PASS

PERSONALIZATION: 183 / 0 / 0 / 0

APP ICON + PWA: PASS

AUTH: PASS

SECURITY: PASS

NAVIGATION: PASS

PRODUCTION SOURCE LINT: PASS

PRISMA SCHEMA: PASS

PROMPT 28 MIGRATION: PRESENT AND VALIDATED

PRODUCTION DATABASE MIGRATION: NOT YET APPLIED

PWA MANIFEST: PASS

SERVICE WORKER PRECACHE: PASS

NO TRACKED PRODUCTION SECRET: VERIFIED

NO SERVER SECRET IN CLIENT BUNDLE: VERIFIED

NO REQUIRED SOURCE FILE IGNORED: VERIFIED

NATIVE APP ICON SOURCE: EXPORT-SAFE

CALENDAR IMPORT SOURCE: EXPORT-SAFE

PRISMA MIGRATIONS: EXPORT-SAFE

IOS PROJECT: EXPORT-SAFE

PWA SOURCE: EXPORT-SAFE

.agents/*: CLEAN

SOURCE RELEASE CANDIDATE: APPROVED

DOWNLOAD: APPROVED

GITHUB EXPORT: APPROVED

PWA DEPLOYMENT TESTING: APPROVED TO BEGIN

PRODUCTION MIGRATION + NEW BACKEND DEPLOYMENT: REQUIRED BEFORE END-TO-END SCHEDULE IMPORT PRODUCTION TESTING

NATIVE DEVICE VALIDATION: DEFERRED UNTIL AFTER PWA ACCEPTANCE

End EXACTLY:

99'S GUIDE PROMPT 29 STATUS: COMPLETE — DOWNLOAD / GITHUB EXPORT APPROVED

Otherwise end:

99'S GUIDE PROMPT 29 STATUS: BLOCKED — DO NOT DOWNLOAD RELEASE CANDIDATE YET