# 99'S GUIDE — PROMPT 31B
## 5-GiB RESOURCE MULTIPART EXECUTABLE-PROOF + FINAL CLOSURE
### DO NOT REDESIGN PROMPT 31
### DO NOT ADD FEATURES
### TURN "PASS BY SOURCE AUDIT" INTO REAL MOCKED TEST EVIDENCE
### RESTORE EXACT PERSONALIZATION 183 / 0 / 0 / 0 EVIDENCE
### EXECUTE THE PROMPT 31 MIGRATION AGAINST A DISPOSABLE POSTGRESQL DATABASE IF AVAILABLE
### VERIFY R2 MULTIPART FAILURE / CLEANUP / RESUME PATHS
### FINAL RELEASE CLOSURE
### NO CLOUD DEPLOYMENT
### NO PRODUCTION MIGRATION
### NO LIVE R2 WRITES
### NO GIT COMMIT / PUSH
### DO NOT MODIFY `.agents/*`
### DO NOT RETURN INTERMEDIATE PROGRESS UPDATES

Prompt 31 implementation is PROVISIONALLY accepted.

Its architecture is approved:

client
→ authenticated multipart session
→ short-lived presigned UploadPart URLs
→ direct client-to-R2 parts
→ ListParts
→ authoritative part validation
→ CompleteMultipartUpload
→ HeadObject
→ bounded PDF signature Range GET
→ Material persistence

DO NOT redesign this architecture.

Prompt 31 cannot close yet because the previous report relied on source inspection rather than executable tests for several critical multipart safety paths, misreported the personalization gate, and did not execute the new migration against a disposable database.

Repair only what is necessary to prove these gates.

---

# 1. PERSONALIZATION REPORTING DEFECT

Prompt 31 reported:

```text
Personalization:
391 passed
0 failed

391 was the FULL repository total, not the Personalization suite.

Run EXACTLY:

npx tsx --test tests/personalization*.test.ts tests/personalization-runtime.test.tsx

Required:

183 passed
0 failed
0 skipped
0 cancelled

Do not infer this from the full test suite.

Do not modify Personalization merely to satisfy the number.

If the command is not 183/0/0/0, investigate the regression.

2. DO NOT ACCEPT "PASS BY SOURCE AUDIT" FOR CRITICAL MULTIPART SAFETY

The previous report marked many high-risk behaviors as:

PASS by source audit

That is insufficient for this feature.

Convert the critical paths below into deterministic automated tests using mocked:

Prisma/session repository
R2/S3 commands
presigner
HTTP requests
XHR/upload transport where applicable

No live R2.

No live Cloudflare.

No production DB.

3. CREATE FOCUSED MULTIPART SERVICE TESTS

Add a focused test file or files following repository conventions, for example:

tests/resource-multipart-service.test.ts

Do not put every test into server.ts.

Exercise the actual service functions used by production routes.

4. CREATE FOCUSED MULTIPART HTTP TESTS

Add:

tests/resource-multipart-http.test.ts

or repository-consistent equivalent.

Test the real Express router/control-plane boundary using mocked R2/storage/database dependencies.

Required routes include actual equivalents of:

start
status/resume
presign parts
complete
abort

Do not call only internal helpers and claim HTTP behavior.

5. AUTHENTICATION TEST — EXECUTABLE

Through HTTP:

anonymous
→ 401

ordinary student
→ 403

authorized admin
→ allowed

Use actual authentication middleware/test harness.

6. CROSS-ADMIN OWNERSHIP TEST — EXECUTABLE

Admin A creates session.

Admin B attempts:

GET status
POST presign
POST complete
DELETE abort

Every operation must be rejected according to current owner/admin hierarchy.

Do not rely on UUID secrecy.

7. START VALIDATION TESTS

Through service/HTTP boundary verify rejection of:

0-byte file
> 5 GiB
non-PDF MIME
non-PDF extension where contract requires it
invalid/deleted lecture
unsupported material type
too many active sessions

No R2 CreateMultipartUpload should run after a validation failure.

8. ACTIVE SESSION LIMIT TEST

Final reported limit is:

3 active large uploads per admin

Test:

3 active sessions
→ allowed existing state

attempt 4th
→ rejected

terminal session
→ no longer counts

Use actual final session semantics.

9. OBJECT KEY SECURITY TEST

Verify client cannot provide or override:

bucket
object key
prefix
uploadId

Production key must be generated server-side.

Test path-like filename input such as:

../../other-user/file.pdf

and prove it cannot escape the server-owned resource namespace.

10. PART PLAN NUMERIC TESTS

Retain/expand current numeric tests.

Required:

3 GiB
5 GiB
threshold exactly
threshold + 1
5 GiB + 1
non-divisible size

No huge buffer allocation.

For:

5 GiB
part size = 32 MiB

required:

160 parts
11. PRESIGN PART VALIDATION — EXECUTABLE

Test actual endpoint/service behavior for:

part 0
negative
expectedPartCount + 1
duplicate part numbers
more than 16 requested URLs
expired session
aborted session
completed session
cross-admin session

No presigner invocation for invalid requests.

12. PRESIGNED URL TTL TEST

Final reported TTL:

15 minutes

Verify signer receives the bounded intended expiry.

Also verify URL expiry cannot exceed remaining session TTL.

Do not expose signatures in test logs/report.

13. PRESIGNED URL SCOPE TEST

Mock the presigner and assert each UploadPart command contains exactly the server-owned:

Bucket
Key
UploadId
PartNumber

Client must not control any of them except requesting an allowed part number.

14. LISTPARTS AUTHORITY — EXECUTABLE HARD GATE

This is critical.

Mock the client sending misleading/fake completion metadata.

The backend must ignore it.

Mock R2 ListParts.

Required:

completion list
=
R2-reported parts

not client ETags.

15. PAGINATED LISTPARTS AUDIT

R2/S3 ListParts can be paginated.

Audit actual implementation.

If the helper currently assumes a single response contains all parts, repair it.

Even though 160 parts is normally below common page limits, the abstraction should correctly follow provider continuation markers if the SDK can paginate.

Add a deterministic multi-page mocked ListParts test if the API supports pagination.

Do not assume one response without checking the actual SDK response contract.

16. RESUME TEST — EXECUTABLE

Mock R2 containing:

parts 1, 2, 4

for a 5-part session.

Required safe resume response:

completed = 1,2,4
missing = 3,5

Frontend uploader must schedule only:

3,5
17. BACKEND RESTART RESUME — EXECUTABLE

Because Prompt 31 explicitly added PostgreSQL persistence for restart safety:

simulate:

service instance A
→ creates persisted session

dispose/recreate service instance B
→ reads session from database mock/test DB
→ ListParts from R2
→ resumes

Do not claim restart-resume solely from source inspection.

18. PART SIZE VALIDATION — EXECUTABLE

Mock ListParts with a non-final part smaller/larger than:

32 MiB

Required:

complete rejected
CompleteMultipartUpload not called
Material create not called
19. FINAL PART SIZE TEST — EXECUTABLE

For a non-divisible declared file size:

mock exact final remainder.

PASS.

Then mock final remainder +/- 1 byte.

Required:

completion rejected
20. TOTAL SIZE TEST — EXECUTABLE

Mock parts whose total does not equal declared source size.

Required:

completion rejected
Material rows = 0
21. MISSING PART TEST — EXECUTABLE

Mock:

1,2,4

when expected:

1,2,3,4

Required:

CompleteMultipartUpload calls = 0
Material creates = 0
22. DUPLICATE / OUT-OF-RANGE PROVIDER PART DEFENSE

If provider/mock state includes malformed duplicate/out-of-range part data:

fail safely.

Do not construct an invalid completion payload.

23. COMPLETE MULTIPART TEST — EXECUTABLE

For a valid mocked source:

assert exact sequence:

ListParts
→ CompleteMultipartUpload
→ HeadObject
→ GetObject Range bytes=0-1023
→ PDF signature validator
→ Material persistence

Material persistence MUST occur last.

24. HEADOBJECT SIZE MISMATCH — EXECUTABLE

Mock valid part list and successful multipart completion.

Then:

HeadObject.ContentLength != declared size

Required:

Material create = 0
object cleanup attempted
session not COMPLETED
25. PDF SIGNATURE VALIDATION — EXECUTABLE

Mock:

Range bytes=0-1023

containing a valid PDF header.

Required:

Material can proceed.

Mock invalid binary bytes with correct declared MIME/extension.

Required:

Material create = 0
DeleteObject invoked
session failed
26. RANGE REQUEST ASSERTION

Explicitly assert the R2 command requests a bounded range.

Required final production request equivalent:

Range: bytes=0-1023

Do not download the entire object during final validation.

27. MATERIAL DB FAILURE AFTER R2 COMPLETE — EXECUTABLE

Mock:

multipart completed
HeadObject valid
PDF signature valid
Material DB insert throws

Required:

success response = NO
DeleteObject attempted
session transitions safely
no duplicate Material
28. IDEMPOTENT COMPLETE — EXECUTABLE

First call:

Material created once
session COMPLETED

Second call:

Material create calls remain 1
existing Material result returned

Do not initiate another R2 completion.

29. ABORT — EXECUTABLE

Active session:

DELETE / abort

must assert:

AbortMultipartUpload called
status → ABORTED
future presign rejected
future complete rejected

Repeated abort should be safe/idempotent according to current route semantics.

30. EXPIRED SESSION — EXECUTABLE

Mock expiresAt < now.

Verify:

status/presign/complete behavior follows safe contract
new URLs rejected
completion rejected
R2 abort/cleanup attempted as designed
31. STALE SESSION CLEANUP TEST

Exercise actual opportunistic cleanup function.

Mock multiple sessions:

active not expired
expired active
completed
aborted

Only stale active sessions should cause incomplete multipart abort behavior.

Do not abort completed Material resources.

32. R2 ABORT FAILURE

Mock AbortMultipartUpload failure during cleanup.

The system must:

not expose raw R2 error
retain enough state for later cleanup/retry
not falsely claim provider cleanup completed

Report actual final semantics.

33. FRONTEND BOUNDED CONCURRENCY — EXECUTABLE

Do not use source-text matching as the only proof.

Create a lightweight fake File / slice source and mocked XHR/part uploader.

Required:

configured concurrency = 3
maximum simultaneous part PUTs observed <= 3

No multi-gigabyte allocation.

34. FRONTEND FILE SLICING TEST

Verify part requests use:

file.slice(start,end)

with exact planned ranges.

No full file read.

No giant FormData to backend.

35. FRONTEND PROGRESS — EXECUTABLE

Simulate incremental XHR progress.

Verify:

uploaded bytes
total bytes
completed parts

are correct.

If a part retries:

progress from the failed attempt must not permanently double-count.

36. FRONTEND TRANSIENT RETRY — EXECUTABLE

Simulate:

part 7
attempt 1 → network error
attempt 2 → 503
attempt 3 → success

Required:

only part 7 retries.

Completed neighboring parts remain completed.

37. EXPIRED PRESIGN REFRESH — EXECUTABLE

Simulate UploadPart receiving an expired-signature-style 400/403.

Required:

new presigned URL request
same part retried
multipart session unchanged
completed parts unchanged

Be careful:

not every arbitrary 400/403 should necessarily be treated as URL expiry forever.

Retry remains bounded.

38. FRONTEND CANCEL — EXECUTABLE

Start several mocked in-flight XHRs.

Cancel.

Required:

all active XHRs aborted
no new part worker starts
backend abort called
success callback never fires
local terminal resume metadata cleared
39. FRONTEND RESUME — EXECUTABLE

Matching reselected file:

filename
title
size
MIME
lastModified

matches stored session identity.

R2 reports some completed parts.

Required:

only missing parts upload.

Mismatched file identity:

required:

resume blocked
no PUT started
40. LOCAL STORAGE / RESUME METADATA

Audit lightweight browser resume metadata.

Ensure it contains NO:

R2 credential
presigned URL
secret
whole file bytes
file content

Only safe session/file identity metadata.

Add test if practical.

41. SERVICE WORKER

Keep existing result but explicitly verify:

non-GET PUT → network only
/api multipart control → no service-worker cache persistence
presigned R2 URL → no cache

No production service-worker change unless an actual bug exists.

42. R2 CORS — DO NOT OVERCLAIM CAPACITOR

The previous report listed:

capacitor://localhost
ionic://localhost

conditioned on R2 acceptance.

Do NOT claim these origins are verified without a real provider/device test.

Final report should separate:

PWA/browser R2 CORS contract

from:

Capacitor direct-R2 CORS behavior: requires real device/provider validation

Do not weaken R2 CORS to wildcard as a workaround.

43. R2 CORS PWA CONTRACT

For the deployed PWA/browser path, report the required explicit origin and actual signed-request headers.

Audit the exact UploadPart presigned request.

Do not assume only Content-Type if the signed request includes additional required headers.

Final external CORS instructions must match the implementation exactly.

44. DOWNLOAD PATH

Reconfirm current Resource access/download path bypasses Node for large PDF bytes.

If backend produces a signed GET/redirect:

add/source-test that access authorization occurs before the signed URL is issued.

No public bucket conversion.

45. PROMPT 31 MIGRATION — EXECUTION TEST

Prompt 31 created:

prisma/migrations/20260922000000_add_resource_multipart_uploads/migration.sql

Prompt 31 only reviewed its SQL.

That is not enough for final closure.

Attempt a disposable PostgreSQL migration execution.

Use the same safe approach previously used during Prompt 28B if still available.

Do NOT use production.

46. FULL MIGRATION CHAIN PREFERRED

Preferred:

fresh disposable PostgreSQL
→ apply repository migrations in chronological order
→ verify Prompt 28 Calendar schema
→ verify Prompt 31 ResourceMultipartUpload schema

The historical primary-owner migration may require the same disposable fixture handling previously identified.

Do not rewrite old migrations.

47. IF FULL CHAIN IS BLOCKED BY PRE-EXISTING FIXTURE

If the known historical migration requires a primary-owner fixture:

seed ONLY the disposable fixture needed to reproduce the production-equivalent prior state.

Then continue migration execution.

Report this clearly.

48. VERIFY FINAL MULTIPART TABLE

After disposable migration execution verify actual PostgreSQL columns/indexes/relations corresponding to:

ResourceMultipartUpload
owner/user relation
lecture relation if defined
status
uploadId
storagePath/key
partSize
expectedPartCount
expiresAt
timestamps

and actual indexes.

Use final schema as authority.

49. MIGRATION_LOCK AUDIT

Prompt 31 reported a NEW untracked:

prisma/migrations/migration_lock.toml

Audit whether this file already belongs to the repository migration convention.

There should normally be ONE migration lock file at the migration root, not one accidentally recreated because it was missing from the working tree.

Determine:

required canonical Prisma file
or accidental Prompt 31 artifact

If required:
keep it.

If duplicate/unnecessary:
remove only the Prompt-generated duplicate.

Do not damage migration history.

Report exact conclusion.

50. SMALL RESOURCE REGRESSION — EXECUTABLE

Run the existing Material upload validation tests.

Verify:

<= 50 MiB
→ old authenticated backend route

remains intact.

Multipart changes must not alter its signature validation/storage semantics.

51. AI LIMIT SEPARATION

Test or source-contract assert:

Resource max = 5 GiB
AI PDF max remains existing bounded value

No shared constant accidentally expanded AI uploads.

52. PROMPT 30B AI REGRESSION

Run the complete Prompt 30/30B AI reliability matrix.

No failure accepted.

53. CALENDAR AI REGRESSION

Run Calendar Import service/unit + HTTP tests.

No failure.

Prompt 28 and Prompt 31 migration ordering must remain:

20260920000000_add_calendar_schedule_import
→
20260922000000_add_resource_multipart_uploads
54. PERSONALIZATION — HARD GATE

Run EXACTLY:

npx tsx --test tests/personalization*.test.ts tests/personalization-runtime.test.tsx

Required exactly:

183 passed
0 failed
0 skipped
0 cancelled

Do NOT print the repository total as Personalization.

55. APP ICON / PWA

Run:

npx tsx --test tests/app-icon.test.ts tests/app-icon-pwa-assets.test.ts

Required:

15 passed
0 failed
56. AUTH

Run:

npm run test:auth

Expected:

4 passed
0 failed
57. SECURITY

Run:

npm run security:check

Expected existing contract:

13 passed

Run Multipart HTTP security tests separately.

58. NAVIGATION

Run:

npx tsx --test tests/client-navigation.test.ts

Expected:

4 passed
0 failed
59. TYPESCRIPT

Run:

npm run typecheck

Required:

PASS
0 errors
60. PRISMA

Run:

npx prisma validate
npx prisma generate

with safe non-production environment config.

Required:

PASS.

61. PRODUCTION BUILD

Run:

npm run build

Required:

PASS.

Frontend and backend.

62. TARGETED ESLINT

Run ESLint on all Prompt 31/31B changed production/shared/test files.

Required:

0 errors

Do not modify .local/skills.

63. FULL REPOSITORY SUITE

Prompt 31 reported:

391 passed
0 failed

Prompt 31B will add tests, so final count should likely increase.

Run the authoritative FULL suite.

Report:

passed
failed
skipped
cancelled

Hard requirement:

failed = 0
64. CLEAN NPM CI

Run final:

npm ci

or disposable clean-install equivalent.

Required:

PASS.

Lockfile stable.

No internal Replit registry blocker.

65. NPM AUDIT

Run:

npm audit --omit=dev

Do NOT run forced fix.

Report final advisory totals and existing reachability disposition.

66. SECRET SCAN

Reconfirm:

TRACKED PRODUCTION SECRET FOUND: NO
SERVER / STORAGE SECRET IN CLIENT BUNDLE: NO

No presigned URL literals embedded at build time.

67. NO LIVE R2

Do NOT perform real:

CreateMultipartUpload
UploadPart
CompleteMultipartUpload
AbortMultipartUpload

against production R2.

All tests mocked.

68. NO PRODUCTION DB

Do NOT apply migrations to production.

Disposable/local database only.

69. NO CLOUD DEPLOYMENT

Do not change:

live R2 CORS
live lifecycle rules
Render deployment
Cloudflare deployment
production environment
70. GIT REVIEW

Run:

git status --short --untracked-files=all
git diff --stat
git diff --check

Required:

git diff --check
→ NO OUTPUT
71. .agents/*

Run:

git status --short -- .agents
git diff -- .agents
git diff --cached -- .agents

Required:

NO OUTPUT
72. PASS DEFINITION

Prompt 31B may PASS only if executable evidence proves:

HTTP authorization
cross-admin isolation
presign validation
presigner server-owned scope
R2 ListParts authority
resume missing-part calculation
backend restart session recovery
part-size validation
total-size validation
missing-part rejection
HeadObject validation
bounded PDF Range validation
invalid-PDF object cleanup
DB-failure object cleanup
idempotent complete
abort
expired sessions
stale cleanup
frontend concurrency <= 3
frontend slice-based parts
progress correctness
part retry
expired URL refresh
cancel
resume
small upload regression
AI limit separation

and:

Prompt 31 migration executed successfully against disposable PostgreSQL
OR a genuine unavailable-environment blocker is explicitly reported rather than claiming validation

and all existing regression gates pass.

73. MANDATORY FINAL REPORT

Return:

99'S GUIDE — PROMPT 31B REPORT
EXECUTABLE MULTIPART PROOF + FINAL RELEASE CLOSURE
1. Status

PASS / BLOCKED

2. Prompt 31 Architecture

Confirm unchanged final architecture.

3. Prompt 31B Repairs

List only actual changes needed.

4. Multipart Automated Test Files

List exact paths.

5. Multipart Service Tests

Report exact count/pass/fail.

6. Multipart HTTP Tests

Report exact count/pass/fail.

7. Frontend Multipart Tests

Report exact count/pass/fail.

8. Authentication

Report anonymous / student / admin.

9. Cross-Admin Isolation

Report executable result.

10. Start Validation

Report.

11. Active Session Limit

Report.

12. Object-Key Security

Report.

13. Presign Validation

Report.

14. Presigned URL TTL

Report.

15. Presigned Command Scope

Report exact server-controlled fields.

16. ListParts Pagination

Report whether implemented/needed and test.

17. ListParts Authority

Report executable result.

18. Resume

Print mocked completed/missing part example.

19. Backend Restart Resume

Report executable result.

20. Part Size Validation

Report.

21. Final Part Validation

Report.

22. Total Size Validation

Report.

23. Missing Part

Report.

24. Completion Sequence

Print actual tested call sequence.

25. HeadObject Mismatch

Report.

26. PDF Range Validation

Print exact:
Range: bytes=0-1023

27. Invalid PDF Cleanup

Report executable calls.

28. DB Failure Cleanup

Report.

29. Idempotent Complete

Report.

30. Abort

Report.

31. Expired Session

Report.

32. Stale Cleanup

Report.

33. Abort Cleanup Failure

Report semantics.

34. Frontend Concurrency

Report maximum observed.

35. File Slicing

Report.

36. Frontend Progress

Report.

37. Part Retry

Report.

38. Expired URL Refresh

Report.

39. Frontend Cancel

Report.

40. Frontend Resume

Report.

41. Resume Metadata Secret Safety

Report.

42. Service Worker

Report.

43. R2 CORS — PWA

List actual required browser contract.

44. R2 CORS — Capacitor

State:
REAL CAPACITOR DIRECT-R2 CORS BEHAVIOR: DEFERRED TO DEVICE / LIVE R2 VALIDATION

45. Resource Download Path

Report auth → signed R2 GET behavior.

46. Prompt 31 Migration

Print exact path.

47. Disposable Migration Execution

Report:
RUN / BLOCKED BY ENVIRONMENT

If RUN:
report exact result and schema verification.

48. Migration Lock

Explain migration_lock.toml conclusion.

49. Production Migration

State:
NO PRODUCTION DATABASE MIGRATION APPLIED.

50. Small Resource Regression

Report.

51. AI Limit Separation

Report exact Resource vs AI limits.

52. Prompt 30B AI Reliability

Report.

53. Calendar AI

Report.

54. Personalization

Required:
183 passed
0 failed
0 skipped
0 cancelled

55. App Icon / PWA

Report.

56. Auth

Report.

57. Security

Report.

58. Navigation

Report.

59. TypeScript

Report.

60. Prisma Validate / Generate

Report.

61. Production Build

Report.

62. ESLint

Report.

63. Full Test Suite

Report:
passed
failed
skipped
cancelled

64. Clean npm ci

Report.

65. npm Audit

Report.

66. Secret Scan

State:
TRACKED PRODUCTION SECRET FOUND: YES / NO
SERVER / STORAGE SECRET IN CLIENT BUNDLE: YES / NO

67. Cloud / Production State

State exactly:
NO CLOUD DEPLOYMENT PERFORMED.
NO LIVE R2 CONFIGURATION CHANGED.
NO PRODUCTION DATABASE MIGRATION APPLIED.

68. Git Review

Print:
git status --short --untracked-files=all
git diff --stat
git diff --check

69. .agents/*

Print exact three command results.

Then:
No .agents/* file was created or modified.

70. Final Release Gate

If all gates pass, state exactly:

5-GIB MULTIPART ARCHITECTURE: VERIFIED

MULTIPART CRITICAL PATH: EXECUTABLE MOCK TESTS PASS

HTTP AUTHORIZATION: VERIFIED

CROSS-ADMIN ISOLATION: VERIFIED

SERVER-GENERATED OBJECT KEY: VERIFIED

PRESIGNED PART SCOPE: VERIFIED

R2 LISTPARTS AUTHORITY: VERIFIED

R2 LISTPARTS PAGINATION: VERIFIED / NOT REQUIRED BY FINAL BOUNDED PLAN AS APPLICABLE

BACKEND-RESTART RESUME: VERIFIED

PART SIZE VALIDATION: VERIFIED

TOTAL SIZE VALIDATION: VERIFIED

MISSING PART REJECTION: VERIFIED

HEADOBJECT VALIDATION: VERIFIED

BOUNDED PDF SIGNATURE RANGE: VERIFIED

INVALID PDF CLEANUP: VERIFIED

POST-COMPLETE DB FAILURE CLEANUP: VERIFIED

IDEMPOTENT COMPLETE: VERIFIED

ABORT: VERIFIED

EXPIRED / STALE SESSION CLEANUP: VERIFIED

FRONTEND MAX CONCURRENCY 3: VERIFIED

SLICE-BASED CLIENT UPLOAD: VERIFIED

FRONTEND PROGRESS: VERIFIED

PART RETRY: VERIFIED

EXPIRED PRESIGN REFRESH: VERIFIED

FRONTEND CANCEL: VERIFIED

FRONTEND RESUME: VERIFIED

NO WHOLE-FILE BUFFER: VERIFIED

LARGE RESOURCE BYTES BYPASS RENDER: VERIFIED

SMALL RESOURCE REGRESSION: PASS

AI LIMITS: UNCHANGED

PROMPT 30B AI RELIABILITY: PASS

CALENDAR AI: PASS

PERSONALIZATION: 183 / 0 / 0 / 0

APP ICON / PWA: PASS

AUTH: PASS

SECURITY: PASS

NAVIGATION: PASS

TYPESCRIPT: PASS

PRISMA: PASS

PROMPT 31 MIGRATION: EXECUTION-VALIDATED ON DISPOSABLE POSTGRESQL

PRODUCTION MIGRATION: NOT APPLIED

PRODUCTION BUILD: PASS

FULL TEST SUITE: PASS

CLEAN NPM CI: PASS

NO REPLIT-INTERNAL REGISTRY BLOCKER: VERIFIED

NO TRACKED PRODUCTION SECRET: VERIFIED

NO SERVER / STORAGE SECRET IN CLIENT BUNDLE: VERIFIED

LIVE R2 CORS: PENDING DEPLOYMENT ACTION

REAL LARGE-FILE R2 TEST: DEFERRED TO DEPLOYMENT

REAL CAPACITOR LARGE-FILE TEST: DEFERRED TO MAC/XCODE/PHYSICAL DEVICE

.agents/*: CLEAN

SOURCE RELEASE CANDIDATE: APPROVED

DOWNLOAD: APPROVED

GITHUB EXPORT: APPROVED

End EXACTLY:

99'S GUIDE PROMPT 31B STATUS: COMPLETE — DOWNLOAD / GITHUB EXPORT APPROVED

Otherwise:

99'S GUIDE PROMPT 31B STATUS: BLOCKED — REPAIR REQUIRED