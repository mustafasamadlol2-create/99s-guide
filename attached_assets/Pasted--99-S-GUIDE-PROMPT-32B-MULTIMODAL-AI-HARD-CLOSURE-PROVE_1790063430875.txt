# 99'S GUIDE — PROMPT 32B
## MULTIMODAL AI HARD-CLOSURE
### PROVE SEMANTIC SCANNED-PDF EXTRACTION
### REMOVE PRODUCTION DEPENDENCY ON `pdftoppm`
### VERIFY COMPLETE MULTI-PAGE COVERAGE BEYOND ONE 20-PAGE WINDOW
### MAKE HEIC / HEIF SUPPORT TRUTHFUL
### RESTORE EXACT PERSONALIZATION 183 / 0 / 0 / 0
### RUN ENVIRONMENT-DEPENDENT TESTS AGAINST DISPOSABLE POSTGRESQL
### NO CALENDAR FEATURE WORK
### NO HERO WORK
### NO CLOUD DEPLOYMENT
### NO PRODUCTION MIGRATION
### DO NOT MODIFY `.agents/*`
### COMPLETE EVERYTHING BEFORE RETURNING
### DO NOT RETURN INTERMEDIATE PROGRESS UPDATES

Prompt 32 is PROVISIONALLY accepted.

The shared multimodal architecture and the fixes already implemented in Prompt 32 should be PRESERVED.

Do NOT restart the feature.

Do NOT revert:

- original PDF preservation
- visual/document fallback
- internal `image/png` page representation
- mixed-PDF handling
- binary zero-result recovery
- enhancement status repair
- enhancement confidence repair
- shared MCQ/Flashcard source architecture
- Prompt 30/30B background jobs
- provider retries
- media reuse
- Prompt 31B Resource multipart work

Prompt 32 remains BLOCKED for four concrete reasons:

1. The four-page scanned-PDF fixture proves rendering, but does NOT prove semantic Q1–Q20 extraction.
2. Visual fallback currently depends on the external `pdftoppm` executable, which is not a safe production portability assumption.
3. The reported 20-page fallback bound needs proof that it is a WINDOW, not silent whole-document truncation.
4. Personalization returned 177 rather than the established 183-test baseline.
5. The full suite skipped 13 environment-dependent Resource tests instead of running them against the available disposable PostgreSQL setup.

Repair these issues only.

---

# 1. REAL USER REGRESSION CHARACTERISTICS

The supplied real regression document is:

`MP mid exam99(2).pdf`

Known characteristics:

```text
PDF pages: 4
Useful text layer: none
Visual content: clear
Page 1: Q1–Q5
Page 2: Q6–Q10
Page 3: Q11–Q15
Page 4: Q16–Q20
Each question: four choices
Visible answer marker on the page

The automated suite must NOT depend on the user's real file.

Create a deterministic synthetic IMAGE-ONLY four-page PDF with the same semantic structure.

2. HARD SEMANTIC FIXTURE

Create a fixture whose rasterized pages visibly contain:

Q1 ... Q20
A) ...
B) ...
C) ...
D) ...
Ans: <letter>

Required:

Page 1 = Q1–Q5
Page 2 = Q6–Q10
Page 3 = Q11–Q15
Page 4 = Q16–Q20

The PDF must contain NO useful PDF text layer.

Do NOT merely create blank image pages.

The rendered page pixels must contain the question/option/answer text.

3. NO LIVE PROVIDER REQUIRED

The semantic fixture test may use a deterministic mocked multimodal provider.

The purpose is to prove the entire production pipeline:

image-only PDF
→ source preparation
→ visual/document transport
→ all four pages supplied
→ provider result
→ engine validation
→ completeness merge
→ review candidates

The mock MUST inspect the media/page descriptors it receives.

Do NOT bypass production source preparation by directly injecting 20 candidates into MCQAIEngine.

4. SCANNED MCQ SEMANTIC HARD GATE

Required test result:

input:
  4-page image-only PDF
  useful text layer = 0

visual pages supplied to provider:
  1,2,3,4

final candidates:
  20

sourceOrdinal:
  1...20

sourcePage:
  Q1-Q5   → 1
  Q6-Q10  → 2
  Q11-Q15 → 3
  Q16-Q20 → 4

missing:
  0

duplicates:
  0

Also verify representative answers from every page survive normalization.

5. DO NOT CLAIM VISUAL READING FROM RENDERING ALONE

The following is NOT sufficient:

PDF rendered to PNG successfully.

The hard gate is:

PDF visual content
→ reaches model transport
→ returns semantically mapped candidates
→ survives engine normalization
6. REAL USER FILE — OPTIONAL LOCAL DIAGNOSTIC

If MP mid exam99(2).pdf exists locally in the Replit workspace:

you MAY run NON-LIVE diagnostic checks:

valid PDF
page count = 4
normal PDF text extraction = empty/near-empty
visual fallback selected
visual page descriptors = pages 1–4

Do NOT:

commit the PDF
copy it into tests
call live Gemini
call live Cloudflare
make automated tests depend on it
7. pdftoppm PORTABILITY — CRITICAL

Prompt 32 introduced:

pdftoppm

as the visual-page renderer.

This is NOT acceptable as the ONLY production visual fallback unless the deployment image explicitly guarantees Poppler.

The application currently deploys to normal Node hosting.

A shell binary that may not exist must not determine whether scanned PDFs work.

8. FINAL RENDERER REQUIREMENT

Implement a portable Node/server rendering path that does NOT require the operating system to preinstall:

pdftoppm
poppler
pdfinfo

First audit already installed dependencies.

Preferred decision order:

Reuse an existing portable Node PDF rendering capability already present.
If none exists, add the smallest appropriate maintained Node dependency capable of rendering PDF pages without relying on host-installed Poppler.
Avoid browser-only PDF rendering packages in server runtime unless they have a supported Node path.
Avoid a large unrelated framework.

Examples may include an appropriate PDF.js + Node canvas/prebuilt rendering path if compatible with the repository.

Choose based on ACTUAL current dependencies and runtime compatibility.

9. pdftoppm MAY NOT BE REQUIRED

Final production correctness must satisfy:

pdftoppm absent
→ scanned PDF visual fallback still works

You may:

REMOVE pdftoppm

or:

retain pdftoppm only as an OPTIONAL optimization

but it must never be required.

10. PORTABILITY TEST

Create an automated test that explicitly simulates:

pdftoppm unavailable / ENOENT

Required:

portable Node renderer selected
visual pages produced
valid supported image MIME
pipeline continues

If pdftoppm is removed entirely, prove there is no runtime shell dependency.

11. NO SILENT SHELL FALLBACK FAILURE

If an optional external renderer is attempted and fails:

do NOT convert that directly into:

unreadable
unsupported_format
0 candidates

Use the portable fallback.

12. PORTABLE RENDERED PAGE CONTRACT

Every rendered page must still contain:

actual binary image bytes
supported MIME
page number
source PDF identity
filename/label
byte size
SHA-256

Preferred final MIME:

image/png

unless the selected renderer safely produces another supported image format.

13. IMAGE QUALITY

The visual page must be sufficiently readable for small MCQ text.

The Prompt 32 value was:

144 DPI

Audit whether the portable renderer produces equivalent readable quality.

Do not aggressively shrink the page.

Do not generate unnecessarily massive images.

Use bounded dimensions/memory.

14. 20-PAGE LIMIT — CLARIFY SEMANTICS

Prompt 32 reported:

maximum pages per bounded fallback: 20

This MUST mean:

maximum pages per rendering/processing WINDOW

not:

maximum pages read from the whole source

The user explicitly needs the AI to inspect ALL supported source content.

15. NO SILENT PAGE 21+ LOSS

Forbidden:

25-page scanned PDF
→ only pages 1–20 processed
→ pages 21–25 silently lost

Required:

window 1 → pages 1–20
window 2 → pages 21–25

or smaller bounded windows according to final architecture.

16. TOTAL PDF PAGE SAFETY LIMIT

A separate total document safety cap may exist.

If one exists:

report it explicitly
validate it before processing
if exceeded, return a clear AI_SOURCE_TOO_LARGE-style error
NEVER silently truncate

Do not confuse:

window size

with:

total document page maximum
17. >20 PAGE COVERAGE TEST

Create a synthetic image-only PDF with:

25 pages

No huge real media allocation is necessary beyond a lightweight test fixture.

Required:

processed page coverage:
1...25

missing pages:
0

duplicate pages:
0

Verify multiple bounded rendering windows were used.

18. LAST-PAGE CONTENT TEST

Put a unique marker/item only on:

page 25

Required:

final multimodal source/provider plan contains Page 25.

This proves the architecture is not first-window-only.

19. PAGE ORDER

Across multiple visual windows final order must remain:

1,2,3,...,25

No:

reorder
duplicate
missing page
20. MIXED PDF > ONE WINDOW

Add or extend a mixed-document test where useful visual-only information occurs after the first processing window.

Prove it reaches the provider.

21. DEFERRED VISUAL RANGE ACROSS WINDOWS

Deferred recovery must work for a range outside the first 20 pages.

Example:

pages 22–24

Required:

range deferred
→ portable renderer
→ provider
→ merged result
22. ZERO-RESULT RECOVERY BY RANGE

For a multi-window source:

if one visual range returns zero but other ranges succeed:

retain successful ranges.

Retry/split only the failed range.

Do not restart all pages.

Preserve Prompt 30B semantics.

23. CLOUDFARE VISUAL FALLBACK

Audit Cloudflare specifically.

The final Cloudflare path for a scanned PDF must be:

Markdown conversion useful
→ use fast text result

Markdown conversion empty/diagnostic/incomplete
→ portable visual pages
→ visual-capable provider path

Do not merely convert rendered PNG pages back through a mechanism that still cannot inspect images.

Prove the final provider actually consumes image media or obtains meaningful visual conversion from the rendered page.

24. GEMINI PATH

Gemini should continue using the ORIGINAL PDF natively where current Gemini transport supports it.

Do not rasterize every Gemini PDF unnecessarily.

Required principle:

Gemini PDF-native path first
portable page renderer only when genuinely needed

Preserve upload-once/reuse behavior.

25. MIXED-PDF DUPLICATION

When both:

converted text
+
rendered visual page

represent the same content:

avoid double-producing duplicate MCQs/Flashcards.

Preserve global dedupe/review warnings.

Do not drop visual pages merely to avoid duplicates.

26. HEIC / HEIF TRUTHFUL SUPPORT — HARD AUDIT

Prompt 32 reported:

Gemini retains provider-native HEIC/HEIF MIME transport

Do NOT assume this is correct merely because input validation accepts those MIME types.

Audit the actual active provider SDK/API transport contract.

For EACH provider determine whether:

image/heic
image/heif

is genuinely supported.

27. IF GEMINI DOES NOT SUPPORT HEIC / HEIF DIRECTLY

Normalize server-side to:

image/png
or
image/jpeg

before provider transport.

Use the existing safe image normalization library where possible.

Do not send a MIME the provider does not support.

28. HEIC / HEIF DECODING

Input acceptance means the server must actually be able to decode the format.

Add deterministic tests using valid tiny fixtures if the repository can support them.

If the current runtime/library cannot reliably decode HEIC/HEIF:

do NOT falsely advertise support.

Either:

A. add a safe minimal decoder/normalization path

or

B. remove HEIC/HEIF from accepted formats and return a clear unsupported-format message

Option A is preferred only if it is reliable and does not create a large unsafe dependency.

Truthful behavior is mandatory.

29. IMAGE FORMAT MATRIX

Final report must include a table for:

JPEG
PNG
WebP
HEIC
HEIF

with:

accepted by app
decoded by server
Gemini transport representation
Cloudflare transport representation
tests
30. VALID JPEG/PNG VISUAL TEST

Retain direct image tests.

Verify the actual binary reaches provider media preparation.

No mock that bypasses the media transport.

31. ENHANCEMENT CONFIDENCE — VERIFY UI CONTRACT

Prompt 32 repaired stale confidence.

Add a UI/response-contract test for:

extraction confidence = 1.0
enhancement confidence = 0.4

After enhancement:

the displayed/current relevant confidence MUST NOT remain misleadingly:

100%

while the system warns enhancement confidence is below threshold.

32. ENHANCEMENT STATUS — VERIFY UI CONTRACT

Test:

requested fields:
hint + explanation

provider returns:
hint only

Required final state:

NOT fully Enhanced
needs review / partial equivalent
missing explanation warning

Then recovery returns explanation:

required:

fully Enhanced
obsolete missing-explanation warning removed
33. SOURCE EVIDENCE WARNING TEST

Existing visual source contains evidence.

Provider enhancement successfully cites/uses visual source.

Required:

NO false:

Source evidence was not provided.
34. TRUE MISSING EVIDENCE TEST

Source genuinely does NOT support the requested explanation.

Required:

warning remains.

Do not remove useful warnings globally.

35. SCANNED FLASHCARD SEMANTIC TEST

Do not test only "image source accepted".

Create a small image-only PDF/source whose visual pages encode deterministic concept/answer pairs.

Mock multimodal provider based on actual page descriptors.

Required:

visual pages supplied
nonzero flashcards
source page evidence correct
36. MCQ GENERATE — MULTI-PAGE VISUAL COVERAGE

Use a multi-page scanned lecture fixture where:

fact A only on early page
fact B only on final page

Mock provider/source planner.

Required generated result includes source coverage from BOTH ends.

No first-page bias.

37. FLASHCARD GENERATE — MULTI-PAGE VISUAL COVERAGE

Same principle.

Final-page information must reach generation.

38. ENHANCE VISUAL SOURCE — LAST PAGE

Existing MCQ/Flashcard requires evidence found only on the LAST source page.

Required:

enhancement source plan includes last page.

No false missing-evidence warning.

39. DIRECT SOURCE PIPELINE TESTS

Test actual shared source preparation for:

plain text
text PDF
image-only PDF
mixed PDF
PNG
JPEG

HEIC/HEIF according to final truthful contract.

Do not bypass AIInputService.

40. NO TEXT REGRESSION

Plain pasted text must remain the cheapest/fastest source path.

No visual renderer invoked.

41. NO TEXT-PDF PERFORMANCE REGRESSION

For a normal text-rich PDF where the text path is sufficient:

portable page renderer call count should be:

0

unless there is verified mixed visual content requiring it.

42. SCANNED PDF RENDER CALL COUNT

For the four-page image-only fixture:

verify only required pages are rendered.

No duplicate render of Page 1 etc.

Job-level page/media reuse should prevent redundant work.

43. CANCELLATION DURING RENDER

Add a test:

visual PDF rendering begins
→ abort signal
→ remaining render work stops
→ temp images removed
→ provider not called for cancelled remaining pages
44. RENDER FAILURE CLEANUP

Simulate portable renderer error on one page.

Required:

already created temporary pages cleaned
source PDF remains under correct owner lifecycle
safe normalized error returned
no arbitrary path deletion
45. PROVIDER FAILURE AFTER RENDER

Visual pages prepared successfully.

Provider fails permanently.

Required:

job-local rendered page files are cleaned.

No leaked temp directory.

46. MEMORY SAFETY

Report exact final limits for visual rendering:

render window pages
visual concurrency
maximum page pixel dimensions / scale/DPI
maximum decoded image bytes where applicable
total document page limit if any
AI source byte limit

No unbounded bitmap accumulation.

47. PERSONALIZATION 177 → 183 INVESTIGATION

Prompt 31B immediately before Prompt 32 reported:

183 passed
0 failed
0 skipped
0 cancelled

Prompt 32 changed no Personalization source/tests but reported:

177 passed
0 failed
0 skipped
0 cancelled

This MUST be investigated.

Do not add six fake tests.

48. PERSONALIZATION TEST FILE INVENTORY

Run:

find tests -maxdepth 1 -type f -name 'personalization*' -print | sort

or repository-safe equivalent.

List exact files.

Compare current inventory with the Prompt 31B repository state using Git/history/worktree evidence available locally.

49. PERSONALIZATION PER-FILE COUNTS

Run every personalization test file individually.

Report:

file
passed
failed
skipped

Identify exactly where six tests disappeared.

50. TEST DISCOVERY AUDIT

Run the authoritative command exactly:

npx tsx --test tests/personalization*.test.ts tests/personalization-runtime.test.tsx

Capture actual resolved input files.

Check for:

shell glob behavior
file extension mismatch
duplicate omission
conditional describe/test definition
environment-dependent conditional declaration
accidental rename
accidental deletion
51. NO PERSONALIZATION SOURCE REGRESSION

Confirm:

git diff -- src shared tests | ...

with focus on Personalization.

Prompt 32 must not have modified accepted Personalization semantics.

If a test file was accidentally lost from working tree:
restore it from current accepted project history/checkpoint.

Do NOT invent replacement behavior.

52. PERSONALIZATION HARD RESULT

Required closure:

183 passed
0 failed
0 skipped
0 cancelled

If it is IMPOSSIBLE because the previous 183 count was demonstrably an erroneous historical report:

provide exact evidence.

Do NOT simply assert that.

The expected outcome remains 183.

53. 13 SKIPPED RESOURCE TESTS

Prompt 32 full suite reported:

13 skipped

These correspond to environment-dependent Resource multipart HTTP/database tests.

Prompt 31B previously ran them using disposable PostgreSQL.

For final Prompt 32B closure, recreate/use a safe disposable local PostgreSQL database.

Do NOT use production.

54. RUN RESOURCE ENVIRONMENT-DEPENDENT TESTS

Set only the explicit test DB env required, such as the repository's:

PROMPT31B_HTTP_DATABASE_URL

or actual equivalent.

Run the Resource test suite with the disposable database.

Required:

0 skipped
0 failed

for those tests.

55. FULL SUITE WITH SAFE TEST DB

After the disposable DB is ready, run the authoritative full suite in a configuration where those 13 tests EXECUTE rather than skip.

Prompt 32 added more tests, so do not expect old 415 count.

Required:

failed = 0
skipped = 0

unless there is a completely unrelated intentionally skipped test with documented historical justification.

Do not treat .local/skills lint as a test skip.

56. DISPOSABLE DB ONLY

Never point:

PROMPT31B_HTTP_DATABASE_URL
DATABASE_URL
TEST_DATABASE_URL

at production for tests.

Use disposable/local DB only.

57. BROAD ESLINT

The two existing errors under:

.local/skills/artifacts/bootstrap.js
.local/skills/shopify/files/shopify-admin-api.mjs

remain PRE-EXISTING and OUT OF SCOPE.

Do not modify them.

They do NOT block Prompt 32B if:

production/shared/Prompt32 tests = 0 ESLint errors

Classify them separately in the report.

58. PROMPT 30B REGRESSION

Re-run all Prompt 30/30B reliability tests.

No failures.

59. PROMPT 31B REGRESSION

Run ALL Resource multipart tests WITH the disposable DB configuration so environment-dependent tests actually execute.

Required:

0 failed
0 skipped
60. CALENDAR REGRESSION

Run Calendar Import service/unit + HTTP suites.

No behavior changes.

No failures.

61. APP ICON / PWA

Run:

npx tsx --test tests/app-icon.test.ts tests/app-icon-pwa-assets.test.ts

Required:

15 passed
0 failed
62. AUTH

Run:

npm run test:auth

Expected:

4 passed
0 failed
63. SECURITY

Run:

npm run security:check

Expected:

13 passed

plus focused Prompt32 multimodal security tests if added.

64. NAVIGATION

Run:

npx tsx --test tests/client-navigation.test.ts

Expected:

4 passed
0 failed
65. TYPESCRIPT

Run:

npm run typecheck

Required:

PASS
0 errors
66. BUILD

Run:

npm run build

Required:

PASS.

Frontend + backend.

67. DEPENDENCY AUDIT AFTER PORTABLE RENDERER

If a new rendering dependency is added:

run:

npm ci
npm audit --omit=dev

Do NOT use npm audit fix --force.

Report:

dependency added
why needed
native/prebuilt/system dependency behavior
production portability
advisory impact

No Replit-internal registry URLs.

68. CLEAN INSTALL PORTABILITY

The final scanned-PDF implementation must survive:

clean npm ci

without requiring a manual:

apt install poppler-utils

or equivalent host setup for correctness.

This is a HARD GATE.

69. PRODUCTION SERVER SMOKE

Start the built backend with safe non-production configuration.

Verify startup succeeds without pdftoppm/Poppler installed.

Do not call a live provider.

70. NO DATABASE CHANGE

Prompt 32B should require no Prisma schema changes.

Expected:

NO NEW MIGRATION

Do not touch Prompt 28/31 migrations.

71. GIT REVIEW

At completion:

git status --short --untracked-files=all
git diff --stat
git diff --check

Required:

git diff --check
→ NO OUTPUT

Classify any new dependency/lockfile changes.

72. .agents/*

Run:

git status --short -- .agents
git diff -- .agents
git diff --cached -- .agents

Required:

NO OUTPUT.

73. NO CLOUD DEPLOYMENT

Do NOT:

deploy Render
deploy Cloudflare
modify provider settings
call live AI
apply production migration
push Git
74. NO REPORT FILE

Do not create Prompt32B markdown/report files.

Return the report in chat.

75. PASS DEFINITION

Prompt 32B may PASS only if ALL are proven:

4-page image-only PDF semantic MCQ test = 20/20

Q1–Q20 ordering verified

Q1–Q20 sourcePage mapping verified

no useful PDF text layer required

portable page rendering works without pdftoppm

clean npm install does not require host Poppler

valid rendered pages use supported real image MIME

25-page visual source covers pages 1–25

no silent 20-page truncation

last-page content reaches provider

deferred recovery works beyond first visual window

zero-result recovery preserves successful ranges

Gemini original-PDF path preserved

Cloudflare scanned-PDF visual fallback verified

HEIC/HEIF support is truthful

MCQ Extract visual = verified

MCQ Generate visual = verified

MCQ Enhance visual evidence = verified

Flashcard Extract visual = verified

Flashcard Generate visual = verified

Flashcard Enhance visual = verified

enhancement status consistency = verified

enhancement confidence consistency = verified

false source-evidence warning = fixed

text input fast path preserved

text PDF fast path preserved

render cancellation cleanup verified

render failure cleanup verified

Prompt30B regression = pass

Prompt31B Resource suite = pass with 0 skips

Calendar regression = pass

Personalization = 183 / 0 / 0 / 0

App Icon/PWA = pass

Auth = pass

Security = pass

Navigation = pass

TypeScript = pass

Build = pass

full suite = 0 failed / 0 skipped

production correctness does not require pdftoppm

.agents = clean
76. MANDATORY FINAL REPORT

Return exactly:

99'S GUIDE — PROMPT 32B REPORT
MULTIMODAL AI HARD-CLOSURE
1. Status

PASS / BLOCKED

2. Prompt 32 Audit

State what was retained and what remained incomplete.

3. Prompt 32B Repairs

List only actual repairs.

4. Portable PDF Renderer

Report exact library/implementation.

5. System Dependencies

State whether scanned-PDF correctness requires:
pdftoppm
poppler
other OS packages

Required expected:
NO REQUIRED POPPLER / PDFTOPPM DEPENDENCY

6. Clean Install Portability

Report.

7. PDF Rendering Contract

Report MIME, quality, bounds, metadata.

8. Four-Page Semantic Scanned Fixture

Report:
pages
text layer
visible questions
visible answers

9. Scanned MCQ Semantic Result

Print:
expected candidates
actual candidates
sourceOrdinal range
missing
duplicates

10. Source Page Mapping

Print Q1-Q20 page mapping.

11. Provider Media Evidence

Explain how test proves all four visual pages reached production media transport.

12. Scanned Zero-Result Recovery

Report.

13. >20 Page Coverage

Report 25-page test.

14. Last-Page Coverage

Report Page 25 marker behavior.

15. Render Window

Report exact per-window size.

16. Total PDF Page Limit

Report exact value or NONE.
If bounded, state explicit user error beyond it.
Confirm NO silent truncation.

17. Deferred Visual Range >20

Report.

18. Visual Concurrency

Report.

19. Render Cancellation

Report.

20. Render Failure Cleanup

Report.

21. Gemini PDF Path

Report native-original-PDF behavior.

22. Cloudflare Scanned PDF Path

Report exact visual fallback.

23. Media Reuse

Report.

24. Image Format Matrix

Use:

| Format | App accepts | Server decodes | Gemini transport | Cloudflare transport | Test |

Rows:
JPEG
PNG
WebP
HEIC
HEIF

25. HEIC / HEIF Final Contract

Report truthful support.

26. MCQ Extract Visual

Report.

27. MCQ Generate Visual

Report.

28. MCQ Enhance Visual

Report.

29. Flashcard Extract Visual

Report.

30. Flashcard Generate Visual

Report.

31. Flashcard Enhance Visual

Report.

32. Enhancement Status

Report partial/full semantics.

33. Enhancement Confidence

Report test:
extraction confidence 100%
enhancement confidence 40%
final displayed/relevant behavior

34. Source Evidence Warning

Report true-evidence and missing-evidence cases.

35. Text Input Regression

Report renderer call count / fast path.

36. Text PDF Regression

Report visual renderer behavior for clean text PDF.

37. Prompt 30B Regression

Report exact count.

38. Prompt 31B Resource Regression

Report exact:
passed
failed
skipped

Required:
failed 0
skipped 0

39. Calendar Regression

Report.

40. Personalization Investigation

Explain EXACT reason 183 became 177.

41. Personalization File Inventory

List exact test files and counts.

42. Personalization Final

Required:
183 passed
0 failed
0 skipped
0 cancelled

43. App Icon / PWA

Report.

44. Auth

Report.

45. Security

Report.

46. Navigation

Report.

47. TypeScript

Report.

48. Build

Report.

49. ESLint

Report production/shared/Prompt32 scope.
Classify .local/skills separately.

50. Full Test Suite

Report:
passed
failed
skipped
cancelled

Required:
failed 0
skipped 0

51. Production Server Smoke

Report.

52. Dependencies

Report new dependency/version if any.

53. npm ci

Report.

54. npm Audit

Report if dependency changed.

55. Database / Cloud

State exactly:
NO NEW DATABASE MIGRATION CREATED.
NO PRODUCTION DATABASE MIGRATION APPLIED.
NO CLOUD DEPLOYMENT PERFORMED.

56. Git Review

Print:
git status --short --untracked-files=all
git diff --stat
git diff --check

57. .agents/*

Print exact three command results.

Then:
No .agents/* file was created or modified.

58. Prompt 33 Readiness

If all gates pass, state exactly:

4-PAGE IMAGE-ONLY MCQ PDF: 20 / 20 VERIFIED

SCANNED PDF SEMANTIC EXTRACTION: VERIFIED

SCANNED PDF DOES NOT REQUIRE TEXT LAYER: VERIFIED

PORTABLE PDF VISUAL RENDERING: VERIFIED

POPPLER / PDFTOPPM: NOT REQUIRED FOR PRODUCTION CORRECTNESS

25-PAGE VISUAL COVERAGE: VERIFIED

NO 20-PAGE SILENT TRUNCATION: VERIFIED

LAST-PAGE VISUAL CONTENT: VERIFIED

DEFERRED VISUAL RECOVERY BEYOND FIRST WINDOW: VERIFIED

GEMINI ORIGINAL-PDF PATH: VERIFIED

CLOUDFLARE VISUAL FALLBACK: VERIFIED

IMAGE FORMAT CONTRACT: VERIFIED

HEIC / HEIF CONTRACT: TRUTHFUL AND TESTED

MCQ EXTRACT VISUAL: VERIFIED

MCQ GENERATE VISUAL: VERIFIED

MCQ ENHANCE VISUAL: VERIFIED

FLASHCARD EXTRACT VISUAL: VERIFIED

FLASHCARD GENERATE VISUAL: VERIFIED

FLASHCARD ENHANCE VISUAL: VERIFIED

ENHANCEMENT STATUS CONSISTENCY: VERIFIED

ENHANCEMENT CONFIDENCE CONSISTENCY: VERIFIED

SOURCE-EVIDENCE WARNINGS: VERIFIED

TEXT INPUT FAST PATH: PRESERVED

TEXT PDF FAST PATH: PRESERVED

PROMPT 30B RELIABILITY: PASS

PROMPT 31B RESOURCE MULTIPART: PASS — 0 SKIPPED

CALENDAR AI REGRESSION: PASS

PERSONALIZATION: 183 / 0 / 0 / 0

APP ICON / PWA: PASS

AUTH: PASS

SECURITY: PASS

NAVIGATION: PASS

TYPESCRIPT: PASS

PRODUCTION BUILD: PASS

FULL TEST SUITE: PASS — 0 FAILED / 0 SKIPPED

.agents/*: CLEAN

PROMPT 32: CLOSED

PROMPT 33 SIX-FLOW AI QUALITY + END-TO-END HARDENING: APPROVED TO BEGIN

End EXACTLY:

99'S GUIDE PROMPT 32B STATUS: COMPLETE — READY FOR PROMPT 33

Otherwise:

99'S GUIDE PROMPT 32B STATUS: BLOCKED — REPAIR REQUIRED