# FINAL RELEASE REPAIR

## Gemini 3.8 Flash Structured Output Compatibility + Production API Base

The previous final live validation established:

```text
GEMINI_API_KEY: PRESENT
GEMINI_MODEL: gemini-3.8-flash
```

A real minimal Gemini structured-output request succeeded using:

```text
gemini-3.8-flash
```

Therefore:

* API key authentication works.
* The configured model exists and is reachable.
* Basic Gemini structured output works.

Two release blockers remain:

1. The full application MCQ structured-output schema receives live Gemini HTTP 400 `INVALID_ARGUMENT`.
2. The frontend build currently resolves a non-HTTPS `VITE_API_BASE_URL` override.

Your task is a **surgical repair of these two blockers only**.

Do NOT begin new feature development.

Do NOT redesign the AI engines.

Do NOT modify database schemas.

Do NOT change import semantics.

---

# 1. REPAIR PRODUCTION API BASE FIRST

Inspect all frontend environment sources that can affect:

```text
VITE_API_BASE_URL
```

including as applicable:

```text
.env
.env.local
.env.production
.env.production.local
Replit environment
Vite config
existing fallback code
```

Determine exactly why the previous production build contained a non-HTTPS override.

The final production frontend must resolve only to the project's existing HTTPS Render backend.

Requirements:

* `VITE_API_BASE_URL` must use the existing HTTPS Render backend.
* No `http://` production API target.
* Do not introduce a new backend URL.
* Do not alter CORS.
* Do not alter OAuth.
* Do not modify unrelated Capacitor config.
* Do not hardcode secrets.

A public HTTPS API URL is not a secret.

Do NOT place:

```text
GEMINI_API_KEY
```

in any Vite/frontend variable.

---

# 2. REBUILD AFTER API-BASE REPAIR

Run the production frontend build.

Inspect generated assets.

Verify:

```text
non-HTTPS production API URL: NOT PRESENT
HTTPS Render API URL: PRESENT / correctly resolved
```

Also ensure:

```text
GEMINI_API_KEY: NOT PRESENT
VITE_GEMINI_API_KEY: NOT PRESENT
```

Do not print any secret value.

---

# 3. STRUCTURED OUTPUT BLOCKER

Live evidence:

* Minimal Gemini structured-output schema: SUCCESS
* Full MCQ operation schema: HTTP 400 `INVALID_ARGUMENT`

This means the provider-facing schema must be isolated and made genuinely compatible with the live Gemini API.

Do NOT assume the cause.

Determine it experimentally and from the current implementation.

---

# 4. INSPECT CURRENT GEMINI REQUEST

Inspect the real `GeminiProvider` request.

Report exactly whether it uses:

```text
responseSchema
```

or:

```text
responseJsonSchema
```

or another current SDK structured-output field.

Also inspect the exact provider-facing JSON schema generated for:

* MCQ extract
* MCQ generate
* MCQ enhance Stage 2
* Flashcard extract
* Flashcard generate
* Flashcard enhance Stage 2

Do not print educational source content.

---

# 5. PROVIDER SCHEMA ≠ APPLICATION VALIDATION SCHEMA

This architectural rule is mandatory:

```text
Provider-facing Gemini schema
```

may be simpler than:

```text
Application Zod validation schema
```

The application MUST continue applying the full strict Zod validation after Gemini returns JSON.

Do NOT remove:

* maximum text validation
* confidence validation
* source validation
* uncertainty validation
* answer validation
* semantic quality gates
* review logic

from the application.

Only make the schema sent to Gemini compatible with Gemini.

---

# 6. CHECK FOR UNSUPPORTED JSON-SCHEMA KEYWORDS

Recursively inspect the actual schema sent to Gemini.

Look specifically for keywords that may be emitted by Zod/JSON-Schema conversion but are not supported by the currently used Gemini schema interface.

Examples to investigate include, depending on the API field being used:

```text
minLength
maxLength
pattern
const
uniqueItems
exclusiveMinimum
exclusiveMaximum
multipleOf
allOf
not
if
then
else
examples
default
```

Do NOT blindly delete keys.

First determine:

* which structured-output API field is used
* which subset that field supports
* which actual unsupported keywords occur in our schema

---

# 7. CHECK NULLABLE / UNION REPRESENTATION

Inspect generated representations of fields such as:

```text
correctAnswer: A|B|C|D|null
hint: string|null
explanation: string|null
difficulty: enum|null
source: object|null
page/imageIndex/section
```

Verify the representation is valid for the live Gemini structured-output API.

Do not rely on a converter if it creates an incompatible union structure.

---

# 8. CHECK SCHEMA COMPLEXITY

Measure safely:

* schema serialized byte size
* maximum nesting depth
* number of properties
* number of `anyOf` / union nodes
* number of duplicated nested definitions

Do not print the whole schema unless necessary.

The goal is to determine whether the provider rejection is caused by:

* unsupported keyword
* incompatible nullable/union structure
* excessive schema nesting
* repeated large structures
* another exact incompatibility

---

# 9. DELTA-DEBUG WITH REAL GEMINI

When provider quota is available, use very small synthetic requests.

Do NOT repeatedly hammer the API.

Start from the already-working minimal schema and incrementally add the application's structures.

Example isolation sequence:

```text
minimal root object
→ one MCQ candidate
→ candidate enums
→ nullable fields
→ source evidence
→ uncertainties
→ skipped items
→ complete envelope
```

Stop once the first schema shape returns:

```text
400 INVALID_ARGUMENT
```

Identify the exact incompatible construct.

Use the minimum number of live calls necessary.

If quota is currently exhausted:

* perform static compatibility repair first
* retain a focused live verification step for when quota is available
* do not claim live PASS until it actually runs

---

# 10. IMPLEMENT A GEMINI-SAFE SCHEMA BOUNDARY

If the current generic JSON-schema conversion emits unsupported keywords, implement a small centralized provider-facing compatibility layer.

Recommended concept:

```text
Application Zod schema
        ↓
Gemini provider schema projection
        ↓
Gemini
        ↓
JSON parse
        ↓
FULL original Zod validation
```

The compatibility layer may:

* retain only provider-supported structural keywords
* simplify constraints that are enforced later by Zod
* preserve required fields
* preserve object/array structure
* preserve enums
* preserve numeric minimum/maximum where supported
* preserve array bounds where supported
* preserve nullable semantics correctly

Do not duplicate the full MCQ/Flashcard schemas manually if a safe generic projection is possible.

But do not over-engineer a universal JSON Schema compiler.

---

# 11. STRICT VALIDATION MUST REMAIN

Example:

If Gemini provider schema cannot express:

```text
maxLength: 4000
```

the provider schema may use:

```text
type: string
```

but after generation the existing Zod schema must still reject a string longer than 4000.

Same principle for every constraint removed from the provider-facing representation.

Provider compatibility must NOT weaken final application validation.

---

# 12. TEST THE COMPATIBILITY LAYER OFFLINE

Add focused tests proving:

* unsupported provider keywords are not emitted
* required fields remain required
* enums remain enums
* nested object shape remains correct
* array structure remains correct
* nullable fields remain valid
* original strict Zod validation still rejects invalid content
* MCQ schemas remain semantically strict
* Flashcard schemas remain semantically strict

Do not replace existing tests.

---

# 13. LIVE MCQ SCHEMA VERIFICATION

Once quota permits, perform ONE small real MCQ Generate request using synthetic text.

Use approximately:

```text
2 MCQs
```

Verify:

* no HTTP 400 schema error
* model `gemini-3.8-flash`
* structured response received
* original strict Zod validation passes
* zero DB writes

This is mandatory before declaring the schema repaired.

---

# 14. LIVE FLASHCARD SCHEMA VERIFICATION

Perform ONE small real Flashcard Generate request.

Approximately:

```text
2 Flashcards
```

Verify:

* no schema `INVALID_ARGUMENT`
* structured response valid
* strict Zod validation passes
* zero DB writes

---

# 15. VERIFY ALL OPERATION SCHEMAS

After fixing the shared provider schema boundary, ensure offline contract tests cover:

### MCQ

* Extract
* Generate
* Enhance Stage 2

### Flashcard

* Extract
* Generate
* Enhance Stage 2

Do not require six live calls if the common provider boundary is proven by the two live calls plus deterministic tests.

---

# 16. LIVE PDF AFTER SCHEMA FIX

If provider quota remains available after the two small live calls, run one tiny synthetic PDF operation.

Verify:

```text
Files API upload
→ ACTIVE
→ structured generation
→ strict validation
→ delete
→ local cleanup
```

Do not use production academic files.

If quota does not allow it, report PDF live verification as pending rather than fabricating success.

---

# 17. LIVE IMAGE AFTER SCHEMA FIX

If quota remains available, perform one tiny synthetic image operation.

Verify structured output and cleanup.

If quota does not allow it, report honestly.

---

# 18. ERROR DETAIL SAFETY

The current client reports:

```text
AI_PROVIDER_ERROR
```

for Gemini `INVALID_ARGUMENT`.

Improve internal diagnostics only if necessary so future server logs can distinguish:

```text
provider_invalid_argument
```

without leaking:

* full schema
* source content
* API key
* provider raw body

Client-facing errors must remain safe.

Do not expose internal schema details to administrators.

---

# 19. DO NOT CHANGE THE MODEL

Keep:

```text
GEMINI_MODEL=gemini-3.8-flash
```

Do not work around schema incompatibility by changing to another Gemini model.

The provider-facing contract should be corrected instead.

---

# 20. DO NOT CHANGE THE API KEY

The live minimal request proved that the key works.

Do not rotate, replace, or expose the key as part of this repair.

---

# 21. DO NOT MODIFY DATABASE OR IMPORT

Do not alter:

* Prisma schema
* MCQ import
* Flashcard import
* duplicate detection
* D1
* outbox
* cache invalidation
* `materials_updated`
* Phase 7 transaction code

This repair concerns only:

1. frontend production API base
2. Gemini provider structured-output compatibility

---

# 22. REGRESSION TESTS

Run:

```text
npm run test:ai
npm run test:ai-input
npm run test:ai-media
npm run test:ai-mcq
npm run test:ai-flashcard
npx tsx --test tests/ai-admin-http.test.ts
npx tsx --test tests/ai-review-validation.test.ts
npx tsx --test tests/ai-import.test.ts
npm run test:auth
npm run build
git diff --check
```

Also run all new provider-schema compatibility tests.

Focused AI TypeScript and ESLint must pass.

---

# 23. CAPACITOR RE-SYNC

After the HTTPS API-base repair and successful production build:

```text
npx cap sync ios
```

Verify it no longer reports the invalid/non-HTTPS API configuration blocker.

Do not modify unrelated native settings.

---

# 24. SECRET SCAN AGAIN

After rebuilding, verify:

```text
Gemini key in frontend: NO
non-HTTPS production API override: NO
HTTPS Render API resolution: YES
```

Do not print secret values.

---

# 25. NO AGENT ARTIFACTS

Do not create or modify:

```text
.agents/*
```

Remove all temporary synthetic validation fixtures when finished.

---

# 26. REQUIRED FINAL REPORT

Return exactly:

# FINAL RELEASE REPAIR REPORT

## 1. Root Cause — Production API Base

Report:

* exact source of non-HTTPS override
* exact correction
* production build result

## 2. Root Cause — Gemini Structured Schema

Report the exact incompatible construct discovered.

Do not answer vaguely with “schema too complex” unless complexity is empirically proven.

## 3. Structured Output API

Report whether the provider uses:

* responseSchema
* responseJsonSchema
* another SDK interface

## 4. Provider Schema Compatibility Fix

Explain exact implementation.

## 5. Application Validation Preservation

Prove strict Zod/post-provider validation remains unchanged.

## 6. Live MCQ Result

Report:

```text
PASS / FAIL / BLOCKED BY QUOTA
```

## 7. Live Flashcard Result

Report:

```text
PASS / FAIL / BLOCKED BY QUOTA
```

## 8. Live PDF Result

Report:

```text
PASS / FAIL / BLOCKED BY QUOTA
```

## 9. Live Image Result

Report:

```text
PASS / FAIL / BLOCKED BY QUOTA
```

## 10. Runtime Model

Expected:

```text
gemini-3.8-flash
```

## 11. Database Safety

Confirm zero smoke-test academic writes.

## 12. HTTPS API Base

Confirm:

```text
production HTTPS API base: PASS
non-HTTPS build override: ABSENT
```

## 13. Capacitor Sync

Result.

## 14. Secret Scan

Result.

## 15. Regression Results

| Suite | Result |
| ----- | ------ |

## 16. Files Modified

| File | Why |
| ---- | --- |

## 17. Temporary Cleanup

Confirm all synthetic fixtures removed.

## 18. Agent Artifact Verification

State exactly:

**No `.agents/*` file was created or modified.**

## 19. Remaining Blockers

List only genuine remaining release blockers.

## 20. Release Status

If MCQ and Flashcard live structured calls pass, HTTPS API base passes, and only device testing remains:

```text
LEVEL B — IMPLEMENTATION READY, MANUAL IPAD VERIFICATION REQUIRED
```

If the structured Gemini calls still fail:

```text
LEVEL C — NOT READY
```

End exactly with one:

**FINAL REPAIR STATUS: READY FOR MANUAL IPAD RELEASE GATE**

or:

**FINAL REPAIR STATUS: NOT READY — BLOCKERS REMAIN**
