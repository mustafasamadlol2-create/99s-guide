# FINAL RELEASE VALIDATION PASS

## 99’s Guide AI Import

### Live Gemini + Production API + Capacitor Verification

All implementation phases are complete.

Do NOT add new features.

Do NOT redesign the AI workflow.

Do NOT modify database schemas.

Do NOT begin another development phase.

The production configuration is now ready:

```text
GEMINI_API_KEY = configured server-side
GEMINI_MODEL = gemini-3.8-flash
VITE_API_BASE_URL = existing production Render API URL
```

Your task is to perform the previously blocked **live production-validation gates**.

---

# 1. FIRST — VERIFY ENVIRONMENT SAFELY

Verify at runtime:

* `GEMINI_API_KEY` is present
* `GEMINI_MODEL` resolves to `gemini-3.8-flash`
* `VITE_API_BASE_URL` resolves to the existing HTTPS Render backend
* Gemini key is server-side only

Do NOT print:

* API key
* partial API key
* secret fingerprint
* environment dump

Report only:

```text
GEMINI_API_KEY: PRESENT
GEMINI_MODEL: gemini-3.8-flash
VITE_API_BASE_URL: PRESENT
```

Do not change the URL if the current production Render URL is correct.

---

# 2. SERVER STARTUP

Restart/use the production-style backend with the configured Gemini key.

Verify:

* server starts
* database connects
* normal app endpoints work
* Gemini configuration no longer returns `AI_NOT_CONFIGURED`

Do not expose the key in logs.

---

# 3. LIVE GEMINI DATA POLICY

Use ONLY synthetic educational fixtures.

Do NOT send:

* real student data
* real user information
* real production private data
* auth/session information
* secrets

Use small synthetic educational material such as:

```text
Staphylococcus aureus is a Gram-positive coccus arranged in clusters.
It is catalase positive and coagulase positive.
Protein A binds the Fc region of IgG.
```

Keep live requests small.

---

# 4. LIVE GEMINI TEST 1 — MCQ GENERATE / TEXT

Perform a real provider request through the actual production AI stack.

Target:

```text
MCQ
Generate
Text
```

Request approximately:

```text
3 MCQs
```

Verify:

* Gemini is actually called
* configured model is `gemini-3.8-flash`
* structured output succeeds
* Zod validation succeeds
* questions/options/answers are returned
* source-grounded behavior
* no DB write
* no import occurs
* no secret appears

Record PASS/FAIL.

---

# 5. LIVE GEMINI TEST 2 — FLASHCARD GENERATE / TEXT

Target:

```text
Flashcard
Generate
Text
```

Generate approximately:

```text
3 Flashcards
```

Verify:

* `clinicalConcept`
* `explanation`
* structured validation
* no DB write

Record PASS/FAIL.

---

# 6. LIVE GEMINI TEST 3 — PDF

Create/use a small temporary synthetic PDF containing several simple educational facts.

Do NOT use a real user Lecture merely for testing.

Run one real PDF AI operation.

Recommended:

```text
MCQ Extract or Generate
PDF
```

Verify actual flow:

```text
local temp validation
→ Gemini Files API upload
→ ACTIVE
→ generation
→ structured validation
→ provider file delete
→ local cleanup
```

Confirm:

* Files API path really executes
* no PDF base64 path
* result valid
* provider file cleanup attempted/succeeds
* local temp file removed

Do NOT print Gemini file names or URIs.

After verification delete the synthetic local fixture.

---

# 7. LIVE GEMINI TEST 4 — IMAGE

Create/use 1–2 safe synthetic educational images.

Run:

```text
Flashcard Extract or Enhance
Images
```

Verify:

* image ordering
* MIME handling
* live Gemini request
* structured response
* cleanup
* no DB write

Do not use personal photos or user content.

---

# 8. COMPLETE OPERATION COVERAGE

Between live tests and already-passing offline tests, confirm all six operations remain valid:

### MCQ

* Extract
* Generate
* Enhance

### Flashcard

* Extract
* Generate
* Enhance

You do NOT need to make six expensive live calls if the minimum live matrix already proves:

* Text
* PDF
* Images
* Files API
* actual Gemini model

Use existing deterministic tests for remaining operation-specific behavior.

Do not consume unnecessary quota.

---

# 9. GEMINI MODEL VERIFICATION

Verify from the actual request configuration that the live calls use:

```text
gemini-3.8-flash
```

Do not rely solely on `.env`.

Verify the provider runtime receives that exact configured model.

Do not change models.

---

# 10. LIVE CLEANUP VERIFICATION

After PDF/image live requests verify:

* no Phase 8/validation temp files remain
* provider cleanup completed where applicable
* no source content persisted
* no provider URI exposed in result/error/log

---

# 11. LIVE ERROR SAFETY

Do NOT intentionally exhaust Gemini quota.

But verify existing tests still cover:

* 429
* unavailable
* timeout
* invalid response
* cancellation

If a live error naturally occurs:

* record it accurately
* verify safe error mapping
* do not hammer/retry provider

---

# 12. PREVIEW MUST NOT WRITE DATABASE

For every live Gemini validation request confirm:

```text
0 MCQ database inserts
0 Flashcard database inserts
```

unless an explicitly isolated test database workflow already exists.

Do NOT import live smoke-test content into production academic data.

---

# 13. PRODUCTION FRONTEND BUILD

Run the actual frontend production build with the current:

```text
VITE_API_BASE_URL
```

Verify:

* build passes
* Render API URL is used correctly
* no Gemini key appears in frontend assets
* no `VITE_GEMINI_API_KEY`
* no broken AI route construction

---

# 14. SECRET SCAN

Scan production frontend output.

Confirm:

```text
GEMINI_API_KEY secret: NOT PRESENT
```

Do not print the key.

Also verify no authorization tokens or environment dumps are embedded.

---

# 15. CAPACITOR SYNC

Now that `VITE_API_BASE_URL` is configured, run the project's normal iOS Capacitor flow.

At minimum:

```text
npm run build
npx cap sync ios
```

or the exact equivalent already used by this repository.

Do NOT modify unrelated native configuration.

Verify:

* sync succeeds
* web assets copied
* production API base resolves
* no missing environment error
* AI frontend compiles for Capacitor

If Xcode cannot run in this environment, state that explicitly.

Do NOT claim iOS device build success without Xcode/device evidence.

---

# 16. CAPACITOR AI COMPATIBILITY CHECK

Inspect/verify:

* text JSON request
* PDF `FormData`
* multi-image `FormData`
* AbortController
* existing authentication transport
* production HTTPS API URL

There must be no browser-desktop-only code that prevents iOS WKWebView operation.

---

# 17. SERVICE WORKER / PWA

Verify once more:

* AI POST requests are not cached
* preview responses are not cached
* import responses are not cached
* offline service worker does not intercept AI requests incorrectly

No changes unless a real defect is found.

---

# 18. RUN COMPLETE REGRESSIONS

Run:

```text
npm run test:ai
npm run test:ai-input
npm run test:ai-media
npm run test:ai-mcq
npm run test:ai-flashcard
npx tsx --test tests/ai-admin-http.test.ts
npx tsx --test tests/ai-review-validation.test.ts
npx tsx --test tests/ai-import.test.ts
npm run test:auth
npm run build
git diff --check
```

Run focused TypeScript/ESLint for all AI files.

Do not fix unrelated historical repository failures.

---

# 19. NO NEW FEATURE WORK

Do NOT add:

* new prompts
* new AI modes
* DB fields
* migrations
* Gemini tools
* search grounding
* URL input
* queues
* history
* persistent drafts

If live verification reveals a genuine defect, apply only the smallest necessary fix and explain it.

---

# 20. NO AGENT ARTIFACTS

Do not create or modify:

```text
.agents/*
```

Do not commit:

* temporary PDF
* temporary images
* screenshots
* secret files
* test credentials

Remove all temporary validation fixtures after testing.

---

# 21. REQUIRED FINAL REPORT

Return exactly:

# FINAL LIVE RELEASE VALIDATION REPORT

## 1. Environment

Report only:

* GEMINI_API_KEY: PRESENT / MISSING
* GEMINI_MODEL
* VITE_API_BASE_URL: PRESENT / MISSING

Never print values of secrets.

## 2. Live Gemini Results

| Test | Live/Mocked | Result |
| ---- | ----------- | ------ |

Include:

* MCQ Text
* Flashcard Text
* PDF
* Image

## 3. Model Verification

Confirm actual runtime model.

## 4. Files API Verification

Report:

* upload
* processing
* generation
* deletion
* local cleanup

## 5. Database Safety

Confirm smoke previews produced no academic DB writes.

## 6. Frontend Production Build

Result.

## 7. Secret Scan

Result.

## 8. Capacitor Sync

Result.

## 9. Capacitor AI Compatibility

Report JSON/FormData/Abort/API-base findings.

## 10. PWA / Service Worker

Result.

## 11. Regression Results

| Suite | Result |
| ----- | ------ |

## 12. Defects Found

If none:

**None**

Otherwise report exact minimal fixes.

## 13. Temporary Resource Cleanup

Confirm no validation fixtures remain.

## 14. Agent Artifact Verification

State exactly:

**No `.agents/*` file was created or modified.**

## 15. Remaining Release Gates

Explicitly list only what still requires physical/manual verification.

Expected remaining item:

```text
REAL AUTHENTICATED IPAD PORTRAIT + LANDSCAPE VERIFICATION
```

if the environment cannot perform it.

## 16. Release Classification

Choose:

### If live Gemini + Capacitor pass but real iPad is unavailable:

```text
LEVEL B — IMPLEMENTATION READY, MANUAL DEVICE VERIFICATION REQUIRED
```

### If another implementation blocker exists:

```text
LEVEL C — NOT READY
```

Do NOT mark Level A unless real authenticated iPad portrait and landscape have also been verified.

End exactly with either:

**FINAL VALIDATION STATUS: READY FOR MANUAL IPAD RELEASE GATE**

or:

**FINAL VALIDATION STATUS: NOT READY — BLOCKERS REMAIN**
