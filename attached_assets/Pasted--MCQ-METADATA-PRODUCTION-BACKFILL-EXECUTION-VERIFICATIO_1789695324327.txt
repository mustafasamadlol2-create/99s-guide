# MCQ METADATA — PRODUCTION BACKFILL EXECUTION & VERIFICATION
## Existing MCQs → AI_GENERATED + Medium
### NO FEATURE DEVELOPMENT

The MCQ Categories & Difficulty implementation has already passed architect review.

Do NOT add new features.

Do NOT redesign MCQ UI.

Do NOT modify AI provider behavior.

Do NOT modify Flashcards.

Do NOT modify Notes/PDF behavior.

This task is ONLY to safely execute and verify the already-implemented MCQ metadata backfill against the canonical database.

The user's explicit migration requirement is:

ALL MCQs that existed before the MCQ Categories & Difficulty feature
→ sourceType = AI_GENERATED
→ difficulty = Medium

where Medium is displayed to users as Normal.

The existing guarded command is:

npm run backfill:mcq-metadata -- --before <ISO_TIMESTAMP> --apply

Do NOT run it blindly.

---

# 1. IDENTIFY THE CANONICAL DATABASE

Before any mutation, verify which database the current environment is connected to.

Confirm safely:

- PostgreSQL/Supabase connectivity
- database host identity/domain only where safe
- total MCQ count
- whether this is the application's canonical MCQ database

Do NOT print:

- DATABASE_URL
- password
- credentials
- connection secret

If the environment is NOT connected to the intended canonical database:

STOP.

Report:

CANONICAL_DATABASE_NOT_CONNECTED

Do not perform a backfill against an unrelated test database.

---

# 2. DETERMINE THE SAFE CUTOFF

Do NOT guess an arbitrary timestamp.

The purpose of the cutoff is:

all MCQs that existed BEFORE this feature
→ migrate

MCQs deliberately created AFTER the feature with explicit:
PREVIOUS_YEAR
RESOURCE
Easy
Hard
etc.
→ must remain untouched.

Determine whether any post-feature explicitly categorized MCQs already exist in the canonical database.

Inspect safely:

- createdAt
- sourceType
- difficulty
- counts only
- IDs if needed internally

Do not print question content unnecessarily.

If no real post-feature MCQs have been created yet, use a cutoff immediately before the feature became available for production use.

If post-feature rows already exist:

derive a cutoff that excludes them.

If the boundary cannot be determined reliably:

STOP.

Do NOT risk overwriting intentionally categorized rows.

Report:

SAFE_BACKFILL_CUTOFF_UNDETERMINED

---

# 3. DRY RUN FIRST

Before `--apply`, run the existing backfill in dry-run mode if supported.

If the script does not currently expose a safe dry-run mode, inspect its existing default behavior.

It was designed to require `--apply`, therefore invoking without `--apply` should not mutate data.

Run:

npm run backfill:mcq-metadata -- --before <SAFE_ISO_TIMESTAMP>

Report:

- total MCQs
- rows eligible before cutoff
- rows that would change category
- rows that would change difficulty
- rows after cutoff excluded
- rows deleted: expected 0
- IDs changed: expected 0

Do not proceed if the dry-run result is unexpected.

---

# 4. PRE-BACKFILL SNAPSHOT

Before applying, record aggregate counts only.

At minimum:

Total MCQs

sourceType distribution:
- AI_GENERATED
- PREVIOUS_YEAR
- RESOURCE
- legacy `ai`
- empty/null
- other legacy values

difficulty distribution:
- Easy
- Medium
- Hard
- null/legacy values

Also record:

MCQs created after cutoff

These counts are only for verification.

Do NOT export full question data.

---

# 5. APPLY THE BACKFILL

Only if:

- canonical DB confirmed
- cutoff confirmed
- dry run correct

execute:

npm run backfill:mcq-metadata -- --before <SAFE_ISO_TIMESTAMP> --apply

Expected mutation for all eligible pre-feature rows:

sourceType = AI_GENERATED
difficulty = Medium

Preserve unchanged:

- id
- question
- optionA
- optionB
- optionC
- optionD
- correctAnswer
- hint
- explanation
- sourceRef
- lectureId
- createdAt

Do NOT delete or recreate rows.

---

# 6. VERIFY ROW COUNT

Immediately after apply:

total MCQ count before
must equal
total MCQ count after

Expected:

deleted = 0
duplicated = 0
IDs changed = 0

If the total count changes unexpectedly:

STOP further operations and report a blocker.

---

# 7. VERIFY ELIGIBLE ROWS

For every eligible row before the cutoff verify:

sourceType = AI_GENERATED
difficulty = Medium

There must be:

0 eligible legacy-category rows

and:

0 eligible non-Medium difficulties

unless a row was explicitly excluded by the backfill contract.

Report exact counts.

---

# 8. VERIFY POST-CUTOFF ROWS

This is mandatory.

Any post-cutoff rows with explicit metadata must remain unchanged.

Example:

PREVIOUS_YEAR / Easy
must remain
PREVIOUS_YEAR / Easy

RESOURCE / Hard
must remain
RESOURCE / Hard

Do not merely rely on unit tests.

Verify against the canonical DB if such rows exist.

---

# 9. RERUN-SAFETY — DRY RUN ONLY

After successful apply, run the same command again WITHOUT `--apply`.

Expected:

rows requiring update = 0

Do NOT run a second mutation unnecessarily.

This proves idempotency against the live canonical dataset.

---

# 10. D1 SYNCHRONIZATION

Reuse the existing content synchronization architecture.

Verify what the backfill service actually does after canonical PostgreSQL updates.

Expected:

PostgreSQL remains canonical.

For changed MCQ rows:
- D1 receives canonical `sourceType`
- D1 receives canonical `difficulty`

If D1 is unavailable:
- use existing outbox/pending behavior
- PostgreSQL remains committed
- do not roll back canonical MCQs

Do NOT create a new D1 sync mechanism.

Do NOT emit thousands of browser events.

Use the existing bounded synchronization pattern.

---

# 11. CACHE / REALTIME

After the backfill completes:

use the existing MCQ/material cache invalidation/realtime mechanism only once or in its established bounded manner.

Do NOT:

- reload the application
- restart user sessions
- emit one browser event per MCQ

---

# 12. READ API VERIFICATION

After backfill, fetch representative MCQs through the real application read path.

Verify old MCQs now resolve as:

category = AI_GENERATED
difficulty = Medium

Student-facing mapping must therefore be:

AI Generated
Normal

Do not mutate during the read test.

---

# 13. STUDENT FILTER SANITY

Using representative existing Lecture data after backfill:

verify old questions appear in:

All
and
AI Generated

and do NOT appear in:

Previous Year
Resources

unless there are genuine post-feature rows explicitly created in those categories.

No provider/AI call should occur.

---

# 14. CONTENT INTEGRITY

Compare aggregate hashes/selected representative row fields before and after where practical.

Prove the backfill changed ONLY:

sourceType
difficulty

No question educational content may change.

---

# 15. NO CLOUDFARE AI CALL

This operation must use:

0 Cloudflare AI calls

No model is needed for migration.

Do not classify old questions with AI.

---

# 16. NO SCHEMA CHANGE

Confirm again:

No Prisma schema change.
No Prisma migration.
No D1 schema change.
No R2 change.

---

# 17. DO NOT MODIFY APPLICATION CODE

Expected code changes from this task:

None.

Only mutate canonical MCQ metadata using the existing approved backfill tool.

If a genuine defect in the backfill implementation prevents safe execution:

STOP.

Do not casually patch production migration behavior while also executing it.

Report the defect first.

---

# 18. REQUIRED FINAL REPORT

Return exactly:

# MCQ PRODUCTION METADATA BACKFILL REPORT

## 1. Canonical Database

State:

CANONICAL DATABASE CONFIRMED

or:

CANONICAL DATABASE NOT CONFIRMED

Do not print credentials.

## 2. Safe Cutoff

Report exact ISO timestamp used and why it is safe.

## 3. Pre-Backfill Counts

Report:

- total MCQs
- category/sourceType distribution
- difficulty distribution
- post-cutoff row count

## 4. Dry Run

Report:

- eligible rows
- category changes planned
- difficulty changes planned
- excluded post-cutoff rows

## 5. Apply Result

Report:

- rows updated
- rows deleted
- rows duplicated
- IDs changed

Expected:

deleted = 0
duplicated = 0
IDs changed = 0

## 6. Post-Backfill Counts

Report exact distributions.

## 7. Eligible Row Verification

Confirm all eligible pre-feature rows are:

AI_GENERATED
Medium

## 8. Post-Cutoff Preservation

Report exact verification.

## 9. Rerun Safety

Report second dry-run result.

Expected:

0 rows requiring change

## 10. D1 Synchronization

Report:

- success / queued / unavailable
- rows synced/queued
- canonical PostgreSQL status

## 11. Read API Verification

Confirm representative old MCQs return:

AI_GENERATED
Medium

and display as:

AI Generated
Normal

## 12. Student Filter Sanity

Report:

- All
- AI Generated
- Previous Year
- Resources

for representative Lecture(s).

## 13. Content Integrity

Confirm only sourceType and difficulty changed.

## 14. Cloudflare AI Usage

Expected:

0 calls

## 15. Database / Schema Impact

State exactly:

No Prisma schema change.
No migration.
No D1 schema change.
No R2 change.

## 16. Code Changes

Expected:

None

## 17. Remaining Release Gates

Expected:

Authenticated real-device/mobile/iPad MCQ UI verification only.

## 18. Final Status

If canonical DB backfill succeeded and verification is clean:

MCQ PRODUCTION BACKFILL: COMPLETE

Otherwise:

MCQ PRODUCTION BACKFILL: NOT COMPLETE — BLOCKER REMAINS

End exactly with one:

MCQ METADATA FEATURE: DATA MIGRATION COMPLETE — READY FOR REAL DEVICE VERIFICATION

or:

MCQ METADATA FEATURE: NOT READY — BACKFILL BLOCKER REMAINS