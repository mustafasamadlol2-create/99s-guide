# MODULE PDF RESOURCES — LIVE R2 RELEASE GATE
## Infrastructure + Real Upload Verification ONLY
### NO FEATURE DEVELOPMENT

The Module PDF Resources implementation is already complete.

Do NOT redesign the feature.

Do NOT refactor working code.

Do NOT touch MCQs, Flashcards, Notes, AI, Lecture PDFs, gestures, auth, or unrelated application areas.

This task is ONLY to close the remaining production infrastructure and live-R2 verification gates.

Current implementation:

- dedicated ModuleResource model
- max PDF size = 209715200 bytes
- direct private R2 upload
- single PUT below 100 MiB
- multipart from 100–200 MiB
- 16 MiB parts
- max 3 concurrent parts
- bounded retries
- backend HEAD verification
- backend `%PDF-` ranged verification
- private authenticated download/open
- Console Resources UI
- Module detail Resources UI

Do not change these architectural decisions unless a genuine live integration defect proves a correction is necessary.

---

# 1. VERIFY ENVIRONMENT FIRST

Before doing anything destructive, determine what environment is actually available.

Verify safely:

- PostgreSQL database identity
- whether it is development or canonical production
- R2 Account ID is configured
- R2 bucket name is configured
- R2 access credentials are configured
- D1 configuration status
- frontend preview origin
- production PWA origin if available

DO NOT print:

- DATABASE_URL
- R2 secret
- R2 access key
- API tokens
- signed URLs

Only report non-secret status.

If R2 credentials are not available:

STOP live R2 tests.

Return:

R2_LIVE_CREDENTIALS_REQUIRED

Do not fabricate a pass.

---

# 2. VERIFY PRISMA MIGRATION STATUS

Inspect the ModuleResource migration.

Confirm the migration only creates the intended ModuleResource structures.

Run:

npx prisma validate

Determine whether the connected database already has the ModuleResource migration applied.

If this is only a development database:

it is acceptable to apply the migration there for testing.

If this is the canonical production database:

do not apply anything unless the environment is explicitly confirmed production and normal project deployment procedure allows it.

Report one:

MODULE_RESOURCE_PRISMA_MIGRATION_APPLIED

MODULE_RESOURCE_PRISMA_MIGRATION_PENDING_PRODUCTION

or

MODULE_RESOURCE_PRISMA_MIGRATION_BLOCKED

Do not alter unrelated tables.

---

# 3. VERIFY D1 MIGRATION STATUS

Inspect the D1 ModuleResource schema/migration.

Confirm:

- no PDF binary in D1
- metadata only
- indexes appropriate
- create/upsert/delete mappings correct

If a real configured D1 environment is available, verify migration/schema status.

If not:

report:

MODULE_RESOURCE_D1_LIVE_MIGRATION_PENDING

Do not claim live D1 verification without a real D1 connection.

---

# 4. INSPECT CURRENT R2 CORS

Read the CURRENT CORS policy of the actual configured R2 bucket.

Do not overwrite it immediately.

Record non-secret policy characteristics:

- AllowedOrigins
- AllowedMethods
- AllowedHeaders
- ExposeHeaders
- MaxAgeSeconds

Determine whether it already supports the direct Module Resource upload architecture.

---

# 5. REQUIRED R2 CORS SEMANTICS

For browser-based presigned uploads, ensure the final policy supports the exact origins that actually need direct upload.

At minimum, evaluate:

- production PWA origin
- development localhost origin
- actual Capacitor/iOS Origin used by the app

DO NOT guess the Capacitor Origin.

Inspect the actual runtime/network Origin if possible.

For upload requests, the policy must support:

AllowedMethods:
PUT

AllowedHeaders:
Content-Type

ExposeHeaders:
ETag

A reasonable preflight cache such as:

MaxAgeSeconds: 3600

is acceptable.

Do not add POST merely because the feature uses multipart.

Multipart UploadPart requests are still PUT requests.

CreateMultipartUpload / CompleteMultipartUpload / AbortMultipartUpload are performed by the trusted backend and therefore do not require browser CORS if that is how the current implementation works.

---

# 6. DO NOT USE WILDCARD ORIGIN UNNECESSARILY

Prefer exact origins.

For example conceptually:

[
  {
    "AllowedOrigins": [
      "<PRODUCTION_PWA_ORIGIN>",
      "http://localhost:5173"
    ],
    "AllowedMethods": ["PUT"],
    "AllowedHeaders": ["Content-Type"],
    "ExposeHeaders": ["ETag"],
    "MaxAgeSeconds": 3600
  }
]

This is only an example.

Use the REAL origins discovered from the project/runtime.

Do not copy placeholder values into Cloudflare.

Do not add "*" if exact origins work.

---

# 7. CAPACITOR ORIGIN GATE

This is especially important.

Determine the actual Origin header sent when the Capacitor/iOS WebView performs the direct R2 PUT.

Potential project origins may include custom schemes, but DO NOT assume.

If the exact Capacitor origin can be configured successfully in R2 CORS:

verify it.

If R2 rejects the actual custom-scheme Origin or native WebView direct upload cannot pass CORS:

do NOT weaken bucket security with `*` automatically.

Report:

CAPACITOR_DIRECT_R2_CORS_BLOCKER

and describe the exact observed request/response.

Do not reroute 200 MiB files through Render as a quick workaround.

Do not modify architecture until the blocker is reviewed.

---

# 8. APPLY CORS ONLY IF REQUIRED

If the current R2 bucket policy is insufficient and credentials/permissions permit configuration:

apply the minimal required CORS policy.

Preserve unrelated valid existing R2 CORS rules if they serve other production features.

Do NOT erase unrelated bucket CORS configuration.

After applying:

read the policy back and verify it.

If the environment cannot modify bucket CORS:

provide the exact JSON required for manual Cloudflare Dashboard application.

Report:

R2_CORS_CONFIGURATION_REQUIRED_MANUALLY

Do not claim it is configured.

---

# 9. SINGLE PUT LIVE TEST

Create/generate a safe small valid PDF fixture locally.

Do NOT commit the fixture.

Recommended size:

a few KB or MiB

Use the actual Module Resource frontend/API flow if authenticated test access is available.

Otherwise exercise the same init → signed PUT → finalize path programmatically against the real R2 bucket.

Use a safe temporary resource ID/key through the normal application architecture.

Verify:

- init succeeds
- signed URL generated server-side
- direct browser/client PUT succeeds
- Content-Type = application/pdf
- backend HEAD sees correct size
- ranged first-byte validation sees `%PDF-`
- ModuleResource becomes READY
- list endpoint returns it
- signed/open endpoint resolves the correct object

Then delete the test resource.

Verify:

- DB cleanup
- R2 object cleanup
- D1 delete/queue if enabled

Report:

SINGLE_PUT_LIVE_TEST: PASS

or exact failure.

---

# 10. CORS PREFLIGHT TEST

From an actual browser origin, not just Node/curl, verify the R2 upload.

Confirm:

- OPTIONS/preflight succeeds if emitted
- Access-Control-Allow-Origin matches exact request Origin
- PUT succeeds
- JavaScript can read ETag
- no CORS console error

Remember:

curl success alone does NOT prove browser CORS.

If only Node/curl is available:

state:

BROWSER_CORS_TEST_REQUIRED

Do not claim browser CORS PASS.

---

# 11. MULTIPART REAL FILE TEST

This is mandatory to close the large-resource gate.

Create or obtain a temporary valid PDF larger than 100 MiB.

Recommended:

105–120 MiB

There is no need to test the full 200 MiB limit live if >100 MiB correctly exercises the multipart path.

Do NOT commit this file.

It must still be a structurally valid PDF starting with `%PDF-`.

Execute the REAL multipart path.

Verify:

- CreateMultipartUpload succeeds
- expected part size = 16 MiB
- no more than 3 parts active concurrently
- each part uploads through presigned PUT
- ETag captured for every successful part
- ETags paired with correct part numbers
- parts are ordered correctly for complete
- CompleteMultipartUpload succeeds
- final HEAD size equals source file size
- ranged header equals `%PDF-`
- DB resource becomes READY
- resource appears in module list
- resource PDF opens correctly

Report exact:

source size
part count
part size
maximum observed concurrency

Do not expose signed URLs.

---

# 12. MULTIPART MEMORY CHECK

During the >100 MiB test confirm the implementation does NOT create:

file.arrayBuffer() for the entire file

or an equivalent full-file JS buffer.

Confirm upload uses:

File.slice(...)

per part.

Backend must not receive the entire PDF binary.

Report bounded-memory behavior.

---

# 13. ETAG VERIFICATION

For multipart browser upload:

confirm JavaScript can actually access each UploadPart response ETag.

If upload technically succeeds but:

response.headers.get("ETag")

is unavailable because CORS did not expose it:

the test is FAIL.

Fix the R2 CORS policy with:

ExposeHeaders:
ETag

and retest.

Do not work around it by guessing ETags.

---

# 14. CANCEL / ABORT LIVE TEST

Start a multipart upload.

After at least one part succeeds:

cancel it.

Verify:

- frontend abort signal stops remaining requests
- backend AbortMultipartUpload is invoked
- no READY ModuleResource appears
- no complete R2 object remains
- temporary/pending metadata is cleaned safely

Report:

MULTIPART_CANCEL_TEST: PASS

or exact failure.

---

# 15. INVALID PDF LIVE TEST

Upload a small object declared:

Content-Type: application/pdf

but whose first bytes are NOT:

%PDF-

Example temporary content:

NOTPDF

Use only the verification environment.

Verify finalize:

- rejects it
- object is deleted
- no READY ModuleResource exists
- safe error returned
- no raw R2 error exposed

Report:

INVALID_PDF_CLEANUP_TEST: PASS

---

# 16. OVERSIZE SERVER CONTRACT

Do not generate a >200 MiB fixture solely for this.

Use focused contract tests to verify:

209715200 bytes
→ accepted boundary

209715201 bytes
→ rejected

Confirm frontend precheck and backend authoritative validation agree.

---

# 17. MODULE ISOLATION

Create a temporary PHC resource.

Verify:

PHC list includes it.

NT list does not.

Delete afterward.

No cross-module leakage.

---

# 18. PRIVATE DOWNLOAD

Verify Module Resource object itself remains private.

No permanent public R2 URL.

Opening must require application authentication before a temporary signed GET/open URL is issued.

Verify a random unauthenticated request cannot ask the application for an arbitrary R2 key.

Do not change bucket to public.

---

# 19. SIGNED URL SAFETY

Inspect logs/output.

Confirm no complete presigned query strings are written to:

server logs
browser logs
analytics

Do not print them in this report.

Only report:

signed URL generated successfully

---

# 20. SERVICE WORKER

Using the live/simulated resource open path, confirm the large signed R2 PDF URL is not being permanently cache-first stored by the PWA service worker.

Do not download/cache a 100+ MiB Resource automatically.

Resource metadata may use ordinary application caching.

---

# 21. REGRESSION

After any CORS/infrastructure integration work, rerun:

npm run build
npm run test:auth
npx tsx --test tests/client-navigation.test.ts

run the Module Resource test suites

run:

npm run security:check
git diff --check
npx cap sync ios

Run focused Notes and MCQ regression tests.

Do not change unrelated features.

---

# 22. CODE CHANGE RULE

Expected source-code changes:

NONE

unless a REAL live integration defect is discovered.

If a defect is discovered:

make the smallest possible repair.

Document:

- exact problem
- exact file
- exact fix
- regression tests

Do not refactor working implementation.

---

# 23. TEST ARTIFACT CLEANUP

After live verification:

delete every temporary ModuleResource DB row

delete every temporary R2 object

abort every temporary multipart upload

delete every locally generated >100 MiB fixture

Do not commit large test files.

Do not leave test textbooks/resources visible in the application.

---

# 24. REQUIRED FINAL REPORT

Return:

# MODULE PDF RESOURCES LIVE R2 RELEASE REPORT

## 1. Overall Status

PASS / FAIL / PARTIAL

## 2. R2 Connection

State whether live R2 was available.

## 3. Prisma Migration Status

State:

APPLIED DEVELOPMENT
PENDING PRODUCTION
APPLIED PRODUCTION
or BLOCKED

## 4. D1 Migration Status

Exact status.

## 5. R2 CORS Before

Summarize non-secret configuration.

## 6. R2 CORS After

Summarize final non-secret configuration.

## 7. Verified Origins

Table:

| Origin | Direct PUT | ETag readable | Result |
|---|---|---|---|

Include only origins actually tested.

Do not claim iPad/Capacitor if not truly tested.

## 8. Single PUT Live Test

Report exact result.

## 9. Browser CORS Test

Report exact result.

## 10. Multipart >100 MiB Live Test

Report:

- source size
- number of parts
- part size
- maximum concurrency
- ETag collection
- CompleteMultipartUpload
- HEAD verification
- PDF magic verification
- DB READY result

## 11. Memory Safety

Report real behavior.

## 12. Multipart Cancel Test

Report.

## 13. Invalid PDF Cleanup Test

Report.

## 14. 200 MiB Boundary Contract

Report.

## 15. Module Isolation

Report.

## 16. Private Download

Report.

## 17. Service Worker

Report.

## 18. R2 / DB Cleanup

Confirm no temporary artifacts remain.

## 19. Code Changes

Expected:

None

If any, list exact files/reasons.

## 20. Regression Results

Table of commands/results.

## 21. Production Infrastructure Actions Remaining

Only genuine remaining actions.

Examples:

- apply Prisma production migration
- apply D1 migration
- add production PWA CORS origin
- real Capacitor Origin test

## 22. Real Device Gate

State whether iPad/Capacitor was genuinely tested.

Do not claim device PASS from browser emulation.

## 23. Agent Artifact Verification

State exactly:

No `.agents/*` file was created or modified.

## 24. Final Classification

If R2 single + multipart + CORS work and only real-device verification remains:

MODULE RESOURCES R2: PRODUCTION IMPLEMENTATION COMPLETE — READY FOR REAL DEVICE GATE

If infrastructure still blocks upload:

MODULE RESOURCES R2: NOT READY — INFRASTRUCTURE BLOCKER

End exactly with one of:

MODULE PDF RESOURCES LIVE R2 VERIFICATION: PASS — READY FOR DEVICE GATE

or

MODULE PDF RESOURCES LIVE R2 VERIFICATION: NOT COMPLETE — BLOCKER REMAINS