# MULTIPLE LECTURE NOTES — FINAL VERIFICATION PASS

The implementation is already complete.

Do NOT redesign or refactor the feature.

Do NOT change runtime code unless this verification discovers a genuine defect.

Do NOT touch unrelated app features.

This task is ONLY to verify the remaining architect-review gates for the multi-NOTE implementation.

---

## 1. DATABASE / SCHEMA

Confirm from the actual diff:

- No Prisma schema change.
- No migration.
- Existing Material one-to-many relation is reused.
- No D1 schema change.
- No R2/storage architecture change.

---

## 2. NOTE APPEND SEMANTICS

Verify with focused tests that:

Lecture with 1 NOTE
→ upload second NOTE
→ both Material rows remain.

Explicitly prove:

- previous NOTE row is not deleted
- previous NOTE storagePath is not deleted
- previous NOTE D1 row is not deleted
- only the new NOTE gets syncContentUpsert

Do not rely only on source inspection.

---

## 3. PRIMARY PDF REGRESSION

Verify separately:

type=PDF
→ still replaces the existing primary PDF

Confirm:

- previous PDF DB row cleanup behavior unchanged
- old Storage cleanup unchanged
- D1 delete/upsert behavior unchanged

NOTE multi-file support must not modify primary PDF semantics.

---

## 4. AUTHORITATIVE 10-NOTE LIMIT

Test:

10 existing NOTE rows
→ upload 11th
→ HTTP 409 NOTE_LIMIT_REACHED

Verify:

- final count stays 10
- no new Material survives
- no D1 upsert for rejected NOTE
- no materials_updated for rejected NOTE
- newly uploaded Storage object is cleaned up

---

## 5. REAL CONCURRENCY / RACE TEST

This is mandatory.

Start with:

9 existing Notes

Trigger TWO NOTE upload commits concurrently against the same Lecture.

Verify final state:

10 Notes maximum

Expected:

- one succeeds
- one receives NOTE_LIMIT_REACHED or equivalent safe transaction conflict resolution
- final count never becomes 11
- losing upload's Storage object is deleted
- no orphan Material
- no orphan D1 row

Prove the SELECT ... FOR UPDATE lock and count occur inside the SAME transaction/transaction connection.

Do not merely describe the expected behavior.

---

## 6. DELETE FREES SLOT

Test exactly:

10 Notes
→ DELETE one specific Material
→ 9 Notes
→ upload one new NOTE
→ 10 Notes

Verify:

- correct Material deleted
- correct Storage object deleted
- D1 delete called
- list/count refreshed
- new upload succeeds

---

## 7. D1 CONTRACT VERIFICATION

Local runtime may have D1 disabled.

That is acceptable, but verify with mock/injected sync tests or exact repository inspection:

NOTE creation:
→ one syncContentUpsert(new Material)

NOTE creation must NOT:
→ syncContentDelete(existing Notes)

Individual NOTE deletion:
→ syncContentDelete(deleted Material id)

Also verify the D1 lecture-detail/read mapping returns ALL NOTE Material rows rather than collapsing them with `.find()`.

If D1 runtime cannot be exercised live, state:

D1 behavior verified through contract/mock tests only.

---

## 8. LECTURE DETAIL READ

Verify both available read paths:

- Supabase/PostgreSQL path
- D1-enabled path

For a Lecture with 3 Notes:

GET /api/lectures/:id

must expose all 3 NOTE Materials in `materials`.

The legacy singular `notesPdfUrl` may exist for compatibility but must not replace/collapse the materials array.

---

## 9. STUDENT NOTES VIEW

Add/execute focused UI tests or runtime verification proving:

0 Notes:
→ normal empty state

1 Note:
→ one card opens correct Material

3 Notes:
→ all three cards render
→ each opens its OWN Material id/file

10 Notes:
→ all ten accessible

Critical test:

Click Note 2
→ must not open Note 1.

Verify the new handler uses the selected Material rather than only `lecture.notesPdfUrl`.

---

## 10. LEGACY FALLBACK

Test a legacy Lecture where:

materials contains no NOTE rows

but:

notesPdfUrl exists

Verify the old single-note UI still opens successfully.

If real NOTE Materials exist, they must take precedence over the legacy fallback.

---

## 11. ADMIN MULTI-FILE FLOW

Verify:

- multiple PDFs selected in one picker action
- multiple drag/drop works on desktop
- individual titles
- selected-file removal
- current count displayed
- remaining slots displayed
- cannot intentionally queue more than remaining slots
- maximum 2 concurrent upload workers
- retries: 0 for NOTE write requests
- partial success keeps failed files retryable
- successful files are not uploaded again accidentally

---

## 12. SERVER REMAINS AUTHORITATIVE

Simulate stale frontend state.

Example:

UI believes Lecture has 8 Notes.

Another admin brings it to 10.

Current admin then uploads.

Backend must still reject with:

NOTE_LIMIT_REACHED

Frontend should display the safe message and refresh count.

---

## 13. NO PAGE RELOAD

Search modified implementation for:

window.location.reload
location.reload
history.go(0)

Confirm none are used in:

- NOTE upload
- NOTE delete
- Notes list refresh

Lecture and Console state remain mounted.

---

## 14. RESPONSIVE UI VERIFICATION

Inspect/render the NOTE admin UI and student Notes UI at representative widths:

Mobile:
~375 px

iPad portrait:
~1024 px class

iPad landscape:
~1366 px class

Desktop:
~1440 px

Confirm:

- no horizontal overflow
- no clipped filename/title
- no overlapping delete/open buttons
- no giant empty right-side layout
- practical touch targets
- 10-note list remains usable

Browser emulation is acceptable for this verification.

Do not claim real iPad-device validation unless actually performed.

---

## 15. ARABIC / RTL

Use representative Arabic NOTE titles.

Verify:

- admin queue
- count
- upload statuses
- delete confirmation
- student Note cards
- filenames
- Open action

No broken RTL.

Technical filenames should remain readable.

---

## 16. PROGRESS / POINTS

Verify multiple Notes did NOT accidentally create:

- per-file completion rewards
- 5 points per Note
- repeated Lecture Notes rewards

Existing `notesCompleted` remains Lecture-level.

Opening different Notes must not create 10× points.

---

## 17. SECURITY

Verify every NOTE still requires:

- actual PDF MIME/signature validation
- `%PDF-`
- <= 50 MiB

Filename must not become a server path.

No arbitrary URL uploads.

Existing authenticated PDF access remains unchanged.

---

## 18. RUN TESTS

Run:

npm run build
npm run test:auth
npx tsx --test tests/material-note-policy.test.ts

Run any additional NOTE concurrency/UI tests added during verification.

Run existing PDF access/security tests.

If the known pdf-access negative-expiry fixture still fails before reaching the actual assertion, report it separately as pre-existing.

Also:

git diff --check

Focused TypeScript and ESLint on all files touched by multi-NOTE work.

---

# REQUIRED REPORT

Return:

# MULTIPLE LECTURE NOTES FINAL VERIFICATION REPORT

## 1. Overall Status

PASS / FAIL

## 2. Prisma / Database

Confirm no schema/migration.

## 3. NOTE Append Test

Exact result.

## 4. Primary PDF Regression

Exact result.

## 5. 10-Note Limit

Exact result.

## 6. Concurrent 9 + 2 Upload Test

Report:

- starting count
- concurrent requests
- success count
- rejected count
- final NOTE count
- orphan Storage status

## 7. Delete / Refill

Report:

10 → 9 → 10

## 8. Storage Cleanup

Exact behavior.

## 9. D1 Contract

Exact behavior and whether live or mocked.

## 10. Lecture Detail Read

Confirm all Notes returned.

## 11. Student Notes UI

Confirm each Material opens independently.

## 12. Legacy notesPdfUrl

PASS / FAIL

## 13. Admin Multi-Upload

Report queue/concurrency/partial retry behavior.

## 14. Responsive Verification

Table:

| View | Result |
|---|---|
| Mobile | |
| iPad portrait emulation | |
| iPad landscape emulation | |
| Desktop | |

## 15. Arabic / RTL

PASS / FAIL

## 16. Progress / Points

Confirm Lecture-level behavior only.

## 17. Security

Report PDF validation/auth behavior.

## 18. No-Refresh Verification

PASS / FAIL

## 19. Regression Results

Exact commands/results.

## 20. Files Changed During Verification

Expected:
None

unless a genuine defect was discovered.

## 21. Remaining Limitations

Only genuine limitations.

## 22. Agent Artifact Verification

State exactly:

No `.agents/*` file was created or modified.

## 23. Final Architect Checklist

- [ ] NOTE append proven
- [ ] Previous Notes retained
- [ ] Previous Storage retained
- [ ] No old D1 deletes on upload
- [ ] PDF replacement unchanged
- [ ] 10-note limit proven
- [ ] 11th rejected
- [ ] concurrent uploads cannot produce 11
- [ ] losing race Storage cleaned
- [ ] delete frees slot
- [ ] 10 → 9 → 10 proven
- [ ] Supabase read returns all Notes
- [ ] D1 mapping returns all Notes
- [ ] student renders all Notes
- [ ] each Note opens correct Material
- [ ] legacy note fallback works
- [ ] admin multi-upload works
- [ ] 2-worker concurrency verified
- [ ] retries disabled for NOTE writes
- [ ] mobile layout verified
- [ ] iPad portrait layout verified
- [ ] iPad landscape layout verified
- [ ] desktop layout verified
- [ ] Arabic/RTL verified
- [ ] points remain Lecture-level
- [ ] PDF security unchanged
- [ ] no page reload
- [ ] no Prisma migration
- [ ] production build passes
- [ ] no `.agents/*` changes

End exactly with:

MULTIPLE LECTURE NOTES VERIFICATION: PASS — READY FOR PRODUCTION

or:

MULTIPLE LECTURE NOTES VERIFICATION: FAIL — REPAIR REQUIRED