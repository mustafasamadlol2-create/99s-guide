# PHASE 6A REPAIR — AGENT ARTIFACT CLEANUP ONLY

The Phase 6A implementation is functionally accepted.

There is exactly one scope violation that must be corrected before Phase 6A receives final approval.

The Phase 6A specification explicitly prohibited creating or modifying assistant/agent repository artifacts such as:

```text
.agents/memory/*
```

However the final report states that Phase 6A:

* created `.agents/memory/ai-preview-intake.md`
* modified `.agents/memory/MEMORY.md`

This contradicts the Phase 6A scope.

Your task is to perform a **surgical cleanup only**.

---

# 1. REQUIRED CHANGE

Remove the Phase 6A-specific agent-memory artifact:

```text
.agents/memory/ai-preview-intake.md
```

Then revert ONLY the Phase 6A-specific changes made to:

```text
.agents/memory/MEMORY.md
```

Do not delete or alter unrelated pre-existing memory entries.

The result must restore `.agents/memory/MEMORY.md` to exactly what it would have been if Phase 6A had never modified it.

---

# 2. DO NOT TOUCH IMPLEMENTATION

Do NOT modify any Phase 6A runtime implementation.

Specifically do not change:

* `server.ts`
* `server/services/ai/http/*`
* `server/services/ai/input/*`
* `server/services/ai/mcq/*`
* `server/services/ai/flashcard/*`
* authentication
* routes
* rate limiting
* concurrency behavior
* cancellation
* upload handling
* temporary file handling
* Gemini behavior
* request schemas
* response DTOs
* error mapping
* tests
* package dependencies
* Prisma
* Cloudflare
* frontend

This is not an opportunity to refactor or improve anything.

---

# 3. DO NOT CREATE REPLACEMENT DOCUMENTATION

Do not move the deleted content into:

* another `.agents` file
* a scratch file
* a planning file
* a markdown document
* source comments merely to preserve agent notes

The implementation itself and its existing tests are the source of truth.

---

# 4. VERIFY NO OTHER PHASE 6A AGENT ARTIFACTS EXIST

Check the Phase 6A diff for any other newly created or modified files whose only purpose is:

* agent memory
* assistant memory
* scratch notes
* planning notes
* implementation diary

If any such artifact was introduced specifically by Phase 6A, remove/revert it.

Do NOT touch legitimate runtime files, tests, or user-owned documentation.

---

# 5. VERIFICATION

After cleanup run:

```text
git diff --check
npm run test:ai
npm run test:ai-input
npm run test:ai-media
npm run test:ai-mcq
npm run test:ai-flashcard
npx tsx --test tests/ai-admin-http.test.ts
npm run build
```

Do not fix unrelated pre-existing failures.

---

# 6. REQUIRED FINAL REPORT

Return exactly:

# PHASE 6A REPAIR REPORT

## 1. Files Removed

List every Phase 6A agent/scratch artifact removed.

## 2. Files Reverted

List any pre-existing file from which only Phase 6A agent-memory changes were removed.

## 3. Runtime Code Changes

Expected:

**None**

## 4. Test Code Changes

Expected:

**None**

## 5. Functional Behavior Changes

Expected:

**None**

## 6. Commands Executed

| Command | Result |
| ------- | ------ |

## 7. Verification

Explicitly confirm:

* [ ] `.agents/memory/ai-preview-intake.md` removed
* [ ] Phase 6A additions to `.agents/memory/MEMORY.md` reverted
* [ ] No unrelated memory content removed
* [ ] No runtime implementation changed
* [ ] No test implementation changed
* [ ] AI foundation tests pass
* [ ] AI input tests pass
* [ ] AI media tests pass
* [ ] MCQ tests pass
* [ ] Flashcard tests pass
* [ ] AI HTTP tests pass
* [ ] Production build passes
* [ ] `git diff --check` passes

End exactly with:

**PHASE 6A REPAIR STATUS: CLEANUP COMPLETE — READY FOR FINAL APPROVAL**
