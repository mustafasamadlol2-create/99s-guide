I have attached a collection of screenshots from the application taken on an iPhone.

IMPORTANT:
These screenshots are NOT a list of pages that should be modified only.

They are examples/reference images that reveal multiple frontend, responsive-design, layout, spacing, scrolling, navigation, dark-mode, light-mode, and usability problems.

I want you to use these screenshots as visual evidence and then perform a GLOBAL FRONTEND UI/UX AUDIT AND IMPROVEMENT of the entire application.

The goal is NOT to redesign the application from scratch.

The goal is to make the existing application feel polished, modern, responsive, consistent, and production-quality across:

- iPhone
- iPad
- Laptop/Desktop

The iPhone experience should receive the strongest attention because the attached screenshots were captured on iPhone.

==================================================
1. IMPORTANT SCOPE
==================================================

DO NOT modify only the pages shown in the screenshots.

The screenshots are examples of problems.

After identifying the patterns shown in the screenshots, inspect the ENTIRE application and find the same types of problems everywhere.

For example:

If a spacing problem exists on one page, check other pages for the same responsive-spacing problem.

If a scrolling problem exists on one page, check all pages for similar bottom-overflow issues.

If a dark-mode contrast problem exists on one component, inspect the entire application's dark-mode system.

If a mobile navigation problem exists, inspect the entire mobile navigation architecture.

The final result should be a globally consistent UI.

==================================================
2. RESPONSIVE DESIGN — PRIORITY
==================================================

Audit the entire application at:

1. iPhone
2. iPad Portrait
3. iPad Landscape
4. Laptop/Desktop

Do NOT simply scale the desktop UI down to fit mobile.

Mobile, tablet, and desktop should have layouts appropriate for their available screen size.

Pay particular attention to:

- horizontal spacing
- vertical spacing
- card sizes
- typography
- buttons
- navigation
- headers
- bottom navigation
- sidebars
- modals
- dropdowns
- forms
- tables
- schedules
- scroll containers
- fixed elements
- floating elements
- safe areas

The UI must feel intentionally designed for each screen size.

==================================================
3. IPHONE MOBILE NAVIGATION
==================================================

Implement/improve the mobile navigation for iPhone.

On iPhone, use a FLOATING BOTTOM NAVIGATION BAR.

It should behave conceptually like modern applications such as Instagram.

Requirements:

- Floating rather than permanently attached to the absolute bottom edge
- Modern appearance
- Slightly translucent
- Backdrop blur where supported
- Rounded corners
- Appropriate shadow/elevation
- Proper spacing between navigation items
- Large enough touch targets
- Safe-area aware
- Must not overlap page content
- Must not block buttons or important content
- Must remain visually consistent with the application's design language
- Smooth interaction
- No lag
- No unnecessary animation

The navigation should intelligently account for:

env(safe-area-inset-bottom)

and should work correctly on iPhones with a Home Indicator.

Do not create a black strip underneath the navigation bar.

The content must remain scrollable behind/around the floating navigation without important content being hidden.

==================================================
4. DARK MODE
==================================================

The attached screenshots are primarily Dark Mode examples.

Perform a GLOBAL Dark Mode audit.

Do not only fix the visible screenshots.

Inspect the entire application for:

- poor contrast
- inconsistent backgrounds
- random black areas
- cards that are too bright
- cards that are too dark
- borders that disappear
- text that is difficult to read
- secondary text with insufficient contrast
- buttons with poor contrast
- icons with incorrect colors
- yellow/accent colors that become too aggressive
- shadows that do not work in dark mode
- overlays
- modals
- dropdowns
- input fields
- disabled states
- hover states
- selected states
- active states
- navigation
- schedule components
- calendar components
- cards

Create a consistent visual hierarchy.

Dark mode should look intentional and premium rather than simply being "black backgrounds".

Preserve the application's existing branding and accent colors.

Do not randomly change the brand identity.

==================================================
5. LIGHT MODE
==================================================

Perform an equally comprehensive GLOBAL Light Mode audit.

The Light Mode currently needs significant improvement.

Make Light Mode:

- clean
- modern
- readable
- visually balanced
- consistent
- comfortable for long periods of use
- clearly structured

Check:

- backgrounds
- cards
- borders
- shadows
- typography
- icons
- buttons
- inputs
- navigation
- schedule
- calendar
- headers
- modals
- dropdowns
- empty states
- disabled states

Avoid excessive pure-white surfaces everywhere.

Use a clear visual hierarchy.

Light Mode should feel like the same application as Dark Mode, not like a completely different design system.

==================================================
6. HERO / MAIN HERO SECTION
==================================================

The main Hero section shown in the screenshots needs responsive improvements.

Do not replace the Hero design unnecessarily.

Instead, optimize it for:

- iPhone
- iPad
- desktop

Pay special attention to:

- hero image
- image cropping
- aspect ratio
- content positioning
- text placement
- badges
- buttons
- spacing
- height
- readability
- overlay
- responsive breakpoints

On iPhone, the Hero should use the available width intelligently without becoming excessively tall.

The image must not be awkwardly cropped.

Text and controls must not overlap.

On iPad and desktop, preserve the larger visual experience.

==================================================
7. MOBILE SCROLLING — CRITICAL
==================================================

There are several pages where the user cannot properly scroll to the bottom.

This is a CRITICAL bug.

In some pages:

- the user cannot reach the bottom
- some buttons disappear
- some content becomes inaccessible
- the page ends before the actual content ends
- fixed elements may overlap the final content
- the bottom navigation may cover controls

Audit EVERY scrollable page in the application.

Check:

- body overflow
- html overflow
- root container height
- 100vh
- 100dvh
- 100svh
- fixed elements
- absolute elements
- nested scroll containers
- overflow-y
- min-height
- max-height
- position: fixed
- safe-area insets
- bottom navigation
- modal scrolling
- drawer scrolling

Make sure users can ALWAYS reach the actual bottom of the content.

No button or interactive element should become inaccessible because of the viewport or navigation.

This must work on:

- iPhone
- iPad Portrait
- iPad Landscape
- Desktop

==================================================
8. SAFE AREA / IOS VIEWPORT
==================================================

Because this application runs on iPhone and iPad, perform a complete iOS viewport audit.

Check:

- viewport-fit=cover
- env(safe-area-inset-top)
- env(safe-area-inset-bottom)
- env(safe-area-inset-left)
- env(safe-area-inset-right)
- 100vh
- 100dvh
- 100svh
- 100lvh
- visualViewport
- orientation changes

The application should not produce:

- black bars
- black rectangles
- inaccessible bottom content
- content behind the Home Indicator
- navigation overlapping buttons
- incorrect viewport heights
- broken layouts after rotation

Pay special attention to iPad Landscape.

==================================================
9. SCHEDULE SYSTEM — MAJOR UI IMPROVEMENT
==================================================

The Schedule system currently has a poor mobile/tablet experience.

It needs to be redesigned/improved substantially while preserving its functionality.

The Week / Day / Month views currently do not feel sufficiently different or optimized for their intended purpose.

Improve the Schedule system so that:

DAY VIEW:
- Clearly focuses on a single day
- Easy to scan
- Time-based organization is obvious
- Events are readable
- Mobile-friendly
- No excessive horizontal scrolling

WEEK VIEW:
- Clearly represents a full week
- Days are visually distinguishable
- Events are organized clearly
- Works well on mobile and tablet
- Avoid unusable compressed columns

MONTH VIEW:
- Clearly represents the calendar month
- Dates are easy to understand
- Events are distinguishable
- Appropriate density
- Proper mobile behavior

The three views should feel meaningfully different.

Do not simply resize the same layout three times.

Create appropriate responsive behavior for each view.

The Schedule must work beautifully in both Dark Mode and Light Mode.

==================================================
10. OTHER PAGES
==================================================

The screenshots also reveal problems in other areas such as:

- cards
- sections
- headers
- buttons
- spacing
- scrolling
- navigation
- content density
- responsive behavior

Use these as examples and inspect the rest of the application.

Audit pages such as:

- Welcome
- Login
- Sign Up
- Dashboard
- Subjects
- Lectures
- Schedule
- Calendar
- Profile
- Settings
- Search
- Modals
- Drawers
- Any other application page

Do not assume a page is correct simply because it is not included in the screenshots.

==================================================
11. COMPONENT CONSISTENCY
==================================================

Look for duplicated UI patterns.

If the application has multiple implementations of:

- cards
- buttons
- headers
- navigation
- badges
- tabs
- inputs
- modals
- dropdowns

make sure they follow the same responsive and visual rules.

Do not unnecessarily rewrite the entire application.

Prefer fixing shared components/styles so that improvements propagate globally.

==================================================
12. TOUCH INTERACTION
==================================================

Optimize the iPhone/iPad experience for touch.

Check:

- touch target sizes
- spacing between buttons
- accidental taps
- hover-only interactions
- dropdown behavior
- scrolling
- swipe behavior
- navigation
- modal interaction

Do not rely on desktop hover behavior for important functionality.

==================================================
13. PERFORMANCE
==================================================

The UI must remain smooth.

Do not solve visual problems by introducing:

- unnecessary re-renders
- excessive JavaScript
- repeated layout calculations
- excessive resize listeners
- expensive animations
- unnecessary setInterval/setTimeout
- forced page reloads

Use CSS where appropriate.

Avoid causing regressions in Safari/iOS performance.

==================================================
14. DO NOT BREAK EXISTING FUNCTIONALITY
==================================================

This is extremely important.

DO NOT:

- change backend functionality
- change database logic
- change authentication
- change API contracts
- remove existing features
- remove working functionality
- replace the entire architecture
- perform an unnecessary large refactor

This task is primarily a FRONTEND UI/UX and RESPONSIVE DESIGN improvement.

Preserve the existing application logic.

==================================================
15. DO NOT MAKE IT LOOK LIKE A COMPLETELY DIFFERENT APP
==================================================

The goal is NOT to create a new design from scratch.

Preserve:

- existing branding
- logo
- primary identity
- core visual language
- existing features
- general application structure

Improve the quality, consistency, responsiveness, and usability.

==================================================
16. TESTING REQUIREMENT
==================================================

After making the changes, test the application at multiple viewport sizes.

At minimum:

iPhone:
- small iPhone
- modern large iPhone

iPad:
- Portrait
- Landscape

Desktop:
- Laptop
- Large desktop

Test both:

Dark Mode
Light Mode

Test:

- navigation
- scrolling
- bottom navigation
- Hero
- cards
- Schedule
- modals
- forms
- buttons
- page transitions

Also test:

Portrait → Landscape
Landscape → Portrait

Make sure layouts recover correctly after orientation changes.

==================================================
17. VISUAL REGRESSION
==================================================

Use the attached screenshots as the baseline for the problems.

However, do NOT simply reproduce the screenshots.

The goal is to improve the application beyond these examples.

After changes, compare the implementation against the screenshots and verify that the identified problems are actually resolved.

==================================================
18. FINAL REQUIREMENT
==================================================

Before finishing, perform a GLOBAL FRONTEND REVIEW.

Search the entire codebase for responsive patterns that could cause the same problems.

Especially search for:

- 100vh
- fixed heights
- overflow hidden
- overflow-y
- position fixed
- position absolute
- safe-area
- viewport calculations
- mobile breakpoints
- tablet breakpoints
- dark mode
- light mode
- duplicated responsive CSS
- hardcoded widths
- hardcoded heights
- mobile-only hacks

Do not stop after fixing the pages visible in the screenshots.

The screenshots are examples.

The objective is a globally polished application.

==================================================
FINAL REPORT
==================================================

When finished, provide:

1. Summary of all problems found.
2. Root cause of the most important responsive issues.
3. What was changed globally.
4. What was changed specifically for iPhone.
5. What was changed for iPad.
6. What was changed for Desktop.
7. Dark Mode improvements.
8. Light Mode improvements.
9. Hero improvements.
10. Mobile floating navigation implementation.
11. Scrolling/bottom-content fixes.
12. Schedule Day/Week/Month improvements.
13. Files modified.
14. Confirmation that backend/database functionality was not changed.
15. Confirmation that existing features were preserved.
16. Testing performed on mobile, tablet, and desktop.
17. Any remaining issues that could not be verified directly.

IMPORTANT:
Do not just make the screenshots look better.

Fix the underlying responsive UI system so that the entire application becomes more polished and consistent.