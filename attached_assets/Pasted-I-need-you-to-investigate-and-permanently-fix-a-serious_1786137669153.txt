I need you to investigate and permanently fix a serious UI rendering/re-rendering issue that occurs when navigating between pages in the application.

IMPORTANT:
Do NOT treat this as a simple visual/CSS issue.
I want you to identify the actual root cause and fix the underlying rendering/lifecycle problem.

PROBLEM DESCRIPTION:

When the user navigates between different pages/tabs, for example:

Welcome Page
→ Login Page
→ another page
→ back to Welcome Page

the Welcome Page sometimes comes back in a broken/incompletely-rendered visual state.

I have attached a screenshot showing the exact behavior.

In the screenshot, the Welcome Page appears to exist, but some of its cards/components look like they have not been rendered, loaded, painted, or positioned correctly.

The page looks partially empty/broken instead of returning to its normal fully-rendered state.

This can happen after navigating away from a page and then returning to it.

The problem is especially noticeable on iPad/Safari, but I want the investigation to cover ALL supported devices and browsers.

DO NOT assume this is only a Welcome Page problem.

The same underlying issue could potentially affect:
- Welcome Page
- Login Page
- Sign Up Page
- Dashboard
- Subject pages
- Lecture pages
- Settings
- Profile
- Any other route/page
- Any reusable Card/Panel/Modal/Component

GOAL:

Make page navigation completely reliable.

Whenever the user leaves a page and returns to it, the page must render exactly as it should on a fresh load.

No partially rendered components.
No missing cards.
No blank areas.
No broken layouts.
No components stuck in an intermediate state.
No elements appearing only after manually resizing the window.
No elements requiring scrolling before they appear.
No visual glitches after navigation.

INVESTIGATE THE ROOT CAUSE

Perform a comprehensive code-level investigation.

Check the following:

1. React component lifecycle

Investigate:
- mount/unmount behavior
- useEffect dependencies
- cleanup functions
- stale state
- stale refs
- state preserved between routes
- components failing to remount
- components being reused when they should be recreated
- incorrect keys
- unstable keys
- state updates occurring after unmount
- race conditions between navigation and rendering

2. Rendering and re-rendering

Check whether components correctly re-render when:
- the route changes
- the user returns to a previous page
- data changes
- authentication state changes
- viewport changes
- orientation changes

Look for components that incorrectly depend on:
- initial mount only
- window.innerWidth
- window.innerHeight
- visualViewport
- ResizeObserver
- orientationchange
- resize events

3. CSS rendering

Investigate:
- opacity: 0
- visibility: hidden
- display: none
- transform
- translate
- scale
- position: fixed
- position: absolute
- overflow
- height calculations
- min-height
- max-height
- z-index
- stacking contexts
- CSS animations
- CSS transitions
- animation-fill-mode
- elements remaining in an intermediate animation state

Pay particular attention to CSS that behaves differently in Safari/iPadOS.

4. Viewport-related rendering

Because this application must work on iPhone and iPad, investigate:

- 100vh
- 100dvh
- 100svh
- 100lvh
- window.innerHeight
- document.documentElement.clientHeight
- visualViewport
- safe-area insets
- orientation changes
- Safari viewport resizing

Make sure viewport calculations cannot leave components incorrectly sized or positioned after navigation.

5. Lazy loading / dynamic imports

Check:
- React.lazy
- Suspense
- dynamic imports
- route-level code splitting
- loading states
- fallback components
- components that fail to render after returning to a route

Make sure a temporary loading state cannot become permanently stuck.

6. Data loading

Investigate:
- API requests
- asynchronous effects
- race conditions
- aborted requests
- cached data
- stale data
- loading states
- error states
- requests triggered during navigation

A page must not become visually broken simply because an async request finishes after navigation.

7. Animation system

This is especially important.

Investigate whether page transition animations or component animations can leave elements stuck in a state such as:

opacity: 0
transform: translate(...)
visibility: hidden
display: none

Check for animations that start correctly on the first mount but fail when the component mounts again.

If necessary, make animations lifecycle-safe.

8. IntersectionObserver / ResizeObserver

Search the entire project for:
- IntersectionObserver
- ResizeObserver
- MutationObserver
- requestAnimationFrame
- setTimeout
- setInterval

Check whether observers/timers are correctly cleaned up when components unmount.

Make sure old observers from previous pages cannot interfere with newly mounted components.

9. Navigation system

Inspect the routing/navigation architecture.

Determine whether pages are:
- actually unmounted
- kept alive
- cached
- conditionally rendered
- hidden instead of unmounted

Make sure navigation does not leave previous page state interfering with the new page.

10. Browser-specific behavior

Because the issue is particularly visible on iPad/Safari, test for Safari-specific rendering behavior.

Do NOT introduce a Safari-only hack unless absolutely necessary.

Prefer a correct architectural/rendering solution that works everywhere.

REPRODUCTION TEST

Use this exact test sequence:

1. Open the application.
2. Open Welcome Page.
3. Confirm that every card/component renders correctly.
4. Navigate to Login.
5. Navigate back to Welcome.
6. Repeat this multiple times.
7. Navigate between several other pages.
8. Return to Welcome.
9. Repeat on different viewport sizes.
10. Test iPad Portrait.
11. Test iPad Landscape.
12. Test iPhone.
13. Test desktop browser.

Also test:

Welcome → Login → Welcome → Login → Welcome

and:

Welcome → another page → another page → Welcome

and repeatedly navigate back and forth at least 10 times.

The UI must remain completely stable.

IMPORTANT REQUIREMENTS:

- Do NOT redesign the UI.
- Do NOT change the visual design.
- Do NOT remove existing features.
- Do NOT remove animations unless an animation is proven to be causing the bug.
- Do NOT replace the current architecture unnecessarily.
- Do NOT perform a large refactor.
- Do NOT fix only the screenshot/page.
- Do NOT add arbitrary delays such as setTimeout just to hide the problem.
- Do NOT force a full page reload on every navigation.
- Do NOT use window.location.reload() as a workaround.
- Do NOT simply add random keys to force re-rendering.
- Do NOT hide broken elements with CSS.
- Do NOT create a separate hack for every device.

Instead:

Find the underlying cause and implement the smallest reliable architectural fix.

GLOBAL REQUIREMENT:

The fix must apply to the entire application's page/component lifecycle, not only the Welcome Page.

After fixing the issue, inspect other pages for the same potential problem and correct any identical patterns that could cause the same rendering failure.

REGRESSION TEST:

After implementing the fix, verify:

✓ Welcome Page renders correctly on first load
✓ Welcome Page renders correctly after returning from Login
✓ Welcome Page renders correctly after returning from multiple pages
✓ Cards always appear
✓ Components do not remain partially rendered
✓ No blank sections appear
✓ No elements remain invisible
✓ No broken transforms remain
✓ No stuck animations
✓ No stale loading states
✓ No console errors caused by the fix
✓ No memory leaks
✓ No duplicated event listeners
✓ No duplicated observers
✓ No duplicated API requests caused by remounting
✓ iPhone works correctly
✓ iPad Portrait works correctly
✓ iPad Landscape works correctly
✓ Desktop works correctly

FINAL REPORT:

After completing the work, provide me with:

1. The exact root cause.
2. Why the problem occurred after navigating between pages.
3. Why it was particularly noticeable on iPad/Safari.
4. Which files were modified.
5. What was changed in each file.
6. What tests you performed.
7. Confirmation that the fix is global and not limited to the Welcome Page.
8. Confirmation that no unnecessary UI/design changes were made.

Do not stop at identifying the symptom.
I want the underlying lifecycle/rendering/navigation problem fixed permanently.