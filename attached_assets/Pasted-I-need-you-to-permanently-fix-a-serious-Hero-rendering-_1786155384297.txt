I need you to permanently fix a serious Hero rendering/visual-state bug that occurs when navigating back to the Welcome Page.

This issue affects BOTH:

- iPhone/mobile
- iPad

It must be fixed globally and correctly.

==================================================
CURRENT BEHAVIOR
==================================================

The application has a Welcome Page containing a prominent Hero section / Hero image.

The Hero looks correct when the application is initially loaded.

However, when the user navigates away from the Welcome Page and later returns to it, the Hero can appear visually incorrect.

For example:

- the Hero may appear underneath/behind the fullscreen area
- the Hero can appear visually different
- its brightness may change
- its color/overlay may change
- its rendering may look different from the initial state
- the Hero may appear as though a layer or fullscreen state has remained active

IMPORTANT:

If the user navigates to another page and then returns to Welcome again, the Hero may sometimes become correct.

This means the problem appears to be related to:

- component lifecycle
- rendering
- navigation
- animation state
- CSS state
- viewport calculation
- browser repaint
- mount/unmount behavior

Do NOT simply refresh the page.

==================================================
EXPECTED BEHAVIOR
==================================================

The Hero must have ONE consistent visual appearance.

When the user:

1. Opens Welcome
2. Leaves Welcome
3. Opens another page
4. Returns to Welcome

the Hero must look exactly the same as it did on the initial correct render.

Its:

- brightness
- color
- opacity
- overlay
- image
- position
- scale
- size
- crop
- z-index
- background
- animation state

must remain correct.

Navigation must NEVER change the Hero's visual state.

==================================================
ROOT CAUSE INVESTIGATION
==================================================

Investigate the actual Hero implementation.

Inspect:

- React component lifecycle
- mount/unmount
- useEffect
- useLayoutEffect
- refs
- state
- props
- CSS classes
- conditional rendering
- animation classes
- transition classes
- keyframes
- CSS variables
- pseudo-elements
- overlays
- gradients
- filters
- opacity
- brightness
- backdrop-filter
- mix-blend-mode
- transform
- z-index
- stacking contexts
- position: fixed
- position: absolute
- fullscreen layers

Search specifically for:

opacity: 0
opacity: ...
filter: brightness(...)
filter: ...
transform: ...
scale(...)
translate(...)
backdrop-filter
mix-blend-mode
transition
animation
animation-fill-mode
will-change
z-index
isLoading
isMounted
isVisible
isActive
isEntering
isExiting

==================================================
NAVIGATION LIFECYCLE
==================================================

Determine what happens when:

Welcome mounts
→ user navigates away
→ Welcome unmounts or remains mounted
→ user returns
→ Hero mounts/re-renders

Check whether any state is incorrectly preserved.

Check for:

- stale state
- stale refs
- animation state
- transition state
- loading state
- fullscreen state
- overlay state
- route state
- viewport state

Make sure the Hero initializes into the correct visual state every time it mounts.

==================================================
IOS / IPAD SPECIFIC INVESTIGATION
==================================================

Because the issue is particularly relevant to iPhone and iPad, investigate:

- Safari rendering
- WebKit repaint behavior
- visualViewport
- orientation
- 100vh
- 100dvh
- 100svh
- safe-area
- fullscreen layers
- position: fixed
- GPU compositing
- transform layers
- backdrop-filter
- CSS filters

Do NOT create a random iOS-only hack unless absolutely necessary.

Prefer fixing the underlying rendering/lifecycle problem.

==================================================
HERO IMAGE
==================================================

Verify:

- image source
- image loading
- object-fit
- object-position
- aspect ratio
- responsive sizing
- overlay
- gradient
- brightness
- contrast
- opacity

The image should not change appearance merely because the user navigated away and returned.

==================================================
FULLSCREEN / OVERLAY INTERACTION
==================================================

The phrase "Hero appears underneath the fullscreen area" is especially important.

Investigate whether another fullscreen element or application shell is remaining above the Hero.

Check:

- z-index
- stacking contexts
- fixed overlays
- fullscreen containers
- modal layers
- navigation layers
- mobile navigation
- loading screens
- page transition overlays

Make sure an overlay from another page cannot remain visually above the Welcome Hero.

==================================================
ANIMATIONS
==================================================

If the Hero uses entrance animations:

Make sure the animation lifecycle is safe.

The Hero should never become permanently stuck in:

- opacity 0
- partially transparent state
- incorrect transform
- incorrect filter
- incorrect scale
- incorrect position

If an animation is unnecessary and is causing the bug, simplify it rather than adding complicated JavaScript workarounds.

==================================================
DO NOT USE THESE WORKAROUNDS
==================================================

Do NOT:

- force window.location.reload()
- reload the Welcome Page
- add arbitrary long setTimeout()
- randomly change z-index until it looks correct
- duplicate the Hero component
- create an iPhone-only fake Hero
- hide the problem with opacity
- remove the Hero
- redesign the Hero unnecessarily

Fix the actual lifecycle/rendering problem.

==================================================
REGRESSION TEST
==================================================

Perform these exact tests:

Test 1:

Open application
→ Welcome
→ verify Hero
→ Login
→ Welcome

Test 2:

Welcome
→ Dashboard
→ Subjects
→ Welcome

Test 3:

Welcome
→ several pages
→ Welcome

Test 4:

Welcome
→ rotate Portrait → Landscape
→ rotate Landscape → Portrait
→ verify Hero

Test 5:

Welcome
→ leave page
→ return
→ repeat at least 10 times

Test on:

- iPhone Portrait
- iPhone Landscape
- iPad Portrait
- iPad Landscape
- Desktop

Test:

- Dark Mode
- Light Mode

The Hero must remain visually consistent every time.

==================================================
FINAL REPORT
==================================================

After fixing the issue, provide:

1. Exact root cause.
2. Why the Hero changed appearance after navigation.
3. Why navigating away and returning sometimes temporarily fixed it.
4. Whether an animation, overlay, z-index, viewport, or lifecycle issue was involved.
5. Files modified.
6. Exact architectural/CSS/React fix.
7. Testing performed.
8. Confirmation that the Hero now initializes correctly every time.
9. Confirmation that no page reload workaround was used.
10. Confirmation that the Hero design itself was preserved.